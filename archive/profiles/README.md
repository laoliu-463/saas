# Archived Spring Profiles

归档原因：test / real-pre 两套环境合并，统一使用 `docker-compose.yml` + `.env.test` / `.env.real-pre` 切换。

**保留：**
- `application.yml` — 主配置
- `application-test.yml` — test profile 补充配置
- `application-real-pre.yml` — real-pre profile 补充配置

**已归档：**
- `application-dev.yml`
- `application-local-mock.yml`
- `application-prod.yml`
- `application-real.yml`
