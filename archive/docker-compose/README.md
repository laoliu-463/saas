# Archived Docker Compose

Historical `docker-compose.test.yml` / `docker-compose.real-pre.yml` snapshots (`.bak`) are kept here for reference only.

**Current stack:** use repo root `docker-compose.yml` with `scripts/start-test.ps1` or `scripts/start-real-pre.ps1` (project `saas-active`).
