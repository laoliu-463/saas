# 17-项目剩余事项看板

更新时间：2026-05-17

## 一、当前状态

- TEST/mock 阶段已完成收口。
- `runtime/qa/out/p1-warning-risk-regression-20260516-214324-277` 已落盘，`8/8 PASS`。
- `mvn "-Dtest=PickSourceMappingServiceTest,ProductServiceTest" test` 已通过。
- `node runtime/qa/p1-warning-risk-regression.test.cjs` 已通过。
- real-pre 准入已通过，进入真实业务闭环验证。
- 当前状态链：`real-pre 准入通过 -> P3 单样本业务闭环通过 -> P3-5 全角色账号业务流程通过 -> P3-6 SYNC_OK_NO_SAMPLE -> P3-7 Dashboard 与 PostgreSQL 口径对账 PASS -> P3-8 异常分支 PARTIAL_PASS -> 当前约 94%`
- 当前阶段判断：P3 real-pre 单样本业务闭环、P3-5 全角色账号业务流程验收与 P3-7 Dashboard 数据可信对账均已通过，后续只聚焦真实 `pick_source` 归因样本观察与异常分支补证。
- 当前发布判断：系统已经具备 real-pre 业务可用性；但还不能宣称可生产发布，仍缺“真实订单归因可信、异常分支可解释”两层证据；Dashboard 数据可信对账已通过。
- real-pre 只读 preflight 已于 2026-05-17 复跑并通过；随后已进入 real-pre 单活运行态。
- 2026-05-17 09:00 已使用新 OAuth code 完成本机 token 初始化；真实 code、access token、refresh token、client secret 均未写入文档或 Git。
- `npm run e2e:real-pre` 已复跑通过，结果 `1/1 PASS`，覆盖 Token、授权主体、活动商品、SKU、订单同步、Dashboard 与前端页面联调状态。
- `npm run e2e:real-pre:business` 已新增并复跑通过，结果 `1/1 PASS`，覆盖单样本活动商品同步、商品本地状态、真实转链、`pick_source_mapping`、订单同步、Dashboard 与核心页面。
- `npm run e2e:real-pre:roles` 已新增并复跑通过，结果 `2/2 PASS`，覆盖管理员、招商组长、招商、渠道、运营、渠道组长全角色业务流。
- `scripts/qa/run-real-pre-attribution-evidence.ps1` 已新增并在 real-pre 复跑通过，首轮产出 `reportStatus=SYNC_OK_NO_SAMPLE`，完成 P3-6 一次真实订单窗口扫描且未伪造成功样本。
- `scripts/qa/watch-real-pre-pick-source-orders.ps1` 已新增为 P3-6 专用轻量观察入口，只做最近窗口同步、`pick_source` 检查与 mapping 安全校验，不必每次重跑全量证据脚本。
- `scripts/qa/watch-real-pre-pending-evidence.ps1` 已新增为 P3 Pending Evidence 统一观察入口，统一观察 `P3-6-E / EXC-004 / EXC-005 / EXC-006`，不再每轮手工拆判断。
- `scripts/qa/run-real-pre-dashboard-reconcile.ps1` 已新增并在 real-pre 复跑通过，最新证据目录 `runtime/qa/out/real-pre-dashboard-reconcile-20260517-122911`，结果 `status=PASS`，`diff.json` 为空且 `candidateReasons=[]`，`/api/dashboard/summary` 与 PostgreSQL `10` 个核心指标逐项一致。
- 当前 active 环境为 `real-pre`：`saas-backend / saas-frontend / saas-postgres / saas-redis` 均已 `healthy`。
- 本轮已切 real-pre，未清库，未把真实密钥写入文档或 Git。
- `scripts/qa-real-pre-preflight.ps1` 已补两处准入防护：Docker engine 不可连时不中断报告生成；`SPRING_PROFILES_ACTIVE` 收紧为 `real-pre`，并校验 `JWT_SECRET` 非占位。

## 二、当前 Blocking

| 优先级 | 项目 | 当前状态 | 证据 | 下一步 |
| --- | --- | --- | --- | --- |
| - | real-pre 运行态准入与 E2E | PASS | `runtime/qa/out/qa-real-pre-preflight-20260517-090308-762/report.md`、`runtime/qa/out/real-pre-e2e-20260517-090457-069/report.md` | 允许进入 real-pre 下一步验证 |
| - | P3-7 Dashboard 数据库口径对账 | PASS | `runtime/qa/out/real-pre-dashboard-reconcile-20260517-122911/report.md` | 固化为后续 real-pre 只读回归入口 |
| 中 | P3-8 异常分支证据补齐（首轮只读） | `PARTIAL_PASS` | `runtime/qa/out/real-pre-exception-audit-20260517-124432/report.md` | 用 `scripts/qa/watch-real-pre-pending-evidence.ps1` 持续观察 `EXC-004/005/006`，并分离 live 样本等待与安全路径设计 |
| 高 | P3-6 真实订单归因样本 | `SYNC_OK_NO_SAMPLE` | `runtime/qa/out/real-pre-attribution-evidence-20260517-104656/report.md` | 使用 `scripts/qa/watch-real-pre-pending-evidence.ps1` 持续观察下一真实订单窗口，拿到样本后补 `P3-6-E` |

当前 preflight 结论（2026-05-17 09:03）：

- `overallPass=true`
- blocking checks：无
- `real_sdk_config_present`：`PASS`
- `real_security_config_present`：`PASS`
- `runtime_container_env_safe`：`PASS`
- `runtime_health_up`：`PASS`
- `runtime_system_env_safe`：`PASS`
- 已复核 `APP_TEST_ENABLED=false`、`DOUYIN_TEST_ENABLED=false`
- `/api/system/env` 返回 `activeProfiles=["real-pre"]`、`environmentLabel=REAL-PRE`、`database=saas_real_pre`
- 结论：允许进入 real-pre 下一步验证

real-pre E2E 结论（2026-05-17 09:04）：

- `POST /api/douyin/tokens` 初始化成功，`hasAccessToken=true`、`hasRefreshToken=true`、`reauthorizeRequired=false`
- 后端 SKU 探针返回形状为 `ApiResult.code=200`、`data.status=success`、`remoteResponse.data.skus` 非空；已修正 E2E helper 的成功判定
- `npm run e2e:real-pre` 结果 `1/1 PASS`
- 页面 `/system/douyin` 一键刷新后显示 `Token 正常`、`授权主体正常`、`活动商品已刷新`、`商品 SKU 已验证`、`订单同步成功`、`Dashboard 已读取真实订单`
- 结论：允许进入 real-pre 下一步验证

本轮进入过程已处理的启动阻塞：

- `.env.real-pre` 原 `SPRING_PROFILES_ACTIVE=real` 无法加载当前 `application-real-pre.yml`，已改为 `real-pre`
- `.env.real-pre` 原 `JWT_SECRET` 为占位符，已在本机生成随机值；真实值未写入文档或 Git
- `RealDouyinTokenGateway -> InstitutionApi -> DouyinApiClient -> DouyinTokenService` 循环依赖已用 `@Lazy` 最小拆环，并通过 `mvn "-Dtest=RealDouyinTokenGatewayTest" test`
- 前端慢响应已定位为 Docker + Vite polling 间隔过短；本机 `.env.real-pre` 设置 `CHOKIDAR_INTERVAL=30000` 后，`/login` 主机侧复核约 `0.02s`
- 浏览器已打开 `http://127.0.0.1:3000/login` 并用 `admin/admin123` 登录到 `/dashboard`，页面显示 `环境：REAL-PRE`

## 三、已完成但需保留证据的事项

| 项目 | 状态 | 证据 |
| --- | --- | --- |
| P1 warning 风险专项 | PASS (`8/8`) | `runtime/qa/out/p1-warning-risk-regression-20260516-214324-277/report.md` |
| PickSourceMapping / ProductService 定向单测 | PASS | `mvn "-Dtest=PickSourceMappingServiceTest,ProductServiceTest" test` |
| p1-warning-risk-regression 单测 | PASS | `node runtime/qa/p1-warning-risk-regression.test.cjs` |
| real-pre 运行态 preflight | PASS | `runtime/qa/out/qa-real-pre-preflight-20260517-090308-762/report.md` |
| RealDouyinTokenGateway 循环依赖回归 | PASS (`5/5`) | `mvn "-Dtest=RealDouyinTokenGatewayTest" test` |
| SampleControllerTest 定向回归 | PASS (`29/29`) | `backend mvn "-Dtest=SampleControllerTest" test` |
| git diff --check | PASS | `git diff --check` |
| real-pre 浏览器登录验证 | PASS | `runtime/qa/out/real-pre-browser-check-20260517-0847/summary.json` |
| real-pre 前端 + 后端 E2E | PASS (`1/1`) | `runtime/qa/out/real-pre-e2e-20260517-090457-069/report.md` |
| P3 real-pre 业务闭环 smoke | PASS (`1/1`) | `runtime/qa/out/real-pre-business-e2e-20260517-091828/summary.json` |
| P3-5 全角色账号业务流程验收 | PASS (`11/11` steps, Playwright `2/2`) | `runtime/qa/out/real-pre-role-business-e2e-20260517-101101/summary.json` |
| P3-8 首轮只读异常审计 | `PARTIAL_PASS` | `runtime/qa/out/real-pre-exception-audit-20260517-124432/report.md` |

## 四、下一步

1. 继续保持 `.env.real-pre` 的 `APP_TEST_ENABLED=false`、`DOUYIN_TEST_ENABLED=false`。
2. 当前只做 `P3-6`、`P3-7`、`P3-8` 三件事，不扩到生产写操作、批量导出、批量发货或达人爬虫大规模采集。
3. 后续 real-pre 专项继续先明确接口清单；如涉及活动创建、取消等可能触发上游状态变更的动作，需单独确认。
4. 前端 `CHOKIDAR_INTERVAL=30000` 为本机 real-pre 启动优化项；如后续改回低间隔导致慢响应，再按 Docker + Vite polling 方向排查。
5. 不清库、不写真实密钥到文档或 Git。

### P3-6 真实订单归因样本

状态：部分收口，等待真实 `pick_source` 订单样本

目标：

- 证明 `pick_source`、`pick_source_mapping`、真实订单同步、归因结果与 Dashboard 之间存在一条可追溯的真实样本链路。

最小通过标准：

- 至少 `1` 条真实订单同步成功
- 订单 `pick_source` 可追溯
- `pick_source_mapping` 可命中
- `mapping.created_at <= order.create_time`
- `attribution_status` 正确
- Dashboard 已包含该订单，或明确说明为何当前口径不包含

本轮结果（2026-05-17 10:47）：

- 证据目录：`runtime/qa/out/real-pre-attribution-evidence-20260517-104656`
- 执行结果：`SYNC_OK_NO_SAMPLE`
- 环境守卫：`activeProfiles=["real-pre"]`、`environmentLabel=REAL-PRE`、`APP_TEST_ENABLED=false`、`DOUYIN_TEST_ENABLED=false`、`health=UP`
- 订单同步：`totalFetched=100 / created=2 / updated=98 / attributed=0 / unattributed=100 / failed=0`
- raw probe：最近 `3` 天抓取 `20` 条真实订单样本，`rawOrdersWithPickSource=0`
- 本地近 `30` 天订单语料：`totalOrders=721 / ordersWithPickSource=0 / attributedOrders=0 / unattributedOrders=721`
- Dashboard 当前总览：`orderCount=721 / unattributedOrderCount=721 / upstreamProductUncoveredCount=718 / cannotAutoAttributionCount=3`
- 本轮结论：订单同步链路正常，`/api/orders/sync` 执行成功且 `failed=0`。本轮真实订单窗口与本地近 `30` 天订单均未发现 `pick_source`，因此无法形成真实归因样本；当前结论为 `SYNC_OK_NO_SAMPLE`，不伪造归因通过。

本轮已完成：

- `P3-6-A`：真实订单同步窗口扫描完成
- `P3-6-B`：无 `pick_source` 情况验证完成
- `P3-6-C`：无可归因样本时未伪造通过
- `P3-6-D`：Dashboard 未归因口径有真实数据支撑

本轮未完成：

- `P3-6-E`：真实 `pick_source` 订单归因样本通过

后续：

- 保留统一观察入口：`scripts/qa/watch-real-pre-pending-evidence.ps1`
- 如只需专看 `pick_source` 订单样本，仍可使用：`scripts/qa/watch-real-pre-pick-source-orders.ps1`
- 下一步进入 `P3-8` 异常分支证据补齐；`P3-7` 保留 `scripts/qa/run-real-pre-dashboard-reconcile.ps1` 作为只读复跑入口

### P3-7 Dashboard 数据库口径对账

状态：`PASS`

目标结论：

- `Dashboard 展示的数据 = PostgreSQL 聚合出来的数据`

目标指标：

- `orderCount`
- `orderAmount`
- `serviceFee`
- `attributedOrderCount`
- `unattributedOrderCount`
- `upstreamProductUncoveredCount`
- `cannotAutoAttributionCount`
- `ambiguousMappingCount`
- `unsafeBecauseCreatedAfterOrderCount`
- `nativeKeyMismatchCount`

通过标准：

- `/api/dashboard/summary` 返回值 = PostgreSQL 聚合值

本轮结果（2026-05-17 12:29）：

- 证据目录：`runtime/qa/out/real-pre-dashboard-reconcile-20260517-122911`
- 执行入口：`scripts/qa/run-real-pre-dashboard-reconcile.ps1`
- 执行结果：`status=PASS`
- 环境守卫：`activeProfiles=["real-pre"]`、`environmentLabel=REAL-PRE`、`APP_TEST_ENABLED=false`、`DOUYIN_TEST_ENABLED=false`、`health=UP`
- 核心指标：
  - `orderCount=808`
  - `orderAmount=1548347`
  - `serviceFee=22935`
  - `attributedOrderCount=0`
  - `unattributedOrderCount=808`
  - `unsafeBecauseCreatedAfterOrderCount=0`
  - `upstreamProductUncoveredCount=802`
  - `cannotAutoAttributionCount=6`
  - `nativeKeyMismatchCount=0`
  - `ambiguousMappingCount=0`
- 产物：`summary.json`、`report.md`、`api-dashboard-summary.json`、`db-dashboard-summary.json`、`diff.json`、`orders-breakdown.json`
- 结论：`/api/dashboard/summary` 与 PostgreSQL 聚合值逐项一致，`diff.json` 为空且 `candidateReasons=[]`，P3-7 可标记通过。

### P3-8 real-pre 异常分支证据

状态：`PARTIAL_PASS`，部分可冻结

证据目录：

- `runtime/qa/out/real-pre-exception-audit-20260517-124432`

已通过：

- `EXC-001` 无订单同步成功：`PASS`
- `EXC-002` 无 `pick_source` 订单保留：`PASS`
- `EXC-003` 无映射订单保留且 `dryRun` 不写回：`PASS`

待补充：

- `EXC-004` 映射创建时间晚于订单时间：`NO_LIVE_SAMPLE`
- `EXC-005` 重复转链幂等：`DEFERRED_NO_SAFE_READ_ONLY_PATH`
- `EXC-006` SKU 空 / 异常不中断主流程：`DEFERRED_NO_SAFE_READ_ONLY_PATH`

结论：

- 异常分支已完成第一批只读审计收口，但尚未达到 `P3-8` 全量 `PASS`。
- 当前不能把 `EXC-005 / EXC-006` 写成通过；如果 real-pre 没有安全只读路径，就保持 `DEFERRED_NO_SAFE_READ_ONLY_PATH`。

统一观察：

- 脚本：`scripts/qa/watch-real-pre-pending-evidence.ps1`
- 最新证据：`runtime/qa/out/real-pre-pending-evidence-watch-20260517-125831`
- 当前观察结果：`P3-6=SYNC_OK_NO_SAMPLE / EXC-004=NO_LIVE_SAMPLE / EXC-005=READ_ONLY_CANDIDATE_FOUND / EXC-006=NO_HISTORICAL_SAMPLE`

拆分两条线：

- `P3-8-W`：等待 `EXC-004` live 样本
- `P3-8-S`：设计 `EXC-005 / EXC-006` 的安全验证路径

## P3 real-pre 真实业务闭环验证

### P3-1 活动商品链路

- [x] 首轮 smoke：真实活动列表读取成功
- [x] 首轮 smoke：指定活动 `3916506` 商品同步成功，raw 商品 `20` 条，业务商品 `20` 条
- [x] 首轮 smoke：样本商品 `3810562766247428542` SKU 探针成功，`skuCount=1`
- [x] 首轮 smoke：商品本地状态为 `LINKED`，`selectedToLibrary=true`
- [ ] 继续补证：更多活动 / 商品样本稳定性

### P3-2 转链与归因链路

- [x] 首轮 smoke：一键转链成功
- [x] 首轮 smoke：提取 `pickSource=v.MxZLIw`
- [x] 首轮 smoke：`pick_source_mapping` 命中 `mappingCount=1`
- [x] 首轮 smoke：已转链商品重复转链后状态保持 `LINKED / READY`
- [ ] 继续补证：真实订单携带归因字段后的归因闭环

### P3-3 订单同步与 Dashboard 对账

- [x] 首轮 smoke：订单同步任务成功执行，`totalFetched=100 / created=4 / updated=96 / failed=0`
- [x] 首轮 smoke：Dashboard 核心指标接口成功返回，`todayOrderCount=223 / todayGmv=4284.97`
- [ ] 继续补证：无订单、少量订单、有归因订单、无归因订单的展示
- [ ] 继续补证：Dashboard 指标与数据库口径一致

### P3-4 前端页面验收

- [x] `/system/douyin`
- [x] Dashboard
- [x] 商品库
- [x] 订单页
- [x] 数据平台

## P3-5 全角色账号业务流程验收

当前状态：已通过，可冻结（2026-05-17）

目标：

验证管理员、招商组长、招商、渠道组长、渠道、运营六类账号的菜单权限、数据范围、核心业务动作和越权拦截。

验收范围：

- 管理员：系统配置、员工权限、全局数据、抖音配置页
- 招商组长：活动同步、商品同步、商品分配、组内数据
- 招商：商品审核、寄样审核、自己负责数据
- 渠道：商品库、达人 CRM、寄样申请、真实转链、`pick_source` 映射
- 运营：物流录入、寄样发货状态推进
- 渠道组长：组内渠道数据、组内 Dashboard

通过标准：

- 所有账号登录成功
- 所有角色菜单符合权限
- 所有核心页面无 401/403/500
- 关键业务动作状态变化正确
- 越权页面访问被拦截
- 数据范围符合角色定义

执行入口：

- `npm run e2e:real-pre:roles`
- 证据目录：`runtime/qa/out/real-pre-role-business-e2e-20260517-101101`

执行结果：

- Playwright：`2/2 PASS`
- 验收步骤：`11/11 PASS`
- SampleControllerTest：`29/29 PASS`
- `git diff --check`：无空白错误
- 环境守卫：`activeProfiles=["real-pre"]`、`environmentLabel=REAL-PRE`、`APP_TEST_ENABLED=false`、`DOUYIN_TEST_ENABLED=false`
- 业务闭环：招商组长同步并分配商品 -> 招商审核商品上架 / 拒绝样本商品 -> 渠道转链并命中 `pick_source_mapping` -> 渠道申请寄样 -> 招商审核寄样 -> 运营发货并推进至 `PENDING_TASK` -> 管理员复核日志、订单同步和 Dashboard
- 关键证据：`pickSource=v.MxZLIw`、`mappingCount=1`、`sampleRequestNo=SM20260517661BF393`、`sampleFinalStatus=PENDING_TASK`
- 订单同步：`totalFetched=100 / created=7 / updated=93 / failed=0`
- 越权与退出：各角色越权页面被拦截；六个账号退出后再次访问受保护接口均返回 `401`

本轮最有价值的修正：

- 管理员 `/system/douyin` 配置页一键刷新已纳入角色 E2E，不再只是页面可打开
- 寄样申请支持从 `product_snapshot` 物化商品，补上“快照已上架但本地 product 缺失”断点
- 招商审核寄样按“商品分配给自己”放行，口径与真实业务职责一致
- mapper 已改为通过 `product.product_id` 做分配关联，降低真实抖音商品 ID、本地商品记录与快照商品 ID 体系错位风险
