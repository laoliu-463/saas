# 17-项目剩余事项看板

更新时间：2026-05-16

## 一、当前状态

- TEST/mock 阶段已完成收口。
- `runtime/qa/out/p1-warning-risk-regression-20260516-214324-277` 已落盘，`8/8 PASS`。
- `mvn "-Dtest=PickSourceMappingServiceTest,ProductServiceTest" test` 已通过。
- `node runtime/qa/p1-warning-risk-regression.test.cjs` 已通过。
- real-pre 只读 preflight 已执行，但当前未通过。
- 本轮未切 real-pre、未访问真实三方接口、未清库。

## 二、当前 Blocking

| 优先级 | 项目 | 当前状态 | 证据 | 下一步 |
| --- | --- | --- | --- | --- |
| P0 | real-pre 抖店 SDK 凭据 | BLOCKED | `runtime/qa/out/qa-real-pre-preflight-20260516-215205-426/blocking-report.md` | 在本机补齐 `.env.real-pre` 的 `DOUYIN_APP_ID`、`DOUYIN_CLIENT_KEY`、`DOUYIN_CLIENT_SECRET` 后重跑 preflight |

当前 preflight 结论：

- blocking check：`real_sdk_config_present`
- 缺失字段：`DOUYIN_APP_ID`
- 缺失字段：`DOUYIN_CLIENT_KEY`
- 缺失字段：`DOUYIN_CLIENT_SECRET`
- 运行态检查为 `SKIP`，原因是本轮未启动 real-pre 运行栈；这不是额外阻塞项

## 三、已完成但需保留证据的事项

| 项目 | 状态 | 证据 |
| --- | --- | --- |
| P1 warning 风险专项 | PASS (`8/8`) | `runtime/qa/out/p1-warning-risk-regression-20260516-214324-277/report.md` |
| PickSourceMapping / ProductService 定向单测 | PASS | `mvn "-Dtest=PickSourceMappingServiceTest,ProductServiceTest" test` |
| p1-warning-risk-regression 单测 | PASS | `node runtime/qa/p1-warning-risk-regression.test.cjs` |

## 四、下一步

1. 从 `.env.real-pre.example` 复制生成本机 `.env.real-pre`。
2. 仅在本机补齐真实 `DOUYIN_APP_ID`、`DOUYIN_CLIENT_KEY`、`DOUYIN_CLIENT_SECRET`。
3. 保持 `APP_TEST_ENABLED=false`、`DOUYIN_TEST_ENABLED=false`。
4. 重跑 `powershell -ExecutionPolicy Bypass -File .\scripts\qa-real-pre-preflight.ps1 -EnvFile .\.env.real-pre`。
5. 只有 preflight 通过后，才进入 real-pre 准入判断。
