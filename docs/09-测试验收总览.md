# 测试验收总览

> 范围标记：`V1 必做`、`V1 简化`、`V1 不做`、`V2 预留`、`历史归档`。

## 验收原则

- [V1 必做] 每个验收项必须对应脚本、SQL/API 或证据路径。
- [V1 必做] test / mock 证据只能证明本地闭环，不能替代 real-pre。
- [V1 必做] real-pre 验收必须有真实 Token、真实接口响应和真实环境日志。
- [V1 必做] 文档重构验收必须证明文件结构、范围标记、归档索引、冲突清单完整。

## 脚本入口

| 命令 | 用途 | 证据文档 | 范围 |
| --- | --- | --- | --- |
| `npm run e2e:v1-p0` | V1 P0 浏览器验收 | [验收/V1-P0验收清单.md](验收/V1-P0验收清单.md) | V1 必做 |
| `npm run e2e:real-pre:p0:preflight` | real-pre 预检 | [验收/real-pre联调手册.md](验收/real-pre联调手册.md) | V1 必做 |
| `npm run e2e:real-pre:p0` | real-pre P0 联调验收 | [验收/real-pre联调手册.md](验收/real-pre联调手册.md) | V1 必做 |
| `npm run test:all` | 前端 / E2E 聚合测试入口 | [验收/回归测试脚本索引.md](验收/回归测试脚本索引.md) | V1 简化 |
| `cd backend; mvn test` | 后端单元 / 集成测试 | [验收/回归测试脚本索引.md](验收/回归测试脚本索引.md) | V1 必做 |
| `cd frontend; npm run build` | 前端构建验证 | [验收/回归测试脚本索引.md](验收/回归测试脚本索引.md) | V1 必做 |

## 经营指标矩阵验收补充（2026-06-05）

- [V1 必做] 数据看板必须展示 9 个经营指标：总订单数、订单额、服务费收入、技术服务费、服务费支出、服务费收益、招商提成、媒介/渠道提成、毛利。
- [V1 必做] 总订单数和订单额使用“成交 / 结算”两行；其余金额指标使用“预估 / 结算”两行。
- [V1 必做] 服务费收入和收益验收必须分别检查两轨公式：预估服务费收入 = 预估订单额 × 服务费率（未扣除技术服务费），结算服务费收入 = 结算订单额 × 服务费率 - 技术服务费；预估服务费收益 = 预估服务费收入 - 预估服务费支出 - 技术服务费；结算服务费收益 = 结算服务费收入 - 结算服务费支出。
- [V1 必做] 验证方式包括：`/dashboard/metrics` 响应、前端单测、页面 smoke；如涉及 real-pre 结论，还需 API/SQL 对账证据。

## 活动分配与推广中入库验收补充

- [V1 必做] 活动分配后端回归：`cd backend; mvn "-Dtest=ColonelActivityControllerTest,ProductDisplayRuleServiceTest,ProductServiceActivityAssignTest" test`。
- [V1 必做] 活动分配前端回归：`cd frontend; npm run test -- activity.test.ts activity-list-display.test.ts product-operation-log-display.test.ts product-assignee-options.test.ts`。
- [V1 必做] 验证点包括：仅管理员调用活动分配接口、候选人来自招商角色、活动列表分配字段回填、分配后短缓存驱逐、推广中活动商品不被同 `product_id` 去重隐藏。

## 主数据与当前用户验收补充（2026-06-02 / t0-master-data）

- [V1 必做] 主数据下拉后端回归：`cd backend; mvn "-Dtest=UserMasterDataServiceTest,ColonelPartnerMasterDataServiceTest,ColonelPartnerMasterDataControllerTest,UserMasterDataControllerTest,CurrentUserControllerTest" test`。
- [V1 必做] 主数据下拉前端单测：`cd frontend; npx vitest run src/api/sys.test.ts src/views/product/product-assignee-options.test.ts src/views/orders/order-user-filter-options.test.ts src/views/sample/sample-user-filter-options.test.ts`。
- [V1 必做] 验证点包括：
  - `/users/master-data/channels|recruiters` 关键词命中 username/realName 模糊匹配；`limit` 50/100 默认与最大；空数据返回空集合；非 admin 用户的 `group-members` 不会因 caller 传 `deptId` 而越权（service 强制回退到当前 deptId）。
  - `/users/current` 响应 `dataScope` 数字与 `dataScopeName` 字符串必须一致；`/users/current/data-scope` 的 `userIds` 在 `all` 范围返回空。
  - `/api/colonel-partners` 列表 SQL 拼接正确（`like + eq + and(isNotNull...or)` 三条件组合），详情缺失返回 404；`/api/colonel-partners/sources` 返回 distinct 来源。
  - 主数据接口的 controller 鉴权注解（`@RequireRoles`）覆盖；service 层不做鉴权（不自动加 caller 角色过滤）。
- [V1 必做] ⚠ `/api/colonel-partners` 当前未在前端接入：前端 `listPartners` 走的是 `/colonel/partners`（`ColonelPartnerController` 暴露，返回 `PartnerVO`）。两者职责不同，**当前团长主数据接口的 V1 consumer 待定**，详见 [05-API契约总表.md](05-API契约总表.md) 的"主数据与当前用户 API 补充事实"段。

## 佣金规则与规则中心验收补充（2026-06-02 / t6-commission）

- [V1 必做] 佣金规则与规则中心后端回归：`cd backend; mvn "-Dtest=CommissionRuleServiceTest,CommissionRuleControllerTest,RuleCenterServiceTest,RuleCenterControllerTest" test`（4 类，合计 40 case）。Plan 任务 T8 显式列出的最小子集为 `CommissionRuleServiceTest,RuleCenterServiceTest`。
- [V1 必做] 前端构建验证：`cd frontend; npm run build`（确保 `CommissionRuleList.vue` 新增 status / 生效区间 pickers 编译通过）。
- [V1 必做] 验证点包括：
  - `GET /commission-rules` 同时支持 5 个筛选项（`dimensionType` / `commissionType` / `status` / `effectiveStart` / `effectiveEnd`）AND 组合；`status` 非法值（既非 0 也非 1）按"不筛选"处理不抛错；`effectiveStart > effectiveEnd` 抛 `BusinessException.param`。
  - 生效区间"区间重叠"判定：规则 `effective_end IS NULL` 视为长期有效，永远与查询区间相交；规则 `effective_start IS NULL` 视为即时生效，永远与查询区间相交。
  - 规则中心 7 个端点（`/rule-center` 全集）均委托 service，Controller 不消费 `dataScope`；`enabled=false` 的 V2 预留项前端 `:disabled` 且后端 validate 拒绝修改。
  - `RuleCenterControllerTest.controller_shouldNotConsumeDataScopeAttribute` 显式断言 dataScope 透传不影响返回。
- [V1 必做] **dataScope 在规则中心 / 佣金规则不应用**：配置域全局可见，admin dataScope=ALL 隐式覆盖；变更历史与事件消费返回全量，详见 [领域/配置域.md](领域/配置域.md) 的 `dataScope 在配置域（含规则中心 / 佣金规则）不应用` 段与 [05-API契约总表.md](05-API契约总表.md) 的"佣金规则与规则中心 API 补充事实"段。

## 证据索引

- [V1 必做] P0 映射见 [验收/V1-P0验收清单.md](验收/V1-P0验收清单.md) 与 `.claude/qa/P0验收映射.md`。
- [V1 必做] E2E 映射见 [验收/E2E浏览器测试手册.md](验收/E2E浏览器测试手册.md) 与 `.claude/qa/E2E映射.md`。
- [V1 必做] real-pre 证据见 [验收/real-pre联调手册.md](验收/real-pre联调手册.md) 与 [验收/验收证据索引.md](验收/验收证据索引.md)。
- [V1 必做] 覆盖率口径见 `.claude/qa/覆盖率映射.md`。

## 状态分类

| 状态 | 含义 | 是否可收口 | 范围 |
| --- | --- | --- | --- |
| PASS | 验收通过且证据完整 | 是 | V1 必做 |
| PASS_NEEDS_CLEANUP | 主链路通过，但有日志、数据或文档清理项 | 条件通过 | V1 简化 |
| BLOCKED | 受外部 Token、权限包、真实样本等阻塞 | 否，需证据 | V1 必做 |
| PENDING | 未执行或证据不足 | 否 | V1 必做 |
| FAIL | 已复现失败 | 否，需缺陷单 | V1 必做 |
