# 验收证据索引

> 范围标记：`V1 必做`、`V1 简化`、`V1 不做`、`V2 预留`、`历史归档`。

## 使用规则

- [V1 必做] 每次验收必须登记环境、命令、时间、账号角色、脚本结果、API/SQL 证据和日志路径。
- [V1 必做] real-pre 证据必须明确真实接口响应，不得用 mock 代替。
- [V1 必做] BLOCKED 也必须登记证据，不能只写“等外部”。

## 证据登记表

| 验收项 | 环境 | 命令 / 操作 | API / SQL | 日志 / 报告路径 | 状态 | 范围 |
| --- | --- | --- | --- | --- | --- | --- |
| 后端回归 | test | `mvn -f backend test` | N/A | Maven 输出，1591 tests / 0 failures / 0 errors | PASS | V1 必做 |
| 前端回归 | local | `npm --prefix frontend run test -- --run` | N/A | Vitest 输出，52 files / 213 tests passed | PASS | V1 必做 |
| 前端构建 | local | `npm --prefix frontend run build` | N/A | Vite build 输出 | PASS | V1 必做 |
| real-pre 预检 | real-pre | `npm run e2e:real-pre:p0:preflight` | `/api/system/health`、real-pre env guard、Token readiness、schema readiness | `runtime/qa/out/real-pre-preflight-20260526-113128/` | PENDING_PICK_SOURCE | V1 必做 |
| 登录与权限 | test / real-pre | `npm run e2e:v1-p0` 或 real-pre 脚本 | `/api/auth/me`、角色 API | `playwright-report/` | PENDING | V1 必做 |
| 商品同步 | real-pre | 同步操作或 E2E | 商品 API、商品表 | 后端日志、第三方响应 | PENDING | V1 必做 |
| 转链映射 | real-pre | 商品转链操作 | `pick_source_mapping` | 第三方响应、SQL | PENDING | V1 必做 |
| 订单同步 | real-pre | 订单同步操作 | 订单表、订单 API | 同步日志、第三方响应 | PENDING | V1 必做 |
| 寄样交作业 | test / real-pre | 寄样 E2E | 寄样状态、订单事件 | Playwright、后端日志 | PENDING | V1 必做 |
| 业绩计算 | test / real-pre | 业绩 API / E2E | 业绩明细、汇总表 | 计算日志 | PENDING | V1 必做 |
| Dashboard | test / real-pre | dashboard E2E | dashboard API、汇总 SQL | 截图、Network | PENDING | V1 必做 |
| 文档结构 | local | 结构检查命令 | N/A | shell 输出 | PENDING | V1 必做 |

## 历史证据入口

- [历史归档] `docs/归档/旧版V2.2完整方案/11-real-pre证据索引.md`
- [历史归档] `docs/archive/records/20-2026-05-08-新授权码三方全流程联调报告.md`
- [历史归档] `docs/archive/records/25-20260504-全系统可视化测试记录.md`
- [历史归档] `README-e2e.md`

## 待补规则

- [V1 必做] 本页是登记入口，不能替代真实日志或报告。
- [V1 必做] 后续每次验收应把状态从 PENDING 更新为 PASS / FAIL / BLOCKED / PASS_NEEDS_CLEANUP。

## 2026-05-26 阶段性证据

- [V1 必做] 修复 `DataControllerTest` 中 `getOrderSummary` 直接调用缺少 `channelName` 参数的问题；目标用例 `mvn -f backend -Dtest=DataControllerTest test` 通过，31 tests / 0 failures / 0 errors。
- [V1 必做] 后端全量回归 `mvn -f backend test` 通过，1591 tests / 0 failures / 0 errors。
- [V1 必做] 修复 `OrderList.vue` 中 `OrderListFilters.channelName` 初始化 / 重置缺失的问题；订单相关前端测试 `OrderList.test.ts`、`order-list-query.test.ts`、`data.test.ts` 通过，3 files / 8 tests passed。
- [V1 必做] 前端构建 `npm --prefix frontend run build` 通过；存在 Vite chunk size warning，属于构建体积提示，不阻塞当前验证。
- [V1 必做] 前端全量单测 `npm --prefix frontend run test -- --run` 通过，52 files / 213 tests passed。
- [V1 必做] real-pre 预检 `npm run e2e:real-pre:p0:preflight` 生成证据 `runtime/qa/out/real-pre-preflight-20260526-113128/`；前端 3001、后端 8081、环境守卫、admin login、抖音 Token、schema readiness 均 PASS，但 `reusable promotion mapping` 为 `PENDING_PICK_SOURCE`，因此 `canRunBusinessFlows=false`，不能把 real-pre 全业务流记为通过。
- [V1 必做] real-pre 只读 SQL 复核：`valid_mapping_count=0`；`pick_source_mapping` 总数 8，均为 active 且有 `pick_source`，但 `converted_url=0`、`promotion_link=0`。最近 8 条均为 `colonel_native_{colonel_buyin_id}` 种子，`activity_id`、`product_id`、`promotion_link_id`、`converted_url` 为空，不能作为商品转链闭环证据。
- [V1 必做] real-pre 数据样本复核：`product=0`、`product_snapshot=784`、`colonel_activity=0`、`colonel_activity_product=0`、`promotion_link=0`。阶段性结论为缺真实商品/活动/推广链接闭环样本，不是环境守卫或 Token 失败。
