# 18-管理员账户系统功能排查记录

创建时间：2026-05-15
测试环境：test（`saas-active` 单活 Docker 容器，3000/8080）
测试账户：admin / admin123

## 当前结论（2026-05-15 12:52 后）

本记录中较早的“401 噪音”和“`/system` 落到 `/data`”描述属于修复前观察。当前有效状态如下：

- 管理员访问 `/system`：✅ 自动落到 `/system/users`
- 非管理员访问 `/system`：✅ 仍会被重定向到有权限首页
- 匿名访问 `/api/system/env`：✅ 返回 200
- 页面 `/api/system/env` 401 Console / Network 噪音：✅ 已消除
- `scripts/qa-api-smoke.ps1`：✅ 通过
- `scripts/qa-page-smoke.ps1`（admin 默认路由）：✅ 通过
- `scripts/qa-page-smoke.ps1`（biz_leader 业务路由）：✅ 通过

## 环境治理记录（2026-05-15）

为避免 test 和 real-pre 容器混用导致排查结果不可信，当前本机环境已收口为单活模式：

- 统一入口：`docker-compose.yml`
- test 启动：`scripts/start-test.ps1`
- real-pre 启动：`scripts/start-real-pre.ps1`
- 停止全部：`scripts/stop-all.ps1`
- 状态检查：`scripts/status.ps1`
- 固定 compose project name：`saas-active`
- 固定容器名：`saas-frontend`、`saas-backend`、`saas-postgres`、`saas-redis`
- 启动任一环境前，start 脚本都会先执行 `stop-all.ps1`
- 旧 `docker-compose.test.yml`、`docker-compose.real-pre.yml` 已保留为 `.bak`，不再作为排查入口

环境职责：

| 环境 | Profile | Mock 开关 | 数据库 | 用途 |
|---|---|---|---|---|
| test | `test` | `APP_TEST_ENABLED=true`、`DOUYIN_TEST_ENABLED=true` | `saas_test` | 系统功能、管理员和招商组长权限测试默认环境 |
| real-pre | `real-pre` | `APP_TEST_ENABLED=false`、`DOUYIN_TEST_ENABLED=false` | `saas_real_pre` | 仅真实 SDK 联调 |

验收补充：

- `docker ps` 不应同时出现 test 和 real-pre 两套 backend/frontend
- 浏览器前端页头显示 `环境：TEST` 或 `环境：REAL-PRE`
- 后端启动日志必须出现 `SAAS environment | activeProfiles=... | app.test.enabled=... | douyin.test.enabled=... | db.name=...`
- 本文档后续管理员和招商组长权限测试只在 test 环境继续记录

## 测试顺序

1. 登录与导航
2. 系统管理：员工、角色、规则、日志
3. 商品与活动：活动绑定、商品同步、分配、审核、商品库展示、转链
4. 达人 CRM：新增、搜索、认领、刷新、兜底录入
5. 寄样台：申请、审核、发货、签收、完成、拒绝、导出
6. 订单与数据平台：订单列表、筛选、详情、归因、dashboard、导出
7. 权限验证：管理员、招商组长、招商、渠道组长、渠道、运营
8. 性能验证：记录每个核心页面最慢接口和重复请求情况

## 环境基线

| 检查项 | 结果 | 备注 |
|---|---|---|
| 后端 8080 健康检查 | ✅ UP | `/api/actuator/health` 返回 200 |
| 前端 3000 容器状态 | ✅ | Docker `saas-frontend` healthy（2026-05-15 `qa-env`） |
| 后端 8080 容器状态 | ✅ | Docker `saas-backend` healthy |
| PostgreSQL | ✅ | Docker `saas-postgres:5432`，库名与 Bearer `system/env` 中 `saas_test` 一致 |
| Redis | ✅ | Docker `saas-redis:6379` healthy |

---

## TEST/mock 数据覆盖审计收口（2026-05-16）

阶段结论：`P1-TEST/mock 主流程数据覆盖收口完成`。

本轮 mock 数据硬缺口已清零，主流程覆盖通过。当前覆盖率 `84.58%`，`overallPass=true`，`api-errors=[]`。剩余 `missing-scenarios` 均为 warning 型专项样本，暂不阻塞 TEST/mock 主流程收口，但需进入后续专项回归或 real-pre 前置风险清单。

本轮继续在 TEST/mock 环境内收口数据覆盖，未切换 `real-pre`，未访问真实三方接口，未执行清库操作。

### 本轮调整

- 后端 TEST seed 补齐商品审核通过 / 拒绝、私海与保护期达人、待审核 / 快递中寄样、商品未覆盖订单、退款关闭订单、今日数据、停用账号样本。
- 审计脚本补齐对 API 可见字段的识别：商品 `auditStatus=2/3`、达人 `protectedUntil`、订单 `attributionRemark`、结算佣金字段、数据页达人 / 活动字段、系统用户 `status=0`。
- 空日期范围从 `startTime/endTime` 修正为数据平台实际使用的 `startDate/endDate`。

### 验证结果

| 命令 | 结果 |
|---|---|
| `node --check runtime/qa/mock-data-audit.cjs` | ✅ |
| `node --check runtime/qa/mock-data-audit.test.cjs` | ✅ |
| `node runtime/qa/mock-data-audit.test.cjs` | ✅ 6/6 |
| `cd backend && mvn -DskipTests compile` | ✅ BUILD SUCCESS |
| `scripts/qa-mock-data-audit.ps1` | ✅ Exit code 0 |

固定回归入口：

- `scripts/qa-test-mock-final.ps1`
- 顺序执行 `node --check`、mock audit 单测、后端 `mvn -DskipTests compile` 与 `scripts/qa-mock-data-audit.ps1`

本轮基线报告：

- `runtime/qa/out/qa-mock-data-audit-20260516-194040-956/summary.json`（最新）
- `runtime/qa/out/qa-mock-data-audit-20260516-193210-553/report.md`（同日较早，两轮结论一致）

关键结果：

| 项 | 结果 |
|---|---|
| 环境 | TEST，`activeProfiles=test`，`database=saas_test` |
| health | UP |
| API 错误 | 0 |
| `overallPass` | `true` |
| 总覆盖率 | 84.58% |
| 硬缺口 | 0 |
| `api-errors.json` | `[]` |

硬覆盖已全部通过；剩余为 warning 型专项样本，主要包括投流支持 / 不支持、寄样要求 / 不要求、转链失败、爬虫成功 / 失败兜底、多人认领、订单歧义映射、独家达人 / 商家、无订单日期等，不阻塞本轮 TEST/mock 主流程覆盖收口。分级清单见 `docs/19-TEST-mock-warning专项样本清单.md`。

后续进入：

1. 状态流转自动化回归
2. 角色业务操作 smoke
3. Dashboard 聚合对账
4. real-pre 前置 P1 warning 补齐

## 五阶段回归补录（2026-05-15 12:12 - 12:30，仅 test）

本轮严格按要求仅使用 test 环境，不切 real-pre，不对未确认问题做代码修复。

### 阶段一：环境验收

执行顺序：

1. `scripts/stop-all.ps1`
2. `scripts/start-test.ps1`
3. `scripts/status.ps1`

验收结果：

| 检查项 | 结果 | 说明 |
|---|---|---|
| 停止全部环境 | ✅ | `stop-all.ps1` 执行完成，历史 `saas-*` 容器已清理 |
| 启动 test 环境 | ⚠️ | `start-test.ps1` 首次执行后，`saas-backend`、`saas-frontend` 停在 `Created`，命令输出出现 `dependency failed to start: Error response from daemon: No such container: ...` |
| 当前状态 | ✅ | 补执行 `docker compose --env-file .env.test --project-name saas-active -f docker-compose.yml up -d` 后恢复正常 |
| 仅一套 `saas-active` 容器 | ✅ | `saas-frontend`、`saas-backend`、`saas-postgres`、`saas-redis` 四容器单活 |
| `/api/actuator/health` | ✅ | HTTP 200，`status=UP` |
| `/api/system/env` | ✅ | 管理员 Bearer 调用返回 `environmentLabel=TEST`、`database=saas_test`、`activeProfiles=["test"]` |
| 前端页头环境标识 | ✅ | Playwright 登录后读取到 `环境：TEST` |

阶段一报告：

- `runtime/qa/out/qa-env-20260515-122125/report.md`
- `runtime/qa/out/qa-env-20260515-122125/summary.json`

### 阶段二：QA 脚本

本轮检查并补齐如下脚本：

| 脚本 | 状态 | 输出约定 |
|---|---|---|
| `scripts/qa-env.ps1` | 已修正并验证通过 | `runtime/qa/out/qa-env-{timestamp}/summary.json` + `report.md` |
| `scripts/qa-api-smoke.ps1` | 已新增并验证通过 | `runtime/qa/out/qa-api-smoke-{timestamp}/summary.json` + `report.md` |
| `scripts/qa-page-smoke.ps1` | 已新增并验证通过 | `runtime/qa/out/qa-page-smoke-{timestamp}/summary.json` + `report.md` |

已生成报告：

- `runtime/qa/out/qa-api-smoke-20260515-122013/report.md`
- `runtime/qa/out/qa-api-smoke-20260515-122013/summary.json`
- `runtime/qa/out/qa-page-smoke-20260515-122400-166/report.md`
- `runtime/qa/out/qa-page-smoke-20260515-122400-166/summary.json`
- `runtime/qa/out/qa-page-smoke-20260515-122537-986/report.md`
- `runtime/qa/out/qa-page-smoke-20260515-122537-986/summary.json`

补充说明：

- `qa-page-smoke.ps1` 时间戳已提升到毫秒，避免同秒并发执行时输出目录冲突。

### 阶段三：管理员回归（admin / admin123）

管理员登录成功，`authStore.roleCodes = ["admin"]`。

| 页面 | 是否可打开 | 是否报错 | 是否有 500 | 是否 loading 卡死 | 备注 |
|---|---|---|---|---|---|
| `/dashboard` | ✅ | ⚠️ | 否 | 否 | 页面可打开；存在 `GET /api/system/env` 401 噪音 |
| `/product/library` | ✅ | ⚠️ | 否 | 否 | 页面可打开；当前数据为空 |
| `/product/activity` | ✅ | ⚠️ | 否 | 否 | 路由重定向到 `/product/manage` 后正常打开 |
| `/orders` | ✅ | ⚠️ | 否 | 否 | 页面可打开；当前数据为空 |
| `/talent` | ✅ | ⚠️ | 否 | 否 | 页面可打开 |
| `/sample` | ✅ | ⚠️ | 否 | 否 | 页面可打开 |
| `/system` | ❌ | 否 | 否 | 否 | 直接访问落到 `/data`，不是系统管理落地页 |
| `/system/users` | ✅ | ⚠️ | 否 | 否 | 系统管理实际入口正常可打开 |

管理员回归结论：

- 真正不可打开的是 `/system` 根路由，不是系统管理功能本身。
- 其余页面均可打开，未见 500，也未见 loading 卡死。
- 所有已访问页面都有同源的 `GET /api/system/env` 401 Network/Console 噪音。

### 阶段四：招商组长权限回归（biz_leader / admin123）

招商组长登录成功，登录返回：

- `roleCodes=["biz_leader"]`
- `dataScope=2`
- `deptId=11111111-1111-1111-1111-111111111111`

界面可见性：

- 可见顶部导航：`数据看板`、`商品库`、`商品管理`、`寄样审核`
- 不可见：`达人 CRM`、`系统管理`
- `/product/activity` 页面可见 `活动列表`、`商品列表`，并存在 `进入工作台` 入口，可视为已看到活动管理/商品分配入口

直达路由检查：

| 页面 | 菜单可见性 | 路由可达性 | 接口状态判断 | 页面结果 |
|---|---|---|---|---|
| `/dashboard` | 可见 | ✅ | 无 403；页面可渲染 | 可打开，指标为 0 |
| `/product/library` | 可见 | ✅ | 无 403；HTTP 200 | 可打开，数据为空 |
| `/product/activity` | 可见 | ✅ | 无 403；HTTP 200 | 重定向到 `/product/manage`，活动列表有数据，存在 `进入工作台` 入口 |
| `/orders` | 可见 | ✅ | 无 403；HTTP 200 | 可打开，数据为空 |

招商组长权限回归结论：

- 本轮未复现“菜单不可见、路由不可达、接口 403”的权限问题。
- `Sider.vue`、`router meta.roles`、`authStore.roleCodes` 这条权限链对本轮 4 个目标页面是通的。
- 本轮异常不在招商组长权限本身，而在公共环境接口噪音和管理员 `/system` 根路由。
- 当前 `userInfo` 中未看到 `teamId/groupId` 字段，但本轮 4 个页面未因此触发 403；数据为空的页面更接近测试种子覆盖不足，而不是权限拒绝。

### 阶段五：问题记录

#### TST-20260515-01

- 问题编号：`TST-20260515-01`
- 测试账号：无
- 页面：环境启动
- 操作步骤：
  1. 执行 `scripts/stop-all.ps1`
  2. 执行 `scripts/start-test.ps1`
- 期望结果：test 环境一次启动成功，四个 `saas-active` 容器全部进入 `Up/healthy`
- 实际结果：`saas-backend`、`saas-frontend` 首次停在 `Created`，需再执行一次 `docker compose ... up -d` 才恢复
- Network 状态：不适用
- Console 报错：`dependency failed to start: Error response from daemon: No such container: ...`
- 后端日志：无，后端首次未真正启动
- 初步原因：Docker Compose 启动阶段存在依赖竞态，`start-test.ps1` 对首次失败没有补偿重试
- 等级 `P1`
- 状态：已修复

#### TST-20260515-02

- 问题编号：`TST-20260515-02`
- 测试账号：admin / biz_leader / 匿名
- 页面：`/dashboard`、`/product/library`、`/product/activity`、`/orders`、`/talent`、`/sample`、`/system/users`
- 操作步骤：
  1. 登录页面
  2. 进入任一业务页面
  3. 观察 Network 与 Console
- 期望结果：前端读取环境标识时不应产生 401 噪音
- 实际结果：页面每次都会触发 `GET /api/system/env`，返回 401；Console 出现 `Failed to load resource: the server responded with a status of 401 (Unauthorized)`
- Network 状态：`GET /api/system/env -> 401`
- Console 报错：`Failed to load resource: the server responded with a status of 401 (Unauthorized)`
- 后端日志：未产生对应异常堆栈；`docker logs saas-backend --since 20m` 未看到该请求的 500/403 栈
- 初步原因：
  - 前端 [Header.vue](/D:/Projects/SAAS/frontend/src/views/layout/Header.vue:74) 在挂载时直接 `fetch('/api/system/env')`
  - 后端 [SecurityConfig.java](/D:/Projects/SAAS/backend/src/main/java/com/colonel/saas/config/SecurityConfig.java:30) 与 [JwtAuthenticationFilter.java](/D:/Projects/SAAS/backend/src/main/java/com/colonel/saas/security/JwtAuthenticationFilter.java:25) 放行的是 `/system/env`
  - 实际请求路径带 `/api` 前缀，导致放行规则未命中
- 等级 `P2`
- 状态：已修复

#### TST-20260515-03

- 问题编号：`TST-20260515-03`
- 测试账号：admin
- 页面：`/system`
- 操作步骤：
  1. 管理员登录
  2. 浏览器直接访问 `/system`
- 期望结果：进入系统管理默认落地页，或显示明确的系统首页
- 实际结果：前端路由落到 `/data`，未进入系统管理
- Network 状态：无 500 / 无 403；仅伴随 `GET /api/system/env -> 401`
- Console 报错：`Failed to load resource: the server responded with a status of 401 (Unauthorized)`（同环境接口噪音）
- 后端日志：无相关异常；属于前端路由行为
- 初步原因：
  - [router/index.ts](/D:/Projects/SAAS/frontend/src/router/index.ts:16) 只定义了 `/system/users`、`/system/roles`、`/system/config`、`/system/douyin`、`/system/operation-logs`
  - 未定义 `/system` 根路由，最终命中兜底 `/:pathMatch(.*)* -> /data`
- 等级 `P2`
- 状态：待确认

### 追加修复记录（2026-05-15 12:46 - 12:52）：`/api/system/env` 401 噪音

修复目标：

1. 匿名访问 `/api/system/env` 返回 200
2. 仅返回安全环境信息，不暴露密钥、token 或数据库密码
3. 页面访问不再产生 `/api/system/env` 401 噪音
4. `qa-api-smoke`、`qa-page-smoke` 将该项纳入通过条件

代码调整：

- [SystemEnvController.java](/D:/Projects/SAAS/backend/src/main/java/com/colonel/saas/controller/SystemEnvController.java) 保持只返回：
  - `activeProfiles`
  - `environmentLabel`
  - `appTestEnabled`
  - `douyinTestEnabled`
  - `database`
- [SecurityConfig.java](/D:/Projects/SAAS/backend/src/main/java/com/colonel/saas/config/SecurityConfig.java) 仅追加放行 `/api/system/env`
- [JwtAuthenticationFilter.java](/D:/Projects/SAAS/backend/src/main/java/com/colonel/saas/security/JwtAuthenticationFilter.java) 仅追加公共前缀 `/api/system/env`
- [WebConfig.java](/D:/Projects/SAAS/backend/src/main/java/com/colonel/saas/config/WebConfig.java) 将 `/api/system/env` 加入 JWT 拦截器排除名单
- [runtime/qa/api-smoke.cjs](/D:/Projects/SAAS/runtime/qa/api-smoke.cjs) 将匿名环境接口期望从 `401` 改为 `200 + code=200 + environmentLabel=TEST`
- [runtime/qa/page-smoke.cjs](/D:/Projects/SAAS/runtime/qa/page-smoke.cjs) 新增 `hasEnv401` 判定；若任一路由仍出现 `/api/system/env` 401，则 smoke 失败

验收结果：

| 检查项 | 结果 | 说明 |
|---|---|---|
| 匿名访问 `/api/system/env` | ✅ | `curl http://127.0.0.1:8080/api/system/env` 返回 HTTP 200 |
| 返回内容安全 | ✅ | 仅见环境标签、profile、test 开关、数据库名；未返回 secret / token / password |
| `qa-api-smoke.ps1` | ✅ | `runtime/qa/out/qa-api-smoke-20260515-124718-553/summary.json`，`overallPass=true` |
| 管理员 `qa-page-smoke.ps1` | ✅ | `runtime/qa/out/qa-page-smoke-20260515-124955-497/summary.json`，全部路由 `hasEnv401=false`，`overallPass=true` |
| 招商组长业务页 `qa-page-smoke.ps1` | ✅ | `runtime/qa/out/qa-page-smoke-20260515-124955-515/summary.json`，全部业务路由 `hasEnv401=false`，`overallPass=true` |
| 非管理员系统管理访问限制 | ✅ | 仍保持；`biz_leader` 访问 `/system` 继续跳回 `/dashboard` |

说明：

- 当前 test compose 后端端口实际绑定为 `8080`，因此本轮匿名验收按 `http://127.0.0.1:8080/api/system/env` 执行。
- 本次未放开 `/api/system/**`，只精确放行了 `/api/system/env`，未改动任何业务权限规则。
- 旧问题单 `TST-20260515-02` 现已修复，可关闭。

### 追加复验记录（2026-05-15 14:18）：`/api/system/env` 401 噪音收口确认

本轮按当前 test 单活基线再次复验，确认这项问题在当前代码和当前环境下已经稳定关闭，无需继续改代码。

复验说明：

- 用户最新口径中使用了 `http://localhost:8081/api/system/env`
- 但当前项目 test 单活固定端口仍为 `3000/8080`
- 因此本轮实际按 `http://127.0.0.1:8080/api/system/env` 验收

复验结果：

| 检查项 | 结果 | 证据 |
|---|---|---|
| 匿名访问 `/api/system/env` | ✅ 200 | 返回 `code=200`、`environmentLabel=TEST`、`database=saas_test` |
| 返回内容安全 | ✅ | 仅包含 `activeProfiles`、`environmentLabel`、`appTestEnabled`、`douyinTestEnabled`、`database` |
| `scripts/qa-api-smoke.ps1` | ✅ PASS | `runtime/qa/out/qa-api-smoke-20260515-141802-345/summary.json` |
| `scripts/qa-page-role.ps1 admin` | ✅ PASS | `runtime/qa/out/qa-page-role-admin-20260515-141802-378/summary.json` |
| `scripts/qa-page-role.ps1 biz_leader` | ✅ PASS | `runtime/qa/out/qa-page-role-biz_leader-20260515-141802-452/summary.json` |
| 页面 `Console / Network` 中 `/api/system/env 401` | ✅ 未复现 | 两个角色 smoke 均未抓到过滤后 console error，也未出现 `/api/` 500 |

本轮直接返回内容：

```json
{"code":200,"msg":"操作成功","data":{"activeProfiles":["test"],"environmentLabel":"TEST","appTestEnabled":true,"douyinTestEnabled":true,"database":"saas_test"}}
```

结论：

- `/api/system/env` 允许未登录访问：已满足
- 返回内容未暴露 token、secret、数据库密码：已满足
- `qa-api-smoke` 与 `qa-page-role` 当前都不再依赖过滤 `/api/system/env 401`：已满足
- 当前剩余待收基础问题应切换到 `start-test.ps1` 启动竞态

### 追加修复记录（2026-05-15 14:26 - 14:49）：`start-test.ps1` 启动竞态加固

修复目标：

1. `start-test.ps1` 每次启动前先停止旧环境
2. 停止后等待旧容器完全退出
3. 启动后等待后端 health 返回 `UP`
4. 启动完成前补等前端首页可达，避免紧接着执行 Playwright smoke 时仍停在启动过渡层
5. 超时失败时输出 `docker ps`、`docker compose ps`、`saas-backend` 最后 100 行日志
6. 不切 `real-pre`
7. 不删除 database volume

本轮代码调整：

- [scripts/stack-utils.ps1](/D:/Projects/SAAS/scripts/stack-utils.ps1)
  - 新增 `Read-EnvFile`
  - 新增 `Get-BackendHealthUrl`
  - 新增 `Get-FrontendBaseUrl`
  - 新增 `Wait-ContainersStopped`
  - 新增 `Wait-HttpHealth`
  - 新增 `Wait-HttpOk`
  - 新增 `Show-StartupDiagnostics`
  - 新增 `Assert-LastExitCode`
  - 兼容 `Invoke-WebRequest` 返回 `byte[]` 内容的 JSON 解码
- [scripts/start-test.ps1](/D:/Projects/SAAS/scripts/start-test.ps1)
  - 启动前固定调用 `stop-all.ps1`
  - 增加旧容器退出等待
  - 固定执行 `docker compose --env-file .env.test --project-name saas-active -f docker-compose.yml up -d --build`
  - 等待后端 `/api/actuator/health` 为 `UP`
  - 额外等待前端 `http://localhost:3000` 返回 `200`
  - 启动成功后输出 `compose ps`、`docker ps`、health body
  - 启动失败时输出诊断并 `exit 1`
- [scripts/stop-all.ps1](/D:/Projects/SAAS/scripts/stop-all.ps1)
  - 保留“不使用 `-v`、不删除 volume”约束
  - 停止 `saas-active`
  - 兼容停止旧 `docker-compose.test.yml` / `docker-compose.real-pre.yml` / `docker-compose.prod.yml` 历史入口
  - 停止后等待 `saas-*` 容器完全退出
  - 停止后输出当前 `docker ps`
- [scripts/status.ps1](/D:/Projects/SAAS/scripts/status.ps1)
  - 增加当前 backend health 输出
- [scripts/tests/start-stack-helper.test.ps1](/D:/Projects/SAAS/scripts/tests/start-stack-helper.test.ps1)
  - 新增脚本级辅助测试，覆盖 env 解析、health URL、frontend URL、容器等待、health 等待、frontend 200 等待

端口说明：

- 用户本轮要求中写的是 `http://localhost:8081/api/actuator/health`
- 但当前仓库在 **2026-05-15** 的 test 单活事实仍是 `.env.test -> BACKEND_HOST_PORT=8080`
- 因此本轮脚本按 `.env.test` 实际端口探活，当前验收地址为 `http://localhost:8080/api/actuator/health`

脚本级红绿过程：

1. 初始新增 [scripts/tests/start-stack-helper.test.ps1](/D:/Projects/SAAS/scripts/tests/start-stack-helper.test.ps1) 时，因 `stack-utils.ps1` 不存在而失败
2. 抽出 `stack-utils.ps1` 后，辅助测试转绿
3. 首轮真实启动暴露 `Invoke-WebRequest` 返回 `byte[]` 导致 health 解析误判；补测试后修复
4. 第二轮回归暴露“仅等 backend，不等 frontend”会让紧随其后的 Playwright smoke 超时；补测试后修复

验收执行：

```powershell
powershell -ExecutionPolicy Bypass -File scripts/start-test.ps1
powershell -ExecutionPolicy Bypass -File scripts/start-test.ps1
powershell -ExecutionPolicy Bypass -File scripts/start-test.ps1

powershell -ExecutionPolicy Bypass -File scripts/qa-env.ps1
powershell -ExecutionPolicy Bypass -File scripts/qa-api-smoke.ps1
powershell -ExecutionPolicy Bypass -File scripts/qa-page-role.ps1 admin
powershell -ExecutionPolicy Bypass -File scripts/qa-page-role.ps1 biz_leader
```

验收结果：

| 检查项 | 结果 | 证据 |
|---|---|---|
| `start-test.ps1` 第 1 次 | ✅ PASS | 串行执行通过，backend=`UP`，frontend=`200` |
| `start-test.ps1` 第 2 次 | ✅ PASS | 串行执行通过，backend=`UP`，frontend=`200` |
| `start-test.ps1` 第 3 次 | ✅ PASS | 串行执行通过，backend=`UP`，frontend=`200` |
| 仅存在一套 `saas-active` 容器 | ✅ | `runtime/qa/out/qa-env-20260515-152041-501/summary.json` |
| backend health 返回 `UP` | ✅ | `runtime/qa/out/qa-env-20260515-152041-501/summary.json` |
| 前端显示 `环境：TEST` | ✅ | `runtime/qa/out/qa-env-20260515-152041-501/summary.json` |
| 无 test / real-pre 双开 | ✅ | `scripts/status.ps1` + `docker ps` 当前仅见 `saas-frontend` / `saas-backend` / `saas-postgres` / `saas-redis` |
| `qa-api-smoke.ps1` | ✅ PASS | `runtime/qa/out/qa-api-smoke-20260515-152105-710/summary.json` |
| `qa-page-role.ps1 admin` | ✅ PASS | `runtime/qa/out/qa-page-role-admin-20260515-152110-942/summary.json` |
| `qa-page-role.ps1 biz_leader` | ✅ PASS | `runtime/qa/out/qa-page-role-biz_leader-20260515-152241-319/summary.json` |

补充说明：

- 两个 `qa-page-role` 在一次“并发执行”中会因为共享同一前端启动/登录节奏而落入过渡态页面，报告会出现“正在加载业务看板”；这不是当前权限回归失败
- 按本记录中的串行验收命令重新执行后，`admin` 与 `biz_leader` 两个角色 smoke 均已通过
- 本轮未改业务逻辑、未切 `real-pre`、未删除任何 volume

### 追加记录（2026-05-15 12:56 - 14:13）：单角色页面 QA 脚本

本轮在既有 `qa-page-smoke` 基础上补出“按角色单独执行”的页面 smoke，优先改为配置驱动，不复制多套路由逻辑。

新增文件：

- [runtime/qa/role-page-cases.json](/D:/Projects/SAAS/runtime/qa/role-page-cases.json)
- [runtime/qa/page-role-smoke.cjs](/D:/Projects/SAAS/runtime/qa/page-role-smoke.cjs)
- [runtime/qa/page-role-smoke.test.cjs](/D:/Projects/SAAS/runtime/qa/page-role-smoke.test.cjs)
- [scripts/qa-page-role.ps1](/D:/Projects/SAAS/scripts/qa-page-role.ps1)
- [scripts/qa-page-roles-all.ps1](/D:/Projects/SAAS/scripts/qa-page-roles-all.ps1)

脚本口径：

- `role-page-cases.json` 按角色定义 `username`、`password`、`mustAccess`、`mustReject`、`mustSeeText`、`mustNotSeeText`
- `page-role-smoke.cjs` 会自动登录角色账号，逐项校验页面可达 / 不可达、文案可见 / 不可见
- 监听全部同源 `/api/` 响应，命中 `500` 直接失败
- 监听 `console error`，并仅过滤已确认无害噪音（当前保留 `ResizeObserver` 与 `favicon 404` 一类）
- 每次执行都会单独落 `summary.json` + `report.md`

本轮先覆盖角色：

- `admin`
- `biz_leader`

定向自测：

- `node --test runtime/qa/page-role-smoke.test.cjs`：通过（4/4）

角色 smoke 输出：

| 角色 | 执行命令 | 结果 | 证据 |
|---|---|---|---|
| `admin` | `scripts/qa-page-role.ps1 admin` | ✅ PASS | `runtime/qa/out/qa-page-role-admin-20260515-125808-182/summary.json` |
| `biz_leader` | `scripts/qa-page-role.ps1 biz_leader` | ✅ PASS | `runtime/qa/out/qa-page-role-biz_leader-20260515-125954-129/summary.json` |

验收结论：

1. `admin` 访问 `/system` 最终落到 `/system/users`，符合系统管理默认落地页口径
2. `biz_leader` 访问 `/system` 不进入系统管理，最终回到 `/dashboard`
3. `biz_leader` 可访问 `/dashboard`、`/product/library`、`/product/activity`（重定向到 `/product/manage`）、`/orders`
4. 两个角色本轮 smoke 过程中均未捕获 `/api/` `500`
5. 两个角色本轮 smoke 过程中均未捕获过滤后 `console error`

补充说明：

- `biz_leader` 活动页文案断言已按真实页面口径收口为 `活动列表` + `商品列表`；当测试种子为空时，不再强依赖 `进入工作台` 文案
- loading 判定已从粗粒度 `.n-spin-body` 可见性收口为“是否仍存在 `spinning` 状态的容器”，避免把正常骨架容器误判为卡死
- 后续扩展其他角色时，只需继续在 `role-page-cases.json` 增加角色配置，并复用 `scripts/qa-page-role.ps1 {role}` / `scripts/qa-page-roles-all.ps1`

### 追加修复记录（2026-05-15 12:30 - 12:38）：管理员 `/system` 默认落地页

修复结果：

- 前端已显式定义 `/system -> /system/users`
- 侧边栏系统管理父菜单 path 已与 router 对齐为 `/system`
- 管理员直接输入 `/system` 不再命中 `resolveHomePath()` 的 `/data` 兜底
- 非管理员访问 `/system` 仍按原有权限链路跳回自身首页

验收证据：

- `runtime/qa/out/qa-page-smoke-20260515-123609-043/summary.json`
- `runtime/qa/out/qa-page-smoke-20260515-123712-109/summary.json`


## 模块一：登录与导航

### 测试时间：2026-05-15 10:00

#### 通过项（17/17 核心 API）

| # | 接口 | 方法 | 状态码 | 耗时 | 备注 |
|---|---|---|---|---|---|
| 1 | `/api/auth/login` | POST | 200 | 272ms | 正确用户名密码登录成功，返回 token |
| 2 | `/api/auth/login` | POST | 401 | <50ms | 错误密码返回 401 |
| 3 | `/api/auth/login` | POST | 401 | <50ms | 不存在用户返回 401 |
| 4 | `/api/auth/refresh` | POST | 200 | <100ms | 刷新令牌成功，返回新 accessToken |
| 5 | `/api/auth/logout` | POST | 200 | <100ms | 登出成功，令牌加入黑名单 |
| 6 | `/api/users` | GET | 200 | <100ms | 用户列表正常返回 |
| 7 | `/api/roles` | GET | 200 | <100ms | 角色列表正常返回 |
| 8 | `/api/products` | GET | 200 | <200ms | 商品列表正常返回（分页） |
| 9 | `/api/talents` | GET | 200 | <200ms | 达人列表正常返回 |
| 10 | `/api/samples` | GET | 200 | <200ms | 寄样列表正常返回 |
| 11 | `/api/orders` | GET | 200 | <200ms | 订单列表正常返回 |
| 12 | `/api/dashboard/summary` | GET | 200 | <200ms | Dashboard 汇总数据正常 |
| 13 | `/api/configs` | GET | 200 | <100ms | 系统配置列表正常 |
| 14 | `/api/operation-logs` | GET | 200 | <100ms | 操作日志列表正常 |
| 15 | `/api/actuator/health` | GET | 200 | <50ms | 健康检查正常 |
| 16 | 无 Token 请求 | GET | 401 | <50ms | 未认证请求正确拦截 |
| 17 | 无效 Token 请求 | GET | 401 | <50ms | 无效令牌正确拦截 |

#### 失败项（2 项）

| ID | 页面/场景 | 操作步骤 | 期望结果 | 实际结果 | 接口 | 初步原因 | 等级 |
|---|---|---|---|---|---|---|---|
| F1 | Dashboard 页面 | 前端导航到 Dashboard | 正常加载数据 | 500 错误 | `GET /api/dashboard` | 前端调用 `/api/dashboard` 而非 `/api/dashboard/summary`，Spring 将未匹配路径当静态资源返回 `NoResourceFoundException` | HIGH |
| F2 | 系统配置页面 | 前端导航到系统配置 | 正常加载配置 | 500 错误 | `GET /api/system/configs` | 前端调用 `/api/system/configs` 而非 `/api/configs`，SysConfigController 基路径为 `/configs` | HIGH |

#### 待确认项（5 项）

| ID | 页面/场景 | 待确认内容 | 备注 |
|---|---|---|---|
| P1 | 全局 | 浏览器 Network/Console 实际表现验证 | 需要用真实浏览器操作确认前端实际请求路径 |
| P2 | 商品模块 | `/products/library` 和 `/products/activities` 路径调查 | ProductController 无此子路径，返回 400 "id 格式不正确"，需确认前端是否调用这些路径 |
| P3 | 全局 | HeadlessChrome 刷新时 401 噪音 | E2E 测试中 HeadlessChrome 请求偶尔 401，可能为 token 过期正常行为 |
| P4 | Token 刷新 | 刷新后旧 refreshToken 是否仍有效 | 当前实现不吊销旧 refreshToken，需确认是否为设计意图 |
| P5 | 登出流程 | 登出后前端重定向行为 | 需浏览器验证登出后是否正确跳转登录页 |

#### 模块一结论

- **通过**：17/17 核心 API 功能正常
- **失败**：2 项（均为前端 API 路径配置问题，后端接口本身正常）
- **待确认**：5 项（需浏览器实操验证）

---

## 模块二：系统管理

### 测试时间：2026-05-15 11:00

#### 通过项（14 项）

| # | 接口 | 方法 | 状态码 | 耗时 | 备注 |
|---|---|---|---|---|---|
| 1 | `/api/users` | GET | 200 | <100ms | 用户列表正常返回（6 个用户） |
| 2 | `/api/users?keyword=admin` | GET | 200 | <100ms | 关键字搜索正常 |
| 3 | `/api/users?status=1` | GET | 200 | <100ms | 状态筛选正常 |
| 4 | `/api/users?page=1&size=2` | GET | 200 | <100ms | 分页正常 |
| 5 | `/api/users/{id}` | GET | 200 | <100ms | 用户详情正常返回 |
| 6 | `/api/roles` | GET | 200 | <100ms | 角色列表正常返回（6 个角色） |
| 7 | `/api/roles/{id}` | GET | 200 | <100ms | 角色详情正常返回 |
| 8 | `/api/configs` | GET | 200 | <100ms | 配置列表正常返回 |
| 9 | `/api/configs?grouped=true` | GET | 200 | <100ms | 分组配置正常返回 |
| 10 | `/api/configs/{id}` | GET | 200 | <100ms | 配置详情正常返回 |
| 11 | `/api/configs?keyword=app` | GET | 200 | <100ms | 配置关键字搜索正常 |
| 12 | `/api/operation-logs` | GET | 200 | <100ms | 操作日志列表正常 |
| 13 | `/api/operation-logs?module=auth` | GET | 200 | <100ms | 日志按模块筛选正常 |
| 14 | `/api/operation-logs?action=LOGIN` | GET | 200 | <100ms | 日志按操作类型筛选正常 |

#### 失败项（3 项）

| ID | 页面/场景 | 操作步骤 | 期望结果 | 实际结果 | 接口 | 初步原因 | 等级 |
|---|---|---|---|---|---|---|---|
| F3 | 部门管理 | `GET /api/departments` | 返回部门列表 | 500 错误 | `GET /api/departments` | 后端无 DepartmentController，Spring 将未匹配路径当静态资源返回 `NoResourceFoundException` | HIGH |
| F4 | 菜单管理 | `GET /api/menus` | 返回菜单列表 | 500 错误 | `GET /api/menus` | SysMenuController 无基础 GET 端点，仅支持 `/menus/tree` 和 `/menus/all`，返回 `HttpRequestMethodNotSupportedException` | HIGH |
| F5 | 菜单管理 | `GET /api/menus/tree` 或 `/menus/all` | 返回菜单树 | 500 错误 | `GET /api/menus/tree` | **CRITICAL** 数据库缺失 `sys_role_menu` 表。当前 sys 相关表仅有 `sys_role`、`sys_user`、`sys_user_role`，导致所有菜单端点 SQL 查询失败（`PSQLException: relation "sys_role_menu" does not exist`） | CRITICAL |

#### 待确认项（2 项）

| ID | 页面/场景 | 待确认内容 | 备注 |
|---|---|---|---|
| P6 | 用户管理 | `colonel_leader` 角色用户不存在 | RoleCodes 定义了 `COLONEL_LEADER`，但数据库无此用户名，登录失败。需确认是否应创建该用户 |
| P7 | 用户管理 | `roleId` 筛选可能不准确 | `GET /api/users?roleId={adminRoleId}` 返回 6 个用户（全量），需确认筛选逻辑是否正确 |

#### 权限验证（模块二附加）

| 场景 | 操作 | 期望 | 实际 | 结果 |
|---|---|---|---|---|
| biz_staff 访问用户列表 | `GET /api/users` | 403 | 403 Forbidden | ✅ 通过 |
| admin 访问用户列表 | `GET /api/users` | 200 | 200 | ✅ 通过 |

#### 模块二结论

- **通过**：14 项（用户/角色/配置/日志 CRUD + 搜索 + 筛选 + 分页均正常）
- **失败**：3 项（F3 部门接口缺失、F4 菜单列表端点缺失、F5 菜单数据库表缺失 **CRITICAL**）
- **待确认**：2 项（P6 colonel_leader 用户不存在、P7 roleId 筛选逻辑）
- **关键发现**：`sys_role_menu` 关联表完全缺失，导致菜单功能全部不可用

---

## 模块三：商品与活动

### 测试时间：2026-05-15 12:00

---

## 环境治理记录（2026-05-15）

### 变更背景

test 和 real-pre 两套容器混用，导致排查结果不可信。同一台机器上可能同时运行两套环境，端口冲突、数据干扰。

### 变更目标

1. 合并启动入口，统一使用 `docker-compose.yml`
2. 同一时间只允许一个 SAAS 环境运行
3. 保留 test / real-pre 两套配置，互斥运行
4. 功能测试和权限测试默认使用 test 环境
5. real-pre 仅用于真实抖店 SDK 联调

### 文件变更

#### 新增文件

| 文件 | 说明 |
|------|------|
| `scripts/start-test.ps1` | 启动 test 环境（project name 固定为 saas-active） |
| `scripts/start-real-pre.ps1` | 启动 real-pre 环境（project name 固定为 saas-active） |
| `scripts/stop-all.ps1` | 停止所有 SAAS 容器（含历史遗留容器名清理） |
| `scripts/status.ps1` | 查看当前运行环境状态 |
| `archive/docker-compose.prod.yml` | 原 prod compose 归档 |
| `runtime/qa/role-page-cases.json` | 单角色页 QA 用例（admin / biz_leader，可扩展） |
| `runtime/qa/page-role-smoke.cjs` | 单角色页 Playwright 执行器 |
| `scripts/qa-page-role.ps1` | 单角色入口：`.\scripts\qa-page-role.ps1 <role>` |
| `scripts/qa-page-roles-all.ps1` | 顺序执行多角色（当前 admin、biz_leader） |

#### 修改文件

| 文件 | 变更 |
|------|------|
| `docker-compose.yml` | 新增顶层 `name: saas-active`；容器名已统一为 `saas-{postgres,redis,backend,frontend}` |
| `docs/18-管理员账户系统功能排查记录.md` | 本节记录 |

### 环境配置对比

| 配置项 | test | real-pre |
|--------|------|----------|
| `SPRING_PROFILES_ACTIVE` | test | real-pre |
| `APP_TEST_ENABLED` | true | false |
| `DOUYIN_TEST_ENABLED` | true | false |
| `DB_NAME` | saas_test | saas_real_pre |
| `REDIS_DATABASE` | 1 | 0 |
| `VITE_ENV_LABEL` | TEST | REAL-PRE |
| `SAAS_ENV_SLUG` | test | real_pre |
| `ORDER_SYNC_ENABLED` | false | true |
| `DOUYIN_REAL_UPSTREAM_MODE` | mock | live |

### 容器命名规范

所有容器统一命名，不再区分 test/real-pre 后缀：

```
saas-frontend     # 前端（统一端口 3000）
saas-backend      # 后端（统一端口 8080）
saas-postgres     # PostgreSQL（统一端口 5432）
saas-redis        # Redis（统一端口 6379）
```

Docker Compose project name 固定为 `saas-active`，切换环境时 stop-all 先执行，确保不会双开。

### 启动流程

```powershell
# 启动 test 环境（默认，用于功能/权限测试）
.\scripts\start-test.ps1

# 启动 real-pre 环境（仅用于真实 SDK 联调）
.\scripts\start-real-pre.ps1

# 查看状态
.\scripts\status.ps1

# 停止所有环境
.\scripts\stop-all.ps1
```

### 前端环境标识

Header.vue 右上角始终显示当前环境标签（`VITE_ENV_LABEL`）：
- test 环境 → `环境：TEST`
- real-pre 环境 → `环境：REAL-PRE`

### 后端启动日志

`StartupEnvironmentLogger.java` 在启动时打印：
```
SAAS environment | activeProfiles={profile} | app.test.enabled={bool} | douyin.test.enabled={bool} | db.name={db}
```

### 验收状态

| 验收项 | 状态 |
|--------|------|
| `start-test.ps1` 启动后只存在一套 saas-active 容器 | ✅ `status.ps1` / `qa-env.ps1`：`saas-frontend`、`saas-backend`、`saas-postgres`、`saas-redis`（2026-05-15） |
| `start-real-pre.ps1` 启动后 test 容器不继续运行 | ⏭ 本轮按要求不启 real-pre，未做切换验证 |
| `docker ps` 不同时出现 test 和 real-pre 两套容器 | ✅ 当前仅 `saas-active` 一套（2026-05-15） |
| 浏览器前端显示明确环境标识 | ✅ 已实现（Header.vue env-badge）；Playwright 页冒烟采集为 `环境：TEST` |
| `/api/actuator/health` 返回 UP | ✅ `qa-env.ps1`、`qa-api-smoke.ps1` 均判定 UP |
| 后端日志明确显示 active profile | ✅ 已实现（StartupEnvironmentLogger） |
| 功能/权限测试只在 test 环境继续 | ✅ 已明确（real-pre 仅用于联调） |

---

## 五阶段回归归档（2026-05-15，仅 test / 未启 real-pre）

执行前提：`scripts/status.ps1` 显示 `saas-active` 单活四容器；本轮未再执行 `stop-all`→`start-test`（栈已健康），其余步骤按顺序完成。

### 阶段一：环境验收

| 检查项 | 结果 | 说明 |
|--------|------|------|
| 单活 compose | ✅ | 项目名 `saas-active`，容器名与 `docs/18` 环境治理一致 |
| 后端健康 | ✅ | `GET http://127.0.0.1:8080/api/actuator/health` → `UP`（liveness/readiness） |
| 后端环境标签 | ✅（Bearer） | 匿名 `GET /api/system/env` 为 **401**（与「公开环境接口」设想冲突，**未改后端**，见问题表） |
| 前端环境展示 | ✅ | 页头 `环境：TEST`；Bearer 下 `environmentLabel=TEST`，`database=saas_test`，`activeProfiles` 含 `test` |

### 阶段二：QA 脚本产物

| 脚本 | 输出目录 | overallPass |
|------|-----------|---------------|
| `scripts/qa-env.ps1` | `runtime/qa/out/qa-env-20260515-122128/` | ✅ |
| `scripts/qa-api-smoke.ps1` | `runtime/qa/out/qa-api-smoke-20260515-122151/` | ✅ |
| `scripts/qa-page-smoke.ps1`（admin 默认路由） | `runtime/qa/out/qa-page-smoke-20260515-122155/` | ✅ |
| `scripts/qa-page-smoke.ps1`（biz_leader 全路由，同进程 `&` 调用） | `runtime/qa/out/qa-page-smoke-20260515-122533-401/` | ❌（见阶段四） |

各目录均含 `summary.json` 与 `report.md`。

### 阶段三：管理员（admin / admin123）页冒烟

默认路由：`/dashboard`、`/product/library`、`/product/manage`、`/orders`、`/talent`、`/sample`、`/system/users`。  
**结论**：`overallPass=true`；多页出现 **匿名** `GET /api/system/env` → **401**（Network/Console 记录），页头仍显示 TEST（`VITE_ENV_LABEL` 或已缓存 token 场景下可接受）。

### 阶段四：招商组长（biz_leader / admin123）

**API 冒烟**（`qa-api-smoke`）：登录成功；`/dashboard/summary`、`/products`、`/orders` → 业务码 200；`/talents`、`/configs`、`/users` → HTTP 200 且 **业务码 403**（与契约一致）。

**页冒烟**（`qa-page-smoke-20260515-122533-401`）：`/dashboard`、`/product/library`、`/product/activity`（重定向至 `/product/manage`，与 `page-smoke.cjs` 白名单一致）、`/orders`、`/sample` 为 `opened=true`；**`/talent`、`/system/users`** 最终落在 **`/dashboard`** 且 **`opened=false`**（路由守卫/菜单隐藏导致「未打开目标页」，非接口 403 页）。  
**结论**：`overallPass=false` 来自上述两路由的 **opened** 判定；与后端 403 并存时表现为「无菜单 + 重定向」而非独立 403 页，**需产品/前端口径确认**，本轮未改代码。

**脚本说明**：`qa-page-smoke.ps1` 已将路由序列改为 `|` 连接；若用外层再嵌一层 `powershell -File ... -Routes $数组`，Windows 传参可能只收到首路由，**推荐在同一 PowerShell 会话内** `& .\scripts\qa-page-smoke.ps1 -Routes $r`。

### 阶段五：问题与观察（本轮登记）

| 编号 | 账号 | 页面/接口 | 步骤 | 期望 | 实际 | Network / Console | 初步原因 | 等级 | 状态 |
|------|------|-----------|------|------|------|-------------------|----------|------|------|
| R-20260515-01 | 匿名 | `/api/system/env` | 无 Authorization 访问 | 视产品：公开可读或 401 | **401** | 401 Unauthorized | `SecurityConfig` / JWT 过滤器将 `/system/env` 置于认证后；与「匿名可读环境」验收口径冲突 | **P1** | 待产品确认后再改 |
| R-20260515-02 | admin、biz_leader | 多页 | 正常浏览 | 无红色 Console | 多次 `GET /api/system/env` **401** | 同上 | 前端在未带 token 或拦截器顺序下先打 env；Header 回退 `VITE_ENV_LABEL` | **P2** | 观察；与 R-01 同源 |
| R-20260515-03 | biz_leader | `/talent`、`/system/users` | 从页冒烟访问路由 | 403 页或明确无权限态 | **重定向到 `/dashboard`**，`opened=false` | 无 403 资源错误 | 路由 meta + `authStore` 守卫未进入目标页 | **P2** | 待与「403 vs 空数据」口径对齐 |
| R-20260515-04 | qa-env 脚本 | 前端 badge 文本 | `qa-env` Playwright 读 Header | 可读中文「环境：TEST」 | `summary.json` 中 `badgeText` **乱码** | 脚本内编码/字体 | Windows 控制台 UTF-8 与 headless 文本抓取 | **P3** | 低优；以 `page-smoke` 与人工页为准 |

---

## 单角色页 QA（`qa-page-role`，2026-05-15）

在 `qa-page-smoke` 之外增加**按角色、配置驱动**的页面验收：用例集中在 `runtime/qa/role-page-cases.json`，执行逻辑在 `runtime/qa/page-role-smoke.cjs`（不复制整套 `page-smoke` 路由循环，仅按 JSON 声明访问/拒访/文案）。

### 脚本用法

```powershell
# 单角色（各角色单独目录：runtime/qa/out/qa-page-role-{role}-{timestamp}/）
.\scripts\qa-page-role.ps1 admin
.\scripts\qa-page-role.ps1 biz_leader

# 顺序跑 admin → biz_leader（后续在 JSON 与脚本中扩展角色即可）
.\scripts\qa-page-roles-all.ps1
```

每个输出目录含 **`summary.json`**、**`report.md`**。`runtime/` 目录默认 **gitignore**，报告仅本机留存；换机请重跑。

### 配置字段（`role-page-cases.json` → `roles.<role>`）

| 字段 | 说明 |
|------|------|
| `username` / `password` | 登录账号 |
| `textCheckPath` | 登录后用于 `mustSeeText` / `mustNotSeeText` 的页面路径 |
| `mustAccess` | `{ id, goto, allowedFinalPaths[] }`：导航后 `pathname` 须匹配其一（支持前缀子路径） |
| `mustReject` | `{ id, goto, mustNotStayUnderPrefix }`：导航后**不得**仍位于该前缀下（用于无系统权限账号） |
| `mustSeeText` / `mustNotSeeText` | 在 `textCheckPath` 取 `body` 内文本做子串断言 |
| `consoleErrorAllowSubstrings` | 与脚本内置白名单合并（当前默认过滤 `ResizeObserver`、**`/api/system/env` 的 401** 控制台噪音） |

脚本另监听 **`/api/`** 响应，**出现 HTTP ≥500 即判失败**；控制台 `error` 经白名单后若有残留亦失败。

### 本轮验收（test 栈已起，本机执行）

| 命令 | overallPass | 输出目录（示例） |
|------|-------------|------------------|
| `.\scripts\qa-page-role.ps1 admin` | ✅ | `runtime/qa/out/qa-page-role-admin-20260515-125312-810/` |
| `.\scripts\qa-page-role.ps1 biz_leader` | ✅ | `runtime/qa/out/qa-page-role-biz_leader-20260515-125430-440/` |

**admin**：`/system` → **`/system/users`**；直达 **`/system/users`** 成功；`mustSeeText` 含「环境：TEST」「系统管理」。  
**biz_leader**：**mustAccess** `/dashboard`、`/product/library`、`/product/activity`（落地 **`/product/manage`**）、`/orders`；**mustReject** `/system` 与 `/system/users` 均回到 **`/dashboard`**（未停留在 `/system` 下）；`mustNotSeeText`「系统管理」在 `/dashboard` 正文中未出现。

---

## 5 阶段回归（2026-05-15 13:00–13:30）

基于 `bright-chasing-eclipse.md` 计划执行的系统性回归。

### Phase 1：环境验收（7/7 PASS）

| # | 检查 | 结果 |
|---|------|------|
| 1.1 | stop-all | ✅ 无残留容器 |
| 1.2 | start-test | ✅ 4 容器运行 |
| 1.3 | /actuator/health → UP | ✅ |
| 1.4 | qa-env.ps1 → overallPass | ✅ |
| 1.5 | status.ps1 → 4 容器 | ✅ |
| 1.6 | activeProfiles=test | ✅ |
| 1.7 | 前端 TEST 徽标 | ✅ |

### Phase 2：QA 冒烟（2/2 核心 PASS）

| 脚本 | 结果 |
|------|------|
| qa-api-smoke | ✅ |
| qa-page-smoke (admin) | ✅ |

### Phase 3：管理员全量回归（14/14 PASS）

所有 14 个路由 + 对应 API 均通过（F1-F5 已在此轮前修复）。

### Phase 4：渠道组长（channel_leader）权限回归

#### 侧边栏菜单验证（5 section 全覆盖）

| 导航路径 | Section | 可见菜单 | 结果 |
|----------|---------|---------|------|
| /dashboard | attribution | 归因工作台 > 归因概览、订单工作台 | ✅ |
| /data | data | 数据平台 > 核心看板、订单明细、独家状态 | ✅ |
| /product | product | 商品库 | ✅ |
| /talent | talent | 达人 CRM > 团队公海、我的达人、自然出单达人、达人黑名单 | ✅ |
| /sample | sample | 寄样审核 | ✅ |

**不可见**（符合预期）：商品管理（BIZ_LEADER/BIZ_STAFF）、系统管理（ADMIN）、寄样发货台（OPS_STAFF）

#### 路由访问验证

| 路由 | 预期 | 结果 |
|------|------|------|
| /dashboard | 可访问 | ✅ |
| /product | 可访问 | ✅ |
| /talent | 可访问 | ✅ |
| /sample | 可访问 | ✅ |
| /data | 可访问 | ✅ |
| /data/orders | 可访问 | ✅ |
| /ops/exclusive | 可访问 | ✅ |
| /product/manage | 拒绝→/dashboard | ✅ |
| /system/users | 拒绝→/dashboard | ✅ |
| /system/roles | 拒绝→/dashboard | ✅ |
| /ops/shipping | 拒绝→/dashboard | ✅ |

#### API 权限验证（channel_leader）

| 接口 | 预期 | 结果 |
|------|------|------|
| GET /api/dashboard/summary | 业务码 200 | ✅ |
| GET /api/products | 业务码 200 | ✅ |
| GET /api/orders | 业务码 200 | ✅ |
| GET /api/users | 业务码 403 | ✅ |
| GET /api/configs | 业务码 403 | ✅ |
| GET /api/roles | 业务码 403 | ✅ |

### F1-F5 验证结果

| 编号 | 接口 | HTTP | 业务码 | 状态 |
|------|------|------|--------|------|
| F1 | GET /api/dashboard/summary | 200 | 200 | **已解决** |
| F2 | GET /api/configs | 200 | 200 | **已解决** |
| F3 | GET /api/departments | 200 | **500** | **仍存在** — 无 DepartmentController |
| F4 | GET /api/menus | 200 | **500** | **仍存在** — SysMenuController 无基础 GET |
| F5 | GET /api/roles/{id}/menus | 200 | 200 | **已解决** — 原用整数 id `1`，实际需 UUID |

> **F3/F4 说明**：这两接口在前端无实际调用，属于"遗留接口 stub"。建议：补全 controller 或从 API 文档中移除，避免冒烟脚本误报。

### Phase 5 汇总

| 阶段 | 项目数 | 通过 | 说明 |
|------|--------|------|------|
| Phase 1 环境验收 | 7 | 7 | 全部通过 |
| Phase 2 QA 冒烟 | 2 | 2 | admin API + page |
| Phase 3 管理员回归 | 14 | 14 | F1-F5 已修复 |
| Phase 4 渠道组长 | 19 | 19 | 菜单5 + 路由11 + API 6（含拒绝类） |
| F1-F5 验证 | 5 | 3 | F3/F4 仍 500（无前端调用，P3） |

**新增问题**：

| 编号 | 账号 | 描述 | 等级 | 状态 |
|------|------|------|------|------|
| F3 | admin | GET /api/departments → 业务码 500（无 DepartmentController） | P3 | OPEN（无前端调用） |
| F4 | admin | GET /api/menus → 业务码 500（SysMenuController 无基础 GET） | P3 | OPEN（无前端调用） |
| R-05 | admin | GET /api/roles/{int}/menus → 400（需 UUID 非整数 id） | — | 已解决（冒烟脚本用 UUID 即可） |

---

## 完整浏览器业务流程回归（2026-05-15 16:06–16:11）

新增脚本：

- `runtime/qa/business-browser-flow.cjs`
- `scripts/qa-business-browser-flow.ps1`
- `runtime/qa/business-browser-flow.test.cjs`

脚本行为：

- 先调用 `/api/system/env` 与 `/api/actuator/health`，仅在 TEST/mock 环境继续执行。
- 真实浏览器登录 admin，覆盖首页、商品库、活动/商品管理、商品审核入口、达人 CRM、寄样台、订单列表、数据平台、系统管理。
- 额外新建干净浏览器上下文登录 `biz_leader`，验证可访问业务页且不能停留在 `/system`。
- 全程监听 console error、pageerror、`/api/` 4xx/5xx、慢接口；输出 `summary.json`、`report.md`、截图、trace、`api-errors.json`、`console-errors.json`、`slow-apis.json`。

验收命令：

| 命令 | 结果 | 输出目录 |
|------|------|----------|
| `node --check runtime/qa/business-browser-flow.cjs` | ✅ | — |
| `node --test runtime/qa/business-browser-flow.test.cjs` | ✅ 5/5 | — |
| `powershell -ExecutionPolicy Bypass -File scripts/qa-business-browser-flow.ps1` | ✅ Exit code 0 / overallPass=true | `runtime/qa/out/qa-business-browser-flow-20260515-160621-949/` |

本轮结果：

| 模块 | 结果 |
|------|------|
| 环境与首页准备 | ✅ |
| 登录管理员 | ✅ |
| 首页 / 数据看板 | ✅ |
| 商品库 | ✅ |
| 活动与商品管理 | ✅ |
| 商品审核 / 状态动作 | ✅ skipped（`/product/review` 当前落到商品库，按可选入口处理） |
| 达人 CRM | ✅ |
| 寄样台 | ✅ |
| 订单列表 | ✅ |
| 数据平台 | ✅ |
| 系统管理 | ✅ |
| 权限边界抽查 | ✅ |

观测：

- API 错误：0
- Console 错误：0
- 慢接口：46（均已记录在 `slow-apis.json`，本轮不作为阻塞失败）
- 当前 checked-in TEST compose 端口为前端 `3000`、后端 `8080`；包装脚本会优先读取 `.env.test`，仍支持通过 `E2E_BASE_URL` / `API_BASE_URL` 覆盖。

## 业务 CRUD / 状态流转浏览器 QA（2026-05-15 新增）

与「页面巡检」脚本分离，专门验证 TEST/mock 环境下的业务数据闭环。

| 脚本 | 用途 | 不修改 |
|---|---|---|
| `runtime/qa/business-browser-flow.cjs` | 全模块页面巡检 | 是 |
| `runtime/qa/page-role-smoke.cjs` | 单角色页面权限 smoke | 是 |
| **`runtime/qa/business-crud-flow.cjs`** | **业务 CRUD / 状态流转** | — |

### 新增文件

| 文件 | 说明 |
|---|---|
| `runtime/qa/business-crud-flow.cjs` | Playwright 主脚本 |
| `scripts/qa-business-crud-flow.ps1` | PowerShell 包装（默认 `3002` / `8081`，可读 `.env.test` 端口） |
| `runtime/qa/business-crud-cases.json` | 角色账号、保护账号、mock 活动 ID |
| `runtime/qa/business-crud-flow.test.cjs` | 辅助函数单元测试 |

### 运行方式

```powershell
cd D:\Projects\SAAS
powershell -ExecutionPolicy Bypass -File scripts/start-test.ps1
powershell -ExecutionPolicy Bypass -File scripts/qa-env.ps1
powershell -ExecutionPolicy Bypass -File scripts/qa-api-smoke.ps1
powershell -ExecutionPolicy Bypass -File scripts/qa-business-crud-flow.ps1
```

有头调试：

```powershell
powershell -ExecutionPolicy Bypass -File scripts/qa-business-crud-flow.ps1 -Headful -SlowMo 100
```

验收：

```powershell
node --check runtime/qa/business-crud-flow.cjs
node runtime/qa/business-crud-flow.test.cjs
```

### 核心原则

- 每次运行生成 `QA_RUN_ID`，新建数据字段均带该 ID
- 仅允许 **TEST** 环境；探测到 real-pre 或非 test profile 时 **立即失败退出**
- 不调用真实三方、不删库卷、不修改业务代码
- 清理仅针对含 `QA_RUN_ID` 的数据；**不触碰** `admin`、`leader_zs`、`seller_zs`、`channel_user`、`ops_user`、`biz_leader`

### 覆盖模块

| 模块 | 类型 | 说明 |
|---|---|---|
| admin-login-env | 环境与登录 | `/api/system/env` + health + TEST 页头 + `/dashboard` |
| talent-crud | 完整 CRUD（部分 skipped） | 新增/搜索/详情；**U 跳过**（无编辑 UI）；D 尝试认领+释放 |
| system-user-crud | 完整 CRUD | 新增 QA 用户 → 查询 → 改姓名 → 禁用/删除 |
| product-activity-flow | 状态流转 | 活动同步/分配入口 + 商品库 |
| product-review | 状态流转 | 待审核入口；无稳定 QA 商品时不提交审核 |
| promotion-link | 转链 | `product-copy-link` 或文案按钮 |
| sample-flow | 状态流转 | 申请寄样 → 列表 → 详情 → 审核/发货/签收（有则点） |
| orders-readonly | 只读 | 列表 + `filter-options` + 详情 |
| dashboard-refresh | 只读 | `/dashboard` + `/data` + summary 接口 |
| permission-boundary | 权限 | admin 可进 `/system`；`biz_leader` 不可 |

### 输出目录

`runtime/qa/out/qa-business-crud-flow-{timestamp}/`

必含：`summary.json`、`report.md`、`api-errors.json`、`console-errors.json`、`slow-apis.json`、`screenshots/`、`created-entities.json`、`cleanup-result.json`。

### 已执行报告路径

- 首轮（部分 CRUD 步骤待稳定）：`runtime/qa/out/qa-business-crud-flow-20260515-154217-870/`（`QA_RUN_ID=QA20260515_154224_dbvz`，`overallPass=false`；达人创建/用户创建/权限登出后重登已做脚本加固）

### 常见 skipped 项

| ID | 原因 |
|---|---|
| `mock-reset` / `mock-seed` | 可选接口 404/405 |
| `talent-edit-not-supported` | 前端无达人编辑表单 |
| `talent-delete-not-supported` | 无删除且释放不可用 |
| `no-review-products` | 无待审核商品 |
| `product-audit-submit-skipped` | 无法确认 QA/mock 商品，不提交审核 |
| `promotion-link-not-visible` | 商品库无转链按钮 |
| `sample-no-talent` / `sample-status-actions-skipped` | mock 达人或状态按钮不足 |
| `no-orders-data` | 订单空状态（页面仍须正常） |

### 失败项（硬失败）

- 非 TEST / real-pre 环境
- admin 登录失败
- `/dashboard`、`/product/library`、`/sample`、`/orders` 打不开
- admin 不能进系统用户管理；`biz_leader` 能进 `/system/users`
- 任意 `/api/` **500**；非预期的 **401**
- Playwright 未捕获异常

### 后续需补 `data-testid`

- 达人编辑表单（备注/标签）
- 寄样申请收件人/地址/手机号（当前 Apply 页仅备注）
- 商品审核弹窗提交与「仅 QA 数据」标记
- 系统用户行内「禁用」快捷操作

### 后续需稳定 mock 样本

- 已认领达人（供寄样申请选达人）
- 待审核活动商品（供审核补充信息闭环）
- 订单列表至少 1 条（供详情/筛选断言）
- Dashboard 指标前后对比基线

---

## TEST/mock 数据业务场景覆盖审计（2026-05-15 新增）

本轮新增只读审计脚本，用于判断当前 TEST/mock 数据是否覆盖 V2.2 主流程与特殊异常分支。该能力不修改业务逻辑、不切 real-pre、不调用真实三方接口、不清理或写入数据。

### 新增文件

| 文件 | 说明 |
|---|---|
| `runtime/qa/mock-scenario-matrix.json` | TEST/mock 业务场景覆盖矩阵，覆盖角色权限、商品与活动、达人、寄样、订单与归因、数据平台、系统管理 |
| `runtime/qa/mock-data-audit.cjs` | Node 只读审计脚本；先检查 `/api/system/env` 必须为 TEST，再检查 health、登录 admin、拉取内部 API 数据 |
| `scripts/qa-mock-data-audit.ps1` | PowerShell 包装入口；默认 `API_BASE_URL=http://localhost:8081`，当前 `.env.test` 端口可达时自动对齐到 `8080` |
| `runtime/qa/mock-data-audit.test.cjs` | 纯函数单测，覆盖环境判定、ApiResult 解包、覆盖计算、页面 QA 报告推断与 seed 建议 |

### 运行方式

```powershell
cd D:\Projects\SAAS
powershell -ExecutionPolicy Bypass -File scripts/start-test.ps1
powershell -ExecutionPolicy Bypass -File scripts/qa-env.ps1
powershell -ExecutionPolicy Bypass -File scripts/qa-api-smoke.ps1
powershell -ExecutionPolicy Bypass -File scripts/qa-mock-data-audit.ps1
```

输出目录格式：

`runtime/qa/out/qa-mock-data-audit-{timestamp}/`

固定输出：

- `summary.json`
- `report.md`
- `missing-scenarios.json`
- `seed-suggestions.json`
- `raw-counts.json`
- `api-errors.json`

### 覆盖矩阵说明

矩阵按模块定义：

- 角色权限：6 个必备角色 + 数据范围 / 菜单 / 路由 / API 拒绝特殊场景
- 商品与活动：7 个主状态 + 活动绑定、投流、寄样要求、转链成败等特殊场景
- 达人：公海、私海、认领、保护期有效 / 过期 + 爬虫兜底、联系方式、多人认领等特殊场景
- 寄样：待审核、待发货、快递中、待交作业、完成、拒绝、关闭 + 7 天重复、超时关闭等特殊场景
- 订单与归因：已归因、无 `pick_source`、映射缺失、商品未覆盖、结算 / 未结算、退款关闭等主场景；独家达人 / 商家、歧义映射按 warning 处理
- 数据平台：今日 / 7 日 / 30 日、跨月、空日期、各类筛选、导出权限
- 系统管理：用户、角色、配置、提成、操作日志

页面覆盖会读取 `runtime/qa/out` 下最近的 `qa-page-role-*`、`qa-business-browser-flow-*`、`qa-business-crud-flow-*` 报告。若找不到报告，`pageCovered=unknown`，不直接 fail，但报告会提示先跑页面 QA。

### 首轮审计结果（2026-05-15，历史记录）

> 2026-05-16 已完成硬缺口收口，当前最新结果见本文上方「TEST/mock 数据覆盖审计收口（2026-05-16）」。

首轮报告：

- `runtime/qa/out/qa-mock-data-audit-20260515-223240-806/summary.json`
- `runtime/qa/out/qa-mock-data-audit-20260515-223240-806/report.md`

执行结果：

| 项 | 结果 |
|---|---|
| 环境 | TEST，`database=saas_test` |
| health | UP |
| API 错误 | 0 |
| 总体结论 | `overallPass=false` |
| 总覆盖率 | 64.22% |

本次 `overallPass=false` 是覆盖缺口导致，不是脚本异常。PowerShell 返回非 0 符合审计判定标准。

原始计数摘要：

| 数据 | 数量 |
|---|---:|
| users | 6 |
| roles | 6 |
| products | 2 |
| productPicks | 100 |
| activities | 2 |
| colonelActivities | 20 |
| activityProducts | 160 |
| talents | 7 |
| samples | 5 |
| orders | 6 |
| unattributedOrders | 8 |
| operationLogs | 100 |

### 首轮硬缺失场景（fail，已在 2026-05-16 收口）

| 模块 | 缺失 |
|---|---|
| 商品与活动 | `APPROVED`、`REJECTED` |
| 达人 | `PRIVATE`、`PROTECTION_ACTIVE`、`PROTECTION_EXPIRED` |
| 寄样 | `PENDING_REVIEW`、`SHIPPING` |
| 订单与归因 | `PRODUCT_UNCOVERED`、`REFUNDED_OR_CLOSED` |
| 数据平台 | `cross_month_data`、`empty_date_range`、`filter_by_talent`、`filter_by_activity` |
| 系统管理 | `user_disabled_case` |

### 首轮建议补充的 seed 数据（已处理硬缺口，warning 后续按专项处理）

- 商品：补明确 `APPROVED`、`REJECTED` 状态的活动商品 / 商品库样本；补活动无商品、投流支持 / 不支持、寄样要求 / 不要求、转链失败样本。
- 达人：补私海达人、保护期内达人、保护期过期达人；补爬虫成功、爬虫失败手动兜底、寄样资格通过 / 不通过、多人认领、不完整资料样本。
- 寄样：补待审核与快递中样本；补 7 天重复申请、订单命中完成、达人不满足资格但填写原因样本。
- 订单：补商品未覆盖、退款 / 关闭订单；补歧义映射、独家达人、独家商家、映射创建前 / 后订单、零服务费、跨月、无订单日期样本。
- 数据平台：补跨月订单、空日期范围可稳定返回空列表、达人筛选项、活动筛选项。
- 系统管理：补 1 个停用账号样本，用于 `user_disabled_case`。

### 有数据但页面 QA 仍不完整

- 商品与活动：已有 `LISTED / UNLISTED / PENDING_REVIEW / ASSIGNED` 等数据，也有商品库与商品管理页面 QA；但最近浏览器报告中商品审核 / 转链等入口存在 skipped，因此 `pageCovered=partial`。

其余模块本轮页面覆盖推断为：

| 模块 | pageCovered |
|---|---|
| 角色权限 | true |
| 达人 | true |
| 寄样 | true |
| 订单与归因 | true |
| 数据平台 | true |
| 系统管理 | true |

### 暂按 warning 处理的特殊情况

- 商品与活动：`activity_without_products`、`support_traffic`、`not_support_traffic`、`sample_required`、`sample_not_required`、`convert_failed`
- 达人：`crawler_success`、`crawler_failed_manual_fallback`、`qualified_for_sample`、`not_qualified_for_sample`、`multi_claim`、`incomplete_profile`
- 寄样：`duplicate_within_7_days`、`completed_by_order`、`talent_not_qualified_but_reason_filled`
- 订单与归因：`AMBIGUOUS_MAPPING`、`EXCLUSIVE_TALENT`、`EXCLUSIVE_MERCHANT`、`order_created_before_mapping`、`order_created_after_mapping`、`zero_service_fee`、`cross_month`、`no_order_date`

---
