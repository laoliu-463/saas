# 达人域上线审查报告

审查日期：2026-05-27 | 审查基线：feature/auth-system @ cfd7236

## 一、达人 CRUD 与归属

**状态：PASS（有 P1 改进项）**

- TalentService 提供完整的达人增删改查
- 认领/释放使用 Redis 分布式锁（10s TTL），防止并发冲突
- 保护期由 BusinessRuleConfigService 配置控制
- 乐观锁（OptimisticLockSupport）防止并发更新覆盖

**发现：**
- **[P1] T-01 loadBoardSamples 无数量限制**：`TalentService.loadBoardSamples()` 查询精选联盟看板数据未设 LIMIT，大量数据可能导致内存溢出
  - 文件：`TalentService.java`
  - 修复：增加分页或数量上限

## 二、达人信息丰富（Enrich）

**状态：CRITICAL — P0**

- Enrich 任务状态机：PENDING → RUNNING → SUCCESS/FAILED/WAIT_MANUAL
- `ThirdPartyTalentProvider` 已接入 `ConfigurableHttpTalentProvider`：配置 `talent.profile.http.enabled=true` 与 endpoint 后，可通过第三方 HTTP 服务返回昵称、头像、粉丝、获赞、关注、作品数和 IP 属地等字段。
- `InternalBusinessTalentProvider` 仍是内部映射占位，未提供真实数据。

**发现：**
- **[P1] T-02 Enrich Provider 占位实现**：第三方 Provider 已具备配置化 HTTP 接入通道，但 real-pre 是否产生真实数据仍取决于外部 endpoint/凭证；内部 Provider 仍是占位。
  - 修复：配置并验证真实第三方 HTTP endpoint，或将内部 Provider 标记为 V2。

- **[P1] T-03 DouyinTalentCrawler 使用非公开 API**：爬虫调用的抖音接口非官方开放 API，存在封禁风险
  - 文件：`DouyinTalentCrawler.java`
  - 修复：迁移到官方 Open API 或明确标注为临时方案

## 三、达人批量导入

**状态：PARTIAL**

- TalentInputParser 解析 Excel 导入数据
- 去重逻辑存在，基于达人名称匹配
- 独家判断基于 30 天服务比率

**发现：**
- **[P1] T-04 超时查询无 LIMIT**：达人查询方法在大数据量场景下无分页限制，可能触发超时
  - 文件：`TalentService.java`
  - 修复：增加 LIMIT 或强制分页

## 四、达人标签系统

**状态：PASS（有 P2 改进项）**

- 标签归一化到预设标签库
- 每位达人最多 3 个标签
- 标签匹配支持模糊搜索

**发现：**
- **[P1] T-05 敏感词过匹配**：标签敏感词过滤可能误伤正常业务标签
  - 文件：`TalentService.java`
  - 修复：细化敏感词规则，增加白名单

## 五、达人黑名单

**状态：PASS**

- 黑名单 CRUD 完整
- DataScope 断言保证数据权限

## 六、发货地址管理

**状态：PASS**

- 基于认领关系的隔离，A 用户无法看到 B 用户的地址

## 七、状态机实现

**状态：PARTIAL**

**发现：**
- **[P1] T-06 状态机在 Controller 中实现**：部分状态流转逻辑在 Controller 层而非 Service 层
  - 修复：下沉到 Service 层，使用 State Pattern 或 Spring StateMachine

## 八、测试覆盖

**状态：FAIL**

**发现：**
- **[P1] T-07 TalentDataProviderTest 失败**：`defaultPriorityShouldBeOneHundred:15` — NoClassDefFoundError
  - 文件：`TalentDataProviderTest.java:15`
- **[P1] T-08 TalentProfileSyncServiceTest 大量失败**：NoClassDefFoundError for TalentInputParser/TalentProfileQuery/TalentProfileResult
  - 文件：`TalentProfileSyncServiceTest.java`

## 问题汇总

| 优先级 | 编号 | 问题 | 文件 |
|--------|------|------|------|
| P1 | T-01 | loadBoardSamples 无数量限制 | `TalentService.java` |
| P1 | T-02 | Enrich Provider 占位实现 | `ThirdPartyTalentProvider.java` |
| P1 | T-03 | DouyinTalentCrawler 非公开 API | `DouyinTalentCrawler.java` |
| P1 | T-04 | 超时查询无 LIMIT | `TalentService.java` |
| P1 | T-05 | 敏感词过匹配 | `TalentService.java` |
| P1 | T-06 | 状态机在 Controller 层 | `TalentService.java` |
| P1 | T-07 | TalentDataProviderTest 失败 | `TalentDataProviderTest.java:15` |
| P1 | T-08 | TalentProfileSyncServiceTest 大量失败 | `TalentProfileSyncServiceTest.java` |

## 2026-05-27 复核状态

| 编号 | 状态 | 证据 |
|------|------|------|
| T-01/T-04 | 已修复/已验证 | 当前 `TalentService` 公共池查询已将 `orderByDesc(fans)` 与 `limit 500` 下推到 SQL；私域池限制 500；`TalentQueryService` 单批拉取限制 200；`TalentServiceTest,TalentQueryServiceTest` 共 61/61 PASS |
| T-02 | 代码通道已收口，真实源待验证 | `ThirdPartyTalentProvider` 已委托 `ConfigurableHttpTalentProvider` 并映射旧 Enrich 字段；real-pre 已暴露 `TALENT_PROFILE_HTTP_*` 配置；`TalentDataProviderTest` 3/3 PASS。仍需真实 endpoint/凭证验证，`InternalBusinessTalentProvider` 作为 V2/内部映射项保留 |
| T-03/R-10 | 误启风险已收口，替代方案未完成 | protected profile 下已禁止 `talent.data.public-page-crawl-enabled=true` 和 `talent.collect.mode=crawler/api_then_crawler`；`DouyinTalentCrawler` 仍保留，官方/第三方替代源仍需外部权限或 V2 专项 |
| T-05 | 证据不足 | 当前未找到敏感词过滤实现证据，不能下最终结论 |
| T-06 | 证据不足 | 原报告证据写为 `TalentService.java`，与“Controller 层”结论不一致；需补充具体 Controller 方法证据 |
| T-07/T-08 | 已验证通过 | `TalentDataProviderTest`、`TalentProfileSyncServiceTest` 历史 NoClassDefFound 当前未复现；本轮定向核心回归 255/255 PASS |
