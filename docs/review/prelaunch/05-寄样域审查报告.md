# 寄样域上线审查报告

审查日期：2026-05-27 | 审查基线：feature/auth-system @ cfd7236

## 一、寄样流程

**状态：PASS**

- SampleController 提供寄样申请、审批、发货、签收、作业完成全链路
- 寄样状态机完整：PENDING → APPROVED → SHIPPED → RECEIVED → COMPLETED
- 通过订单已同步事件判断作业完成（不变量）

## 二、寄样 Controller 问题

**状态：已修复（P0 Controller 阻断项已收口）**

**关键发现：**
- **[P0] D-01 SampleController God Class（2736 行）**：`SampleController.java` 包含所有业务逻辑、VO/Request/Response 内部类、直接 Mapper 访问、16 个构造器依赖
  - 文件：`SampleController.java`（2736 行）
  - 问题：严重违反单一职责，可维护性极差
  - 修复：拆分为 SampleService + SampleController + DTO/VO 类

- **[P0] D-02 @Transactional 在 Controller 层**：5 个写操作方法在 Controller 上标注 @Transactional，违反分层架构
  - 文件：`SampleController.java`
  - 修复：移至 Service 层

## 三、寄样与订单联动

**状态：PASS**

- 寄样完成依赖订单域的已同步事件
- pick_source 在订单同步时写入，作为归因依据

## 四、DataController God Class

**状态：已修复（P1 Controller 阻断项已收口）**

- **[P1] R-03 DataController God Class（1850 行）**：包含 126 行聚合逻辑在 Controller 中，重复的 centToYuan 工具方法
  - 文件：`DataController.java`（1850 行）
  - 修复：拆分为 DashboardService + DashboardController

## 问题汇总

| 优先级 | 编号 | 问题 | 文件 |
|--------|------|------|------|
| P0 | D-01 | SampleController God Class（2736 行） | `SampleController.java` |
| P0 | D-02 | @Transactional 在 Controller 层 | `SampleController.java` |
| P1 | R-03 | DataController God Class（1850 行） | `DataController.java` |

## 2026-05-27 复核状态

该报告中的核心问题需按当前工作区事实拆分判断：

| 编号 | 状态 | 证据 |
|------|------|------|
| D-02 | 已修复 | Controller 目录 `rg "@Transactional" backend/src/main/java/com/colonel/saas/controller` 无匹配；寄样写接口事务由 `SampleWriteTransactionService` 承担；`SampleControllerTest,SampleLifecycleServiceTest` 在本轮 clean 回归中通过 |
| D-01 | 已修复 | `SampleController.java` 当前 374 行，HTTP 入口和 `@RequireRoles` 切面执行点保留在 controller 包；寄样业务实现下沉到 `service.sample.SampleApplicationService`；`SampleControllerTest` 68/68 PASS，核心回归 255/255 PASS |
| R-03 | 已修复 | `DataController.java` 当前 204 行，订单聚合/导出实现下沉到 `service.data.DataApplicationService`；`DataControllerTest` 35/35 PASS，`RoleGuardAspectTest,SampleControllerTest,DataControllerTest` 108/108 PASS |
