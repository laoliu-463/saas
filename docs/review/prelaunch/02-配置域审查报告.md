# 配置域上线审查报告

审查日期：2026-05-27 | 审查基线：feature/auth-system @ cfd7236

## 一、系统配置管理

**状态：PASS（有 P2 改进项）**

- SysConfigController 提供 CRUD 操作，@RequireRoles({ADMIN}) 类级保护
- 配置分组查询 /grouped 放开更多角色访问，符合业务需求
- BusinessRuleConfigService 为各业务域提供配置读取接口

**发现：**
- **[P2] CO-01 缺少 @Valid 注解**：SysConfigController create/update 方法的 @RequestBody 参数未加 @Valid，绕过 Bean Validation
  - 文件：`SysConfigController.java`
  - 修复：在 @RequestBody 参数上添加 @Valid 注解

## 二、配置变更审计

**状态：PASS**

- SystemConfigChangeLog 记录配置变更历史
- 包含变更前后的值对比
- 仅管理员可查询

## 三、环境配置 Profile 管理

**状态：PASS（环境已收口）**

| Profile | 行数 | 完整度 | 问题 |
|---------|------|--------|------|
| application.yml (base) | ~180 | 完整 | 默认 profile 指向 `test` |
| application-test.yml | — | 完整 | 测试环境 |
| application-real-pre.yml | — | 完整 | 真实上游 / 生产形态环境，关闭 test 标志 |

**关键发现：**
- **已修复 OPS-05/OPS-06/OPS-11**：独立 `application-prod.yml`、`application-real.yml`、`application-dev.yml`、`application-local-mock.yml` 已删除；生产形态配置统一进入 `application-real-pre.yml`。
- **已修复 CORS 占位风险**：real-pre 的 CORS 必须由 `CORS_ALLOWED_ORIGIN_PATTERNS` 注入，Guard/Jenkins 校验空值和不安全通配符。

## 四、RealProdEnvironmentGuard

**状态：PARTIAL**

Guard 在应用启动时只对 protected profile（real-pre）进行安全校验；`real-pre` 不允许与 `test` 混合启动。

**已校验项：**
- 无 test profile 混合
- app.test.enabled=false
- douyin.test.enabled=false
- douyin.webhook.verify-sign=true
- JWT secret 非空/非占位值
- Douyin 凭据非空
- CORS 模式安全

**未校验项：**
- **[P1] OPS-10 未校验 Redis 密码和 DB 密码**
- **[P1] OPS-12 未校验关键业务开关**（order.sync.enabled、logistics.provider、exclusive.enabled）
- `RuntimeExposurePolicy` 只在 `test` 暴露 Swagger 与 `/system/env`，real-pre 默认保护。

## 问题汇总

| 优先级 | 编号 | 问题 | 文件 |
|--------|------|------|------|
| P0 | OPS-05 | 独立 prod 配置严重残缺 | 已合并进 `application-real-pre.yml` |
| P0 | OPS-06 | prod CORS 占位域名 | 已合并为 real-pre 环境变量注入 |
| P1 | OPS-10 | Guard 未校验 Redis/DB 密码 | `RealProdEnvironmentGuard.java` |
| P1 | OPS-11 | real profile 配置不完整 | 已删除，统一 real-pre |
| P1 | OPS-12 | Guard 未校验业务开关 | `RealProdEnvironmentGuard.java` |
| P1 | R-09 | protected profile 识别不完整 | `RuntimeExposurePolicy.java` |
| P2 | CO-01 | SysConfigController 缺 @Valid | `SysConfigController.java` |

## 2026-05-27 复核状态

| 编号 | 状态 | 证据 |
|------|------|------|
| OPS-05/OPS-06 | 已修复 | 独立 prod 配置已合并到 `application-real-pre.yml`；CORS 无占位默认域名 |
| OPS-10 | 已修复 | Guard 已校验 `REDIS_PASSWORD` 与 `DB_PASSWORD` |
| OPS-11 | 已修复 | `application-real.yml` 已删除，统一 `real-pre` profile |
| OPS-12 | 已修复 | Guard 已校验 `order.sync.enabled`、`logistics.provider`、`exclusive.enabled` |
| R-09 | 已修复 | `RuntimeExposurePolicy` 改为 protected profile 口径 |
| CO-01 | 已修复 | `SysConfigController` create/update 请求体已加 `@Valid` |
