# 运维部署审查报告

审查日期：2026-05-27 | 审查基线：feature/auth-system @ cfd7236

## 一、Docker 部署

**状态：PASS（环境已收口，真实执行证据待补）**

| 配置文件 | 用途 | 问题 |
|---------|------|------|
| `docker-compose.test.yml` | 本地测试 | 仅测试用 |
| `docker-compose.real-pre.yml` | 真实上游 / 生产形态环境 | 合并原 prod 资源限制、镜像 tag、迁移入口 |

**关键发现：**
- **已修复 OPS-01/OPS-02/OPS-03**：独立 prod 入口已合并进 real-pre，`docker-compose.prod.yml`、`Dockerfile.dev`、`application-prod.yml` 不再作为运行入口。
- **阶段性修复 OPS-04**：Jenkinsfile 已收口到 real-pre 生产形态，使用版本化镜像 tag；真实回滚执行证据仍需补。
- **[P0] OPS-07 默认密码**：`init-db.sql:890` 使用 admin/admin123 作为种子数据密码
  - 文件：`db/init-db.sql:890`
- **[P0] OPS-08 无 HTTPS**：Nginx 配置仅监听 80 端口，无 SSL/TLS 配置
  - 文件：`frontend/Dockerfile`（nginx 配置内嵌）

## 二、Nginx 配置

**状态：CRITICAL — P1**

- `frontend/Dockerfile` 中内嵌 Nginx 配置
- 仅监听 80 端口，无 HTTPS
- 无安全响应头（HSTS、X-Content-Type-Options、X-Frame-Options、CSP）
- 无请求速率限制
- 代理目标硬编码为 `http://backend:8080`

**发现：**
- **[P1] OPS-13 Nginx 无安全头**：缺少 HSTS、X-Content-Type-Options、X-Frame-Options、CSP 等安全头
- **[P1] OPS-14 Nginx 无速率限制**：API 端点无 rate limiting，易受 DDoS 攻击
- **[P2] OPS-15 代理目标硬编码**：`proxy_pass http://backend:8080` 硬编码，应使用环境变量

## 三、数据库迁移

**状态：PARTIAL**

- 47 个 SQL 迁移脚本，仍以聚合脚本方式维护
- 已新增 real-pre 迁移执行脚本 `scripts/run-real-pre-db-migrations.sh`：启动/等待 `postgres-real-pre`、执行挂载的 `99-migrate-all.sql`、写入 `schema_migration_log(version, checksum, applied_at)`。
- Jenkins real-pre 部署顺序已调整为先启动 Postgres/Redis、执行迁移、再启动 backend/frontend。

**发现：**
- **[P1] OPS-16 无数据库迁移管理工具**：已补 real-pre 迁移执行脚本和迁移日志表；仍不是 Flyway/Liquibase 级别的逐脚本迁移管理，真实执行证据待补。
- **[P1] OPS-17 非幂等 DML**：`migrate-all.sql` 包含非幂等 UPDATE，重复执行结果不一致

## 四、Redis 配置

**状态：PARTIAL**

- Redis 用于 token 黑名单、分布式锁、缓存
- real-pre 环境 Redis 配置存在

**发现：**
- **[P2] OPS-18 Redis 无连接池配置**：未配置 Jedis/Lettuce 连接池参数
- **[P3] OPS-19 测试环境 Redis 无密码**：`docker-compose.test.yml` 中 Redis 未设置密码

## 五、Profile 管理

**状态：PASS**

- 2 个 Profile：test、real-pre
- RealProdEnvironmentGuard 只校验 real-pre protected profile，且禁止与 test 混用

**关键发现：**
- **已修复 OPS-05/OPS-06/OPS-11**：prod/real/dev/local-mock 配置文件已合并或删除，real-pre 为唯一真实上游 / 生产形态 profile。
- **[P1] OPS-10 Guard 未校验 Redis/DB 密码**：RealProdEnvironmentGuard 不校验数据源密码是否为空
- **[P1] OPS-12 Guard 未校验业务开关**：未校验 order.sync.enabled、logistics.provider、exclusive.enabled

## 六、镜像管理

**状态：PARTIAL**

**发现：**
- **[P2] OPS-20 前端基础镜像未版本锁定**：`FROM nginx:alpine` 未指定版本，构建不可重现
  - 文件：`frontend/Dockerfile:20`
- **[P2] OPS-21 pnpm install 无 --frozen-lockfile**：CI 构建可能因 lockfile 变更产生不同依赖
  - 文件：`frontend/Dockerfile:14`

## 七、资源限制

**状态：PARTIAL**

**发现：**
- **[P2] OPS-22 Docker 容器无资源限制**：docker-compose 文件未配置 CPU/内存限制
- **[P2] OPS-23 JVM 未配置容器感知**：未设置 `-XX:+UseContainerSupport` 等容器感知参数

## 八、日志与监控

**状态：PARTIAL**

- Spring Boot Actuator 暴露 health + info 端点
- 日志级别在 application.yml 中为 debug（非生产）

**发现：**
- **[P2] OPS-24 real-pre 日志级别未确认**：base profile 日志级别为 debug，real-pre profile 必须覆盖
- **[P3] OPS-25 缺少 .env.example 模板**：部署人员无法快速了解需要配置哪些环境变量

## 问题汇总

| 优先级 | 编号 | 问题 | 文件 |
|--------|------|------|------|
| P0 | OPS-01 | 独立生产环境 docker-compose 缺失 | 已合并进 `docker-compose.real-pre.yml` |
| P0 | OPS-02 | 生产 Dockerfile 未验证 | `Dockerfile` |
| P0 | OPS-03 | 无生产 pipeline | 已合并为 real-pre Jenkins 流水线 |
| P0 | OPS-04 | 部署无回滚机制 | `Jenkinsfile:141` |
| P0 | OPS-05 | prod 配置严重残缺 | 已合并进 `application-real-pre.yml` |
| P0 | OPS-06 | prod CORS 占位域名 | 已合并为 real-pre 环境变量注入 |
| P0 | OPS-07 | 默认密码 admin123 | `db/init-db.sql:890` |
| P0 | OPS-08 | 无 HTTPS | `frontend/Dockerfile` |
| P1 | OPS-10 | Guard 未校验 Redis/DB 密码 | `RealProdEnvironmentGuard.java` |
| P1 | OPS-11 | real profile 配置不完整 | 已删除，统一 real-pre |
| P1 | OPS-12 | Guard 未校验业务开关 | `RealProdEnvironmentGuard.java` |
| P1 | OPS-13 | Nginx 无安全头 | `frontend/Dockerfile` |
| P1 | OPS-14 | Nginx 无速率限制 | `frontend/Dockerfile` |
| P1 | OPS-16 | 无数据库迁移管理工具 | `db/migrate-all.sql` |
| P1 | OPS-17 | 非幂等迁移 DML | `db/migrate-all.sql` |
| P2 | OPS-15 | 代理目标硬编码 | `frontend/Dockerfile` |
| P2 | OPS-18 | Redis 无连接池配置 | `application.yml` |
| P2 | OPS-20 | 基础镜像未版本锁定 | `frontend/Dockerfile:20` |
| P2 | OPS-21 | pnpm 无 frozen-lockfile | `frontend/Dockerfile:14` |
| P2 | OPS-22 | 容器无资源限制 | `docker-compose.*.yml` |
| P2 | OPS-23 | JVM 未配置容器感知 | `docker-compose.*.yml` |
| P2 | OPS-24 | 生产日志级别未确认 | `application.yml` |
| P3 | OPS-19 | 测试 Redis 无密码 | `docker-compose.test.yml` |
| P3 | OPS-25 | 缺少 .env.example | 项目根目录 |

## 2026-05-27 复核状态

| 编号 | 状态 | 证据 |
|------|------|------|
| OPS-01 | 已修复 | 独立 prod 入口已合并进 `docker-compose.real-pre.yml`，`docker-compose.prod.yml` 已删除 |
| OPS-02 | 已修复 | real-pre 使用生产 Dockerfile / 版本化镜像口径；`Dockerfile.test` 仅用于 test |
| OPS-03/OPS-04 | 已修复 | Jenkinsfile 已收口到 real-pre 生产形态，包含环境守卫、版本化镜像部署和迁移串联 |
| OPS-05/OPS-06 | 已修复 | 独立 prod 配置已合并进 `application-real-pre.yml`，CORS 无占位默认域名 |
| OPS-07 | 已修复 | 种子密码改为 `ADMIN_PASSWORD` 注入，SQL 缺失变量时 fail-fast |
| OPS-08 | 配置已收口，real-pre 运行待验证 | `frontend/nginx/default.conf.template` 已启用 443、证书变量、HTTP→HTTPS 跳转；Jenkins real-pre 守卫校验证书文件存在；真实证书运行仍需部署环境验证 |
| OPS-10/OPS-11/OPS-12 | 已修复 | Guard 和 `application-real-pre.yml` 已补齐真实上游 / 生产形态配置安全网 |
| OPS-13/OPS-14/OPS-20/OPS-21 | 已修复 | 前端生产镜像锁定版本，pnpm frozen lockfile，Nginx 模板有安全头和 rate limit |
| OPS-16/OPS-17 | 工具化已补，real-pre 执行待验证 | 新增 `scripts/run-real-pre-db-migrations.sh`，Jenkins 部署阶段已串联迁移执行并记录 `schema_migration_log`；仍需真实 real-pre 流水线执行记录 |
| OPS-18/OPS-22/OPS-23/OPS-24/OPS-25 | 已修复/配置已收口 | Redis Lettuce 连接池已配置并补 `commons-pool2`；real-pre compose 已有资源限制和 JVM 容器参数；`.env.real-pre.example/.env.test.example` 已补关键变量；测试 Redis 已启用密码；real-pre 日志级别覆盖为 info/warn |
| OPS-19 | 已修复 | `docker-compose.test.yml` Redis 已启用 `--requirepass`，backend 注入 `REDIS_PASSWORD`，`.env.test/.env.test.example` 已设置测试密码；test compose 配置解析通过 |
