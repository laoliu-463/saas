# 三方联调审查报告

审查日期：2026-05-27 | 审查基线：feature/auth-system @ cfd7236

## 一、抖音 OAuth 授权

**状态：PARTIAL**

- DouyinOAuthController 提供授权入口和回调
- DouyinOAuthService 处理授权码换取 access_token
- DouyinTokenService 管理 token 刷新和存储

**发现：**
- **[P1] R-07 OAuth 重定向使用 HTTP**：`application.yml` 中 OAuth redirect_uri 默认使用 `http://`，生产环境应强制 HTTPS
  - 文件：`application.yml:138-140`
  - 修复：redirect_uri 默认改为 `${OAUTH_REDIRECT_URI:https://...}`

- **[P2] R-10 DouyinTalentCrawler 使用非公开接口**：爬取达人信息的 API 非官方开放平台接口
  - 文件：`DouyinTalentCrawler.java`
  - 修复：迁移到 Open API 或标注为临时方案

## 二、抖音订单同步

**状态：PASS（有 P1 改进项）**

- 订单同步从抖音开放平台拉取订单数据
- 支持增量同步（基于时间戳）
- pick_source 匹配形成归因链路

**发现：**
- **[P1] R-08 订单同步超时无断路器**：抖音 API 超时可能导致同步任务长时间阻塞
  - 修复：增加断路器或超时重试上限

## 三、抖音 Webhook

**状态：PASS**

- Webhook 端点已注册在公开路径（RuntimeExposurePolicy）
- 签名验证由 `douyin.webhook.verify-sign` 配置控制
- RealProdEnvironmentGuard 校验 real-pre 环境必须开启签名验证

## 四、百应平台集成

**状态：PASS**

- colonel_buyin_id 用于百应平台订单匹配
- 百应订单数据通过精选联盟 API 获取

## 五、快递 100 物流集成

**状态：PARTIAL**

- 物流配置在 real-pre 环境存在
- 快递 100 API 调用代码存在

**发现：**
- **[P1] R-09 物流配置曾在 prod 残缺**：原 `application-prod.yml` 缺少物流相关配置（logistics.provider、kdniao 等），现已合并进 real-pre
  - 文件：`application-real-pre.yml`
  - 修复：统一在 real-pre 配置中维护

## 六、精选联盟 API

**状态：PASS**

- 商品转链通过精选联盟 API 完成
- 看板数据查询通过精选联盟 API
- API 调用有错误处理和重试机制

## 问题汇总

| 优先级 | 编号 | 问题 | 文件 |
|--------|------|------|------|
| P1 | R-07 | OAuth 重定向使用 HTTP | `application.yml:138` |
| P1 | R-08 | 订单同步超时无断路器 | 订单同步 Service |
| P1 | R-09 | 物流配置 prod 残缺 | 已合并进 `application-real-pre.yml` |
| P2 | R-10 | 非公开 API 风险 | `DouyinTalentCrawler.java` |

## 2026-05-27 复核状态

| 编号 | 状态 | 证据 |
|------|------|------|
| R-07 | 已修复 | `application.yml` 中 OAuth redirect 默认值已从 HTTP 改为 HTTPS；real-pre 继续要求环境变量注入 |
| R-08 | 已修复 | `OrderSyncService` 已加入本地连续失败断路器，配置项为 `order.sync.circuit-breaker.failure-threshold/open-duration`；`OrderSyncServiceTest` 10/10 PASS |
| R-09 | 已修复 | `application-real-pre.yml` 已补齐 `logistics.*`、`kdniao.*` 配置 |
| R-10 | 误启风险已收口，替代方案未完成 | protected profile 下禁止 crawler 模式和 public-page crawl；第三方 HTTP 资料通道已通过 `TALENT_PROFILE_HTTP_*` 配置接入旧 Enrich Provider；非公开 crawler 类仍保留，官方替代源仍需外部权限或后续专项 |
