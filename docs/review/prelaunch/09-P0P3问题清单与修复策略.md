# P0-P3 全量问题清单与修复策略

审查日期：2026-05-27 | 审查基线：feature/auth-system @ cfd7236

## 问题统计

| 优先级 | 数量 | 说明 |
|--------|------|------|
| P0 | 8 | 阻断上线，必须修复 |
| P1 | 19 | 严重影响，上线前应修复 |
| P2 | 12 | 改进项，可排入下迭代 |
| P3 | 5 | 低优先级，记录跟踪 |
| **合计** | **44** | |

## 2026-05-27 复核与修复状态

本节基于当前工作区文件和本轮验证结果更新，优先以可执行命令、代码事实和配置解析为证据。未能通过证据闭环的问题不改写为 PASS。

| 状态 | 编号 | 复核结论 | 证据 |
|------|------|----------|------|
| 已修复 | OPS-01 | 独立生产 Compose 已合并进 `docker-compose.real-pre.yml`，仅保留 test 与 real-pre | `docker compose --env-file .\.env.real-pre.example -f .\docker-compose.real-pre.yml config --quiet` PASS |
| 已修复 | OPS-03/OPS-04 | Jenkinsfile 已收口为 real-pre 生产形态守卫、版本化镜像部署和迁移串联；修复了镜像 build 后错误依赖 `latest` tag 的问题 | `Jenkinsfile` real-pre 阶段 |
| 已修复 | OPS-02 | real-pre 使用生产 Dockerfile / 版本化镜像口径，test 使用 `Dockerfile.test` | `mvn package -DskipTests` 与 `docker compose --env-file .\.env.real-pre.example -f .\docker-compose.real-pre.yml build backend-real-pre frontend-real-pre` PASS |
| 已修复 | OPS-05/OPS-06/R-09 | 独立 prod/real 配置已合并进 `application-real-pre.yml`，CORS/OAuth 生产值均改为环境变量注入，无 `saas.example.com` 默认值 | `backend/src/main/resources/application-real-pre.yml` |
| 已修复 | OPS-07 | 管理员和演示用户种子密码改为 `ADMIN_PASSWORD` 注入，SQL 缺失该变量时 fail-fast | `init-db.sql`、`migrate-all.sql`、`00-set-env.sh` |
| 已修复 | OPS-10/OPS-12 | Guard 已校验 DB/Redis 密码、订单同步开关、物流 provider、独家开关 | `RealProdEnvironmentGuard.java` 与测试 |
| 已修复 | OPS-11 | `application-real.yml` 已删除，真实上游 / 生产形态统一使用 `real-pre` profile | `application-real-pre.yml` |
| 已修复 | OPS-13/OPS-14/OPS-20/OPS-21 | Nginx 模板已有安全头，新增 API rate limit；前端生产镜像锁定 nginx 版本并使用 `pnpm --frozen-lockfile` | `frontend/Dockerfile`、`frontend/nginx/default.conf.template` |
| 已修复 | U-01/U-02/U-03/U-08 | protected profile 下 JWT fail-fast；DEPT 访问校验同部门；Refresh Token rotation；Dashboard 缺 deptId 不再退化为 ALL | 关联后端测试 81/81 通过 |
| 已修复 | U-06 | 运行时移除 MVC 层 JWT 双重校验，只保留 Spring Security `JwtAuthenticationFilter` | `WebConfigTest/SecurityConfigTest/JwtAuthenticationFilterTest/JwtAuthInterceptorTest/TestControllerSecurityTest` 26/26 PASS |
| 已加固 | U-07 | 管理员重置密码后强制用户下次改密；是否要求管理员提供原密码仍属于业务策略 | `SysUserServiceTest` 覆盖 `forcePasswordChange=true` |
| 已修复 | CO-01/R-04/R-07 | SysConfig 请求体加 `@Valid`；业绩存在性查询改为 `EXISTS`；OAuth redirect 默认改为 HTTPS | 关联后端测试 35/35 通过 |
| 已修复 | R-08 | 订单同步新增本地连续失败断路器和配置项 | `OrderSyncServiceTest` 10/10 PASS |
| 已修复 | OPS-18/OPS-19 | Redis Lettuce 连接池已配置；测试 Redis 已加密码 | `docker compose --env-file .\.env.test -f .\docker-compose.test.yml config --quiet` 通过 |
| 已修复 | U-04 | 订单分页 mapper 不再叠加 `@DataScope`，订单/Data/OrderAttribution 调用方统一显式拼接数据范围 | `DataScopeAspectTest,DataControllerTest,OrderAttributionControllerTest` 44/44 PASS |
| 代码通道已收口，真实源待验证 | T-02 | `ThirdPartyTalentProvider` 已接入可配置 HTTP Provider；real-pre 暴露 `TALENT_PROFILE_HTTP_*` 配置 | `TalentDataProviderTest` 3/3 PASS；需真实 endpoint/凭证验证 |
| 工具化已补，real-pre 执行待验证 | OPS-16/R-05 | 新增 real-pre 迁移脚本并接入 Jenkins；迁移成功后写 `schema_migration_log`；分区维护检查仍保留 | `bash -n scripts/run-real-pre-db-migrations.sh` PASS；需 real-pre 流水线执行记录 |
| 已修复 | OPS-24/U-09 | real-pre 日志级别已覆盖；CORS 生产值必须由环境变量注入且 Guard 校验不安全通配符 | `application-real-pre.yml`、`RealProdEnvironmentGuardTest` |
| 已验证不成立 | PR-01/T-07/T-08 | 文档中记录的 NoClassDefFound / Mockito 失败当前未复现 | `ProductDisplayAuditServiceTest`、`TalentDataProviderTest`、`TalentProfileSyncServiceTest` 共 14/14 通过 |
| 已修复 | D-02 | Controller 目录已无 `@Transactional`，寄样写事务下沉到 `SampleWriteTransactionService` | `rg "@Transactional" backend/src/main/java/com/colonel/saas/controller` 无匹配；clean 定向回归通过 |
| 已修复 | D-01 | `SampleController` 当前 374 行，HTTP 入口与 `@RequireRoles` 切面执行点保留在 controller 包；寄样业务实现下沉到 `service.sample.SampleApplicationService` | `SampleControllerTest` 68/68 PASS；核心回归 255/255 PASS |
| 已修复 | R-03 | `DataController` 当前 204 行，订单聚合/导出实现下沉到 `service.data.DataApplicationService` | `DataControllerTest` 35/35 PASS；`RoleGuardAspectTest,SampleControllerTest,DataControllerTest` 108/108 PASS |
| 配置已收口，运行待验证 | OPS-08 | Nginx/compose/Jenkins 已补 443、证书挂载、HTTP 跳转和证书守卫；真实证书运行待部署验证 | real-pre compose 配置解析和镜像构建 PASS |
| 外部依赖待确认 | T-03/R-10 | protected profile 已禁止 crawler 误启；官方替代源仍依赖外部权限或 V2 专项 | `RealProdEnvironmentGuardTest` 覆盖 crawler fail-fast |

## P0 — 阻断上线（8 项）

> 以下为原始审查问题清单，保留用于追溯；当前状态以“2026-05-27 复核与修复状态”表为准。

| 编号 | 领域 | 问题 | 证据 | 修复策略 |
|------|------|------|------|----------|
| OPS-01 | 运维 | 生产环境入口分裂 | 曾同时维护 test/real-pre/prod | 合并到 real-pre，删除独立 prod compose |
| OPS-02 | 运维 | 生产 Dockerfile 从未使用 | real-pre 曾使用 `Dockerfile.dev`，`Dockerfile` 未验证 | real-pre 使用生产 Dockerfile，test 使用 `Dockerfile.test` |
| OPS-03 | 运维 | 生产 pipeline 分裂 | `Jenkinsfile` 曾仅覆盖 real-pre，另补 prod stage | 收口为 real-pre 生产形态 pipeline |
| OPS-04 | 运维 | 部署无回滚 | `Jenkinsfile:141` 使用 `down && up -d --build` | 使用版本化镜像（git sha tag），保留上一版本镜像用于回滚 |
| OPS-05 | 配置 | prod 配置严重残缺 | `application-prod.yml` 缺少 douyin.token、logistics、talent.*、performance 等 10+ 配置节 | 合并到 `application-real-pre.yml` 后删除 |
| OPS-06 | 配置 | prod CORS 占位域名 | `application-prod.yml:40` 默认 `https://saas.example.com` | 改为 real-pre 环境变量注入 |
| OPS-07 | 运维 | 默认密码 admin123 | `db/init-db.sql:890` | 种子数据密码改为环境变量注入，首次登录强制修改 |
| OPS-08 | 运维 | 无 HTTPS | `frontend/Dockerfile` nginx 仅监听 80 | 配置 SSL 证书（Let's Encrypt 或云厂商证书），强制 HTTPS 重定向 |
| D-01 | 寄样 | SampleController God Class（2736 行） | `SampleController.java` 含全部业务逻辑、VO、16 个依赖 | 拆分为 SampleService + Controller + DTO/VO |
| D-02 | 寄样 | @Transactional 在 Controller | `SampleController.java` 5 个写方法 | 移至 Service 层 |

> 注：D-01/D-02 虽为代码质量问题，但因文件过大（2736 行）严重影响可维护性，且影响运行时事务边界，提升为 P0。

## P1 — 上线前应修复（19 项）

| 编号 | 领域 | 问题 | 证据 | 修复策略 |
|------|------|------|------|----------|
| U-01 | 用户 | JWT 密钥占位值 | `application.yml:64` 默认 `dev-secret-key...` | 启动时 fail-fast，未设置 JWT_SECRET 直接拒绝启动 |
| U-02 | 用户 | DEPT 数据权限越权 | `SysUserService.java:394` assertCanAccess 仅检查 PERSONAL | 增加 DEPT 范围的部门校验 |
| U-03 | 用户 | Refresh Token 无轮换 | `AuthService.java:220` 复用原 refresh token | 实现 rotation：签发新 refresh token，旧的加入黑名单 |
| T-01 | 达人 | loadBoardSamples 无数量限制 | `TalentService.java` | 增加分页或数量上限（如 500） |
| T-02 | 达人 | Enrich Provider 占位实现 | `ThirdPartyTalentProvider.java`（37 行）返回空 | 接入真实 API 或在 real-pre 标记 PENDING |
| T-03 | 达人 | 非公开 API 风险 | `DouyinTalentCrawler.java` | 迁移到 Open API 或明确标注临时方案 |
| T-04 | 达人 | 超时查询无 LIMIT | `TalentService.java` | 增加强制分页，max 1000 |
| T-05 | 达人 | 敏感词过匹配 | `TalentService.java` | 细化规则，增加业务白名单 |
| T-06 | 达人 | 状态机在 Controller | `TalentService.java` 部分状态流转 | 下沉到 Service 层 |
| T-07 | 达人 | TalentDataProviderTest 失败 | `TalentDataProviderTest.java:15` NoClassDefFound | 检查类路径，补全依赖 |
| T-08 | 达人 | TalentProfileSyncServiceTest 大量失败 | `TalentProfileSyncServiceTest.java` NoClassDefFound | 补全 TalentInputParser 等类 |
| R-03 | 跨域 | DataController God Class（1850 行） | `DataController.java` 含 126 行聚合逻辑 | 拆分为 Service + Controller |
| R-04 | 订单 | hasPerformanceRecords 全表 COUNT | `PerformanceService.java` | 改用 EXISTS 子查询或 LIMIT 1 |
| R-05 | 订单 | 分区过期风险 | `db/init-db.sql` pg_cron 未配置 | 配置 pg_cron 或部署脚本添加检查 |
| R-06 | 订单 | 非幂等迁移 DML | `db/migrate-all.sql:1092` | 改用 INSERT ON CONFLICT |
| R-07 | 三方 | OAuth 重定向 HTTP | `application.yml:138` | 默认改为 HTTPS |
| R-08 | 三方 | 订单同步超时无断路器 | 订单同步 Service | 增加 Resilience4j CircuitBreaker |
| OPS-10 | 运维 | Guard 未校验 Redis/DB 密码 | `RealProdEnvironmentGuard.java` | 增加密码非空校验 |
| OPS-11 | 运维 | real profile 配置不完整 | `application-real.yml` 51 行 vs real-pre 66 行 | 删除 real profile，统一 real-pre |
| OPS-12 | 运维 | Guard 未校验业务开关 | `RealProdEnvironmentGuard.java` | 增加 order.sync.enabled 等校验 |
| OPS-13 | 运维 | Nginx 无安全头 | `frontend/Dockerfile` | 添加 HSTS、X-Content-Type-Options 等 |
| OPS-14 | 运维 | Nginx 无速率限制 | `frontend/Dockerfile` | 添加 limit_req_zone |

## P2 — 下迭代改进（12 项）

| 编号 | 领域 | 问题 | 证据 |
|------|------|------|------|
| U-04 | 用户 | 双重数据权限实现（注解 + 手动） | `OrderController.java`, `DataScopeAspect.java` |
| U-05 | 用户 | SystemEnvController 手动权限 | `SystemEnvController.java` |
| U-06 | 用户 | 双重 JWT 校验 | `JwtAuthenticationFilter.java` + `JwtAuthInterceptor.java` |
| R-09 | 配置 | protected profile 识别不完整 | `RuntimeExposurePolicy.java` |
| R-10 | 三方 | 非公开 API 风险 | `DouyinTalentCrawler.java` |
| CO-01 | 配置 | SysConfigController 缺 @Valid | `SysConfigController.java` |
| PR-01 | 商品 | 审计服务测试失败（2 用例） | `ProductDisplayAuditServiceTest.java` |
| OPS-15 | 运维 | Nginx 代理目标硬编码 | `frontend/Dockerfile` |
| OPS-18 | 运维 | Redis 无连接池配置 | `application.yml` |
| OPS-20 | 运维 | 基础镜像未版本锁定 | `frontend/Dockerfile:20` |
| OPS-21 | 运维 | pnpm 无 frozen-lockfile | `frontend/Dockerfile:14` |
| OPS-22 | 运维 | 容器无资源限制 | `docker-compose.*.yml` |
| OPS-23 | 运维 | JVM 未配置容器感知 | `docker-compose.*.yml` |
| OPS-24 | 运维 | 生产日志级别未确认 | `application.yml` |

## P3 — 记录跟踪（5 项）

| 编号 | 领域 | 问题 | 证据 |
|------|------|------|------|
| U-07 | 用户 | 管理员重置密码无需原密码 | `SysUserService.java:336` |
| U-08 | 用户 | DEPT 范围 deptId null 退化为 ALL | `DashboardService.java:582` |
| U-09 | 用户 | CORS 生产配置确认 | `WebConfig.java` |
| OPS-19 | 运维 | 测试 Redis 无密码 | `docker-compose.test.yml` |
| OPS-25 | 运维 | 缺少 .env.example | 项目根目录 |

## 修复优先级建议

### 第一阶段：阻断项（P0，预计 5-8 人天）

1. **配置收口**（OPS-05, OPS-06）：从 prod/real 合并到 real-pre，替换占位域名
2. **部署基础设施收口**（OPS-01, OPS-02, OPS-03, OPS-04）：删除独立 prod，real-pre 使用生产形态 compose、Dockerfile 和 pipeline
3. **安全加固**（OPS-07, OPS-08）：密码环境变量化、HTTPS 配置
4. **代码拆分**（D-01, D-02）：已完成，当前以复核表证据为准

### 第二阶段：关键改进（P1，预计 8-12 人天）

1. **安全修复**（U-01, U-02, U-03）：JWT fail-fast、DEPT 越权、Token 轮换
2. **测试修复**（T-07, T-08）：修复 NoClassDefFound 错误
3. **性能优化**（R-04, T-01, T-04）：全表 COUNT、无 LIMIT 查询
4. **Guard 增强**（OPS-10, OPS-11, OPS-12）：补齐校验规则
5. **Nginx 安全**（OPS-13, OPS-14）：安全头 + 速率限制

### 第三阶段：质量提升（P2，预计 3-5 人天）

1. 消除 JWT 双重校验（U-06）
2. 统一数据权限实现（U-04）
3. 镜像版本锁定（OPS-20, OPS-21）
4. 容器资源限制（OPS-22, OPS-23）
