# 商品域增强上线验收清单

## 数据库

- [ ] 执行 `migrate-all.sql` 或 `alter-product-domain-enhanced-20260523.sql`
- [ ] 确认 `product_display_audit_log`、`product_display_rule_version` 存在
- [ ] 确认 `domain_event_outbox.event_key` 唯一索引
- [ ] 确认 `colonel_partner` 扩展列、`sample_request` 外部字段

## 后端

- [ ] `mvn -q -DskipTests compile` 通过
- [ ] 展示规则单测：`ProductDisplayRuleServiceTest`
- [ ] Outbox 单测：`OutboxEventAppenderTest`
- [ ] 快速寄样：`QuickSampleApplyTest`（含 LOCAL_FALLBACK）、`DouyinQuickSampleGatewayTest`
- [ ] 置顶过期清理：`ProductPinServiceTest`、`ProductPinCleanupJobTest`
- [ ] 团长同步：`ColonelPartnerSyncServiceTest`

## 前端

- [ ] `npm run build` 通过
- [ ] 商品库筛选项 UI 与 API 参数一致
- [ ] 快速寄样弹窗与 LOCAL_FALLBACK 提示
- [ ] 投流规则详情弹窗（点击「可投流」→ 规则文本 / 「暂无投流规则」）

## E2E

- [ ] `npx playwright test tests/e2e/product-domain-final-gap.spec.ts`（LOCAL_FALLBACK 提示）
- [ ] `npx playwright test tests/e2e/product-domain-enhanced-complete.spec.ts`（需 stack 运行）

## 外部能力诚实口径

- [x] quick_sample_apply：**UNSUPPORTED_BY_SDK**（适配层完成，非真实上游）
- [ ] 官方 API 确认后更新为 REAL_CONNECTED

## 已知仓库既有问题

- `KdniaoConfig` 导致部分测试编译/上下文加载失败（非本次商品域引入）
