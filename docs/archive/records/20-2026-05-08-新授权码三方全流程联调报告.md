# 20-2026-05-08 新授权码三方全流程联调报告

执行时间：2026-05-08 09:56 ~ 09:58（Asia/Shanghai）

## 1. 执行结论

本轮使用新的 OAuth 授权码完成 `POST /api/douyin/tokens` 初始化，真实 token 写入 real-pre Redis 后，继续完成授权主体、活动、活动商品、转链 raw probe、订单同步、订单列表、Dashboard 与店铺侧订单权限探针。

结论：

- 新授权码换 token：成功
- Token 状态与刷新：成功
- 授权主体：成功，上游 `10000 / success`
- 活动列表与活动详情：成功，上游 `10000 / success`
- 活动商品列表与业务刷新：成功，上游 `10000 / success`
- 转链上游 raw probe：成功，上游 `10000 / success`，返回 `data.product_url`
- 团长侧订单同步：成功，近 30 分钟窗口拉取 10 单，新增 7 单，更新 3 单，失败 0
- 订单列表与 Dashboard：成功，订单列表总数 31，Dashboard createTime 口径今日订单 20、今日 GMV 370.88
- 前端对接与端到端验证：成功，新增管理员入口 `/system/douyin`，real-pre 前端 E2E `1/1 PASS`
- 店铺侧订单 `order.searchList / order.orderDetail`：仍为 `30001 / isv.app-permissions-insufficient`
- 日志敏感字段扫描：最近 500 行 real-pre 后端日志未命中新授权码、`access_token=`、`refresh_token=`、`sign=`、`DOUYIN_CLIENT_SECRET`、`app_secret`

## 2. 环境与脱敏

| 项 | 值 |
| --- | --- |
| 环境 | real-pre |
| Base URL | `http://localhost:8081/api` |
| AppId | `7623...7199` |
| 授权码 | `f33e...5801` |
| Spring Profile | `local-mock` |
| 真实上游开关 | `DOUYIN_TEST_ENABLED=false`、`APP_TEST_ENABLED=false`、`ORDER_SYNC_ENABLED=false` |
| HTTP 超时 | `DOUYIN_CONNECT_TIMEOUT=3s`、`DOUYIN_READ_TIMEOUT=5s` |

报告中不记录完整 OAuth code、JWT、access token、refresh token、client secret 或签名值。

## 3. 分步结果

| 步骤 | 接口 | 结果 |
| --- | --- | --- |
| 健康检查 | `GET /api/actuator/health` | HTTP 200，`UP` |
| 登录 | `POST /api/auth/login` | HTTP 200，JWT 获取成功 |
| Token 初始化 | `POST /api/douyin/tokens` | HTTP 200，`code=200`；`hasAccessToken=true`、`hasRefreshToken=true`、`reauthorizeRequired=false` |
| Token 状态 | `GET /api/douyin/tokens` | HTTP 200，token 与 refresh token 均存在 |
| Token 刷新 | `POST /api/douyin/token-refreshes` | HTTP 200，刷新后 token 状态正常 |
| 授权主体 | `GET /api/douyin/institution-info` | HTTP 200，上游 `code=10000 / msg=success`，`logId=202605080956307ED1F0FD3D0D997455F9` |
| 活动列表 | `GET /api/douyin/activities` | HTTP 200，上游 `code=10000 / msg=success`，选定 `activityId=3916506` |
| 活动详情 | `GET /api/douyin/activities/3916506` | HTTP 200，上游 `code=10000 / msg=success` |
| 活动商品 raw | `GET /api/douyin/activity-product-list` | HTTP 200，上游 `code=10000 / msg=success`，返回 20 条活动商品 |
| 活动商品业务刷新 | `GET /api/colonel/activities/3916506/products?refresh=true` | HTTP 200，业务总数 28，首屏 20 条 |
| 业务转链尝试 | `POST /api/colonel/activities/3916506/products/{productId}/promotion-links` | 本轮样本触发业务前置拦截：已转链样本返回 `LINKED`，未入库样本返回“请先将商品加入商品库”；非三方上游失败 |
| 转链 raw probe | `POST /api/douyin/promotion-link-probes/raw` / `buyin.instPickSourceConvert` | HTTP 200，上游 `code=10000 / msg=success`，返回 `data.product_url`，`logId=20260508095812B88BCD9D21AE0D8B025B` |
| 订单同步 | `POST /api/orders/sync` | HTTP 200，窗口 `2026-05-08 09:26:35 ~ 09:56:35`，拉取 10，新增 7，更新 3，失败 0 |
| 订单列表 | `GET /api/orders?page=1&size=5` | HTTP 200，当前总数 31，首屏 5 条 |
| Dashboard | `GET /api/dashboard/metrics?timeField=createTime` | HTTP 200，今日订单 20，今日 GMV 370.88 |
| 前端 E2E | `node runtime/qa/real-pre-douyin-frontend-e2e.cjs` | `1/1 PASS`，报告 `runtime/qa/out/real-pre-douyin-frontend-20260508105215/report.md` |
| 店铺订单列表 | raw `method=order.searchList` | HTTP 200 包装返回，上游业务失败 `30001 / isv.app-permissions-insufficient`，`logId=20260508095634720A58E7394B6255A930` |
| 店铺订单详情 | raw `method=order.orderDetail` | HTTP 200 包装返回，上游业务失败 `30001 / isv.app-permissions-insufficient`，`logId=2026050809563599491A933274DD821B32` |

## 4. 关键校验

### 4.1 新授权码是否生效

`POST /api/douyin/tokens` 返回成功，并且随后的 `GET /api/douyin/tokens` 与 `POST /api/douyin/token-refreshes` 均确认：

- `hasAccessToken=true`
- `hasRefreshToken=true`
- `tokenExpiringSoon=false`
- `reauthorizeRequired=false`

说明这次不是沿用旧 token 的只读 smoke，而是新授权码已完成初始化。

### 4.2 上游链路是否可达

授权主体、活动列表、活动详情、活动商品、转链 raw probe 均返回上游 `10000 / success`，说明当前应用、签名、access token 与真实上游网络链路正常。

### 4.3 业务主链路是否可读

订单同步后：

- `POST /api/orders/sync`：`created=7 / updated=3 / failed=0`
- `GET /api/orders`：总数 31
- `GET /api/dashboard/metrics?timeField=createTime`：今日订单 20、今日 GMV 370.88

说明团长侧订单主同步、落库、订单列表和 Dashboard createTime 统计链路可用。

### 4.4 转链业务前置条件

本轮业务转链接口没有选到一个“已入库且状态为 ASSIGNED”的新商品：

- 已转链样本会被状态机拦截，返回“当前状态不允许执行 PROMOTION_LINK，当前状态：LINKED”
- 未入库样本会被业务前置校验拦截，返回“请先将商品加入商品库后再生成推广链接”

这属于项目业务状态机约束，不是抖店上游转链能力失败。为隔离三方能力，本轮补跑 raw probe，确认 `buyin.instPickSourceConvert` 在合规 `pick_extra=ch_0508` 下上游成功。

### 4.5 店铺侧订单阻塞

`order.searchList` 与 `order.orderDetail` 仍被上游权限包拦截：

- 错误码：`30001`
- 子码：`isv.app-permissions-insufficient`
- 信息：应用无权限调用该接口，请先申请接口权限包

该结论与此前店铺侧订单备忘录一致。权限包未生效前，不继续实现收货人信息补全或 `order.batchDecrypt` 成功样本验证。

### 4.6 前端对接与 E2E

本轮已将抖店联调状态接入管理员前端入口：

- 路由：`/system/douyin`
- 菜单：系统管理 / 抖店联调
- 页面动作：`一键刷新联调状态`
- 覆盖状态：Token、授权主体、活动商品刷新、订单同步、Dashboard createTime 指标、店铺侧订单权限阻塞
- E2E：`runtime/qa/real-pre-douyin-frontend-e2e.cjs`
- 报告：`runtime/qa/out/real-pre-douyin-frontend-20260508105215/report.md`（`1/1 PASS`）
- review 修复：活动 ID 留空时改为优先使用活动列表探测结果；E2E 脚本去掉机器绝对路径绑定；一键刷新前先清空旧卡片状态与“最近一次执行”摘要，失败步骤会显式落为 `error`

页面只调用内部 `/api`，未直连抖店上游；执行摘要中的 appId 已脱敏展示，仍不展示 token、secret 或签名。

## 5. 后续动作

1. 等订单管理接口权限包、店铺授权、敏感数据授权生效后，按 `docs/archive/records/18-抖店店铺侧订单接口联调备忘录.md` 第 11 节继续跑 `order.searchList / order.orderDetail / order.batchDecrypt`。
2. 若要重新验证业务转链完整状态机，需要先选一个新活动商品，按“加入商品库 -> 审核通过 -> 分配到招商 -> 转链”的前置流程推进到 `ASSIGNED`。
3. 商品详情 / SKU 仍需商品详情接口权限包，不能用活动商品快照替代上游商品详情样本。
4. Webhook 仍需抖店控制台真实投递、业务消费、幂等落库与重放补偿验证。
