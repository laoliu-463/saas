# 接口整改决策备忘录

> 文档状态：持续更新  
> 创建时间：2026-04-28  
> 用途：记录接口导入 APIFOX 整改过程中的待确认项、最终决策与影响范围，便于后续追溯。

## 1. 决策记录格式

后续新增记录统一使用以下格式：

| 编号 | 主题 | 当前状态 | 最终结论 | 影响范围 | 结论日期 | 备注 |
|---|---|---|---|---|---|---|
| D-001 | 示例 | 待确认 | 未确定 | `OrderController`、前端 `order.ts` | - | 示例占位 |

## 2. 当前待确认项

| 编号 | 主题 | 当前状态 | 最终结论 | 影响范围 | 结论日期 | 备注 |
|---|---|---|---|---|---|---|
| D-001 | 业务接口分页参数是否统一为 `page` + `size` | 待确认 | 未确定 | `OrderController`、`ColonelActivityController`、相关前端 API、相关测试 | - | 联调 / 透传接口建议保留 `pageSize` |
| D-002 | 达人释放接口是否调整命名 | 待确认 | 未确定 | `TalentController`、前端 `talent.ts`、`docs/05` | - | 当前保留 `POST /{id}/release` |
| D-003 | `/douyin/webhooks` 是否在 OpenAPI 中公开展示 | 待确认 | 未确定 | `DouyinWebhookController`、OpenAPI 暴露策略、APIFOX | - | 若隐藏，考虑 `@Hidden` 或路径排除 |
| D-004 | 业务分组是否统一合并重命名 | 待确认 | 未确定 | 多个 Controller、`docs/05`、APIFOX 分组展示 | - | 当前先不动现有业务 Tag |

## 3. 枚举说明待补全清单

以下参数如代码中缺少明确常量、枚举类或映射说明，Swagger 中只能先标注“待确认：取值含义请联系产品”：

| 参数名 | 所在接口/模块 | 当前依据 | 状态 |
|---|---|---|---|
| `status` | 团长活动、订单、联调相关多个接口 | 不同模块语义可能不同 | 待逐一核实 |
| `searchType` | 团长活动、联调活动查询 | 需核对代码常量或上游 SDK 文档 | 待确认 |
| `sortType` | 团长活动、联调活动查询 | 需核对代码常量或上游 SDK 文档 | 待确认 |
| `retrieveMode` | 联调或商品查询相关接口 | 需核对代码定义 | 待确认 |

## 4. 已确认共识

| 编号 | 主题 | 结论 | 影响范围 | 结论日期 | 备注 |
|---|---|---|---|---|---|
| C-001 | `/test` 接口分组 | 统一归到 `测试工具`，summary 加 `[测试]` | `TestController`、APIFOX 分组 | 2026-04-28 | 直接执行项 |
| C-002 | `/douyin` 接口分组 | 统一归到 `抖音联调`，summary 加 `[联调]` | `DouyinController`、`DouyinWebhookController`、APIFOX 分组 | 2026-04-28 | 直接执行项 |
| C-003 | 废弃商品接口展示口径 | 保留废弃接口，但通过 `[已废弃]` 和替代说明降噪 | `ProductController`、APIFOX 分组、`docs/05` | 2026-04-28 | 直接执行项 |
| C-004 | 文档编码约束 | 文档统一使用 UTF-8 保存，避免二次转码 | `docs/*.md` | 2026-04-28 | 参照 `docs/12` |
| C-005 | OpenAPI 编码校验 | 导出的 OpenAPI JSON 字节流为 UTF-8 正常，PowerShell 控制台可能出现假性乱码，验收以 UTF-8 解析结果和 APIFOX 导入结果为准 | `SwaggerConfig`、`application.yml`、`out/openapi-*.json` | 2026-04-28 | 已通过 Node UTF-8 解析验证 |
| C-006 | 前端 API 兼容性 | 本轮整改未改动已确认的业务路径和参数名，前端 `src/api/*.ts` 当前无需联动修改；旧 `/products/*` 调用仍按废弃兼容接口保留 | `frontend/src/api/*.ts`、`docs/05` | 2026-04-28 | 分页统一、达人路径重命名仍待后续单独决策 |

## 5. 执行后补记规则

- 每完成一项“待确认项”的决策，都要先更新本文档，再改代码或改文档
- 每次改动若影响前端 `src/api/*.ts`、测试用例、OpenAPI 暴露范围，必须在“影响范围”中写明
- 若后续上游抖音 SDK 字段或能力项变更，也在本文档追加记录，避免 Swagger 描述再次过期
