# 06-部署与对接计划

更新时间：2026-05-22

---

## Docker 启动命令速查

### 环境一览

| 环境 | 用途 | 前端 | 后端 | 数据库 |
|------|------|------|------|--------|
| `test` | 系统功能 / 权限测试 / 本地 Mock 联调 / 自动化测试 | `3000` | `8080` | `saas_test` |
| `real-pre` | 真实 SDK / 真实抖店上游联调 | `3001` | `8081` | `saas_real_pre` |

执行原则：

- 同一时间只允许启动一个 SAAS 环境
- `test` 使用 `docker-compose.test.yml`，`real-pre` 使用 `docker-compose.real-pre.yml`
- `test` compose project name 为 `saas-test`，`real-pre` 为 `saas-active`
- 环境切换前必须先执行 `scripts/stop-all.ps1`，启动脚本已内置该动作
- `test` 是管理员、招商组长等系统功能与权限测试默认环境
- `real-pre` 仅用于真实 SDK 联调，不作为默认权限验收环境

---

### 启动 test

```powershell
cd D:\Projects\SAAS
powershell -ExecutionPolicy Bypass -File .\scripts\start-test.ps1
```

验证：
```powershell
curl http://localhost:8081/api/system/health
npm run e2e:real-pre:preflight
powershell -ExecutionPolicy Bypass -File .\scripts\status.ps1
```

---

### 启动 real-pre

```powershell
cd D:\Projects\SAAS
powershell -ExecutionPolicy Bypass -File .\scripts\start-real-pre.ps1
```

验证：
```powershell
curl http://localhost:8080/api/system/health
powershell -ExecutionPolicy Bypass -File .\scripts\status.ps1
```

查看容器状态：
```powershell
powershell -ExecutionPolicy Bypass -File .\scripts\status.ps1
```

停止：
```powershell
powershell -ExecutionPolicy Bypass -File .\scripts\stop-all.ps1
```

### 停止环境

```powershell
powershell -ExecutionPolicy Bypass -File .\scripts\stop-all.ps1
```

---

### 查看容器状态

```powershell
powershell -ExecutionPolicy Bypass -File .\scripts\status.ps1
```

---

### 注意事项

- `docker-compose.real.prod.yml` **不存在**，不要使用
- `test` 环境使用 `docker-compose.test.yml`，`real-pre` 环境使用 `docker-compose.real-pre.yml`，各自独立
- 旧 `docker-compose.yml` 已删除，不再使用
- 启动前确认没有其他进程占用端口
- 不要同时启动本机 Vite 和 Docker 前端占用 `3000`
- `real-pre` 是"真实联调预备环境"，不等同生产环境
- real-pre 自动化脚本固定使用前端 `3001`、后端 `8081`；`3000/8080` 只属于 test/mock 基线
- real-pre 业务流验收应使用 `npm run e2e:real-pre:all`，预检若输出 `BLOCKED/PENDING` 代表外部前置条件未满足，不可按 PASS 处理
- Docker Desktop 启动后需等待托盘图标稳定，再执行 docker compose
- WSL 内无法直接操作 Windows 侧的 Docker，请在 Windows CMD/PowerShell 中执行 docker 命令

---

## 二、数据库初始化与迁移

### 全新初始化（干净库）

```bash
# 1. 创建空库
psql -h 127.0.0.1 -p 5433 -U colonelsaas -d postgres \
  -c "CREATE DATABASE colonel_saas;"

# 2. 执行初始化脚本
psql -h 127.0.0.1 -p 5433 -U colonelsaas -d colonel_saas \
  -f backend/src/main/resources/db/init-db.sql

# 3. 执行迁移脚本（包含所有 alter-*.sql，按正确顺序合并）
psql -h 127.0.0.1 -p 5433 -U colonelsaas -d colonel_saas \
  -f backend/src/main/resources/db/migrate-all.sql
```

### 棕地增量迁移（已有库）

已在运行的库增量执行迁移（所有语句幂等，可重复执行）：

```bash
psql -h 127.0.0.1 -p 5433 -U colonelsaas -d colonel_saas \
  -f backend/src/main/resources/db/migrate-all.sql
```

> 注意：`migrate-all.sql` 已合并所有 `alter-*.sql`，无需逐个执行。
> 已删除冗余文件：`alter-merchant-owner-columns.sql`（被 `alter-merchant-ownership.sql` 包含）。

---

## 三、当前目标

当前部署与对接工作的重点不是直接上生产，而是：

1. 保证 `test` 默认功能与权限测试基线可稳定启动
2. 保证 `real-pre` 真实 SDK 联调可清晰切换
3. 避免 test 与 real-pre 容器混用导致排查结果不可信

当前阶段补充：

- `M1.7 部署验证` 已于 2026-05-12 完成首轮收口
- 2026-05-15 环境治理后，test / real-pre 各自使用独立 compose 文件
- 本轮收口依据：
  1. `test` 使用 `docker-compose.test.yml`
  2. `real-pre` 使用 `docker-compose.real-pre.yml`
  3. 启动任一环境前先停止所有 SAAS 容器
  4. 后端启动日志打印 active profile、Mock 开关和数据库名
  5. 前端登录后显示当前环境标识

## 四、当前环境划分

### 0. 当前标准拓扑

从 2026-05-15 起，本机环境启动统一按以下单活拓扑执行：

| 用途 | 前端 | 后端 | PostgreSQL | Redis | 来源 |
| --- | --- | --- | --- | --- | --- |
| `test` 系统功能 / 权限测试 / 自动化基线 | `3000` | `8080` | `5432` | `6379` | `docker-compose.test.yml` + `.env.test` |
| `real-pre` 真实 SDK 联调 | `3001` | `8081` | `5433` | `6380` | `docker-compose.real-pre.yml` + `.env.real-pre` |

约束：

- `3000` 只允许一个本机前端入口
- `8080` 只允许一个后端入口
- `5432` / `6379` 只允许当前 active 环境占用
- 非明确需要时，不单独起本机 `redis-server` 占用 `6379`
- 每次启动完成后，应执行：`powershell -ExecutionPolicy Bypass -File .\scripts\status.ps1`

### 0.1 当前必须容器清单（2026-05-15）

当前只保留一套 active 容器名，环境通过 `.env.test` / `.env.real-pre` 切换：

| 容器名 | 作用 | 端口 |
| --- | --- | --- |
| `saas-test-frontend-1` | test 前端 | `3000` |
| `saas-test-backend-1` | test 后端 | `8080` / `5005` |
| `saas-test-postgres-1` | test PostgreSQL | `5432` |
| `saas-test-redis-1` | test Redis | `6379` |
| `saas-frontend-real-pre-1` | real-pre 前端 | `3001` |
| `saas-backend-real-pre-1` | real-pre 后端 | `8081` |
| `saas-postgres-real-pre-1` | real-pre PostgreSQL | `5433` |
| `saas-redis-real-pre-1` | real-pre Redis | `6380` |

说明：

- `docker ps` 不应同时出现两套环境的前端/后端容器同时运行
- `scripts/stop-all.ps1` 会停止两套环境的容器
- 不执行 `docker compose down -v`，数据通过独立命名 volume 保留

### 0.2 环境口径收口与旧容器处理（2026-05-15）

当前已经执行“启动入口合并”和“单活容器名收口”。旧 compose 文件保留为 `.bak`，用于追溯历史配置，不再作为日常启动入口。

当前保留两条环境线：

| 环境 | 职责 | Mock / Real | 数据隔离 |
| --- | --- | --- | --- |
| `test` | 系统功能、权限测试、本地业务联调、Mock 数据、回归测试、演示闭环 | `APP_TEST_ENABLED=true`，`DOUYIN_TEST_ENABLED=true` | `saas_test` / Redis DB 1 |
| `real-pre` | 三方 API 小范围验证、Token、活动、订单、转链冒烟 | `APP_TEST_ENABLED=false`，`DOUYIN_TEST_ENABLED=false` | `saas_real_pre` / Redis DB 0 |

处理原则：

- `local-mock` 不再作为独立容器环境扩展；历史脚本、历史报告和 `application-local-mock.yml` 可继续作为本地 Mock 口径参考，但当前 Mock 联调环境收敛到 `test`
- 旧 `test` / `real-pre` 容器如仍存在，应先执行 `powershell -ExecutionPolicy Bypass -File .\scripts\stop-all.ps1`
- 不要在收口阶段执行 `docker compose down -v` 或删除旧 volume；volume 是否删除单独决策
- 若发现旧容器名仍在运行，应先确认 project label 和端口，再停止容器，不把它作为当前排查依据

旧 `test` volume 只有在以下 5 项全部满足后，才进入清理评估：

1. 新 `test` 环境可以一键启动
2. `http://localhost:8080/api/system/health` 返回 `UP`
3. 登录、商品库、活动、订单、寄样、达人、Dashboard 主链路都能跑通
4. `backend mvn test` 通过
5. `frontend npm run build` 通过

### 1. `local-mock`

历史用途：

- 本地开发
- 人工联调
- 演示基线

当前口径：

- 前端：`http://localhost:3000`
- 后端：`http://localhost:8080/api`
- 调试台：`/dev/test`
- 测试接口：`/api/test/*`
- 作为独立环境名时已进入收敛期；新一轮 Mock 联调、回归和演示口径统一写作 `test`

### 1.1 Docker Desktop / Engine 排查口径（2026-05-04）

2026-05-04 上午，本机出现过一次“Docker Desktop 看起来已启动，但 `docker` CLI 无法连接 engine”的状态。本次排查得到的事实如下：

- `Docker Desktop Service` 状态为 `Running`
- `Docker Desktop.exe` 进程存在
- `wsl -l -v` 中 `docker-desktop` 为 `Running`
- 当前 `docker context` 指向 `desktop-linux`
- 但 `docker ps` / `docker version` / `docker info` 均报错：
  - `failed to connect to the docker API at npipe:////./pipe/dockerDesktopLinuxEngine`
  - `open //./pipe/dockerDesktopLinuxEngine: The system cannot find the file specified.`
- 当时直接检查命名管道：
  - `Test-Path \\\\.\\pipe\\dockerDesktopLinuxEngine` = `False`
  - `Test-Path \\\\.\\pipe\\docker_engine` = `False`
- Docker Desktop 日志尾部可见：
  - `C:\\Program Files\\Docker\\Docker\\resources\\com.docker.backend.exe services: exit status 1`

结论：

> 若出现“Docker Desktop UI / 服务 / WSL 都在，但 `docker ps` 仍提示找不到 `dockerDesktopLinuxEngine` 管道”，应优先按 **Docker Desktop backend / engine 异常** 处理，而不是直接判为项目容器或 compose 配置问题。

本机排查日志路径：

- `%LOCALAPPDATA%\\Docker\\log\\host\\com.docker.backend.exe.log`
- `%LOCALAPPDATA%\\Docker\\log\\host\\monitor.log`

建议恢复动作：

1. 先执行 `docker desktop status`
2. 若返回 `Could not retrieve status`，优先重启 Docker Desktop
3. 重启后再次确认：
   - `docker ps -a`
   - `docker context ls`
   - `Test-Path \\\\.\\pipe\\dockerDesktopLinuxEngine`
4. 只有在 CLI 已恢复连通后，再执行项目 compose 启停、容器排查和浏览器 smoke

补充约束：

- 在 `docker ps` 无法返回前，不要把 `8080 / 5432 / 6379` 的异常直接归因为本项目后端、数据库脚本或前端代理问题
- 依赖 compose 容器的 E2E / smoke 必须先确认 Docker engine 可连接
- 若 Docker Desktop backend 再次异常退出，先保留上述日志现场，再做重启或重新拉起

### 2. `test`

用途：

- 系统功能与权限测试默认环境
- 本地 Mock 联调
- 自动化测试
- 隔离测试栈

说明：

- 使用 `.env.test`
- `SPRING_PROFILES_ACTIVE=test`
- `APP_TEST_ENABLED=true`
- `DOUYIN_TEST_ENABLED=true`
- 数据库为 `saas_test`
- 重点是可重复、可隔离、可回归

### 3. `real-pre`

用途：

- 真实 SDK / 真实上游联调
- 真实 SDK 联调前的预备环境

当前事实：

- 前端：`http://localhost:3001`
- 后端：`http://localhost:8081/api`
- 抖店 OAuth 授权回调：`http://localhost:8081/api/douyin/oauth/callback`
- 抖店 Webhook 消息回调：`http://localhost:8081/api/douyin/webhooks/colonel-open-events`（本地通常需公网穿透或测试域名）
- PostgreSQL：`5433`
- Redis：`6380`
- 当前 `.env.real-pre` 使用 `SPRING_PROFILES_ACTIVE=real` 或 `real-pre`，运行态 `/api/system/env` 必须返回 `environmentLabel=REAL-PRE`
- 当前 `APP_TEST_ENABLED=false`、`DOUYIN_TEST_ENABLED=false`
- 当前 `ORDER_SYNC_ENABLED=true`

补充说明：

- 当前 `real-pre` 是“真实联调预备环境”
- 当前 `real-pre` 不等同生产环境，真实数据、Token、回调不得写入 `test` 或 `local-mock` 基线
- 管理后台 `/system/douyin` 可点击“去抖店授权”自动跳转官方授权页，授权回调成功后后端会使用 `code` 换取 Token 并跳回前端；手填 `code` 入口仅作为兜底。

## 五、本地标准联调 SOP

### 1. 启动 `test` 基础设施

```powershell
powershell -ExecutionPolicy Bypass -File .\scripts\start-test.ps1
```

说明：

- `3000 / 8080 / 5432 / 6379` 由 `test` active compose 接管
- 不再建议额外手工起一套独立 `local-mock` backend 占用 `8080`
- 若后端新增了 Controller 路由、Webhook 收件箱 / 重放逻辑或数据库初始化脚本，而 `8080` 仍表现为旧接口行为，先执行 `powershell -ExecutionPolicy Bypass -File .\scripts\start-test.ps1` 重建 active 环境

### 2. 前端入口

```powershell
http://localhost:3000
```

约束：

- 本机只保留这一套 `3000` 前端入口
- 不要同时启动 Docker 前端和本机 Vite 占用 `3000`

### 3. 数据初始化

登录 `admin / admin123` 后进入 `/dev/test`：

1. 先 `reset`
2. 再 `seed`
3. 需要物流链路时再执行发货 / 签收模拟

### 4. 最小验收口径

人工联调至少确认：

- `/product`
- `/product/activity`
- `/orders`
- `/dashboard`
- `/sample`
- `/talent`
- `/system`（管理员应自动落到 `/system/users`）
- `/system/roles`
- `/api/system/env`（test / real-pre 匿名访问应返回 200，且仅包含安全环境信息；prod 需管理员 JWT）

详细验收步骤见：

- [archive/runbooks/07-Test全链路验收](./archive/runbooks/07-Test全链路验收.md)
- [archive/runbooks/08-test-演示脚本](./archive/runbooks/08-test-演示脚本.md)
- `runtime/qa/local-mock-supplement.cjs`

当前已验证补充结果：

- local-mock 补充验收：`5/5`
- 报告路径：`runtime/qa/out/local-mock-supplement-20260503-1430/report.md`
- 一键命令：`powershell -ExecutionPolicy Bypass -File .\scripts\run-local-mock-supplement.ps1`

## 六、real-pre 回归口径

推荐启动方式：

```powershell
powershell -ExecutionPolicy Bypass -File .\scripts\start-real-pre.ps1
```

启动前要求：

- `start-real-pre.ps1` 会先执行 `stop-all.ps1`
- `3000` 应只对应当前 active 前端来源
- `8080` 应只对应当前 active backend

当前已验证结论：

- 浏览器全路径回归：`45/45`
- 报告路径：`runtime/qa/out/e2e-20260503-1353/report.md`
- QA 脚本：`runtime/qa/full-browser-e2e.cjs`
- 一键命令：`powershell -ExecutionPolicy Bypass -File .\scripts\run-real-pre-e2e.ps1`

### 4.1 2026-05-12 M1.7 部署验证收口

本轮按“像部署一样”而不是“只看页面能不能开”的口径执行，结果如下：

- Compose 配置校验：
  - 历史口径：`docker-compose.real-pre.yml` 配置校验通过
- 重建恢复验证：
  - 历史口径：`backend-real-pre` / `frontend-real-pre` 在重建后恢复为 `healthy`
  - 历史口径：`GET http://localhost:8081/api/actuator/health` 返回 `{"status":"UP"}`
  - 历史口径：`GET http://localhost:3001` 返回 `200`
- 运行态附加事实：
  - 重建后后端定时任务继续正常执行；日志可见 `OrderSyncJob done`
  - 本轮订单同步窗口记录为 `fetched=100 / created=94 / updated=6 / attributed=4`
- 浏览器与联调 smoke：
  - `npm run e2e:real-pre` 通过（`tests/e2e/08-real-pre-douyin-integration.spec.ts`）
  - `tests/e2e/02-dashboard.spec.ts` 通过
  - `tests/e2e/09-full-user-journey.spec.ts` 通过
- 当前结论：
  - 该段为 2026-05-12 历史部署验证记录；2026-05-15 起不再采用双环境并行端口拓扑
  - 当前以 `saas-active` 单活环境为准
- 已知提醒：
  - `scripts/check-env-topology.ps1` 保留为历史巡检参考；当前推荐使用 `scripts/status.ps1`
  - 本机 `redis-server` 若监听 `6379`，会阻止 active compose 完整启动，需先释放端口

### 4.2 2026-05-15 Docker 启动健康审查

- 环境治理后，`real-pre` 应按 `.env.real-pre` 通过 `scripts/start-real-pre.ps1` 拉起为 `saas-active-frontend-real-pre-1`、`saas-active-backend-real-pre-1`、`saas-active-postgres-real-pre-1`、`saas-active-redis-real-pre-1`
- `GET http://localhost:8081/api/system/health` 应返回 `{"status":"UP"}`；`/api/actuator/**` 需携带 JWT，不再作为匿名健康检查入口
- `GET http://localhost:3001/login` 应返回 `200`，页面头部显示 `环境：REAL-PRE`
- 前端 Vite dev server 继续固定容器内 `3000`，并通过 `CHOKIDAR_USEPOLLING=true / CHOKIDAR_INTERVAL=1000` 适配 Docker + Windows 文件监听
- 启动 real-pre 前会停止 test；管理员和招商组长权限测试继续只在 test 环境进行

## 七、测试与构建

```bash
cd backend
mvn test

cd frontend
npm run build
```

当前基线：

- `backend mvn test`：以 `docs/04-开发进度.md` 最近一次全量为准（2026-05-09：`652 tests, 0 failures, 0 errors`）
- `frontend npm.cmd run build`：通过

## 八、真实对接计划

建议顺序：

1. 认证打通：Token 获取 / 刷新 / 授权主体确认
2. 活动与商品打通：活动、活动商品、商品详情字段对齐
3. 转链打通：推广链接、归因参数、`pick_source_mapping`
4. 订单打通：订单回流、归因、详情排查
5. 达人与物流打通：达人刷新、物流状态同步

## 九、切换到真实 SDK 环境前必须确认的事项

- [ ] Test 基线完整可回归
- [ ] Real 配置不覆盖 Test 配置
- [ ] Gateway 切换边界清楚
- [ ] token / secret 不写死在仓库中
- [ ] 回调地址和网络策略明确

### 7.1 快递鸟物流即时查询切换前置

快递鸟第一阶段仅按即时查询 API 推进，当前只完成本地 Gateway 底座，尚未完成 Sandbox 或 real-pre 调用。切换前需额外确认：

- [ ] 已获取快递鸟 `EBusinessID / AppKey`
- [ ] `KDNIAO_ENABLED=false` 仍为默认值，只有 Sandbox 验证时显式开启
- [ ] `KDNIAO_REQUEST_URL` 先指向 Sandbox 地址，不直接切正式地址
- [ ] `KDNIAO_REQUEST_TYPE=1002`
- [ ] `KDNIAO_APP_KEY` 仅通过环境变量或密钥管理注入，不写入仓库
- [ ] 快递鸟查询失败、无轨迹、问题件不会自动推进寄样主状态

## 十、当前不要做的事

- 直接删除 Test 数据链路
- 用 Real 联调临时改前端字段
- 为了跑通真实接口破坏现有 Service
- 把演示环境与真实联调环境混在一起
- 同时运行 `test` 与 `real-pre` 两套 backend/frontend
- 让 `3000` 同时存在 Docker 前端和本机 Vite 两个来源
- 让 `8080` 混成“手工 local-mock backend”和 active backend 双来源
- 在没有明确理由时同时保留 Docker Redis 和本机 Redis 争用 `6379`

## 十一、宝塔 + Docker 部署检查清单

> 适用场景：宝塔面板 + Docker Compose 部署 SaaS 项目。
> 宝塔提供图形化容器管理、健康检查、监控告警、SSL 证书管理，本清单聚焦需要手动确认的关键安全与运维项。

### 部署前检查

```markdown
□ 宝塔 Docker 管理器确认当前只有一套 SAAS 容器运行
  （不同时开 test + real-pre 两套 backend/frontend）
□ 确认容器以非 root 用户运行
  （宝塔容器设置 → 用户 → 勾选"非 root 用户"）
□ 确认以下环境变量已通过宝塔或 .env 文件覆盖：
  - JWT_SECRET          （必须，非默认值）
  - DOUYIN_CLIENT_SECRET
  - DB_PASSWORD         （POSTGRES_PASSWORD）
  - REDIS_PASSWORD
□ 确认 .env 文件中的占位符（如 `***`）已替换为真实值
```

### 端口与健康检查

```markdown
□ 确认 docker-compose.real-pre.yml 端口映射未被其他服务占用：
  - 宿主机 8081  → 容器 8080  (后端 API)
  - 宿主机 3001  → 容器 3000  (前端)
  - 宿主机 5433  → 容器 5432  (PostgreSQL)
  - 宿主机 6380  → 容器 6379  (Redis)
□ 宝塔中添加健康检查：
  - 后端：http://127.0.0.1:8081/api/system/health
  - 前端：http://127.0.0.1:3001/
□ 宝塔健康检查失败告警已配置（邮件/钉钉/企业微信）
□ WSL 出口 IP 已加入微信公众号 IP 白名单（38.148.253.62 / 42.84.233.154）
```

### 数据安全

```markdown
□ 宝塔计划任务已配置 PostgreSQL 自动备份
  - 建议频率：每天 1 次
  - 保留份数：不少于 7 份
  - 备份路径：/www/backup/saas/postgres/
□ 宝塔计划任务已配置 Redis AOF/RDB 备份（如需）
□ 确认 docker-compose.real-pre.yml 使用命名卷持久化数据
  （不要用 bind mount 指向临时目录）
□ 数据库禁止使用 5432 端口（与 test 环境冲突）
```

### 网络与 SSL

```markdown
□ 外网访问已配置 HTTPS（SSL 证书）
  - 宝塔面板 → 网站 → 添加站点 → 申请 Let's Encrypt 免费证书
  - 反向代理到 Docker 内的 8081（后端）和 3001（前端）
□ CORS 配置已锁定域名（不要用通配符 *）
  - 生产环境 WebConfig 中的 CORS_ALLOWED_ORIGIN_PATTERNS
    必须为具体域名而非 .*
□ WebSocket（如有）已正确配置代理
```

### 启动后验证

```markdown
□ 执行以下验证命令：
  # 后端健康检查
  curl http://localhost:8081/api/system/health
  # 预期：{"status":"UP"}

  # 前端可访问
  curl -I http://localhost:3001/

  # 数据库连接（通过后端日志）
  docker logs saas-backend-real-pre-1 2>&1 | grep "HikariPool"
  # 预期：HikariPool 启动成功，无 Connection refused

  # Redis 连接
  docker logs saas-backend-real-pre-1 2>&1 | grep "Redis"
  # 预期：无 RedisConnectionFailureException

□ 登录测试账号，确认 JWT token 正常签发
□ RealProdEnvironmentGuard 在 real-pre 启动日志中打印了安全校验通过信息
```

### 已在宝塔层面覆盖（无需代码处理）

| 问题 | 宝塔处理方式 |
|------|-------------|
| k8s liveness/readinessProbe | 宝塔容器管理自带健康检查 |
| Docker restart 策略 | 宝塔图形化配置 `always` |
| CPU/memory limits | 宝塔容器设置 → 资源限制 |
| Prometheus metrics | 宝塔监控中心 |
| 告警通知 | 宝塔告警 → 邮件/钉钉 |
| 慢查询日志 | 宝塔数据库管理 → 慢查询视图 |
| 安全Headers (X-Frame-Options等) | 宝塔反向代理 → 响应头配置 |

### 仍需代码侧处理（宝塔无法覆盖）

| # | 问题 | 建议 |
|---|------|------|
| 1 | Dockerfile 以 root 运行容器 | 在 Dockerfile 末尾加 `USER 1000:1000`（或其他非 root 用户） |
| 2 | JWT 占位符检测仅 warn，生产误部署会启动 | 在 `RealProdEnvironmentGuard` 中加 startup fail-fast（检测到占位符则抛异常退出） |
| 3 | `application-dev.yml` 含 `saas123` 硬编码 | 确认 `.gitignore` 已排除 dev/test profile yml，或统一用环境变量覆盖 |

---

## 十二、关键配置源清单

> 目的：生产部署前能说明“每个关键开关从哪里来、是否可运行时调整、由谁负责维护”。本清单按当前代码和 compose/env 文件核验。

### 1. 环境变量 / 启动配置

| 配置组 | 关键项 | 来源文件 / 代码读取点 | 运行时可改 | 交付口径 |
| ------ | ------ | --------------------- | ---------- | -------- |
| 环境拓扑 | `SPRING_PROFILES_ACTIVE`、`APP_ENV_LABEL`、`SERVER_PORT` | `.env.*`、`docker-compose.*.yml`、`application.yml` | 否，需重启 | `test`、`real-pre`、`prod` 不混跑 |
| 数据库 | `DB_HOST`、`DB_PORT`、`DB_NAME`、`DB_USER`、`DB_PASSWORD`、Hikari 参数 | `application-*.yml`、`docker-compose.*.yml` | 否，需重启 | prod 必须替换 `DB_PASSWORD` 占位符 |
| Redis | `REDIS_HOST`、`REDIS_PORT`、`REDIS_PASSWORD`、`REDIS_DATABASE` | `application-*.yml`、`docker-compose.*.yml` | 否，需重启 | test 默认 DB 1，real-pre/prod 默认 DB 0 |
| JWT | `JWT_SECRET`、`JWT_EXPIRE_SECONDS` | `application.yml`、`JwtTokenProvider`、`RealProdEnvironmentGuard` | 否，需重启 | real/prod 禁止占位符和短 secret |
| CORS | `CORS_ALLOWED_ORIGIN_PATTERNS` | `application.yml`、`WebConfig` | 否，需重启 | credentials 模式禁止通配公网 origin |
| Test 开关 | `APP_TEST_ENABLED`、`APP_TEST_SEED_ON_STARTUP`、`APP_TEST_*` | `application.yml`、`TestController`、`RealProdEnvironmentGuard` | 否，需重启 | real/pre/prod 必须关闭 |
| 抖音网关 | `DOUYIN_BASE_URL`、`DOUYIN_APP_ID`、`DOUYIN_CLIENT_KEY`、`DOUYIN_CLIENT_SECRET`、`DOUYIN_TEST_ENABLED` | `application.yml`、Real/Test Gateway 条件装配 | 否，需重启 | real/prod 必须真实凭据且 `DOUYIN_TEST_ENABLED=false` |
| 抖音 OAuth | `DOUYIN_OAUTH_AUTHORIZE_URL`、`DOUYIN_OAUTH_REDIRECT_URI`、`DOUYIN_OAUTH_FRONTEND_SUCCESS_URL`、`DOUYIN_OAUTH_FRONTEND_FAILURE_URL`、`DOUYIN_OAUTH_STATE_TTL_MINUTES` | `application.yml`、`DouyinOAuthController`、`DouyinOAuthService` | 否，需重启 | OAuth 回调公开但必须校验 Redis state；不得记录 code/token 明文 |
| 抖音真实写 | `DOUYIN_REAL_PROMOTION_WRITE_ENABLED`、`DOUYIN_REAL_UPSTREAM_MODE` | `application.yml`、`RealDouyinPromotionGateway` | 否，需重启 | 默认禁止真实转链写操作，需专项开启 |
| 抖音 Token | `DOUYIN_TOKEN_AUTO_REFRESH_ENABLED`、`DOUYIN_TOKEN_AUTO_REFRESH_CRON`、阈值/锁时长 | `application.yml`、`DouyinTokenRefreshJob` | 否，需重启 | real/pre/prod 可自动刷新，失败看日志和 Redis lock |
| Webhook | `DOUYIN_WEBHOOK_VERIFY_SIGN` | `application.yml`、`DouyinWebhookController`、`RealProdEnvironmentGuard` | 否，需重启 | real/prod 必须开启验签 |
| 独家机制 | `EXCLUSIVE_ENABLED` | `application.yml`、`AttributionService`、`ExclusiveEvaluateJob` | 否，需重启 | V1 默认关闭，V2 专项开启 |
| 物流 | `APP_LOGISTICS_REFRESH_JOB_ENABLED`、`KDNIAO_*` | `application.yml`、`LogisticsTrackJob`、物流 Gateway | 否，需重启 | 默认手动兜底，真实/sandbox 样本验证后再开启 |
| 达人外部采集 | `TALENT_PUBLIC_PAGE_CRAWL_ENABLED`、`TALENT_PROFILE_HTTP_*` | `application.yml`、达人资料 Provider | 否，需重启 | 默认手动兜底或受控 HTTP provider |
| 订单定时同步 | `ORDER_SYNC_ENABLED` | `application.yml`、`OrderSyncJob` | 否，需重启 | real-pre 按联调节奏开启 |
| 日志保留 | `operation.log.retention-days` | `LogCleanupJob` | 否，需重启 | 默认 90 天 |

### 2. KV 业务配置

| 配置键 | 读取点 | 运行时可改 | 校验 / 审计 |
| ------ | ------ | ---------- | ----------- |
| `sample.restrict_days`、`sample.restrict_enabled` | `BusinessRuleConfigService`、寄样资格判断 | 是 | `ConfigDefinitionRegistry` 强校验，`system_config_change_log` 记录旧值/新值 |
| `sample.timeout_homework_days`、`sample.timeout_pending_ship_days` | `SampleLifecycleService` 自动关闭 | 是 | 强校验，缓存失效后短 TTL 生效 |
| `sample.default_standard` | 寄样默认标准 | 是 | JSON 对象校验 |
| `talent.protection_days` | 达人保护期、状态矩阵 | 是 | 1~365 天强校验 |
| `talent.exclusive.service_fee_ratio`、`talent.exclusive.monthly_samples` | 独家达人底座 | 是 | V1 默认不开独家，仅作配置底座 |
| `merchant.exclusive.service_fee_ratio` | 独家商家底座 | 是 | V1 默认不开独家 |
| `commission.business_default_ratio`、`commission.channel_default_ratio` | `CommissionService`、`performance_records` | 是 | 0~1 小数校验 |
| `promotion.copy_brief_template` | 复制讲解模板 | 是 | 非空、最长 4000 字符 |
| `promotion.pick_extra_rule` | 商品转链 `pickExtra` 生成规则 | 是 | JSON 对象；默认 `channel_{channel_code}`，缓存失效后短 TTL 生效 |
| `talent.preset_tags` | 达人标签预设库 | 是 | JSON 数组，最多 50 项，单项最长 24 字符 |

### 3. 代码常量 / 固定规则

| 规则 | 代码位置 | 是否可配置 | 后续处理 |
| ---- | -------- | ---------- | -------- |
| 商品置顶 24 小时、每人最多 10 个 | `ProductPinService` | 暂不可配置 | 若运营需要调整，纳入配置域 V2 |
| 订单 `pick_source` 有效期默认 3 个月 | `PickSourceMappingService` / `pick.source.valid-months` | 环境变量可改，非 KV | 生产固定后写入部署参数 |
| 登录失败 5 次锁定 15 分钟 | `AuthService` | 暂不可配置 | 已有审计日志；如需动态调整，纳入配置域 V2 |
| Dashboard breakdown 默认 Top 20 | `DashboardService` | 暂不可配置 | 当前不阻塞交付 |
| 短 TTL 缓存容量 | `APP_SHORT_TTL_CACHE_MAX_ENTRIES` | 环境变量 | 调整需重启 |

配置源结论：real-pre 联调前必须先校验 `.env.real-pre` / 当前运行环境变量；业务规则优先走 `system_config`，变更由 `system_config_change_log` 审计；代码常量只保留低频或 V2 待配置项，不能在汇报里说成“后台可随时配置”。

---

## 十三、当前结论
