
## 商品库 local-mock 冻结回归

### 验收结论

商品库 local-mock 主链路已完成收口，可作为全链路 Mock 演示入口使用。

### 已解决问题

- `/api/products` 依赖 `product_snapshot` 导致 local-mock 500 的问题已解决
- `promotion_link` 缺表问题已解决
- `talent_follow_record` 缺表问题已解决
- 商品快照 seed 已补齐
- 商品库复制推广链接可生成新的 `pick_source`
- `pick_source_mapping` 可正常落库
- 生成已归因订单后，订单页与看板统计均正常刷新

### 冻结口径

商品库当前作为全链路 Mock 演示入口已经稳定。
后续商品库不再新增业务能力，只保留缺陷修复、字段展示修正、Mock 数据维护和真实 Gateway 接入前的兼容调整。

### 当前基线

- 全链路 local-mock MVP：已打通
- 商品库 local-mock：已收口
- 寄样台 MVP：已封口
- 订单归因看板：已封口
- 后端：`http://127.0.0.1:18081/api`
- 商品快照：3 条
- 主演示商品：`10901825`
- `backend mvn test`：通过，`388 tests, 0 failures`
- `frontend npm.cmd run build`：通过

### 非阻塞遗留

- Maven `systemPath` 警告
- 前端大 chunk 警告

## 订单页 filter-options 体验补齐

### 验收结论

订单工作台筛选项已从后端真实数据加载，且与 URL query、tab、分页状态保持一致。

### 本轮完成

- 新增并完善 GET /api/orders/filter-options
- 返回订单状态、归因状态、未归因原因、商品、渠道、招商六类筛选项
- /orders 页面改为一次加载后端筛选项，不再前端硬编码订单状态、归因状态、未归因原因
- 保持 	ab / page / pageSize / attributionStatus / unattributedReason / orderStatus / productId / channelKeyword / colonelKeyword 的 query 恢复能力
- 保持 Dashboard -> Orders 跳转参数兼容

