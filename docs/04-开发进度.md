# 04-开发进度

更新时间：2026-05-21

## 一、当前阶段

当前项目阶段为：

> `P1 real-pre 联调收口完成；2026-05-16 TEST/mock 主流程第二阶段验收通过；2026-05-17 real-pre 准入、P3 单样本业务闭环、P3-5 全角色账号业务流程、P3-7 Dashboard 数据可信对账与浏览器可视化全业务剧本回归已通过；当前进入 P3 最后收口：真实 pick_source 归因样本观察与异常分支补证`

导航：

- 上游入口：[README.md](/D:/Projects/SAAS/README.md)
- 当前清单：[10-上线前验收清单.md](/D:/Projects/SAAS/docs/10-上线前验收清单.md)
- 当前证据：[11-real-pre证据索引.md](/D:/Projects/SAAS/docs/11-real-pre证据索引.md)

P1 状态：

- `real-pre 联调收口完成`
- 证据以 `runtime/qa/out/real-pre-order-settlements-watch-20260514-142923` 与 `runtime/qa/out/real-pre-final-regression-20260514-143347` 为准
- 补充说明：real-pre 抖音联盟主链路已完成收口；快递鸟物流即时查询已进入本地 Gateway 底座对接，Sandbox / real-pre 尚未调用，当前寄样履约仍按手动兜底能力处理，不阻塞主链路收口；达人外部数据能力尚未接入

P2 状态：

- `上线前硬化首轮检查完成；P2-FIX 两个硬阻塞修复后复验通过`
- `2026-05-15 管理员系统管理落地页与环境探针噪音修复完成`
- `2026-05-16 TEST/mock 主流程数据覆盖收口完成：overallPass=true / coverage=84.58% / hardMissing=0 / api-errors=[]`
- `2026-05-16 TEST/mock 主流程第二阶段验收通过：状态流转 22/22 PASS，角色业务 smoke 6/6 PASS，Dashboard 对账 23/23 PASS；最终报告见 docs/20-TEST-mock最终验收报告.md`
- 首轮硬化证据：`runtime/qa/out/pre-launch-hardening-20260514-145503`
- P2-FIX 复验证据：`runtime/qa/out/pre-launch-hardening-fix-20260514-153151`
- P2-FIX 结论：real-pre / real / prod 配置 fail-fast、Webhook 默认验签与 `order_sync_dedup_claim` 数据库级 claim 去重复验通过；`/api/actuator/health -> UP`，定向后端测试通过
- P2-FIX 复验明细：定向测试合计 `31 tests / 0 failures / 0 errors`；Webhook 验签为无签名 `401` / 错签 `401` / 正确 HMAC 签名 `200 success`（审计短语：`Webhook 401 / 401 / 200`）；`order_sync_dedup_claim` 并发 claim 同一个 `order_id` 最终只落 `1` 条（审计短语：`order_sync_dedup_claim 并发 claim 只落 1 条`）；migration 连续执行两次退出码均为 `0`（审计短语：`migration 连续两次退出码 0`）
- P2-A 补充结论：三方未接入能力兜底验收已完成，不阻塞抖音联盟主链路；达人寄样候选来源已补齐手动补录 fallback，人工补录达人可认领、进私海并发起寄样
- 2026-05-21 安全专项 P1 修复：登录接口已补 Redis 失败计数与 15 分钟锁定，抖音 Token 状态展示与 SDK token adapter 日志脱敏均改为固定占位符，不再返回 token 前后缀；验证结果为后端定向 `AuthServiceTest,DouyinTokenServiceTest,DoudianTokenGatewayTest` 31/31 通过，排除本机缺少 `saas_test` 数据库的 `ColonelSaasApplicationTests` 后后端 `mvn -Dtest=!ColonelSaasApplicationTests test` 760/760 通过
- 2026-05-21 前端审查补强：达人黑名单/解除黑名单后端已补齐 `DataScope` 对象级校验，寄样申请商品搜索已加 debounce，订单/寄样导出已加前端行数护栏，登录态已支持跨 Tab storage 同步；验证结果为 `frontend npm run build` 通过、后端定向 `TalentServiceTest,TalentControllerTest` 46/46 通过、后端全量 `mvn test` 758/758 通过，V1-P0 定向 `23-v1-rbac-scope` + `24-v1-performance-dashboard` 41/41 通过（均为 TEST/mock，未调用真实三方）
- 2026-05-21 数据口径 Open Questions 审计完成：`colonelsettlement_order.user_id` 已确认是本地归因后的业绩 / 数据范围归属用户，不是触发同步的 JWT 用户或抖店上游用户；主订单同步与按订单号结算补拉按 `order_id` 去重落同一张订单表；当前代码口径为独家商家 / 独家达人优先，其次原生团长字段 `colonel_buyin_id` / `second_colonel_buyin_id`，最后回落 `pick_source` / `pick_extra`；达人归属以有效 `talent_claim` 为主事实，当前不存在持久化 `Talent.dept_id` 同步规则。归档审计见 `docs/archive/audits/16-数据口径OpenQuestions审计.md`
- 2026-05-21 商品链路本地排查（real-pre）：已对照上游接口清单完成端到端实跑并归档 Runbook [archive/runbooks/19-商品链路本地排查](./archive/runbooks/19-商品链路本地排查.md)。结论为 **活动商品同步 PASS**（`alliance.colonelActivityProduct` → `product_snapshot` → `GET /colonel/activities/{id}/products?refresh=true`，全库快照约 `5066` 条，样本活动 `3920684` 字段齐全）；**转链 / 归因部分**（`pick_source_mapping` 共 `8` 条且均为 `colonel_native_*` 种子、无样本活动+商品的 `v.` 映射；渠道对未入库商品转链返回 `460`）；**订单侧**（`335` 单、`0` 归因、`0` 带 `pick_source`，GMV ¥6658.04；`dashboard/activity-products` 当前 `records=0`）；**未实现** `list_partners`、`pin_product`、抖店 `quick_sample_apply`。证据：`runtime/qa/out/product-chain-audit-20260521-122559/`；复跑入口 `scripts/qa/audit-product-chain-real-pre.ps1`
- 2026-05-21 前端商品筛选指标收口：活动商品 / 商品库工具栏已接入店铺关键词、抖音类目、近 30 天销量档、联盟推广状态、转链状态等（`frontend/src/views/product/product-filters.ts`）；服务端商品库仍仅 `keyword/status`，其余维度为客户端过滤；`frontend npm run build` 通过
- 2026-05-15 补充证据：
  - `runtime/qa/out/qa-api-smoke-20260515-124718-553`
  - `runtime/qa/out/qa-page-smoke-20260515-124955-497`
  - `runtime/qa/out/qa-page-smoke-20260515-124955-515`
- 2026-05-15 补充结论：
  - 管理员直达 `/system` 现会稳定落到 `/system/users`
  - 非管理员访问 `/system` 继续被重定向到有权限首页
  - `/api/system/env` 已精确匿名放行，页面不再出现该接口 401 噪音
  - 测试环境商品库“一键复制专属推广链接”已修复：活动详情补水会将 `10 / 5` 这类百分比费率归一为 `0.1000 / 0.0500` 后写入 `colonel_activity`，避免 `numeric(5,4)` 溢出导致 500；同时 `APPROVED + 已入库` 商品允许按当前渠道实时转链并推进到 `LINKED`
- 2026-05-16 TEST/mock 数据覆盖证据：
  - `runtime/qa/out/qa-mock-data-audit-20260516-194040-956`（最新）
  - `runtime/qa/out/qa-mock-data-audit-20260516-193210-553`（同日较早轮次，两轮结论一致）
  - `node --check runtime/qa/mock-data-audit.cjs` 通过
  - `node --check runtime/qa/mock-data-audit.test.cjs` 通过
  - `node runtime/qa/mock-data-audit.test.cjs` 为 `6/6 PASS`
  - `cd backend && mvn -DskipTests compile` 为 `BUILD SUCCESS`
  - `scripts/qa-mock-data-audit.ps1` 为 `Exit code 0`
  - 固定回归入口已新增：`scripts/qa-test-mock-final.ps1`
  - 本轮未切换 real-pre，未访问真实三方接口，未执行清库操作
- 2026-05-16 物流/达人缺失降级联调补充：
  - 新增方案文档：[21-三方接口缺失降级联调方案](./21-三方接口缺失降级联调方案.md)
  - 新增回归脚本：`runtime/qa/third-party-fallback-regression.cjs`
  - 回归证据：`runtime/qa/out/third-party-fallback-regression-20260516-201448-032`，8/8 PASS
  - 物流按 `MANUAL` / `MOCK` 标记来源；达人采集失败保留记录并允许手动补录
  - 该项仅表示 TEST/mock fallback 可回归，不表示真实物流/真实达人三方接口联调完成
- 2026-05-16 TEST/mock 第二阶段最终验收证据：
  - 状态流转回归：`runtime/qa/out/business-state-flow-regression-20260516-201336-212`，22/22 PASS
  - 角色业务 smoke：`runtime/qa/out/page-role-business-smoke-20260516-202802-261`，6/6 PASS
  - Dashboard 对账：`runtime/qa/out/dashboard-reconcile-20260516-203253-797`，23/23 PASS
  - 最终报告：`docs/20-TEST-mock最终验收报告.md`

P3 状态：

- `real-pre 准入通过 -> P3 单样本业务闭环通过 -> P3-5 全角色账号业务流程通过 -> P3-6 SYNC_OK_NO_SAMPLE -> P3-7 Dashboard 与 PostgreSQL 口径对账 PASS -> P3-8 异常分支 PARTIAL_PASS -> 浏览器可视化全业务剧本回归 PASS -> 当前约 94%+`
- 准入证据：`runtime/qa/out/qa-real-pre-preflight-20260517-090308-762`、`runtime/qa/out/real-pre-e2e-20260517-090457-069`
- 单样本业务闭环证据：`runtime/qa/out/real-pre-business-e2e-20260517-091828`
- 全角色账号业务流程证据：`runtime/qa/out/real-pre-role-business-e2e-20260517-101101`
- 浏览器可视化全业务剧本回归证据：`runtime/qa/out/real-pre-full-business-journey-20260517-154251`，`.\scripts\qa\run-real-pre-full-business-journey-visual.ps1 -> 1 passed (9.0m)`，Journey `结论=PASS / overallPass=true / 9/9 PASS / failures=0`；本轮已升级为 headed 慢速导演模式，页面顶部展示当前角色 / 动作 / 判定说明，点击前高亮目标元素并保留 trace / video / screenshot
- P3-6 首轮真实订单归因证据：`runtime/qa/out/real-pre-attribution-evidence-20260517-104656`，当前结论为 `SYNC_OK_NO_SAMPLE`
- P3 Pending Evidence 统一观察证据：`runtime/qa/out/real-pre-pending-evidence-watch-20260517-125831`，当前结果为 `P3-6=SYNC_OK_NO_SAMPLE / EXC-004=NO_LIVE_SAMPLE / EXC-005=READ_ONLY_CANDIDATE_FOUND / EXC-006=NO_HISTORICAL_SAMPLE`
- P3 Pending Evidence 统一观察入口：`scripts/qa/watch-real-pre-pending-evidence.ps1`
- P3-6 专用轻量观察入口：`scripts/qa/watch-real-pre-pick-source-orders.ps1`
- P3-7 Dashboard 对账证据（首轮）：`runtime/qa/out/real-pre-dashboard-reconcile-20260517-110015`，`overallPass=true`
- P3-7 Dashboard 对账证据（当前主引用）：`runtime/qa/out/real-pre-dashboard-reconcile-20260517-122911`，`status=PASS`，`diff.json` 为空，`candidateReasons=[]`，`orderCount=808 / orderAmount=1548347 / serviceFee=22935`，`10` 个核心指标逐项一致
- P3-7 只读回归入口：`scripts/qa/run-real-pre-dashboard-reconcile.ps1`
- P3-8 异常分支只读审计证据：`runtime/qa/out/real-pre-exception-audit-20260517-124432`，`status=PARTIAL_PASS`，其中 `EXC-001/002/003=PASS`、`EXC-004=NO_LIVE_SAMPLE`、`EXC-005/006=DEFERRED_NO_SAFE_READ_ONLY_PATH`
- P3-8 只读回归入口：`scripts/qa/run-real-pre-exception-audit.ps1`
- 当前最强证据链：全角色流程业务步骤 `11/11 PASS`、Playwright `2/2 PASS`；可视化全业务剧本 `9/9 PASS`、Playwright `1/1 PASS`；最新剧本 `pickSource=v.MxZLIw`、`mappingCount=1`、`sampleRequestNo=SM202605175EAB9051`、最终寄样状态 `PENDING_TASK`、六个账号退出后访问受保护接口均返回 `401`
- P3-6 首轮关键事实：`/api/orders/sync -> totalFetched=100 / created=2 / updated=98 / attributed=0 / unattributed=100 / failed=0`；raw probe 最近 `3` 天 `20` 条样本均无 `pick_source`；本地近 `30` 天订单 `721` 条中 `pick_source=0`；Dashboard 当前 `orderCount=721 / unattributedOrderCount=721 / upstreamProductUncoveredCount=718 / cannotAutoAttributionCount=3`
- 发布判断：系统已经具备 real-pre 业务可用性，P3-7 Dashboard 数据可信对账已通过，且主业务流程已可由真实浏览器按账号顺序跑通；但尚不能宣称可生产发布，仍缺真实订单归因可信与异常分支可解释证据

阶段判断依据：

- 本地 Mock 主链路已完成并可复现
- `test` / `real-pre` 双轨职责已经稳定，`local-mock` 补充验收脚本保留为历史回归资产
- real-pre 浏览器全路径回归已通过 `45/45`
- local-mock 交互级补充验收已通过 `5/5`
- `3000/8080` 本地 Mock 可见浏览器分批验收已完成当前计划内批次
- 当前代码基线可构建、可测试、可回归
- 认证安全 5 项补齐已落地（Token 双令牌刷新、登出 / Token 吊销、SecurityConfig 收紧、DataScope、权限管理端点）
- test 环境 QA 冒烟脚本已固定为 `qa-env.ps1`、`qa-api-smoke.ps1`、`qa-page-smoke.ps1`
- TEST/mock 数据覆盖硬缺口已清零；剩余 warning 型专项样本按 `docs/19-TEST-mock-warning专项样本清单.md` 分级跟踪，不阻塞主流程收口

补充判断：

- 当前 `test / real-pre` 基线内，未发现新的 P0 功能阻断；`local-mock` 作为历史 Mock 口径和回滚参考保留
- 当前 TEST/mock 不能继续用“是否还有 missing”判断是否阻塞，应区分 hard missing 与 warning missing：hard missing 已清零，warning missing 转专项
- 当前执行焦点已从“real-pre 准入 / 角色业务闭环验证”切换为：
  1. `P3-6` 真实订单归因样本：当前状态为“部分收口，等待真实 `pick_source` 订单样本”；首轮证据目录 `runtime/qa/out/real-pre-attribution-evidence-20260517-104656` 当前为 `SYNC_OK_NO_SAMPLE`，已完成 `P3-6-A/B/C/D`，仍待补 `P3-6-E` 真实 `pick_source` 订单归因样本通过；统一观察入口已固定为 `scripts/qa/watch-real-pre-pending-evidence.ps1`
  2. `P3-7` Dashboard 数据可信对账：已通过；当前主引用证据目录 `runtime/qa/out/real-pre-dashboard-reconcile-20260517-122911` 为 `status=PASS`，`diff.json` 为空且 `candidateReasons=[]`，`/api/dashboard/summary` 与 PostgreSQL `10` 个核心指标逐项一致
  3. 浏览器可视化全业务剧本回归：已通过；最新证据目录 `runtime/qa/out/real-pre-full-business-journey-20260517-154251`，确认主业务流程可被真实浏览器按账号顺序跑通；本轮入口固定为 `scripts/qa/run-real-pre-full-business-journey-visual.ps1`，headed 慢速执行并输出完整可视化证据；首次失败已归类为 frontend `3000` 未启动的环境前置条件缺失
  4. `P3-8` 异常分支证据补齐：首轮只读审计已拿到 `runtime/qa/out/real-pre-exception-audit-20260517-124432`，已确认“无订单同步成功、无 `pick_source` 订单保留、无映射订单保留且 dry-run 不写回”，仍待补 live unsafe 样本、重复转链幂等、SKU 异常不中断主流程；最新统一观察证据目录 `runtime/qa/out/real-pre-pending-evidence-watch-20260517-125831`
  5. 商品链路样本补证（2026-05-21 排查后）：主同步已通，待按 Runbook [19-商品链路本地排查](./archive/runbooks/19-商品链路本地排查.md) 第八节补「入库 → 渠道转链 → 订单归因 > 0」；不阻塞活动商品字段落库结论
- 2026-05-21 数据库 / 性能专项工程治理已落代码：`colonelsettlement_order` entity/mapper 补齐 `colonel_buyin_id / order_type` 等订单原生列，分页列表避开 `extra_data` JSONB 大字段，订单 / 数据页分页增加上限，补充订单、寄样、商品状态查询索引与 CHECK 约束迁移；`ShortTtlCacheService` 增加容量上限与 Redis 前缀失效广播，默认 HikariCP 与慢 SQL 日志阈值已配置。`colonelsettlement_order.product_id` 硬外键暂不加，原因是真实订单可能先于商品事实入库，强约束会阻断订单同步，后续等商品覆盖率收口后单独评估。

## 二、当前结论

### 1. 已完成能力

- 商品主链路：商品库、商品管理（活动列表 / 商品列表）、审核、分配、Mock 转链、`pick_source` 落库
- 订单归因：已归因 / 未归因识别、订单工作台、详情排查、Dashboard 联动
- 寄样台：申请、审核、发货、签收、待交作业、命中订单自动完成
- 达人 CRM：列表、详情、筛选、寄样关联、订单关联
- 达人经营台一期：团队公海、我的达人、自然出单达人、达人黑名单、经营筛选、黑名单动作
- 系统管理：用户 / 角色列表与 CRUD
- 调试台：`reset`、`seed`、归因订单造数、寄样物流模拟
- 认证安全：Token 双令牌刷新（Access 2h + Refresh 7d）、登出 / Token 吊销（Redis 黑名单）、SecurityConfig 白名单收紧、DataScope 数据权限过滤（本人 / 本组 / 全部）、权限 / 菜单管理端点

### 2. 环境格局

- `local-mock`：历史上的本地人工联调口径，当前不再作为独立容器环境扩展
- `test`：系统功能 / 权限测试 / Mock 联调 / 自动化测试 / 隔离测试栈 / `8080` 后端主口径
- `real-pre`：真实 SDK / 真实上游联调，不作为默认权限测试环境

补充说明：

- 当前启动入口：test 环境用 `docker-compose.test.yml`，real-pre 环境用 `docker-compose.real-pre.yml`，各自独立
- 当前容器名：test 环境为 `saas-test-*`，real-pre 环境为 `saas-*-real-pre-*`
- 当前 `real-pre` 已切换为 `real-pre` profile，`DOUYIN_TEST_ENABLED=false`、`APP_TEST_ENABLED=false`、`ORDER_SYNC_ENABLED=true`
- 当前 `real-pre` 表示“真实联调预备环境”，仍不等同生产环境

### 3. 本轮关键修复

- 商品页缺 Token 时降级本地商品库
- 商品入口已收口为“商品库 / 商品管理”：`/product` 仅展示已入库共享商品，`/product/manage` 承载活动列表与商品列表流转
- 活动商品审核链路已与入库动作合并：审核通过即自动加入商品库，后续分配、推进判断、转链与达人跟进统一基于“已入库商品”继续推进
- 商品管理活动商品页已改为“活动商品推进工作台”：`biz_leader` 可在活动维度筛选待审核 / 待分配 / 已分配 / 渠道推进商品；待审核商品可先分配审核负责人且保持 `PENDING_AUDIT`，由 `biz_staff` 在“我负责的商品”完成审核，审核通过入库后再分配或重新分配招商负责人
- 招商组长权限已继续收口：当前仅保留活动获取与本组招商下属分配，活动商品审核与推进判断统一收敛给招商执行
- 共享商品库 `/product` 已按“入库后全员可见”口径恢复给招商组长 / 招商专员只读可见；渠道转链、达人跟进等执行动作仍仅保留给渠道角色
- 招商组长在寄样台已同步收口为“只看不审”：仅保留寄样管理内容查看与导出能力，寄样审核仅招商执行，发货/签收等物流动作仅运营执行
- 渠道组长权限已补齐到“执行 + 管理”口径：保留商品库转链、本人寄样申请、数据看板与订单导出、本组寄样导出，以及达人经营台“本组达人”查看视角
- 渠道组长达人权限已收紧：当前只允许查看本组已认领达人与释放本人认领达人，不再支持达人归属覆盖，也不能代替组员释放其保护期达人
- 活动商品审核弹窗已补齐 V2.2 要求的审核补充字段：专属价说明、发货信息、商品卖点、推广话术、投流开关、奖励说明、参与要求、活动时间、手卡素材
- 活动商品审核补充字段已继续收口：审核通过前端 / 后端均要求完整补齐核心推广资料，审核通过时会保存到 `audit_payload` 用于商品详情回显；缺字段不再允许直接审核入库
- 商品详情“推广资料包”已回显审核补充信息，并将审核录入的卖点 / 话术透传到详情展示
- test 环境启动脚本已补充旧数据卷幂等补丁，避免重启容器后因缺失 `blacklisted`、`selected_to_library` 等列导致后端启动失败
- Dashboard 兼容后端实际 Summary 字段格式
- `/product/manage/:id`（兼容旧 `/product/activity/:id` 跳转）首屏双请求与快照写入冲突修复
- `/data` 首屏慢网白屏问题修复
- 前端开发容器启动前会按 `pnpm-lock.yaml` 同步依赖，避免命名卷 `node_modules` 过期导致 `/src/views/data/index.vue` 的 ECharts 动态导入在 3000 Vite 服务返回 500
- 浏览器 E2E harness 与当前页面加载态口径对齐
- 停用账号登录提示改为 `账号已停用`
- `ops_staff` 登录数据范围提升为全量，物流发货页可见待发货数据
- `ops_staff` 独家状态页接口权限与菜单口径对齐
- `biz_leader` 商品分配招商弹窗已改为受控负责人候选接口，不再依赖管理员用户列表页权限
- 达人 CRM 重新认领已过期达人时，后端已改为复用历史认领记录续期，避免 `talent_claim` 唯一键冲突
- 达人保护期释放已补充“到期且无产出才自动释放”判断，并新增定时释放 Job；有产出达人不会被到期任务直接打回公海
- 达人经营台已补出“触发周更”和“单达人刷新”前台入口，不再只保留后端接口能力
- 达人经营台一期已补齐统一查询 DTO、四视图过滤、黑名单接口和经营字段展示
- 达人认领模型已从“单 owner”调整为“多人可同时认领”：当前用户未认领时仍可在团队公海继续认领，详情弹窗已补充有效认领人列表
- 达人多人认领闭环已补齐：团队公海保留“他人已认领但我仍可继续认领”的达人，`TalentService.claim` 已修复认领记录落库分支
- 预置组级测试账号已补齐稳定 `deptId`，`channel_leader` / `biz_leader` 不再出现组权限上下文悬空
- `/orders` 顶部归因概览已改为走 `/orders/stats` 全量统计，不再只按当前页计算
- 系统配置已接入本地业务规则：寄样重复申请限制、达人保护期、寄样自动关闭、达人独家阈值不再写死在业务代码中
- `SampleController` 新建寄样单已补齐显式 UUID，避免本地容器环境下插入时退化为“服务器异常”
- 寄样默认标准已补齐“前台资格预检 + 弹窗提醒 + 备注必填 + 后端兜底校验”闭环
- 系统管理已补齐管理员“操作日志中心”：统一查询最近 90 天关键变更日志，当前接入 POST / PUT / PATCH / DELETE 关键操作沉淀
- 系统配置中心已升级为“强约束 KV”口径：新增内置配置键常量与定义注册，管理员修改内置规则时会按类型 / 范围 / JSON 结构做保存前校验；`sample.default_standard` 也已收口为强类型读取 DTO
- 订单导出接口已收紧为管理员 / 组长可用，且后端已补齐数据范围过滤，不再绕开 `DataScope`
- 真实订单手动同步入口已继续收口到管理员联调面：`POST /orders/sync` 不再允许 `biz_leader` 直接调用，避免通过隐藏接口绕开 `/system/douyin` 的管理员联调口径
- 寄样自动关闭原因文案已跟随配置天数动态变化，避免规则中心改值后页面仍显示旧的 30 / 15 天文案
- 权限治理 P0 首轮已补齐：达人详情、寄样详情 / 日志 / 流转 / 删除、订单详情均已补上对象级数据范围校验，避免仅凭主键越权查看或操作他人数据
- `TestController` 已收紧为仅管理员可用；系统配置分组接口对非管理员已默认掩码敏感值，避免测试工具与 Token 类配置继续裸露给普通角色
- `/test/**` 已从鉴权白名单移除，管理员 Token 可正常访问测试调试台后端接口；此前“前端可见但 reset/seed 返回 403”的错位链路已修复
- 达人经营台 `TEAM_PUBLIC` 查询口径已修正为公海视图优先：团队公海不再受 `DEPT/PERSONAL` 已认领范围误伤，未认领达人与“他人已认领但我未认领”的达人会重新进入可认领列表
- 管理面口径继续收紧：`/users/assignable` 已按当前管理角色过滤负责人候选，不再暴露管理员 / 运营等无关角色；角色删除已补齐“系统内置角色不可删、仍被用户占用不可删”保护
- 数据看板演示口子已收口：独家达人 / 商家监控接口不再返回硬编码假数据，活动导出接口已显式关闭，避免前台继续把演示返回误认为真实业务结果
- 达人查询正确性已补齐：达人列表不再固定只扫前 1000 条，经营概况中的“近 30 天销售额 / 订单数 / 服务费贡献”已切回最近 30 天订单窗口
- 共享商品库分页已从“一次性全量拉取后内存截页”改为分批扫描已入库状态，降低商品规模上来后的内存与响应风险
- 负责人候选接口已补齐“角色 + 同组”双重约束：管理员可看业务角色全量候选，招商/渠道组长仅能在本组内选择对应业务角色
- 历史 `colonel_leader` 兼容口径已补齐：前端登录态统一按 `biz_leader` 展示与落地，后端商品分配、活动获取与负责人候选接口继续保留兼容，避免旧角色账号在收口阶段出现前后端断裂
- 登录态收口继续补齐：前端退出登录已接入后端 `/auth/logout` 吊销 access / refresh token，不再只清本地缓存
- 操作日志写入前已增加应用侧当月分区兜底创建，降低未来月份未预建分区时日志静默断流的风险
- 操作日志分区兜底已进一步补成“建表 + 建索引 + 月度缓存”，避免新月份分区没有索引或每次写入重复执行 DDL
- 订单导出与寄样看板已补齐分页遍历，不再因固定 `10000` / 单页上限而静默截断后半段业务数据
- 订单回流排查接口 `/orders/order-attribution-unattributed` 已补齐 `DataScope` 过滤，未归因订单排查不再默认暴露全量团队数据
- 达人搜索兜底链路已从“全量拉 talent 表后内存过滤分页”改为数据库分页筛选，降低达人规模扩大后的查询抖动风险
- 订单主链路统计 `/orders/stats` 与订单回流摘要 `/dashboard/order-attribution-summary` 已改为分页批次聚合，不再一次性全量拉取订单到内存计算
- 数据平台首页核心指标 `/dashboard/metrics` 已改为分页批次读取当天 / 近 7 天订单，减少看板首页在高订单量下的内存抖动
- 寄样列表关键字搜索已改为分批检索商品匹配 ID，不再一次性 `selectList` 拉全量商品；同时 `SampleVO` 组装已复用已加载商品，减少重复 `selectById`
- 达人服务中过期认领释放与独家达人判断已改为分页批次读取近 30 天订单，避免规则判断链路一次性全量拉单
- 活动商品列表按 `bizStatus` 的查询口径已从“页后内存过滤”改为“查询前筛选”，修正了状态筛选下的 `total / hasMore / 翻页结果` 偏差；`PENDING_AUDIT` 继续兼容“无状态记录商品默认待审核”的主链路口径
- 共享商品库分页已继续去掉快照 N+1：同批 `selected_to_library` 状态改为批量 `selectBatchIds` 拉取快照，避免一条状态触发一条快照查询
- 共享商品库列表组装已继续去掉状态回查 N+1：批量拉快照后直接复用当前批次 `ProductOperationState` 组装商品视图，不再对每个商品回头再查一次状态
- 活动商品详情聚合已改为单商品专用查询：详情页不再复用 `Set.of(productId)` 的批量聚合助手，而是分别按单商品读取推进判断、订单摘要、推广链接摘要和商家信息，减少详情页单次查看时的无效集合聚合
- 商品分页主列表 `getPage` 已改为批量装配状态与负责人展示名：当前页快照先批量拉 `ProductOperationState`，再批量拉负责人 `SysUser`，不再对页内每条商品逐条 `selectOne/selectById`
- 共享商品库分页已继续去掉负责人名 N+1：同批入库状态会先批量加载负责人展示名，再回填商品卡片，避免列表页仍因 `assigneeName` 触发逐条查用户
- 活动商品列表也已统一改为批量回填 `assigneeName`：活动商品页和活动商品数据库兜底页复用同批负责人映射，不再在列表渲染阶段逐条查 `SysUser`
- 达人经营列表聚合已改为只按当前批达人查询寄样数与近 30 天订单：`sample_request` 与 `colonelsettlement_order` 不再整表聚合后内存过滤，而是按当前达人 ID / 抖音 UID 分批 `IN` 查询，降低达人列表在数据量扩大后的数据库压力
- 本地活动分页 `/activities` 已从服务内 `ArrayList` 演示数据改为读取 `colonel_activity` 表；空库场景会自动播种 5 条稳定演示活动，避免重启后活动数据口径回退为进程内 Mock
- 本地活动演示播种已补上环境开关：仅 `local-mock` 默认允许空库自动播种 `LOCAL_ACTIVITY_*` 演示活动，其他环境默认不再因访问 `/activities` 被动写入演示数据
- 商品主链路状态机已收口：`绑定活动` 继续保留为元数据修正动作，但不再写入独立 `BOUND` 状态；新流转口径统一为 `PENDING_AUDIT -> APPROVED -> ASSIGNED -> LINKED -> FOLLOWING`
- 招商权限口径已按 V2.2 继续收口：`biz_staff` 当前保留共享商品库只读、“我的商品管理、寄样审核、我的数据看板、个人中心”业务视角；前端已移出活动列表 / 达人 CRM 入口，后端活动获取与商品审核权限也已同步纠正为“组长负责活动分配，招商负责商品审核与寄样审核”
- 渠道专员权限口径已继续按 V2.2 收口：`channel_staff` 当前只保留商品库、达人 CRM、寄样台、个人数据看板与个人中心；商品库“一键复制”改为按当前渠道实时转链，达人公海不再展示他人已认领达人，寄样申请也已收紧为“仅可对自己认领的达人发起”
- 运营权限口径已按 V2.2 收口：`ops_staff` 当前仅保留“寄样发货台”业务入口与物流详情处理；已从商品库、达人 CRM、寄样审核页、数据看板与独家状态中移出，后端也已收紧为“不可创建寄样、不可查看待审核寄样、不可手动完结/关闭寄样”
- 寄样台导出入口已继续与后端契约对齐：前端不再向 `ops_staff` 展示“导出寄样单”按钮，并补齐 `GET /samples/exports -> 403` 负向回归，避免运营页出现“按钮可点但接口必拒绝”的假能力
- 商品状态机底层守门规则已与主链路口径对齐：`APPROVED -> ASSIGNED` 现已直接放行，`BOUND` 仅保留为历史旧数据兼容读取，不再作为新流转目标状态
- 活动商品招商组长分配入口已补齐历史 / 真实联调数据兜底：`APPROVED / ASSIGNED / LINKED / FOLLOWING` 等已过审状态即视为已入库，前端恢复“分配招商 / 重新分配”按钮；后端分配时会自动补写缺失的 `selected_to_library` 标记，避免已审核活动商品被“待入库”误拦截
- 商品旧状态枚举已继续收口：`SYNCED` 已从当前 `ProductBizStatus` 枚举中移除，现网商品主链路仅保留 `PENDING_AUDIT / APPROVED / REJECTED / ASSIGNED / LINKED / FOLLOWING` 与历史兼容 `BOUND`
- 商品旧状态兼容已补齐兜底：历史 `SYNCED` 值当前会在读取时归一到 `PENDING_AUDIT`，避免旧库状态行在主链路详情或列表读取时直接报错
- 数据看板提成计算已支持“全局默认值 + 活动级覆写”混合口径：`/dashboard/metrics` 会优先读取 `commission.business_activity_ratio.{activityId}` 与 `commission.channel_activity_ratio.{activityId}`，未命中时回退全局默认配置；完整提成规则中心 UI 仍未进入本阶段
- 商家库被动填充链路已确认：订单回流成功插入 `colonelsettlement_order` 后会同步触发 `merchantService.ensureMerchantFromOrder`，优先使用订单原始字段 `merchant_id / merchant_name` 建商家，缺失时回退 `shop_id / shop_name`
- 独家达人月寄样阈值口径已明确收口：服务费贡献继续按订单 `settle_time` 统计，月寄样数量改按 `sample_request.ship_time` 统计“实际已发货寄样量”，不再按 `create_time` 混算申请数
- 订单回流摘要 `/dashboard/order-attribution-summary` 已改为 SQL 聚合口径：招商业绩当前明确按 `colonel_user_id` 归集，不再误用渠道运营 `user_id`；同时摘要总量与渠道 / 招商业绩不再把近 30 天订单整批拉进内存聚合
- 数据看板核心指标 `/dashboard/metrics` 已改为 SQL 聚合口径：今日单量 / GMV、近 7 天趋势与活动级提成计算不再把当天 / 近 7 天订单全量拉入内存，降低高订单量下的内存抖动与 OOM 风险
- 数据平台筛选增强已继续补齐：`/data/orders` 当前支持按 `talentId / merchantId` 精确筛选，并与 `/dashboard/metrics`、`/orders/exports` 统一支持 `timeField=createTime|settleTime`；默认 `createTime` 用于覆盖真实同步但尚未结算订单，按结算复核时显式切换 `settleTime`；订单明细返回 `createTime / settleTime`，前端时间列随口径切换，导出 CSV 同时包含创建时间与结算时间；`/activities/exports` 已开放真实 CSV 导出，不再固定抛出演示期异常
- 数据看板首页前端展示已继续真实化：`/data` 指标卡趋势不再使用硬编码“较上周”演示文案，订单数与 GMV 改为消费 `/dashboard/metrics` 返回的 `trend7d` 做昨日对比；服务费 / 毛利仅展示 `createTime` / `settleTime` 统计口径，不伪造独立历史趋势；近 7 日趋势已改为 ECharts 柱线组合图，直接使用真实 `orderCount / gmv` 渲染，并通过懒加载降低数据页首屏 chunk 体积
- 数据订单明细弹窗已移除前端假收件人 / 假手机号 / 假物流兜底：上游未返回的收件、地址、物流字段统一显示“未返回 / 暂无真实物流信息”
- 首页 `/dashboard` 已按 `/dashboard/summary` 的真实累计归因概览重命名指标：累计 GMV、未归因订单、归因成功率、服务费收入，不再展示后端未提供的“活跃达人数”占位
- 业务规则配置读取已补上本地按 `configKey` 缓存，系统配置中心新建 / 更新 / 删除对应配置后会精确失效缓存，降低高频规则读取对 `system_config` 的重复查询压力
- 订单主链路统计 `/orders/stats` 已继续从“分页批次遍历聚合”收口为 SQL 分组聚合：总量、归因状态分布与未归因原因分布改为数据库侧 `COUNT/GROUP BY` 计算，避免 30 天大盘订单量下多轮分页扫描带来的慢查询与内存抖动
- 达人 CRM 查询 `TalentQueryService.page()` 已改为“窗口式分页收集”口径：继续保留跨批过滤与准确 `total`，但不再把全部命中达人累积到内存；同时内部基础拉取页大小不再对小页请求强制放大到 `200`
- `CustomMetaObjectHandler` 已接入请求上下文里的 `userId`：当前 Web 请求链路下 `createBy/updateBy` 会自动回填当前操作人，非法或缺失上下文时保持空值以兼容系统任务 / 非请求线程场景
- 活动商品业务视图已去掉硬编码 `test=true` 污染：`/colonel/activities/{activityId}/products` 及其本地快照回退结果不再把测试标识混入业务返回；同时 `CommissionService` 已改为对提成比例配置查询异常显式 `log.error + throw`，避免数据库/SQL 故障被静默吞掉后长期回退默认费率
- `RealDouyinAuthGateway.ensureToken` 已补为 Redis 缓存兜底：真实网关现在会按 `appId -> token / refresh_token / expire_at` 读取已有缓存，只有缓存缺失或已过期时才返回空，不再把“已有 token 但未初始化到当前实例”的场景静默吞成固定 `null`
- 管理员侧已新增 `/system/douyin` 抖店联调入口：页面可一键刷新 Token、授权主体、活动商品业务刷新、精选联盟 SKU 探针、订单同步与 Dashboard `createTime` 指标，且只调用内部 `/api`
- `SampleLifecycleJob` 已补齐 Redis 分布式锁：多实例部署下每天自动关闭寄样任务只会有一个实例进入执行，避免同一批超时寄样单被重复关闭与重复写状态日志；测试环境 Redis 不可用时回退本地锁，保持现有定向测试稳定
- 操作日志中心已补齐 90 天分区清理收口：`OperationLogService.cleanupOldPartitions` 可按保留天数删除过期月分区，`LogCleanupJob` 默认每日 `02:30` 执行并使用 Redis 分布式锁防重入，测试模式下 Redis 不可用会回退本地锁
- 订单回流沉淀事务边界已收口：`MerchantService.ensureMerchantFromOrder` 不再使用 `REQUIRES_NEW` 脱离订单主事务单独提交，订单落库、`pick_source` 补全、商家入库与寄样自动完成现在默认同事务提交，避免出现“订单后续回滚但商家已先落库”的部分成功状态
- 达人周更任务已从串行逐个刷新改为受控并发：`TalentWeeklyRefreshJob` 现在按可配置并发度批量提交刷新任务，单达人失败仍只影响自身，避免达人量扩大后整轮周更被单线程拖长
- 寄样单部门归属已补齐实体映射与创建落库：`sample_request.dept_id / channel_dept_id` 当前会随新建寄样单一起写入；详情对象级权限校验也已优先按单据落库部门判断，避免渠道人员换部门后出现“列表仍可见但详情被拒绝”或“新单据在组维度下查不出来”的口径漂移
- P2 权限口径已明确：`admin` 是后端超级角色，可覆盖类级 / 方法级 `@RequireRoles`；业务角色仍按模块注解和前端入口收口，商品转链、达人 CRM、真实订单同步等接口的普通角色限制已有定向测试保护。`DouyinWebhookController` 是第三方回调入口，不纳入登录角色注解模型

## 三、验证基线

### 1. 构建与测试

- `frontend npm.cmd run build`：通过
- `backend mvn "-Dtest=SampleControllerTest" test`（2026-05-17 P3-5 寄样闭环回归）：通过（`29 tests, 0 failures, 0 errors`）
- `git diff --check`（2026-05-17）：无空白错误
- 2026-05-10 P2 权限与 Gateway 契约收口后：`backend mvn clean test` 通过（`669 tests, 0 failures, 0 errors`）；`frontend npm run build` 通过（保留既有 Vite 大 chunk warning）；`git diff --check` 无格式错误
- `backend mvn clean test`：`652 tests, 0 failures, 0 errors`（2026-05-09 全量回归确认）
- `backend mvn "-Dtest=ProductServiceTest,ProductControllerTest,ColonelActivityProductControllerTest,ProductBizStatusServiceTest" test`：通过
- `backend mvn "-Dtest=ProductServiceTest,ProductControllerTest,ColonelActivityProductControllerTest" test`（商品审核补充字段补齐后）：通过
- `backend mvn "-Dtest=TalentControllerTest,TalentServiceTest,TalentQueryServiceTest" test`：通过
- `backend mvn "-Dtest=TalentServiceTest" test`（达人保护期自动释放规则补齐后）：通过
- `backend mvn "-Dtest=TalentControllerTest,TalentQueryServiceTest" test`（达人刷新入口补齐后）：通过
- `backend mvn "-Dtest=TalentServiceTest,TalentQueryServiceTest,TalentControllerTest" test`（达人多人认领口径补齐后）：通过
- `backend mvn "-Dtest=TalentServiceTest,TalentQueryServiceTest,TalentControllerTest" test`（达人多人认领落库修复后）：通过
- `frontend npm run build`（达人经营台一期集成后）：通过
- `backend mvn "-Dtest=OperationLogControllerTest,OperationLogInterceptorTest,DataControllerTest,SampleLifecycleServiceTest" test`：通过
- `frontend npm run build`（操作日志中心与导出口径收口后）：通过
- `backend mvn "-Dtest=TalentQueryServiceTest,SampleControllerTest,OrderControllerTest,OrderQueryServiceTest,TalentControllerTest,TestControllerSecurityTest,SysConfigServiceTest" test`：通过（`46 tests, 0 failures, 0 errors`）
- `backend mvn -q -DskipTests compile`：通过
- `backend mvn "-Dtest=SysUserServiceTest,SysRoleServiceTest,SysUserControllerTest,DataControllerTest" test`：通过（`47 tests, 0 failures, 0 errors`）
- `backend mvn "-Dtest=TalentQueryServiceTest,SampleControllerTest,OrderControllerTest,OrderQueryServiceTest,TalentControllerTest,TestControllerSecurityTest,SysConfigServiceTest,SysUserServiceTest,SysRoleServiceTest,SysUserControllerTest,DataControllerTest" test`：通过（`97 tests, 0 failures, 0 errors`）
- `backend mvn "-Dtest=ProductServiceTest,OperationLogServiceTest,AuthServiceTest" test`：通过（`20 tests, 0 failures, 0 errors`）
- `backend mvn "-Dtest=SysUserServiceTest,SysUserControllerTest,ProductServiceTest,OperationLogServiceTest,AuthServiceTest" test`：通过（`42 tests, 0 failures, 0 errors`）
- `backend mvn "-Dtest=DataControllerTest,OperationLogServiceTest" test`：通过（`16 tests, 0 failures, 0 errors`）
- `backend mvn "-Dtest=SampleControllerTest" test`：通过（`16 tests, 0 failures, 0 errors`）
- `frontend npm run build`（登录态刷新 / 登出吊销补齐后）：通过
- `backend mvn "-Dtest=CrawlerTalentInfoServiceTest,OrderAttributionControllerTest" test`：通过（`12 tests, 0 failures, 0 errors`）
- `backend mvn "-Dtest=OrderControllerTest,OrderAttributionControllerTest" test`：通过（`6 tests, 0 failures, 0 errors`）
- `backend mvn "-Dtest=DataControllerTest,OrderControllerTest,OrderAttributionControllerTest" test`：通过（`21 tests, 0 failures, 0 errors`）
- `backend mvn "-Dtest=SampleControllerTest" test`：通过（`16 tests, 0 failures, 0 errors`）
- `backend mvn "-Dtest=TalentServiceTest" test`：通过（`16 tests, 0 failures, 0 errors`）
- `backend mvn "-Dtest=SysConfigServiceTest,BusinessRuleConfigServiceTest,SampleEligibilityServiceTest" test`：通过（`9 tests, 0 failures, 0 errors`）
- `backend mvn "-Dtest=ProductServiceTest" test`：通过（`16 tests, 0 failures, 0 errors`）
- `backend mvn "-Dtest=ProductServiceTest,ProductControllerTest,ColonelActivityProductControllerTest,ColonelActivityControllerTest" test`：通过（`29 tests, 0 failures, 0 errors`）
- `backend mvn "-Dtest=ProductServiceTest" test`（活动商品详情单商品聚合收口后）：通过（`17 tests, 0 failures, 0 errors`）
- `backend mvn "-Dtest=ProductServiceTest,ProductControllerTest,ColonelActivityProductControllerTest,ColonelActivityControllerTest" test`（商品链路二次回归）：通过（`30 tests, 0 failures, 0 errors`）
- `backend mvn "-Dtest=ProductServiceTest" test`（商品分页与活动商品负责人批量装配后）：通过（`19 tests, 0 failures, 0 errors`）
- `backend mvn "-Dtest=ProductServiceTest,ProductControllerTest,ColonelActivityProductControllerTest,ColonelActivityControllerTest" test`（商品链路三次回归）：通过（`32 tests, 0 failures, 0 errors`）
- `backend mvn "-Dtest=TalentQueryServiceTest" test`（达人聚合按当前批 ID / UID 收口后）：通过（`8 tests, 0 failures, 0 errors`）
- `backend mvn "-Dtest=TalentServiceTest,TalentQueryServiceTest,TalentControllerTest" test`（达人链路回归）：通过（`41 tests, 0 failures, 0 errors`）
- `backend mvn "-Dtest=CommissionServiceTest" test`（数据看板差异化提成收口后）：通过
- `backend mvn "-Dtest=RealDouyinAuthGatewayTest,DouyinTokenServiceTest,DouyinControllerTest" test`（真实 Token 网关 Redis 兜底补齐后）：通过（`42 tests, 0 failures, 0 errors`）
- `backend mvn "-Dtest=SampleLifecycleJobTest,OrderSyncJobTest,RealDouyinAuthGatewayTest,DouyinTokenServiceTest,DouyinControllerTest" test`（寄样生命周期分布式锁 + 定时任务异常可见性补齐后）：通过（`50 tests, 0 failures, 0 errors`）
- `backend mvn "-Dtest=MerchantServiceTest,OrderSyncPersistenceServiceTest,PickSourceMappingServiceTest" test`（订单沉淀事务边界收口后）：通过（`24 tests, 0 failures, 0 errors`）
- `backend mvn "-Dtest=TalentWeeklyRefreshJobTest,SampleLifecycleJobTest,OrderSyncJobTest" test`（达人周更并发化与定时任务回归后）：通过（`11 tests, 0 failures, 0 errors`）
- `backend mvn "-Dtest=ProductBizStatusServiceTest,ProductServiceTest,ColonelActivityProductControllerTest" test`（商品状态机守门规则与活动商品主链路再次收口后）：通过（`41 tests, 0 failures, 0 errors`）
- `backend mvn "-Dtest=ColonelsettlementActivityServiceTest,ProductBizStatusServiceTest,ProductServiceTest,ColonelActivityProductControllerTest" test`（活动演示播种环境隔离与商品旧状态兼容兜底后）：通过（`45 tests, 0 failures, 0 errors`）
- `backend mvn "-Dtest=ExclusiveMerchantServiceTest,MerchantServiceTest,OrderSyncPersistenceServiceTest" test`（商家被动填充链路确认后）：通过
- `backend mvn "-Dtest=ExclusiveTalentServiceTest" test`（独家达人月寄样阈值改按 `ship_time` 收口后）：通过
- `backend mvn "-Dtest=OrderAttributionControllerTest,DataControllerTest,CommissionServiceTest" test`（订单回流摘要 owner 修正与数据看板 SQL 聚合收口后）：通过
- `backend mvn "-Dtest=*SampleControllerTest" test`（寄样单部门归属落库与详情权限口径收口后）：通过（`18 tests, 0 failures, 0 errors`）
- `frontend npm run build`（抖店联调前端入口与 real-pre 状态面板接入后）：通过（2026-05-08）
- `backend mvn "-Dtest=SampleControllerTest" test`（寄样导出角色口径与运营假入口收口后）：通过（`28 tests, 0 failures, 0 errors`）
- `frontend npm run build`（寄样导出按钮角色口径收口后）：通过（2026-05-09）
- `backend mvn "-Dtest=OrderControllerTest" test`（真实订单手动同步角色口径收口后）：通过

### 2. 浏览器回归

- real-pre 浏览器全路径回归：`45/45`
- 报告路径：`runtime/qa/out/e2e-20260503-1353/report.md`
- QA 脚本：`runtime/qa/full-browser-e2e.cjs`
- real-pre 抖店联调前端 E2E：`1/1`
- 报告路径：`runtime/qa/out/real-pre-douyin-frontend-20260508105636/report.md`
- real-pre 抖店联调前端 E2E（2026-05-08 21:19 二次复验）：`1/1`
- 报告路径：`runtime/qa/out/real-pre-douyin-frontend-20260508211901/report.md`
- QA 脚本：`runtime/qa/real-pre-douyin-frontend-e2e.cjs`（现转发 Playwright `tests/e2e/08-real-pre-douyin-integration.spec.ts`，`npm run e2e:real-pre`）
- review 修复已补齐：活动 ID 留空时自动使用活动列表探测结果；脚本改为工作区相对输出并去掉机器绝对路径依赖；一键刷新前会清空旧状态与旧摘要，失败步骤显式标红；Token 已确认不可用时会短路后续联调步骤，避免继续触发订单同步或商品刷新等副作用调用
- 2026-05-08 21:19 real-pre 三方联调二次复验完成：证据目录 `runtime/qa/out/real-pre-sdk-retest-20260508-211901`；`POST /api/orders/sync` 本轮 7 天窗口返回 `totalFetched=10 / created=10 / updated=0 / attributed=10 / unattributed=0 / failed=0`，全库订单统计为 `totalOrders=326 / attributedOrders=326 / unattributedOrders=0 / partialOrders=0 / syncFailedOrders=0`；Dashboard `createTime` 口径返回 `todayOrderCount=315 / todayGmv=6035.07 / serviceFee=94.73 / grossProfit=66.31`
- 2026-05-09 15:29 按“只测联盟 / 团长链路、不测店铺侧接口”重新复核：real-pre 后端 `UP`，Token 可用，`buyin.institutionInfo / alliance.instituteColonelActivityList / alliance.colonelActivityProduct / buyin.productSkus.v2` 均返回 `10000`；活动商品样本 `20` 条，SKU 样本 `1` 条；30 分钟订单同步 `totalFetched=10 / created=0 / updated=10 / attributed=10 / unattributed=0 / failed=0`；Dashboard `createTime` 口径 `totalOrders=275 / totalAmount=5708.42`
- 2026-05-09 15:29 全库订单归因重新确认：`totalOrders=675 / attributedOrders=657 / unattributedOrders=18 / partialOrders=0 / syncFailedOrders=0`，未归因样本原因集中为 `COLONEL_MAPPING_NOT_FOUND`，因此“全库 100% 归因”不再作为当前结论
- 2026-05-09 16:31 real-pre 继续复跑后更新为：`totalOrders=916 / attributedOrders=870 / unattributedOrders=46 / partialOrders=0 / syncFailedOrders=0`；`POST /api/orders/replay-attribution` 的 `dryRun(limit=50)` 返回 `scanned=46 / attributed=0 / unattributed=46 / updated=0`，说明当前问题已收敛为本地缺少 `10` 个活动与 `27` 个 `activity_id + product_id` 业务映射组合，而不是同步入口或归因重算代码失效
- 2026-05-10 P1-2 real-pre 真实链路联调完成：环境隔离确认通过（`test=test / colonel_saas_test / Redis DB 1 / Mock`；`real-pre=real / colonel_saas_real / Redis DB 0 / live`），`/api/actuator/health` 返回 `UP`；Token 状态 / 刷新、授权主体、活动列表、活动商品、商品审核入库、分配、真实转链、`pick_source_mapping`、订单同步、订单归因统计与 Dashboard 展示均完成验证。最近 7 天订单同步返回 `totalFetched=100 / created=5 / updated=95 / attributed=95 / unattributed=5 / failed=0`；同步后全库统计为 `totalOrders=1802 / attributedOrders=1665 / unattributedOrders=137 / syncFailedOrders=0`，最终收口复核更新为 `totalOrders=1821 / attributedOrders=1683 / unattributedOrders=138`；未归因仍按真实映射缺口记录，不用 Mock 数据伪装通过。证据目录：`runtime/qa/out/real-pre-p1-2-20260510-020545`、`runtime/qa/out/real-pre-p1-2-product-chain-20260510-020840`
- 2026-05-10 real-pre 数据看板真实口径纯化完成：已软删除历史遗留演示 / 测试 active 数据（订单 1、商品快照 23、推广映射 2、商品状态 6、寄样单 5、达人认领 3、商品 3、达人 7），并新增 `scripts/qa/check-real-pre-real-data.ps1` 一键复核；复核结果为 `status=OK`，环境 `real / APP_TEST_ENABLED=false / DOUYIN_TEST_ENABLED=false / ORDER_SYNC_ENABLED=true / Redis DB 0 / colonel_saas_real`，8 个核心表 active mock/test/演示命中均为 `0`；当前订单汇总 `activeOrders=2187 / attributedOrders=2025 / unattributedOrders=162 / totalGmv=42668.11`，Dashboard `createTime` 返回 `todayOrderCount=1003 / todayGmv=19539.13`

### 3. local-mock 补充验收

- local-mock 交互级补充验收：`5/5`
- 报告路径：`runtime/qa/out/local-mock-supplement-20260503-1430/report.md`
- QA 脚本：`runtime/qa/local-mock-supplement.cjs`

### 3.1 达人经营台一期烟雾验收

- 视图切换 / 私海释放 / 拉黑与恢复：通过
- 公海认领：通过
- 多人认领专项：通过
- 报告路径：`runtime/qa/out/talent-platform-smoke-20260504151355/report.md`
- 多认领专项路径：`runtime/qa/out/talent-multiclaim-smoke-20260504151355/report.md`
- QA 脚本：`runtime/qa/talent-platform-smoke.cjs`

### 4. `3000/8080` 可见浏览器分批验收

- 执行规范已固定：`每个测试用例单独新开一个可见浏览器 -> 真实操作 -> 截图 -> 批次目录归档`
- 已完成模块：认证与用户权限、角色与系统管理、数据平台与归因、商品/活动、达人/寄样、运营与详情路由
- 2026-05-09 数据看板 / Webhook 收口复验：通过
- 复验结果：`/data` 与 `/data/orders` 默认 `createTime`，切换 `settleTime` 正常；`POST /api/douyin/webhook-events/replay?limit=5` 管理员鉴权下返回 `200`，本轮 `scanned=0 / consumed=0 / failed=0`
- 复验证据目录：`out/browser-regression-rerun-2026-05-09T01-57-54-991Z/`
- 运行事实补记：本轮首次回归命中旧实例未刷新，`8080` 上一度报 `No static resource douyin/webhook-events/replay`；重启 `saas-test-backend-1` 后恢复正常，说明新增后端路由 / 收件箱逻辑落地到 `test` 容器时应做一次实例重启确认
- 管理员全功能慢速可视化演示：`17/17` 通过
- 报告路径：`runtime/qa/out/admin-full-demo-20260505-120421/report.md`
- 最新回归路径：`runtime/qa/out/admin-full-demo-20260505-141957/report.md`
- QA 脚本：`runtime/qa/admin-full-demo.cjs`
- 角色业务流慢速可视化演示：`9/9` 通过
- 报告路径：`runtime/qa/out/role-business-flow-visible-20260505-141603/report.md`
- 本轮修复回归目录：`out/e2e-bugfix-regression-20260503113715/`
- 最终烟雾验收目录：`out/e2e-final-smoke-20260503114958/`
- 当前补丁回归目录：`out/e2e-bugfix-verify-20260503203450/`
- 当前规则型功能补测目录：`out/e2e-gap-followup-20260503205740/`（`6/6` 通过）
- 当前数据一致性可见浏览器目录：`out/e2e-data-consistency-visible-20260503212311/`（`3/3` 通过）
- 当前规则中心可见验收目录：`runtime/qa/out/system-config-rules-visible-20260504-155257/`（`4/4` 通过）
- 当前寄样默认标准可见验收目录：`runtime/qa/out/sample-default-standard-visible-20260504-162132/`（`2/2` 通过）
- 典型批次目录：
  - `out/e2e-user-crud-20260503104514/`
  - `out/e2e-auth-a10-a12-20260503110748/`
  - `out/e2e-role-mgmt-rerun-20260503111142/`
  - `out/e2e-data-phase3-20260503111929/`
  - `out/e2e-business-pages-20260503112517/`
  - `out/e2e-detail-routes-20260503112846/`
  - `out/e2e-final-smoke-20260503114958/`
  - `runtime/qa/out/system-config-rules-visible-20260504-155257/`
  - `runtime/qa/out/sample-default-standard-visible-20260504-162132/`

### 5. 补充记录入口

- 本地联调实录： [archive/records/16-local-mock业务联调记录](./archive/records/16-local-mock业务联调记录.md)
- 剩余事项看板： [archive/records/17-项目剩余事项看板](./archive/records/17-项目剩余事项看板.md)
- 浏览器验收 Runbook： [archive/runbooks/07-Test全链路验收](./archive/runbooks/07-Test全链路验收.md)

## 四、当前未完成项

- [~] 商品链路端到端样本补证（2026-05-21）：活动商品同步与快照字段已 PASS；仍待样本商品走完「审核入库 → `POST .../promotion-links` → `pick_source_mapping` 含 `v.` + activity/product」及带 `pick_source` 订单归因样本。Runbook：[19-商品链路本地排查](./archive/runbooks/19-商品链路本地排查.md)；脚本：`scripts/qa/audit-product-chain-real-pre.ps1`；证据：`runtime/qa/out/product-chain-audit-20260521-122559/`
- [ ] 商品库文档口径接口：`list_partners`、`pin_product`、`get_pinned_products`、多维服务端筛选（`partner_id` / `goods_tags` / `sort_by` 等）尚未实现；当前以 `shop_name` + 前端客户端过滤代替
- [~] 商品 SKU 真实样本采集：SKU 已改走精选联盟 `/buyin/productSkus/v2`，raw probe 已返回 `10000 / success`，待 real-pre 重启后复验业务 Gateway 解析
- [x] 联盟测范围已收口：店铺商品 `product.detail / sku.detail`、店铺订单 `order.searchList / order.orderDetail` 不再纳入三方联调进度
- [~] Webhook 本地消费框架、幂等落库与重放补偿已补齐；真实业务事件副作用仍待上游样本确认
- [~] Webhook 本地消费框架、幂等落库与重放补偿已补齐；2026-05-12 进一步修复了 real-pre 历史 volume 缺少 `douyin_webhook_event` 表导致回调 `500` 的问题，应用启动现会幂等补建收件箱表，real-pre 已实投确认 `200 success + CONSUMED + event_key 幂等`；但当前消费结果仍仅 `COLONEL_OPEN_EVENT_CAPTURED`，真实业务事件副作用仍待样本与下游实现确认
- [~] Webhook 已新增“按 payload 中 `order_id / order_ids` 触发定向订单同步”的保守消费实现，会复用现有订单同步、归因、`pick_source_mapping`、merchant 与寄样生命周期副作用链；单测已覆盖，但 real-pre 现场证据仍待 Docker 环境恢复后补齐
- [~] 2026-05-14 已在 real-pre 现场复核 Webhook 定向同步分支：带 `order_id` 的回调会真实命中 `buyin.colonelMultiSettlementOrders`，收件箱记录为 `COLONEL_OPEN_EVENT_SYNCED`；但当前授权主体下该上游接口返回 `orders=[]`，因此暂未拿到 `fetched>0` 的真实副作用样本
- [~] 同日进一步补证：`buyin.instituteOrderColonel` 最新真实订单样本均表现为 `settled_goods_amount=0 / real_commission=0`；后续 Docker 复核又确认本地历史 `settle_time IS NOT NULL` 记录已有 48 条，因此当前 `buyin.colonelMultiSettlementOrders` 空样本更像“当前主体最近窗口暂无结算单”，而非代码故障
- [~] 2026-05-14 13:06 已新增自动探针 `scripts/qa/watch-real-pre-order-settlements.ps1` 并完成首跑；证据目录 `runtime/qa/out/real-pre-order-settlements-watch-20260514-130606`。本轮确认多结算接口 30 天窗口仍空，但主订单 3 天窗口抓到 10 条真实未结算样本；Docker 库证据显示历史 `settle_time` 非空记录已有 48 条，说明剩余问题更偏上游窗口/口径，而不是本地链路失效
- [~] 2026-05-14 14:29 已继续复跑 watcher：证据目录 `runtime/qa/out/real-pre-order-settlements-watch-20260514-142923`。本轮 `buyin.colonelMultiSettlementOrders` 仍是 `status=success / orderCount=0`，`buyin.instituteOrderColonel` 3 天窗口仍抓到 10 条真实未结算样本，且 Docker 库 `settle_time IS NOT NULL` 历史记录仍为 48；因此该问题正式固化为“上游样本等待项”，不是本地代码阻塞项
- [x] 2026-05-14 14:33 已完成 real-pre 主链路最终回归：证据目录 `runtime/qa/out/real-pre-final-regression-20260514-143347`。本轮覆盖健康检查、授权主体、活动、商品业务视图与 refresh、既有转链 / `pick_source_mapping`、主订单同步、归因 dry-run、Dashboard、Webhook 接收、Webhook 重放、Webhook 定向同步；结果显示主链路均可执行，`POST /api/orders/sync` 最近 30 分钟返回 `totalFetched=97 / created=3 / updated=94 / attributed=1 / unattributed=96 / failed=0`，Webhook 定向同步收件箱记录为 `COLONEL_OPEN_EVENT_SYNCED:fetched=0,created=0,updated=0,failed=0`，剩余阻塞仍仅为多结算上游样本缺失
- [~] 快递鸟物流即时查询对接：2026-05-14 已完成 `LogisticsGateway.queryTrack` 契约、`TestLogisticsGateway` 状态模拟、`KdniaoLogisticsGateway` 请求签名 / 表单编码 / 状态映射本地测试底座；定向测试 `GatewayRecordTest,TestLogisticsGatewayTest,KdniaoLogisticsGatewayTest` 为 `19 tests, 0 failures, 0 errors`。当前未配置真实 `EBusinessID / AppKey`，未调用 Sandbox / real-pre，不宣称真实物流已接入
- [x] 2026-05-15 Docker 启动审查补齐物流手动兜底网关：当 `APP_TEST_ENABLED=false` 且 `KDNIAO_ENABLED=false` 时，`ManualFallbackLogisticsGateway` 会返回不推进状态的人工跟进结果，避免 real-pre 因缺少 `LogisticsGateway` Bean 启动失败；定向测试 `ManualFallbackLogisticsGatewayTest,LogisticsTrackServiceTest,LogisticsTrackJobTest,GatewayRecordTest,TestLogisticsGatewayTest,KdniaoLogisticsGatewayTest` 为 `44 tests, 0 failures, 0 errors`
- [ ] 真实限流 / 429 分支验证
- [ ] Real Gateway 逐项联调准备
- [x] Token 状态查询、刷新链路与授权主体确认（2026-05-07 real-pre，`POST /api/douyin/token-refreshes` HTTP 200 / `code=200`；`GET /api/douyin/institution-info` 上游 `code=10000`）
- [x] 活动列表联调接口真实样本采集（2026-05-07 real-pre，`GET /api/douyin/activities` 上游 `code=10000`，首批 20 条）
- [x] 活动列表业务接口真实对照与页面兼容字段收口（2026-05-07 real-pre，`GET /api/colonel/activities` 返回 `data.total=21 / activityList[0].activityId=3916506`）
- [x] 活动详情真实样本采集（2026-05-07 real-pre，`GET /api/douyin/activities/3916506` 上游 `code=10000`，详情字段已脱敏归档）
- [x] 活动商品联调接口真实样本采集（2026-05-07 real-pre，`GET /api/douyin/activity-product-list?activityId=3916506&count=20` 上游 `code=10000`，真实数组位于 `data.data`）
- [x] 活动商品业务接口刷新闭环（2026-05-07 real-pre，`GET /api/colonel/activities/3916506/products?count=20&refresh=true` 可绕过旧快照拉上游并回写，10 条旧快照已刷新为 20 条业务视图）
- [x] 商品素材状态与转链 fallback 补充探针（2026-05-08 real-pre `real` profile，`buyin.materialsProductStatus` 商品详情 URL 入参返回 `10000 / success`；`buyin.kolProductShare` 返回授权主体不匹配；`buyin.getProductShareMaterial` 返回接口已不再提供服务）
- [~] Token 初始化新授权码重放、精选联盟 SKU 样本采集（2026-05-07 real-pre，活动商品业务详情可读；2026-05-09 SKU 代码口径已改为精选联盟 `/buyin/productSkus/v2`，raw probe 用真实商品 ID 已返回 `10000 / success`）
- [~] 多结算订单查询首轮 real-pre 取证已完成：`GET /api/douyin/order-settlements` 成功结构已确认是 `data.cursor / data.orders`，当前仅拿到空样本，可作为后续结算样本补充来源
- [x] 订单主同步真实样本采集与入库已完成：`buyin.instituteOrderColonel` 成功结构为 `data.cursor / data.orders`，时间参数需传 `yyyy-MM-dd HH:mm:ss`；`POST /api/orders/sync` 30 分钟窗口拉取 10 单、落库 10 单、失败 0
- [~] 全库订单归因当前不是 100%：2026-05-09 16:31 复核为 `916` 单中 `870` 已归因、`46` 未归因，未归因原因全部为 `COLONEL_MAPPING_NOT_FOUND`
- [~] `COLONEL_MAPPING_NOT_FOUND` 代码侧已完成两轮收口：已识别 real-pre 未归因样本存在 `colonel_order_info_second` 二级团长信息，当前代码已补“二级团长活动优先归因 + 存在二级活动时禁止泛 seed 映射误归到 admin”保护，并新增 `POST /api/orders/replay-attribution` 历史重算入口；real-pre `dryRun` 已确认当前 46 单仍全部未归因，下一步需要补齐缺失活动 / 商品 / 负责人映射
- [x] 真实订单展示 smoke 已完成：`/api/orders` 与 Dashboard `createTime` 口径可读到真实同步订单；管理员 `/system/douyin` 已完成前端对接并通过 real-pre E2E `1/1`，页面当前按联盟测范围汇总 Token、授权主体、活动商品刷新、SKU、订单同步与 Dashboard；数据平台订单页、指标页与导出默认已切到 `createTime`，仍可显式切换 `settleTime` 做结算复核
- [x] `RealDouyinOrderGateway` 真实订单映射补全首轮完成
- [x] M1.6 数据看板真实化：核心指标、订单明细筛选、导出时间口径、首页 ECharts 趋势展示、订单详情缺失字段提示、`/dashboard` 累计概览话术，以及 `P1-8 Dashboard 全链路真实化验收` 已在 2026-05-12 real-pre 通过；当前结论是“真实数据持续增长下 summary / activity-products / diagnosis drilldown / 前台链路一致”，不是“未归因清零”
- [x] M1.7 部署验证：2026-05-12 已完成 real-pre 首轮部署收口；2026-05-15 环境治理后，运行口径收敛为 `saas-active` 单活环境，test / real-pre 共用 `3000/8080/5432/6379` 与固定容器名，权限测试默认回到 test，real-pre 仅用于真实 SDK 联调
- [x] 2026-05-17 real-pre 运行态准入与前后端 E2E 已通过：证据目录 `runtime/qa/out/qa-real-pre-preflight-20260517-090308-762`、`runtime/qa/out/real-pre-e2e-20260517-090457-069`；`/system/douyin` 一键刷新状态可完整通过
- [x] 2026-05-17 P3 real-pre 单样本业务闭环已通过：证据目录 `runtime/qa/out/real-pre-business-e2e-20260517-091828`；活动商品同步、SKU 探针、商品本地状态、真实转链、`pick_source_mapping`、订单同步与 Dashboard 首轮通过
- [x] 2026-05-17 P3-5 全角色账号业务流程验收已通过：证据目录 `runtime/qa/out/real-pre-role-business-e2e-20260517-101101`；管理员、招商组长、招商、渠道组长、渠道、运营六类账号的菜单权限、数据范围、核心业务动作、越权拦截与寄样闭环状态推进全部通过，业务步骤 `11/11 PASS`、Playwright `2/2 PASS`
- [x] 2026-05-17 real-pre 浏览器可视化全业务剧本回归已通过：最新证据目录 `runtime/qa/out/real-pre-full-business-journey-20260517-154251`；`.\scripts\qa\run-real-pre-full-business-journey-visual.ps1` 返回 `1 passed (9.0m)`，Journey `结论=PASS / overallPass=true / 9/9 PASS / failures=0`，并已补齐页面顶部动作说明、点击高亮、慢速 headed、trace / video / screenshot 证据
- [x] 形成 `local-mock` 是否可下线到 `test` 的正式判定结论：独立容器口径收敛到 `test`，历史脚本 / 报告 / profile 保留为回滚参考
- [~] 非默认场景与归档文档中关于 real-pre 的**历史** profile / 开关表述继续清理；主干文档以本节「环境格局」与 `docs/README.md` 当前基线为准
- [x] `/data` 与 `/data/orders` 订单数口径继续核对并收口
- [x] 本地规则中心首轮接线（寄样限制 / 保护期 / 超时关闭 / 独家阈值）

### V2.2 Gap Closure Phase 1（收口记录）

本轮收口的目标是补齐“数据看板筛选增强”与“操作日志 90 天清理”两块能力，并保持现有 `test / real-pre` 双轨基线不被破坏；`local-mock` 历史口径继续作为回滚参考保留。

#### Gap 4：数据看板筛选增强（已收口）

- `/activities/exports`：活动列表 CSV 导出（支持 `activityName` 关键字筛选，分页流式写出）
- `/data/orders`：订单分页增加 `talentId` / `merchantId` 筛选参数（沿用 `DataScope` 过滤），并支持 `timeField` 时间字段切换
- `/dashboard/metrics`：核心指标增加 `timeField`（`createTime` 默认 / `settleTime`）切换，底层走 SQL 聚合口径
- `/orders/exports`：订单导出沿用订单明细筛选口径，支持 `timeField`，避免页面列表与导出 CSV 时间范围不一致

#### Gap 5：操作日志 90 天清理（已收口）

- `OperationLogService.cleanupOldPartitions(int retentionDays)`：按分区月粒度清理 90 天前分区（`DROP TABLE` 子分区）
- `LogCleanupJob`：每日 `02:30` 定时执行，Redis 分布式锁防重复；`app.test.enabled=true` 且 Redis 不可用时回退本地锁以保持测试稳定

#### 验证证据（已执行）

```bash
cd backend
mvn "-Dtest=DataControllerTest,LogCleanupJobTest,OperationLogServiceTest" test
```

- 2026-05-09 数据平台时间口径收口定向回归：`backend mvn "-Dtest=DataControllerTest" test`，`18 tests, 0 failures, 0 errors`，BUILD SUCCESS
- 2026-05-09 real-pre 迁移契约定向回归：`backend mvn "-Dtest=RealPreMigrationContractTest,ColonelSaasApplicationTests,PickSourceMappingServiceTest,AttributionServiceTest" test`，`27 tests, 0 failures, 0 errors`，BUILD SUCCESS
- 2026-05-09 real-pre Compose 配置校验：`docker compose --env-file .env.real-pre -f docker-compose.real-pre.yml config`，通过
- 2026-05-09 全量回归：`backend mvn clean test`，`652 tests, 0 failures, 0 errors`，BUILD SUCCESS
- 2026-05-09 前端构建：`frontend npm run build`，BUILD SUCCESS（保留 Vite 大 chunk 既有 warning）
- real-pre `pick_source_mapping` 与 Webhook 收件箱迁移已加契约测试：`colonel_buyin_id` schema 变更与数据回填分离，`docker-compose.real-pre.yml` 固定 `11~17` 初始化挂载顺序
- 前端 OrderList.vue 已集成 `talentId`/`merchantId` 筛选输入框、`timeField` 切换控件与 API / 导出传参
- 前端 data/index.vue 已集成 `timeField` 切换控件（默认 `createTime`，可切 `settleTime`），并直接使用 `/dashboard/metrics.trend7d` 展示订单数 / GMV 昨日对比和近 7 日 ECharts 真实趋势图
- 定向可见浏览器回归脚本：`runtime/qa/data-gap4-visible.cjs`
- 一键入口：`scripts/run-data-gap4-visible.ps1`
- 最新报告：`out/e2e-data-gap4-visible-20260506150815/report.md`（`3/3` 通过）
- [ ] 达人单条 enrich 刷新能力是否需要前台入口，仍待产品与联调阶段确认
- [ ] 客户端找达人、合作管理、推广效果仍待单独拆案
- [ ] 招商业绩 / 商家沉淀仍未形成独立前台闭环，当前仍以基础字段与服务层沉淀为主

## 五、当前风险

### 1. 真实联调风险

- Token 缓存、刷新链路、授权主体确认、活动列表联调接口、活动列表业务接口、活动详情、活动商品联调接口、活动商品业务刷新、商品素材状态、精选联盟 SKU raw probe、转链主接口、转链 fallback 负向探针、多结算订单查询首轮取证、订单主同步真实入库与订单展示 smoke 都已完成；其中活动商品业务口已支持 `refresh=true` 显式刷新并回写快照，SKU raw probe 已通过精选联盟 `/buyin/productSkus/v2` 返回真实结构，转链已补齐 `buyin.instPickSourceConvert -> data.product_url` 的真实字段兼容，fallback-1 当前主体不匹配、fallback-2 上游已下线，订单主同步已确认 `buyin.instituteOrderColonel` 时间参数必须为字符串并完成真实入库，真实订单已确认有达人字段但无渠道 `pick_source / pick_extra`，订单重复同步 jsonb 更新已修复，数据平台 `create_time / settle_time` 默认与切换口径已收口。店铺商品与店铺订单接口已从联盟测进度中剔除，剩余主风险已转为渠道归因字段来源、Webhook 真实事件与限流分支
- 限流、空数据、错误码分支尚未全量验证

### 2. 环境认知风险

- `real-pre` 与未来“真实 SDK pre”仍可能被误读为同一环境
- 少量归档文档仍保留历史过渡表述

### 3. 数据与噪音风险

- P0 测试数据仍可继续收口
- 本地第三方噪音日志仍可能干扰排障判断

## 六、下一步建议

1. 保持 `test` / `real-pre` 双轨验收口径稳定，继续保留 `local-mock` 历史脚本与报告作为回滚参考
2. `P3-6` 真实订单归因样本：继续沿用 `runtime/qa/out/real-pre-attribution-evidence-20260517-104656` 与 `scripts/qa/watch-real-pre-pending-evidence.ps1` 组合复跑，直到补到至少 `1` 条真实订单，并校验 `mapping.created_at <= order.create_time`、`attribution_status` 正确，且 Dashboard 明确包含或解释不包含原因；当前首轮结果仍是 `SYNC_OK_NO_SAMPLE`
3. `P3-7` Dashboard 数据库口径对账：保留 `scripts/qa/run-real-pre-dashboard-reconcile.ps1` 作为只读复跑入口，继续确认订单池增长后 `/api/dashboard/summary` 与 PostgreSQL `10` 个核心指标仍逐项一致
4. `P3-8` 异常分支证据补齐：继续沿用 `scripts/qa/run-real-pre-exception-audit.ps1` 作为只读 / dry-run 复跑入口，并用 `scripts/qa/watch-real-pre-pending-evidence.ps1` 统一观察 `EXC-004/005/006`；优先等待 live unsafe 样本，再在单独确认后补“重复转链幂等、SKU 为空或异常但主流程不中断”的 real-pre 证据
5. 在 `P3-6` / `P3-8` 补齐前，不扩到生产写操作、批量导出、批量发货或达人爬虫大规模采集
6. 继续收敛主干文档，只把证据材料留在 `docs/archive/`

## 七、一句话总结

> 当前项目已经具备稳定的 TEST/mock 基线和可复验的 real-pre 真实联调环境；截至 2026-05-17，real-pre 准入、P3 单样本业务闭环、P3-5 全角色账号业务流程验收、P3-7 Dashboard 数据库口径对账与浏览器可视化全业务剧本回归均已通过，系统已具备 real-pre 业务可用性。当前还不能宣称可生产发布，下一步只收口两件事：真实订单归因样本与异常分支证据补齐；Dashboard 数据库口径对账保留只读复跑。
