# 代码审查报告

**分支**: `codex/talent-platform-gap-closure-phase1`
**审查范围**: 97 个主源文件 + 40 个测试文件（最近 5 次提交）
**审查时间**: 2026-05-05

---

## 总览

| 严重级别 | 数量 | 状态 |
|----------|------|------|
| CRITICAL | 0 | — |
| HIGH | 13 | 合并前应修复 |
| MEDIUM | 19 | 建议修复 |
| LOW | 10 | 可选优化 |

---

## HIGH 问题（合并前应修复）

### 性能 / 内存

| # | 文件 | 问题 | 建议 |
|---|------|------|------|
| H1 | `OrderController.java` | `getStats()` 将全量数据加载到内存再做 `stream().filter().count()` 聚合，数据量大时 OOM | 改用 SQL `COUNT(*)` + `GROUP BY` |
| H2 | `OrderAttributionController.java` | `getSummary()` 同样将全量订单加载到内存做聚合 | 同上，改用 SQL 聚合 |
| H3 | `TalentService.java:535` | `loadOrdersInBatches` 无界加载 30 天内全部订单到内存 | 分批查询或加 LIMIT |
| H4 | `SampleController.java` | `getSampleBoard()` 无分页查询全部样本 | 添加 `Pageable` 参数 |
| H5 | `SampleController.java:toVO()` | N+1 查询：每行调用 `productMapper.selectById()` + 3 个用户映射查询，100 行 = 400+ SQL | 批量预加载，使用 `IN` 查询 |

### 安全

| # | 文件 | 问题 | 建议 |
|---|------|------|------|
| H6 | `RedisProbeController.java` | `/ops/redis-probe` 暴露 Redis host/port/db 等基础设施信息 | 只返回健康状态，移除拓扑信息 |

### 架构

| # | 文件 | 问题 | 建议 |
|---|------|------|------|
| H7 | `SampleController.java` | 5 个 Mapper 直接注入 Controller，数据访问逻辑应在 Service 层 | 提取 `SampleBoardService` |
| H8 | `ProductService.java` | 2191 行，远超 800 行规范，违反单一职责 | 按领域拆分为 PromotionLinkService、AuditWorkflowService 等 |

### 数据完整性

| # | 文件 | 问题 | 建议 |
|---|------|------|------|
| H9 | `OrderSyncService.java:222` | `catch (Exception e)` 吞掉所有异常（含 NPE、数据库约束），静默跳过失败订单 | 区分可恢复/不可恢复错误，超过阈值报警 |
| H10 | `OrderSyncService.java:244` | `Long.parseLong(merchantId.replaceAll("\\D", ""))` 当 merchantId 无数字时抛 `NumberFormatException`，整条订单丢失 | 增加空字符串保护 |
| H11 | `OrderSyncService.java:247` | `actualAmount` 始终用 `orderAmount` 覆盖，丢失真实结算金额 | 如网关提供 `actualAmount()`，优先使用 |
| H12 | `DashboardService.java:31` | `selectMaps(...).get(0)` 在空结果时抛 `IndexOutOfBoundsException` | 改为 `.stream().findFirst().orElse(Map.of())` |
| H13 | `SysUserRoleMapper.java:15` | `deleteByUserIdPhysical` 执行硬 DELETE，系统使用软删除时破坏审计轨迹 | 改为软删除或增加 `deleted = 0` 条件 |

---

## MEDIUM 问题（建议修复）

| # | 文件 | 问题 | 建议 |
|---|------|------|------|
| M1 | `TalentService.java:286` | Redis 锁释放不在 `finally` 块，异常时锁残留 10s | 移入 try-finally |
| M2 | `ProductBizStatusService.java:104` | `ensureAllowed` switch 无 default 分支，新增枚举值时静默返回 false | 添加 default 抛 `IllegalStateException` |
| M3 | `ProductBizStatusService.java:62` | 状态机无乐观锁，并发请求可导致状态不一致 | 添加 `@Version` 字段 |
| M4 | `PickSourceMappingService.java` | 3 个重载方法都加了 `@Transactional`，内层参与外层事务即可 | 只保留最外层的 `@Transactional` |
| M5 | `OrderQueryService.java:27` | 只读方法缺少 `@Transactional(readOnly = true)` | 添加注解 |
| M6 | `DashboardService.java:25` | 5 个独立聚合查询可合并为条件聚合 | 使用 `COUNT(CASE WHEN ...)` |
| M7 | `OrderSyncJob.java:35` | `@Scheduled` 任务吞掉所有异常无报警 | 添加失败计数 + 阈值告警 |
| M8 | `OrderSyncService.java:45` | 本地锁回退在多实例部署下失效，各实例独立加锁导致重复同步 | 文档化单实例限制或强制 Redis |
| M9 | `DouyinWebhookController.java` | 签名验证失败仍返回成功 | 返回 401/403 |
| M10 | `OrderController.java:syncOrders()` | 无时间范围上限校验，可一次性同步多年数据 | 限制最大范围（如 90 天） |
| M11 | `OrderController.java` | 多个端点直接返回 JPA 实体 `IPage<ColonelsettlementOrder>` | 映射为 Response DTO |
| M12 | `TalentQueryService.java:75` | 分页循环在 Java 内存中做过滤（poolStatus/ownerKeyword 等） | 尽可能将过滤推到 SQL WHERE |
| M13 | `RealDouyin*Gateway.java` | 多个 Real 网关包含重复工具方法 `pick()`、`asMap()`、`asList()` 等 | 提取 `DouyinResponseParser` 工具类 |
| M14 | `RealDouyin*Gateway.java` | 外部 API 调用无 try-catch 包装 | 捕获异常转为 `BusinessException` |
| M15 | `TalentEnrichModeGuard.java` | Guard 检查 `talent.enrich.mode` 但网关检查 `douyin.test.enabled`，两个不同属性 | 统一配置属性名 |
| M16 | `SampleStatusLog.java:16` | ID 策略从 `ASSIGN_UUID` 改为 `INPUT`，Service 层未设 ID 会插入失败 | 验证所有插入代码显式设 ID |
| M17 | `SwaggerConfig.java` | Swagger 配置 JWT 认证，生产环境如可访问则暴露认证机制 | 生产环境禁用或限制访问 |
| M18 | `CrawlerTalentInfoService.java:50` | `searchTalents` 多次 Mapper 调用无 `@Transactional`，可能读到不一致数据 | 添加注解 |
| M19 | `OrderSyncService.java:330` | `resolveNextCursor`、`resolveHasMore`、`parseOptionalDateTime` 是死代码 | 删除 |

---

## LOW 问题（可选优化）

| # | 文件 | 问题 |
|---|------|------|
| L1 | `JwtAuthInterceptor.java:42` | Auth 错误消息格式不一致（一处详细一处泛化） |
| L2 | `ProductController.java` | `@Tag` 标注"已废弃"但方法未加 `@Deprecated` |
| L3 | `DouyinController.java` | 端点 summary 带 `[联调]` 前缀，发布前应清理 |
| L4 | `AttributionService.java:177` | `pickExtra.substring(length - 20)` 魔法数字应命名常量 |
| L5 | `CrawlerTalentInfoService.java:27` | 使用 `@RequiredArgsConstructor` 与其他 Service 手动构造器不一致 |
| L6 | `TalentService.java:95` | `getPublicPool` 加载全部活跃 claim 到内存再过滤 | 改用 NOT EXISTS 子查询 |
| L7 | `TestDouyinProductGateway.java:59` | 测试数据硬编码日期 "2026-04-XX"，会过期 | 改用相对当前时间的动态日期 |
| L8 | `ColonelsettlementOrder.java` | 使用 `@Data` 但包含冗余手写 getter/setter | 删除手写方法 |
| L9 | `SampleTalentQueryRequest.java` | `keyword`/`region` 字段无 `@Size` 验证 | 添加长度限制 |
| L10 | `ProductBizStatus.java:32` | `fromCode()` 未知编码抛 `IllegalArgumentException` | 根据使用场景决定是否返回默认值 |

---

## 正面发现

- `ColonelActivityController` 重构为网关模式，移除 ~350 行 mock 逻辑
- `TestController` 正确使用 `@Profile` + `@ConditionalOnProperty` 保护
- `TalentController` 现在返回 DTO 而非实体
- `ProductBizStatusServiceTest` 覆盖了状态机所有合法/非法流转路径（18 个测试）
- `PickSourceMappingServiceTest` 覆盖了 13-arg / 14-arg / 15-arg 重载链
- Swagger 注解在修改的 Controller 中统一添加

---

## 推荐修复优先级

1. **立即修复**（H9-H11）: `OrderSyncService` 异常吞掉 + NumberFormatException + actualAmount 覆盖 → 防止静默数据丢失
2. **合并前修复**（H1-H5）: 内存聚合 / N+1 / 无界查询 → 防止生产 OOM
3. **合并前修复**（H6, H13）: 基础设施信息泄露 + 硬删除 → 安全/数据完整性
4. **近期修复**（M1-M3）: Redis 锁、状态机乐观锁、switch default → 并发安全
