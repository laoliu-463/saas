# 05-接口与数据模型

更新时间：2026-04-25

## 一、系统内部 API

### 商品主链路

- `GET /colonel/activities/{activityId}/products`
- `GET /colonel/activities/{activityId}/products/{productId}`
- `PUT /colonel/activities/{activityId}/products/{productId}/audit-result`
- `PUT /colonel/activities/{activityId}/products/{productId}/bind-activity`
- `PUT /colonel/activities/{activityId}/products/{productId}/assignee`
- `POST /colonel/activities/{activityId}/products/{productId}/promotion-links`
- `POST /colonel/activities/{activityId}/products/{productId}/follow`
- `GET /colonel/activities/{activityId}/products/{productId}/operation-logs`

### 订单回流与归因 MVP

- `POST /orders/sync`
  - 手动触发订单同步与归因
- `GET /orders`
  - 订单分页查询
- `GET /orders/unattributed`
  - 未归因订单分页查询
- `GET /dashboard/summary`
  - 订单回流与归因摘要接口

## 二、Gateway 契约

当前已落地：

- `DouyinAuthGateway`
- `DouyinColonelActivityGateway`
- `DouyinProductGateway`
- `DouyinPromotionGateway`
- `DouyinOrderGateway`

原则：

- Controller 只调用系统内部 Service
- Service 只依赖 Gateway 契约
- Mock / Real 只在 Gateway 实现层区分

## 三、核心数据表

### 商品主链路

- `product_snapshot`
- `product_operation_state`
- `product_operation_log`
- `promotion_link`
- `talent_follow_record`

### 订单回流与归因

- `pick_source_mapping`
- `colonelsettlement_order`

## 四、关键字段口径

### 商品主状态

统一主状态来源：

- `product_operation_state.biz_status`

当前状态枚举：

- `PENDING_AUDIT`
- `APPROVED`
- `REJECTED`
- `BOUND`
- `ASSIGNED`
- `LINKED`
- `FOLLOWING`

### 商品操作日志

- `before_status`
- `after_status`
- `success`
- `error_message`

### 订单归因字段

当前订单表对齐字段：

- `order_id`
- `product_id`
- `colonel_activity_id`
- `pick_source`
- `channel_user_id`
- `channel_dept_id`
- `user_id`
- `dept_id`
- `talent_name`
- `order_amount`
- `settle_colonel_commission`
- `settle_time`
- `attribution_status`
- `attribution_remark`
- `extra_data`

### pick_source 映射字段

- `pick_source`
- `promotion_link_id`
- `short_id`
- `user_id`
- `dept_id`
- `product_id`
- `activity_id`
- `source_url`
- `converted_url`
- `valid_from`
- `valid_until`

## 五、当前 Mock 场景

### Mock 转链

- 生成 `shortId`
- 生成 `shortLink`
- 生成 `promoteLink`
- 写入 `pick_source_mapping`

### Mock 订单

- 有映射，可归因
- 无映射，未归因
- 无 `pick_source`，未归因

## 六、当前边界说明

当前 MVP 暂不处理：

- 完整财务结算
- 复杂提成规则
- 自动定时任务
- 敏感信息解密链路重构
- 复杂可视化图表

当前先证明一件事：

> 推广链接产生的订单，系统能够同步回来，并区分已归因与未归因，再向渠道和招商摘要统计输出。

