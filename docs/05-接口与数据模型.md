# 05-接口与数据模型

更新时间：2026-05-03

## 一、接口总览

所有前端接口统一通过 `/api` 前缀访问，前端请求基址为 `/api`。
认证方式统一为：

```http
Authorization: Bearer <token>
```

当前接口文档按以下分组整理：

| 分组 | 路径前缀 | 说明 |
|---|---|---|
| 认证中心 | `/auth` | 登录、Token 刷新、登出 |
| 订单管理 | `/orders` | 订单同步、列表、详情、统计、筛选项 |
| 订单回流与归因 | `/orders/order-attribution-*`、`/dashboard/order-attribution-summary` | 未归因排查与回流摘要 |
| 数据看板 | `/dashboard/summary` | 首页归因概览 |
| 数据平台 | `/data/*`、`/dashboard/metrics`、`/orders/exports` | 数据页专用接口 |
| 团长活动管理 | `/colonel/activities` | 团长活动列表与活动商品列表 |
| 活动商品主链路 | `/colonel/activities/{activityId}/products/*` | 商品主链路详情、绑定、分配、审核、转链、跟进、日志 |
| 活动管理 | `/activities` | 本地活动分页与真实活动详情补充 |
| 寄样管理 | `/samples` | 寄样申请、分页、候选达人、状态流转 |
| 达人CRM | `/talents` | 达人列表、公海私海、认领释放、补全 |
| 系统用户 | `/users` | 后台用户管理 |
| 系统角色 | `/roles` | 后台角色管理 |
| 测试工具 | `/test` | 测试环境专用数据初始化与链路推进 |
| 抖音联调 | `/douyin`、`/douyin/webhooks` | 真实 SDK 联调、Token 管理、Webhook 回调 |
| 商品管理（兼容） | `/products`、`/products/picks` | 共享商品库与旧版选品候选兼容接口 |

## 二、统一返回格式

成功响应统一包装为：

```json
{
  "code": 200,
  "msg": "success",
  "data": {},
  "timestamp": 1713628800000
}
```

分页响应统一表现为：

```json
{
  "code": 200,
  "msg": "success",
  "data": {
    "records": [],
    "total": 100,
    "page": 1,
    "size": 20
  },
  "timestamp": 1713628800000
}
```

## 三、命名与使用口径

### 1. 路径规则

- 资源名优先使用复数：`/orders`、`/samples`、`/talents`、`/users`、`/roles`
- 主链路路径体现层级关系：`/colonel/activities/{activityId}/products/{productId}`
- 子动作后缀统一使用 kebab-case：`audit-result`、`promotion-links`、`manual-fill`

### 2. 分页参数

当前存在两种命名：

- 业务接口部分仍使用 `page` + `pageSize`
- 业务接口部分已经使用 `page` + `size`

当前项目决定是：

- **暂不强行一次性改名**
- 已在 Swagger 中明确标注命名差异
- 后续是否统一为 `page` + `size`，以 `docs/archive/audits/接口整改-决策备忘录.md` 结论为准

### 3. 测试、联调、废弃接口展示规则

- `/test/**`：统一归类到 `测试工具`，接口名称前缀为 `[测试]`
- `/douyin/**`、`/douyin/webhooks/**`：统一归类到 `抖音联调`，接口名称前缀为 `[联调]`
- `/products/**`：统一归类到 `商品管理（兼容）`，其中 `/products` 为共享商品库，`/products/picks` 为旧版选品候选兼容入口
- `/test/**` 仅允许出现在 `local-mock`、`test` 或当前回归型 `real-pre` 环境，不作为未来纯真实 SDK 环境的默认能力

### 4. 枚举说明规则

- 已有代码常量、枚举或映射函数定义的，按代码说明
- 代码中没有明确定义的，Swagger 中统一写：
  - `待确认：取值含义请联系产品`

本文档同样遵守该规则，不臆造未确认枚举值。

## 四、核心接口清单

### 0. 商品管理（兼容）

```http
GET /api/products
GET /api/products/picks
POST /api/colonel/activities/{activityId}/products/{productId}/library-entry
```

说明：

- `GET /api/products`：查询已从选品库沉淀到共享商品库的商品，支持 `page`、`size`、`keyword`
- `GET /api/products/picks`：查询旧版本地选品候选列表，主要用于 Token 缺失时的降级展示
- `POST /api/colonel/activities/{activityId}/products/{productId}/library-entry`：将选品结果加入共享商品库，入库后对全员可见

### 1. 认证中心

```http
POST /api/auth/login
POST /api/auth/refresh
POST /api/auth/logout
```

登录请求：

```json
{
  "username": "admin",
  "password": "admin123"
}
```

登录响应（`LoginResponse`）：

| 字段 | 类型 | 说明 |
|---|---|---|
| `token` | String | Access Token（有效期 2 小时） |
| `refreshToken` | String | Refresh Token（有效期 7 天） |
| `expiresIn` | Long | Access Token 有效期秒数（7200） |
| `refreshExpiresIn` | Long | Refresh Token 有效期秒数（604800） |
| `accessTokenExpiresIn` | Long | Access Token 有效期秒数（与 `expiresIn` 一致） |
| `userId` | Long | 用户 ID |
| `username` | String | 用户名 |

Token 刷新请求：

```json
{
  "refreshToken": "eyJhbGciOi..."
}
```

登出请求：

```json
{
  "accessToken": "eyJhbGciOi...",
  "refreshToken": "eyJhbGciOi..."
}
```

说明：

- Access Token 有效期 2 小时，携带 `type=access` claim
- Refresh Token 有效期 7 天，携带 `type=refresh` claim，使用 SHA-256 哈希存储于 Redis
- 登出后 Access Token 和 Refresh Token 同时加入 Redis 黑名单，立即失效
- 黑名单 key 格式：`auth:blacklist:{tokenHash}`，过期后自动清理

#### DataScope 数据权限

系统支持三级数据权限过滤（`@DataScope` 注解 + `DataScopeAspect`）：

| 枚举值 | 说明 |
|---|---|
| `PERSONAL(1)` | 仅查看本人数据 |
| `DEPT(2)` | 查看本组数据 |
| `ALL(3)` | 查看全部数据 |

### 2. 订单管理

```http
POST /api/orders/sync
GET  /api/orders
GET  /api/orders/unattributed
GET  /api/orders/{orderId}
GET  /api/orders/stats
GET  /api/orders/filter-options
```

订单字段口径补充（2026-05-21 审计）：

- `colonelsettlement_order.user_id`：本地归因后的业绩 / 数据范围归属用户，不是触发同步接口的 JWT 用户，也不是抖店上游用户字段。
- `channel_user_id`：渠道归属人展示字段；当前归因成功时通常与 `user_id` 同值。
- `dept_id` / `channel_dept_id`：随归因负责人写入，用于部门级 DataScope 与渠道归属展示。
- `colonel_user_id`：招商 / 团长侧负责人字段，来自商品操作状态，不参与个人 DataScope 的主过滤。
- `colonel_buyin_id` / `second_colonel_buyin_id`：抖店原生团长字段，当前归因优先级高于 `pick_source` / `pick_extra`；匹配 `pick_source_mapping.source_type=NATIVE`。
- 同一 `order_id` 从主订单同步或按订单号结算补拉进入时，均更新同一条 `colonelsettlement_order` 记录，不按来源拆成两条订单。

典型请求：

```json
{
  "startTime": "2026-04-01 00:00:00",
  "endTime": "2026-04-28 23:59:59"
}
```

### 3. 订单回流与归因

```http
GET /api/orders/order-attribution-unattributed
GET /api/dashboard/order-attribution-summary
```

### 4. 数据看板与数据平台

```http
GET  /api/dashboard/summary
GET  /api/dashboard/metrics
GET  /api/data/orders
GET  /api/orders/exports
GET  /api/operations/exclusive-talents
GET  /api/operations/exclusive-merchants
GET  /api/activities/exports
```

时间字段口径：

- `/api/dashboard/metrics`、`/api/data/orders`、`/api/orders/exports` 均支持 `timeField=createTime|settleTime`
- 默认 `createTime`，用于覆盖真实订单已同步但尚未结算的运营看板和明细核对场景
- 需要按结算收益复核时显式传 `timeField=settleTime`
- `/api/data/orders` 订单明细返回 `createTime` 与 `settleTime`；前端切换 `timeField=settleTime` 时应展示结算时间，未结算订单显示未结算，不使用前端伪造时间
- `/api/orders/exports` 导出列包含 `创建时间` 与 `结算时间`，筛选时间字段仍由 `timeField` 决定

说明：

- 当前项目不再提供订单手机号解密功能
- 订单页和订单详情仅展示上游已返回的联系电话字段；上游未返回时按“未返回”展示

### 5. 团长活动与商品主链路

```http
GET /api/colonel/activities
GET /api/colonel/activities/{activityId}/products

GET  /api/colonel/activities/{activityId}/products/{productId}
PUT  /api/colonel/activities/{activityId}/products/{productId}/bind-activity
PUT  /api/colonel/activities/{activityId}/products/{productId}/audit-assignee
PUT  /api/colonel/activities/{activityId}/products/{productId}/assignee
PUT  /api/colonel/activities/{activityId}/products/{productId}/audit-result
POST /api/colonel/activities/{activityId}/products/{productId}/promotion-links
POST /api/colonel/activities/{activityId}/products/{productId}/follow
GET  /api/colonel/activities/{activityId}/products/{productId}/operation-logs
```

分配审核人请求示例（待审核商品，不改变 `bizStatus`）：

```json
{
  "assigneeId": "22222222-2222-2222-2222-222222222222"
}
```

审核请求示例：

```json
{
  "approved": true,
  "reason": "素材完整，允许推进"
}
```

转链请求示例：

```json
{
  "scene": "PRODUCT_LIBRARY",
  "talentId": "test_talent_001"
}
```

### 6. 活动管理

```http
GET /api/activities
GET /api/activities/{activityId}/douyin-detail
```

- `GET /api/activities`：读取本地 `colonel_activity` 表中的活动分页；空库时自动播种稳定演示活动，不再依赖仅随进程存在的内存 Mock
- `GET /api/activities/{activityId}/douyin-detail`：查询指定团长活动的抖音真实详情

### 7. 寄样管理

```http
POST   /api/samples
GET    /api/samples
GET    /api/samples/talent-candidates
GET    /api/samples/{id}
PUT    /api/samples/{id}/status
DELETE /api/samples/{id}
```

寄样状态流转示例：

```json
{
  "action": "SHIPPING",
  "trackingNo": "SF1234567890",
  "reason": "顺丰发出"
}
```

### 8. 达人 CRM

```http
GET    /api/talents
GET    /api/talents/{id}
POST   /api/talents
PUT    /api/talents/{id}
DELETE /api/talents/{id}
GET    /api/talents/pools/public
GET    /api/talents/pools/private
POST   /api/talents/{id}/claims
POST   /api/talents/{id}/release
POST   /api/talents/{id}/refresh
POST   /api/talents/refresh/weekly
PUT    /api/talents/{id}/manual-fill
GET    /api/talents/{id}/enrich-task/latest
GET    /api/talents/{id}/exclusive-status
```

说明：

- `/{id}/claims`：认领动作，生成认领记录
- `/{id}/release`：释放动作，解除锁定状态
- 当前按动作语义保留现状，不强行改成资源集合命名

### 9. 系统管理

```http
GET    /api/users
GET    /api/users/{id}
POST   /api/users
PUT    /api/users/{id}
DELETE /api/users/{id}
PUT    /api/users/{id}/password
PUT    /api/users/{id}/roles

GET    /api/roles
GET    /api/roles/{id}
GET    /api/roles/enabled
POST   /api/roles
PUT    /api/roles/{id}
DELETE /api/roles/{id}
```

### 10. 测试工具

```http
POST /api/test/seed
POST /api/test/reset
POST /api/test/orders/sync
POST /api/test/orders/generate-attributed
POST /api/test/orders/generate-no-pick-source
POST /api/test/orders/generate-missing-mapping
POST /api/test/logistics/ship/{sampleRequestId}
POST /api/test/logistics/sign/{sampleRequestId}
```

### 11. 抖音联调

```http
GET  /api/douyin/activities
GET  /api/douyin/activities/{activityId}
GET  /api/douyin/activity-products
GET  /api/douyin/activities/{activityId}/products
POST /api/douyin/product-material-status-checks
GET  /api/douyin/order-settlements
POST /api/douyin/activity-product-cancellations
POST /api/douyin/activity-product-cancellations/raw
POST /api/douyin/activities
PUT  /api/douyin/activities/{activityId}
GET  /api/douyin/tokens
POST /api/douyin/token-refreshes
POST /api/douyin/tokens

POST /api/douyin/webhooks/colonel-open-events
POST /api/douyin/webhook-events/replay
```

说明：

- `/douyin` 接口主要用于真实 SDK 联调和 Token 维护
- `GET /api/douyin/order-settlements` 对应上游 `buyin.colonelMultiSettlementOrders`，要求当前 `access_token` 对应主体已完成团长授权；内部接口支持 `startTime + endTime` 或 `orderIds` 查询，也兼容上游风格的 `order_ids`
- `GET /api/douyin/order-settlements` 仅用于团长分次结算订单补充取样，不替代订单主同步所依赖的 `buyin.instituteOrderColonel`
- 商品 SKU 真实 Gateway 当前按精选联盟物料口径调用 `/buyin/productSkus/v2`；代码侧方法名使用已实测通过的 `buyin.productSkus.v2`，不再使用店铺商品侧 `sku.detail` 作为团长主链路前置
- `/douyin/webhooks/*` 默认纳入 `抖音联调` 分组，公开给抖店回调；当前会做签名校验、幂等落库和本地消费状态推进
- `POST /api/douyin/webhook-events/replay`：管理员 / 招商组长可重放 `RECEIVED / FAILED` 状态的 Webhook 事件，返回 `scanned / consumed / failed`
- webhook 是否在 OpenAPI 中继续公开展示，后续可按发布策略单独控制

## 五、前端封装对应关系

| 前端文件 | 主要路径前缀 | 说明 |
|---|---|---|
| `frontend/src/api/auth.ts` | `/auth` | 登录、Token 刷新、登出 |
| `frontend/src/api/order.ts` | `/orders` | 订单主链路 |
| `frontend/src/api/dashboard.ts` | `/dashboard` | 首页看板 |
| `frontend/src/api/data.ts` | `/data`、`/dashboard`、`/orders/*` | 数据页 |
| `frontend/src/api/activity.ts` | `/activities`、`/colonel/activities` | 活动 |
| `frontend/src/api/activityProduct.ts` | `/colonel/activities/*/products` | 商品主链路 |
| `frontend/src/api/product.ts` | `/products` | 旧版废弃商品接口 |
| `frontend/src/api/promotionLink.ts` | 商品转链相关 | 辅助封装 |
| `frontend/src/api/sample.ts` | `/samples` | 寄样 |
| `frontend/src/api/talent.ts` | `/talents` | 达人 CRM |
| `frontend/src/api/sys.ts` | `/users`、`/roles` | 系统管理 |
| `frontend/src/api/douyin.ts` | `/douyin` | 抖音联调 |
| `frontend/src/api/test.ts` | `/test` | 测试工具 |
| `frontend/src/api/operationLog.ts` | 商品日志 | 主链路辅助接口 |

## 六、废弃接口

当前废弃但仍保留兼容的接口如下：

| 路径前缀 | 状态 | 替代接口 |
|---|---|---|
| `/products/*` | 已废弃 | `/colonel/activities/{activityId}/products/*` |

Swagger / APIFOX 展示要求：

- 分组显示为 `商品管理（已废弃）`
- 接口名称统一带 `[已废弃]`
- 描述中必须写明替代路径

## 七、文档与接口一致性说明

本文档以当前代码与已整改的 Swagger 注解为准，重点保证：

1. 中文分组清晰
2. 测试 / 联调 / 废弃接口可单独过滤
3. 不臆造未确认枚举值
4. 分页命名差异如实记录
5. 文档统一使用 UTF-8 编码保存

如后续分页参数统一、达人路径命名、Webhook 暴露策略发生决策变更，请同步更新：

- `docs/archive/audits/接口整改-决策备忘录.md`
- 本文档
