# 测试前检查

[V1 必做] 跑测试前先确认目标环境：`test/mock` 与 `real-pre` 的脚本不能混用。

[V1 必做] test/mock 基线使用 `npm run e2e:v1-p0`。

[V1 必做] real-pre 使用 `npm run e2e:real-pre:p0:preflight` 后再执行 `npm run e2e:real-pre:p0`。

[V1 必做] 后端回归优先 `cd backend && mvn test`；前端构建优先 `cd frontend && npm run build`。

[V1 简化] 文档结构重构至少检查：目标文件存在、旧文档已归档、V1/V2 标记存在、FastAPI/Celery 未进入当前事实。

