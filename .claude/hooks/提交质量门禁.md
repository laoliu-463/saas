# 提交质量门禁

[V1 必做] 提交前必须有变更说明、影响范围、验证结果和未验证风险。

[V1 必做] 不能出现“应该没问题”“已完成但未验证”类结论。

[V1 必做] 不能把 `BLOCKED`、`PENDING`、`PASS_NEEDS_CLEANUP` 写成 `PASS`。

[V1 必做] 不能把历史 V2.2、FastAPI、Celery、local-mock 旧口径写成当前事实。

[V1 简化] 文档变更提交前至少运行结构和关键词检查；业务代码未改时不要求 Maven / npm 全量测试。

