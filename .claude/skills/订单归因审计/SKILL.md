# 订单归因审计

description: 检查订单事实、`pick_source` 映射、默认归因、业绩归属和 Dashboard 口径。

## 守卫

[V1 必做] 订单域只存事实，不应用独家覆盖。

[V1 必做] 最终归属、提成、冲正和双轨金额由业绩域负责。

[V1 必做] 缺真实 `pick_source` 新单样本时，不能写真实归因全场景通过。

## 步骤

[V1 必做] 查 `docs/流程/订单归因链路.md`。

[V1 必做] 查 `docs/对接/转链与pick_source归因.md` 与 `docs/对接/订单同步.md`。

[V1 必做] 查 `docs/验收/V1-P0验收清单.md` 的 P0-4、P0-5、P0-7。

[V1 必做] 输出订单事实、映射事实、业绩事实、看板事实四段证据链。

