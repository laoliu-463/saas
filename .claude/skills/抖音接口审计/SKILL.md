# 抖音接口审计

description: 审计抖音授权、Token、活动、活动商品、转链、订单、物流、达人数据接口。

## 步骤

[V1 必做] 先确认环境是 `real-pre`，且 test flags 为 false。

[V1 必做] 读取 `docs/08-第三方对接总览.md` 和 `docs/对接/*.md`。

[V1 必做] 区分真实通过、权限阻塞、样本等待、历史归档。

[V1 必做] 不把 raw probe 可达写成业务闭环通过。

