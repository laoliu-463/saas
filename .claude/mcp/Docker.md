# Docker MCP / CLI

[V1 必做] test 使用 `docker-compose.test.yml`；real-pre 使用 `docker-compose.real-pre.yml`。

[V1 必做] 不同时运行 test 与 real-pre 两套 backend/frontend。

[V1 必做] real-pre 端口：前端 3001、后端 8081、PostgreSQL 5433、Redis 6380。

