# MCP 安全边界

[V1 必做] 真实凭据只允许存在于未跟踪 env、CI/CD secrets 或服务器环境变量。

[V1 必做] real-pre 不执行真实删除，cleanup 默认 PlanOnly；执行清理需人工审核。

[V1 必做] 任何输出不得包含真实 `access_token`、`refresh_token`、签名、client secret、手机号完整明文。

[V1 必做] 真实上游失败要保留错误码和脱敏上下文，不要为了通过测试吞错误。

