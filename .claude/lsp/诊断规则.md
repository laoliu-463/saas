# 诊断规则

[V1 必做] 优先级：编译错误 > 运行错误 > 数据不一致 > 文档冲突 > 格式问题。

[V1 必做] 发现冲突时先记录证据，不直接修正业务规则。

[V1 必做] 文档检查关键词：`FastAPI`、`Celery`、`local-mock`、`V2.2`、`真实通过`、`已完成`、`PASS`。

[V1 必做] 若关键词出现在当前事实段落，必须确认是否应改为历史归档或 V2 预留。

