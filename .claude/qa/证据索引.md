# 证据索引

[V1 必做] 所有验收证据最终归档到 `runtime/qa/out/` 或 `playwright-report/` / `test-results/playwright/`。

[V1 必做] real-pre 统一验收证据以 `runtime/qa/out/real-pre-p0-*/` 为准。

[V1 必做] 文档静态检查证据可使用文件结构清单、关键词扫描结果、链接检查结果。

[V1 必做] 2026-05-26 阶段性证据已登记到 `docs/验收/验收证据索引.md`：后端 `mvn -f backend test` 1591 tests PASS，前端 build PASS，前端 52 files / 213 tests PASS，real-pre preflight 证据路径为 `runtime/qa/out/real-pre-preflight-20260526-113128/`，状态 `PENDING_PICK_SOURCE`。

[V1 必做] 2026-05-26 real-pre `PENDING_PICK_SOURCE` 已完成只读 SQL 复核：`valid_mapping_count=0`，当前 8 条 `pick_source_mapping` 均为 `colonel_native_*` 种子，`promotion_link=0`，缺少带 `activity_id/product_id/promotion_link_id/converted_url` 的真实渠道转链样本。
