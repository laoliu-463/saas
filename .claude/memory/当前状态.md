# 当前状态

[V1 必做] 当前阶段为 V1 主链路收口、real-pre 补证、P2 工程治理并行。

[V1 必做] 旧 V2.2 完整方案已归档为背景，当前文档以 `docs/README.md` 和 00~10 主干为入口。

[V1 必做] 本次任务只允许修改文档和 `.claude` 工程化结构，不改业务代码。

[V1 必做] 2026-05-26 real-pre 预检状态：前端 3001、后端 8081、`REAL-PRE` 环境守卫、`APP_TEST_ENABLED=false`、`DOUYIN_TEST_ENABLED=false`、真实抖音 Token、数据库 schema readiness 均已通过；统一预检停在 `PENDING_PICK_SOURCE`。

[V1 必做] 当前 real-pre 数据库只有 8 条 `colonel_native_*` 原生团长种子 `pick_source_mapping`，`product`、`colonel_activity`、`colonel_activity_product`、`promotion_link` 当前为 0 条；没有满足预检条件的可复用真实转链映射。
