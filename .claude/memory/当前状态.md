# 当前状态

[V2 必做] 当前阶段为 V2 演进中：V1 基础链路持续维护，DDD 到 100%、real-pre 补证、Harness 工程治理并行推进。

[V2 必做] 当前交付范围主源是 `docs/01-V2交付范围与边界.md`；旧 V1 / V2.2 文档只按 ADR-010 和 ADR-002 作为历史或兼容背景处理。

[V2 必做] 当前 DDD 用户域切片已抽出多个 policy / application service / port，最新 evidence 仍为 PARTIAL，不能宣称 DDD 已完成。

[V2 必做] real-pre 仍必须保持 `APP_TEST_ENABLED=false`、`DOUYIN_TEST_ENABLED=false`、`DOUYIN_REAL_UPSTREAM_MODE=live`。

[V2 必做] 本地项目记忆迁移入口是 `docs/12-项目记忆索引.md`；ChatGPT Project 链接内容当前因登录阻塞未读取，本地仓库 / Downloads / Documents 未发现导出文件，待用户导出或明确授权使用 Chrome 登录态后补迁移。

[V2 必做] Claude 本地项目记忆已迁移到 `docs/13-Claude项目记忆迁移索引.md`；其中 ADR/CONTEXT 真实路径和 git guardrails hook 口径可作为当前操作规则，旧 DDD / 死代码清单只作历史入口。

[V2 必做] Codex 本地会话索引已迁移到 `docs/14-Codex会话记忆迁移索引.md`；8 个高相关会话已完成首轮脱敏摘要抽取，原始会话全文仍不能直接当作当前事实，进入领域文档前必须复核当前代码 / docs / evidence。
