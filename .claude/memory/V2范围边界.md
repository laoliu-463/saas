# V2 范围边界

[V2 必做] V2 主线：独家机制、履约自动化、复杂财务与结算、历史重算、数据导出与分析，以及原 V1 核心链路持续维护。

[技术基线] 继续保持 Spring Boot 模块化单体、PostgreSQL、Redis、Docker Compose，不因 V2 演进自动引入微服务、K8s 或复杂 MQ。

[V2 必做] real-pre 验收不得使用 mock 数据冒充真实闭环；缺 Token、缺权限、缺真实样本时只能标记 BLOCKED / PENDING。

[历史归档] 旧 V1 “不做”限制不再自动约束 V2 新能力，但旧文档仍可作为历史背景和冲突证据。
