# 已知风险

[V2 必做] real-pre 真实 `pick_source` 新单样本仍需持续观察，不能把历史空 `pick_source` 订单当作渠道归因闭环证据。

[V2 必做] 商品 backfill / dry-run 禁止使用同步 HTTP 长请求执行大任务，必须走异步 job、进度查询和 dry-run 边界。

[V2 必做] `buyin.colonelMultiSettlementOrders` 非空样本依赖真实上游窗口；没有样本只能标记 BLOCKED / PENDING。

[V2 必做] Dashboard 不能等同完整业绩结算域，分析模块只读汇总表，不重算业绩归属。

[V2 必做] 真实达人外部采集受风控/API 权限影响，不能用 mock 证明 real-pre 通过。

[V2 必做] 真实物流 API 尚未完成 sandbox/real-pre 闭环；V2 已解禁外部履约自动化，但必须按真实接口证据推进。

[V2 必做] 订单查询 `DataScope.DEPT` 与渠道部门可见性的历史风险仍需用最新 DDD 用户域切片复核，不能仅靠前端筛选兜底认定根因修复。

[V2 必做] 当前工作区存在大量既有未提交 DDD / harness 变更，本轮记忆迁移不得回退这些变更，提交和推送需另行人工确认。
