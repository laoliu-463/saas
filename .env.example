# SAAS local environment example.
# Copy to a local .env file only when a tool explicitly needs root-level env values.
# Do not put real credentials, tokens, or production passwords in tracked files.

SAAS_ENV_SLUG=test
SPRING_PROFILES_ACTIVE=test
APP_TEST_ENABLED=true
DOUYIN_TEST_ENABLED=true
DOUYIN_REAL_UPSTREAM_MODE=contract

BACKEND_HOST_PORT=8080
FRONTEND_HOST_PORT=3000

# Use .env.real-pre.example as the only template for real-pre controlled deployment.
