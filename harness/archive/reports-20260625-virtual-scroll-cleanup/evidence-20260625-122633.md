# Evidence Report - 2026-06-25 12:26:33

## Scope
- Env: local `real-pre`
- Branch: `feature/product-manage-fallback-fix-20260623`
- Commit: `69c445ed`
- Task: product library slow display and DB query performance diagnosis
- Code change: not applied
- Remote deploy: not executed

## Evidence Summary
- `GET /api/products?page=1&size=20&sortBy=default`: `5010ms`, `total=15013`.
- `GET /api/products?page=1&size=500&sortBy=default`: `5131ms`, `total=15013`.
- `GET /api/products?page=1&size=20&keyword=鞋&sortBy=default`: `5534ms`, `total=126`.
- `GET /api/products?page=1&size=20&activityId=3864871&sortBy=default`: `62ms`, `total=80`.
- Backend logs showed `/api/products` slow entries at `6300ms`, `5569ms`, `4952ms`, and `5525ms`.
- SQL single-query probes were much faster: base page about `30ms`, base count about `17ms`, join page about `24ms`, keyword join about `231ms`.

## Code Evidence
- `ProductLibrary.vue` uses `PAGE_SIZE=500`.
- `ProductService.getSelectedLibraryPage` calls `collectSelectedLibraryProducts`, then Java sorting, then `subList`.
- `collectSelectedLibraryProducts` loops through product-operation-state batches of `200`, loads snapshots and metadata per batch, and filters in Java.

## Database Evidence
- `product_operation_state`: `95912` active rows.
- selected-library rows: `18122`.
- displayable service base rows: `15013`.
- `product_snapshot`: `95040` active rows, `77590` distinct products.
- Existing indexes include `idx_pos_displaying_library`, `idx_pos_product_display_status`, `uk_pos_one_displaying_per_product`, and product snapshot activity/product indexes.

## Conclusion
- Result: `PARTIAL`.
- The performance issue is not caused by one slow PostgreSQL query alone.
- The strongest cause is backend full-candidate collection and Java-side filtering/sorting before pagination, amplified by frontend first-page size `500`.
- Recommended next step is a DB-paginated product-library listing query plus targeted partial/trigram indexes.
