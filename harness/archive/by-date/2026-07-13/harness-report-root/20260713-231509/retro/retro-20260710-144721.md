# Retro Summary - DDD E-7

## Scope

- 任务：E-7 转链事件幂等键透传。
- 环境：real-pre。
- 代码提交：`eefec3d1`。
- 远端部署：未执行。

## What changed

- `ProductService` 将带幂等键入口的 `idempotencyKey` 透传到转链领域事件。
- 首次转链和 relink 两条成功路径均写入事件与 outbox payload。
- 保留无幂等键旧入口和原有 public 9 参数内部方法兼容行为。
- 增加幂等 scope、完成回写、失败释放和事件 identity 合同断言。

## Verification

- 定向测试：12 tests / 0 failures / 0 errors / 0 skipped。
- 后端 compile：PASS。
- 后端 package：PASS。
- 前端 build：PASS。
- Docker rebuild/recreate/restart：PASS。
- backend/frontend health：PASS。
- real-pre business preflight：BLOCKED_AUTH；`hasAccessToken=false`，未执行真实业务流。

## Lessons and follow-up

- dirty 工作区中并发 Maven 进程会竞争 `target` 产物，导致无关类缺失的级联编译错误；已停止残留 Maven 进程并串行重建后复验通过。
- 后续迁移批次禁止并发复用同一 backend `target`，所有 Maven 验证串行执行。
- 真实 access token、订单/转链/寄样样本和跨进程 outbox 证据仍需外部条件，不得用本地测试替代。

## Harness upgrade

本次无需新增 Harness 规则；沿用现有 agent-do、DDD acceptance、evidence 和 retro 流程。

## 本次全量 DDD 复核

- DDD acceptance：PASS；`mvn compile` PASS，redline guard 4 tests PASS，宽口径 DDD/架构/契约测试 366 tests / 0 failures / 0 errors / 1 skipped。
- 两个 legacy whitelist 有效项均为 0；三套 runtime profile 的 DDD 开关默认均为 true。
- 矩阵当前为 `DONE 134 / PARTIAL 36 / TODO 0 / BLOCKED 8`。旧 `service/*` 作为兼容实现保留，拆分延期，不作为本轮迁移失败。
- real-pre preflight 仍为 `BLOCKED_AUTH`：缺 access token 和真实订单/寄样样本；不得将真实归因、寄样自动完成、业绩和 Dashboard E2E 写成 PASS。
- 复核期间归档超限 evidence，Harness 50/50/200 检查恢复 PASS；未提交、未推送、未远端部署。
