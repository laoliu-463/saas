# Harness Retro Summary

## 1. Harness execution

- Time: 2026-07-13 21:52:58 +08:00
- Environment: real-pre
- Scope: docs
- Branch: codex/ddd-user-role-application
- Commit: 2ca318a2
- Used agent-do.ps1: True
- Deploy remote requested: False

## 2. Repeated probing

- 设计文件因共享工作区中的并发提交/重置被重复创建和校验；最终等待 Git index 稳定后执行固定 Harness。
- 未重复运行构建、容器重启或业务 E2E；本任务是 `Scope=docs`。

## 3. Script failures

- `agent-do.ps1` 成功，结论为 `PARTIAL`。
- 随后的 `check-harness-limits.ps1` 返回 `FAIL`：`harness/reports` 直接文件数 71，且 13 份其他任务 evidence 超过 200 行。

## 4. Verification sufficiency

Scope=docs. If Scope=docs, build/restart/business E2E may be intentionally skipped and must not be treated as PASS for code.

## 5. Harness issues exposed

- `git-push-safe.ps1` 会提交整个 dirty 工作区，多个任务共享同一工作区时存在误带其他任务文件的风险。
- `agent-do.ps1` 在 push 后才生成 retro，导致 retro 默认不会进入同一次提交。
- 报告目录已有并发归档/恢复改动，本任务不能安全代替其所有者完成全局清理。

## 6. Files to upgrade

- `harness/scripts/commands/git-push-safe.ps1`：后续评估增加显式 path allowlist。
- `harness/scripts/commands/agent-do.ps1`：后续评估把 retro 生成移动到 commit/push 之前。
- 本次不修改 Harness 行为，避免扩大 RBAC 设计任务范围。

## 7. Need new script

No. 优先扩展现有提交脚本的文件范围参数。

## 8. Need new Eval

No. 本次缺口是报告治理与并发提交边界，不是业务 Eval 缺失。

## 9. Need update AGENTS.md

No. 当前规则已明确要求保护共享 dirty 工作区。

## 10. Must fix before next task

- 报告清理任务应在无并发写入时，将 `harness/reports` 降至不超过 50 个直接文件。
- 13 份超 200 行 evidence 需要由其所属任务拆分、截断或归档。
- RBAC 当前仅完成设计采纳，不得把 `PARTIAL` 写成实现完成。
