# Retro Summary - DDD migration slice

## Scope

- 环境：real-pre；分支：`codex/ddd-user-role-application`；远端部署：未执行。
- 本批提交：`c2e805f0 refactor: route product and sample reads through ddd ports`；推送因 GitHub 443 网络不可达，保持未推送状态。
- 商品库：Controller → application service → query port → Legacy adapter；新增应用层游标 DTO。
- 寄样详情：统一查询应用服务 → detail query port → Legacy adapter；列表、看板、导出和命令未混入。

## Verification

- 商品域：352 tests / 0 failures / 0 errors / 0 skipped。
- 宽口径 DDD/架构/合同：368 tests / 0 failures / 0 errors / 1 skipped。
- 后端 package：PASS；前端 `npm ci` + build：PASS。
- Docker real-pre 重建/重启：PASS；backend/frontend health：PASS。
- business preflight：`BLOCKED_AUTH`；admin login、real-pre guard、数据库 schema readiness PASS，抖音 access token readiness BLOCKED，未执行真实上游业务流。
- `check-harness-limits.ps1`：归档原始超长报告并补齐本 retro 后应恢复 PASS；归档目录：`harness/archive/by-date/20260710/reports-limit-cleanup/222300/`。

## Findings

- 当前迁移是安全的边界切片，不是商品库复杂筛选和寄样详情物化的最终迁移；两处 Legacy adapter 仍是下一阶段替换点。
- 曾复现 `target/classes` 类文件污染导致无关类编译错误；串行 Maven 复跑后恢复，后续禁止并发复用同一 `backend/target`。
- 6 个 npm 依赖漏洞仍是独立治理事项，不由本批扩大范围。

## Harness upgrade

本轮无需新增 Harness 能力；已沿用固定 agent-do、证据/retro、DDD 回归和 Harness 50/50/200 门禁。

## Next

- 继续完成 P-10：先确认商品库 `self/group/all` 语义，再替换 Legacy 查询编排并保持行为等价。
- 继续完成寄样详情端口的真实读模型实现；不同时改状态机、订单事件或业绩规则。
- 授权和真实样本可用后再执行 real-pre 商品/订单/寄样闭环。
