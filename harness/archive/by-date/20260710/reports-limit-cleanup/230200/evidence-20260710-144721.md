# Evidence Report - DDD E-7

## Metadata

- Time: 2026-07-10 14:47:22 +08:00
- Environment: real-pre
- Scope: full
- Branch: `codex/ddd-user-role-application`
- Commit: `eefec3d1`
- Worktree: dirty; existing user changes preserved
- Remote deploy: false

## Code and test evidence

- E-7 change: `ProductService` propagates caller `idempotencyKey` into the promotion-link domain event for both first-link and relink success paths.
- Compatibility: no-key legacy entry still delegates with `null`; original public 9-argument internal method remains.
- Contract tests: `DddProductPromotionLinkGeneratedEventContractTest` 5 tests, 0 failures.
- Publisher tests: `ProductDomainEventPublisherTest` 6 tests, 0 failures.
- Integration test: `PromotionLinkCopyIntegrationTest` 1 test, 0 failures.
- Combined targeted result: 12 tests / 0 failures / 0 errors / 0 skipped.
- Backend compile: `mvn -q -DskipTests compile` PASS.

## Build and runtime evidence

- Backend package: `mvn -f backend/pom.xml -DskipTests package` PASS.
- Frontend install/build: `npm --prefix frontend ci` and `npm --prefix frontend run build` PASS.
- Docker rebuild/recreate/restart: PASS.
- Backend container: healthy.
- Frontend container: healthy.
- Backend health: HTTP 200, `{"status":"UP"}`.
- Frontend health: HTTP 200 from `/healthz`.

## Business validation

- Preflight report: `runtime/qa/out/real-pre-preflight-20260710-144718/report.md`.
- Result: `BLOCKED_AUTH`.
- Evidence: `hasAccessToken=false`, `hasRefreshToken=true`, `canRunBusinessFlows=false`.
- Real order, promotion-link, sample-completion and downstream reconciliation were not executed.

## Conclusion

- Code slice: PASS based on targeted tests and compile.
- Runtime gates: PASS for build, restart and health.
- Full task conclusion: PARTIAL because real-pre business validation is blocked by external Douyin authorization.

## Residual risk

- Real upstream sample, scheduler, cross-process outbox idempotency and E2E remain unverified.
- The raw agent-do evidence is archived at `harness/archive/by-date/20260710/evidence-20260710-144721-agent-do.md`.
- Do not treat this report as proof of the complete real-pre business loop.
