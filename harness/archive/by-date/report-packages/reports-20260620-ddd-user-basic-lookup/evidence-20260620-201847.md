# Evidence Report

## Metadata

- Time: 2026-06-20 20:18:47 +08:00
- Environment: real-pre
- Scope: backend
- Selected gate: Gate 3 - Domain Change
- Branch: feature/ddd/DDD-VERIFY-001
- Commit: efc6f0cd
- Worktree: dirty
- Deploy remote: false

## Change Summary

- DDD slice: user domain / `LegacyUserDomainFacade` department option boundary.
- Added `DepartmentOptionLookup` port with `DepartmentEntry` read model.
- Added `SysDepartmentOptionLookupAdapter` as the transitional persistence adapter over `SysDeptMapper`.
- `LegacyUserDomainFacade` now consumes `DepartmentOptionLookup` for department option reads.
- Extended `LegacyUserDomainFacadeBoundaryTest` to prevent direct `SysDeptService` / `SysDept` dependencies from returning.

## Evidence

- code-review-graph first: `get_minimal_context` timed out; `list_graph_stats` succeeded with 12210 nodes / 140900 edges and file summary for `LegacyUserDomainFacade`.
- RED: `LegacyUserDomainFacadeBoundaryTest` failed because the facade did not contain `DepartmentOptionLookup` and still imported `SysDeptService` / `SysDept`.
- GREEN: `LegacyUserDomainFacadeBoundaryTest` 2/0/0.
- Focused integration regression: `LegacyUserDomainFacadeTest` 8/0/0.
- User-domain regression: selected 22-test-class command 216/0/0.
- Boundary scan: `LegacyUserDomainFacade.java` had no `SysDeptService` / `SysDept` match after the slice.
- Residual dependency scan: `OrgStructureApplicationService` and `SysDeptApplicationService` still import mapper/entity classes for later slices.
- PASS: `mvn -f .\backend\pom.xml -DskipTests test-compile`.
- PASS: `mvn -f .\backend\pom.xml -DskipTests package`.
- PASS: backend restart on local real-pre via `restart-compose.ps1 -Env real-pre -Scope backend`.
- PASS: backend health returned `{"status":"UP"}` via `verify-local.ps1 -Env real-pre -Scope backend`.
- PASS: code-review-graph incremental update with minimal postprocess.
- PASS: `harness/scripts/check-harness-limits.ps1`.
- Report capacity rotation: archived `evidence/retro-20260620-131201` to `harness/archive/by-date/report-packages/reports-20260620-ddd-user-basic-lookup/`, then moved `evidence/retro-20260620-141400` into `harness/reports/2026-06-20/`.

## Runtime State

- Docker: `backend-real-pre` rebuilt and healthy on `127.0.0.1:8081`.
- Business validation: backend user-domain tests covered facade, department service, current user, org, data scope, master data, controller and service paths.
- Git worktree: dirty before and after this slice.
- Commit/push: not executed due broad pre-existing dirty worktree changes.

## Conclusion

PARTIAL

## Residual Risk

- `LegacyUserDomainFacade` no longer directly reads `SysUserMapper` / `SysUser` or `SysDeptService` / `SysDept`.
- `domain/user/application/OrgStructureApplicationService` and `SysDeptApplicationService` still contain mapper/entity dependencies and need separate classification.
- No frontend build or authenticated real-pre E2E was executed because this slice only changed backend user-domain facade read boundaries.
- No remote deployment was requested or executed.
