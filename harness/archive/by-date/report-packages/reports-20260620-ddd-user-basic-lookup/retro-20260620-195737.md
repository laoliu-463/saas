# Retro Summary

## Task

- U-7 user-domain DDD slice: remove direct `SysUserMapper` / `SysUser` dependency from `LegacyUserDomainFacade`.

## What Changed

- Introduced `UserBasicLookup` as a user-domain port for basic user reads.
- Introduced `SysUserBasicLookupAdapter` as the transitional adapter over `SysUserMapper`.
- Updated `LegacyUserDomainFacade` to use `BasicUser` read models for user name and user option responses.
- Added a lightweight boundary test so this rule does not depend on Testcontainers startup.

## What Worked

- Moving the source assertion out of the integration test made the RED/GREEN signal precise and fast.
- Existing facade integration tests still passed after the dependency moved behind a port.
- The broader user-domain regression suite passed with the new boundary test included.

## Follow-Up

- Continue U-7 by splitting `LegacyUserDomainFacade` department reads away from `SysDeptService` / `SysDept`.
- Continue classifying `domain/user/application/*` mapper/entity dependencies before deciding whether to port-adapt or service-wrap each one.
- Investigate whether test-profile scheduled jobs should be disabled in Testcontainers-based user-domain tests to avoid Redis log noise.

## Harness Upgrade

- No new harness rule or script was needed.
