# Retro Summary

## Task

- U-7 user-domain DDD slice: remove direct department legacy service/entity dependency from `LegacyUserDomainFacade`.

## What Changed

- Introduced `DepartmentOptionLookup` as a user-domain port for active department option reads.
- Introduced `SysDepartmentOptionLookupAdapter` as the transitional adapter over `SysDeptMapper`.
- Updated `LegacyUserDomainFacade` to map `DepartmentEntry` read models into public `DepartmentOption` DTOs.
- Extended the lightweight boundary test to block direct `SysDeptService` / `SysDept` imports.

## What Worked

- RED was precise and failed on the intended facade dependency boundary.
- Existing facade integration tests still passed after the dependency moved behind a port.
- Adding `SysDeptServiceTest` to the user-domain regression set kept the department-service behavior covered.

## Follow-Up

- Continue U-7 by classifying `domain/user/application/OrgStructureApplicationService` and `SysDeptApplicationService` mapper/entity dependencies.
- Decide per dependency whether it belongs in an infrastructure adapter, a policy read model, or a retained application-service transition layer.
- Keep frontend/E2E validation out of backend-only slices unless a touched API contract changes.

## Harness Upgrade

- No new harness rule or script was needed.
