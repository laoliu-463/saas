# Evidence Report

## Metadata

- Time: 2026-06-20 19:57:37 +08:00
- Environment: real-pre
- Scope: backend
- Selected gate: Gate 3 - Domain Change
- Branch: feature/ddd/DDD-VERIFY-001
- Commit: 4447989c
- Worktree: dirty
- Deploy remote: false

## Change Summary

- DDD slice: user domain / `LegacyUserDomainFacade` user basic lookup boundary.
- Added `UserBasicLookup` port with `BasicUser` read model.
- Added `SysUserBasicLookupAdapter` as the transitional persistence adapter.
- `LegacyUserDomainFacade` now consumes `UserBasicLookup` for user names and user option reads.
- Added `LegacyUserDomainFacadeBoundaryTest` to prevent reintroducing direct `SysUserMapper` / `SysUser` dependencies.

## Evidence

- code-review-graph first: prior graph stats showed 12210 nodes and 140903 edges; focused semantic search had no useful hit, so source tracing fell back to `rg`.
- RED hygiene: a first source assertion placed in `LegacyUserDomainFacadeTest` timed out because it started Testcontainers; the assertion was moved to a lightweight boundary test.
- RED: `LegacyUserDomainFacadeBoundaryTest` failed before implementation because the facade still used `SysUserMapper` / `SysUser`.
- GREEN: `LegacyUserDomainFacadeBoundaryTest` 1/0/0.
- Focused integration regression: `LegacyUserDomainFacadeTest` 8/0/0.
- User-domain regression: selected 21-test-class command 203/0/0.
- Boundary scan: `LegacyUserDomainFacade.java` had no `SysUserMapper` / `entity.SysUser` match after the slice.
- Residual dependency scan: facade still imports `SysDept`; `OrgStructureApplicationService` and `SysDeptApplicationService` still import mapper/entity classes for later slices.
- PASS: `mvn -f .\backend\pom.xml -DskipTests test-compile`.
- PASS: `mvn -f .\backend\pom.xml -DskipTests package`.
- PASS: backend restart on local real-pre via `restart-compose.ps1 -Env real-pre -Scope backend`.
- PASS: backend health returned `{"status":"UP"}` via `verify-local.ps1 -Env real-pre -Scope backend`.
- PASS: code-review-graph incremental update with minimal postprocess.
- PASS: `harness/scripts/check-harness-limits.ps1`.
- Report capacity rotation: archived `evidence/retro-20260620-125715` to `harness/archive/by-date/report-packages/reports-20260620-ddd-user-basic-lookup/`, then moved `evidence/retro-20260620-135130` into `harness/reports/2026-06-20/`.

## Runtime State

- Docker: `backend-real-pre` rebuilt and healthy on `127.0.0.1:8081`.
- Business validation: backend user-domain tests covered facade, current user, org, data scope, master data, controller and service paths.
- Test noise: user-domain integration run logged a test-profile scheduled task Redis connection failure, but the suite still completed 203/0/0.
- Git worktree: dirty before and after this slice.
- Commit/push: not executed due broad pre-existing dirty worktree changes.

## Conclusion

PARTIAL

## Residual Risk

- `LegacyUserDomainFacade` no longer directly reads `SysUserMapper` / `SysUser`, but still delegates department reads through `SysDeptService` and `SysDept`.
- `domain/user/application/OrgStructureApplicationService` and `SysDeptApplicationService` still contain mapper/entity dependencies and need separate classification.
- No frontend build or authenticated real-pre E2E was executed because this slice only changed backend user-domain read boundaries.
- No remote deployment was requested or executed.
