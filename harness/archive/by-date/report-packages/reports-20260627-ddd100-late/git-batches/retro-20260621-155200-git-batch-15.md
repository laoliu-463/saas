# Retro 2026-06-21 15:52 GIT-BATCH-15

## What changed

- ProductService now consumes user display labels, channel codes, and ownership references instead of full `UserOptionResponse`.
- SampleApplicationService now consumes display labels and ownership references instead of full user DTOs.
- ExclusiveMerchantApplicationService now maps recruiter dept ids from ownership references.
- Added architecture tests to lock these three boundaries.

## Evidence notes

- Targeted tests passed: 103.
- Full backend tests passed: 2457 run, 3 skipped.
- Package, Docker image build, compose backend restart, backend health, and auth guard probe passed.
- The first full-test run caught a stale reflection signature in `DddConfig003ConfigRoutingTest`; the final staged set includes only the signature hunk, not unrelated config-domain dirty edits.

## Process note

- Staged-only validation remains required because the main workspace contains parallel uncommitted DDD slices.
- The current Docker runtime was rebuilt from the staged temp worktree and now runs image `sha256:ad725f6f20291a3a9f1349c69527267efc9711dafcd94979df5f859e3bf30229`.

## Harness

- Archived GIT-BATCH-10 reports before adding this report pair.
- Harness upgrade: not needed.
