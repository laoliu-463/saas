# Retro 2026-06-21 18:42 GIT-BATCH-17

## What changed

- Created `SampleActionPermissionPolicy` as the sample-domain module for sample action permission rules.
- `SampleApplicationService` now delegates sample permission decisions to that policy.
- User policy remains responsible for role-code parsing; sample policy owns sample action semantics.

## Evidence notes

- Targeted tests passed: 82.
- Full backend tests passed: 2464 run, 3 skipped.
- Package, Docker image build, compose backend restart, backend health, and auth guard probe passed.
- Earlier incomplete Maven logs were explicitly rejected as evidence and replaced with complete reruns.

## Process note

- Staged-only validation remains required because the main workspace contains parallel uncommitted DDD slices.
- For long Maven runs on Windows, `cmd /c "mvn ... > log 2>&1"` produced a complete full-test log where PowerShell native redirection previously produced an incomplete artifact.
- Direct code-review-graph MCP calls failed with `Transport closed`, but the commit hook completed the local graph update successfully.

## Harness

- Archived GIT-BATCH-12 reports before adding this report pair.
- Harness upgrade: not needed.
