# Retro 2026-06-21 16:15 GIT-BATCH-16

## What changed

- Moved role-code normalization and matching from sample service/port into `CurrentUserPermissionPolicy`.
- Sample code now expresses role-based sample rules through a user-domain policy instead of parsing role strings locally.
- Added a boundary test so local `hasAnyRole` and raw role string parsing do not come back.

## Evidence notes

- Targeted tests passed: 90.
- Full backend tests passed: 2460 run, 3 skipped.
- Package, Docker image build, compose backend restart, backend health, and auth guard probe passed.
- Spring context test was included to validate constructor injection for the new policy dependency.

## Process note

- Staged-only validation remains required because the main workspace contains parallel uncommitted DDD slices.
- Archive directory pressure now requires moving older ignored zip files into subdirectories before adding more git-batch reports.

## Harness

- Archived GIT-BATCH-11 reports before adding this report pair.
- Harness upgrade: not needed.
