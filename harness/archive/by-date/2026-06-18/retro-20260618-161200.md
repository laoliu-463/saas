# Retro Summary - 2026-06-18 Real-Pre Deployment

- Time: 2026-06-18 16:12:00 +08:00
- Env: remote real-pre
- Branch: feature/ddd/DDD-VERIFY-001
- Commit: 96700af8

## What Changed

- Remote deployment moved from blocked to successful after real-pre env URL correction.
- Frontend stopped calling admin-only Douyin institution info for non-admin activity users.
- Roles E2E stopped using synchronous `GET /products?refresh=true` for activity product sync and now uses the async sync endpoint plus polling.
- Server Playwright runtime was completed by installing `ffmpeg-1011`.

## Evidence

- Remote deploy evidence: `/opt/saas/runtime/qa/out/deploy-real-pre-20260618-async-roles-production-validation`.
- Roles evidence preflight: `/opt/saas/app/runtime/qa/out/real-pre-preflight-20260618-160231`.
- P0 evidence: `/opt/saas/app/runtime/qa/out/real-pre-p0-20260618-160815`.

## Lessons

- Real-pre browser validation must provision Chromium, headless shell and ffmpeg together; missing media runtime creates false FAIL before business code runs.
- Harness E2E should not use known long synchronous refresh endpoints when production UI already has async sync semantics.
- Public HTTP reachability is not equivalent to production readiness; HTTPS/domain checks need their own gate.

## Follow-Up

- Add a documented Playwright runtime preflight for browser, headless shell and ffmpeg.
- Convert remaining real-pre E2E specs that still use `refresh=true` to async sync or DB-view polling where appropriate.
- Rerun P0 after real order attribution, sample-audit product and performance samples exist.
- Decide whether production readiness requires HTTPS domain before marking the goal complete.
