# Evidence Report

## Metadata

- Time: 2026-06-19 17:12:50 +08:00
- Environment: real-pre
- Scope: backend
- Branch: feature/ddd/DDD-VERIFY-001
- Commit: 8ac11580
- Worktree: dirty
- Deploy remote: false

## Conclusion

PARTIAL

本轮代码、测试、构建、容器重启、健康检查和 real-pre 只读业务验证均通过。
整体仍标记 PARTIAL，因为 Harness 结构检查存在既有违规：`harness/engineering/` 不是白名单一级目录。

## Files

- `backend/src/main/java/com/colonel/saas/auth/service/SysUserService.java`
- `backend/src/test/java/com/colonel/saas/auth/service/SysUserServiceTest.java`
- `harness/reports/evidence-20260619-171249.md`
- `harness/archive/reports-20260619-ddd-sysuser-query/evidence-20260619-165826.md`
- `harness/archive/reports-20260619-ddd-sysuser-query/retro-20260619-165837.md`

## Commands

```text
mvn "-Dtest=com.colonel.saas.auth.service.SysUserServiceTest" test
mvn "-Dtest=com.colonel.saas.auth.service.SysUserServiceTest,com.colonel.saas.controller.SysUserControllerTest,com.colonel.saas.controller.CurrentUserControllerTest,com.colonel.saas.service.UserMasterDataServiceTest" test
mvn test
mvn -f backend/pom.xml -DskipTests package
mvn -q dependency:build-classpath "-Dmdep.outputFile=target\verify-classpath.txt" "-DincludeScope=test"
jshell --class-path target/classes + target/verify-classpath.txt
git diff --check -- backend/src/main/java/com/colonel/saas/auth/service/SysUserService.java backend/src/test/java/com/colonel/saas/auth/service/SysUserServiceTest.java
rg -n "exists\(.*\?|role_id = \?|role_code = \?" backend/src/main/java/com/colonel/saas/auth/service/SysUserService.java backend/src/test/java/com/colonel/saas/auth/service/SysUserServiceTest.java
powershell -NoProfile -ExecutionPolicy Bypass -File .\harness\scripts\commands\restart-compose.ps1 -Env real-pre -Scope backend
powershell -NoProfile -ExecutionPolicy Bypass -File .\harness\scripts\commands\verify-local.ps1 -Env real-pre -Scope backend
npm run e2e:real-pre:p0:preflight
powershell -NoProfile -ExecutionPolicy Bypass -File .\harness\scripts\check-harness-limits.ps1
```

## Test Result

- RED 1: `SysUserServiceTest` 原始失败 20 tests, 4 failures, 3 errors。
- RED 2: 调整测试为参数绑定断言后，剩余 3 errors 均为 MyBatis-Plus `sql not contains: "{0}"`。
- GREEN: `SysUserServiceTest` 20 tests, 0 failures, 0 errors.
- Related: user-domain 相关 48 tests, 0 failures, 0 errors.
- Full backend: `mvn test` 2296 tests, 0 failures, 0 errors, 3 skipped.
- Build: `mvn -f backend/pom.xml -DskipTests package` BUILD SUCCESS.

## Behavior Probe

- JShell 反射调用 `buildUserPageWrapper(... roleId ...)`：
  `ROLE_ID_SQL` 使用 `#{ew.paramNameValuePairs...}`，`ROLE_ID_PARAM=true`。
- JShell 反射调用 `buildUserPageWrapper(... roleCode=biz_staff ...)`：
  `ROLE_CODE_SQL` 使用 `#{ew.paramNameValuePairs...}`，`ROLE_CODE_PARAM=true`。

## Docker And Health

- `restart-compose.ps1 -Env real-pre -Scope backend`: PASS，backend 镜像重建，backend 容器重启。
- Compose 状态：backend/frontend/postgres/redis real-pre 均 healthy。
- `verify-local.ps1 -Env real-pre -Scope backend`: PASS，`/api/system/health` 返回 `{"status":"UP"}`。

## Business Validation

- `npm run e2e:real-pre:p0:preflight`: PASS。
- Preflight summary: `runtime/qa/out/real-pre-preflight-20260619-171125/summary.json`，status PASS，canRunBusinessFlows true。
- `GET /api/users?page=1&size=5&roleCode=biz_staff`: HTTP 200，`total=1`，`records=1`。
- `GET /api/users/current`: HTTP 200，`username=admin`，`dataScope=all`。

## Static Checks

- Scoped `git diff --check`: PASS.
- `rg` 检查 `exists(... ?)` / `role_id = ?` / `role_code = ?`: no matches.
- `check-harness-limits.ps1`: FAIL，违规项为既有 `harness/engineering` 白名单外一级目录。

## Risk

- 当前工作区有大量既有脏改；本轮只声明 `SysUserService` 查询筛选和对应测试的验证结论。
- `SysUserService.java` 相对 HEAD 还包含其他既有修改（例如审计日志 ID 处理、删除后缓存失效），本轮未回退、未评判这些修改。
- 全量测试日志中出现计划任务尝试连接 `redis` 的异常栈，但 Surefire 结果为 0 errors；建议后续单独收口测试 profile 下定时任务禁用或 Redis host 配置。
- 未执行远端部署，符合本轮“不提交/不推送/不远端部署”限制。

## Next

- 先处理 `harness/engineering/` 白名单冲突，避免后续每轮报告被结构检查标记 PARTIAL。
- 下一轮 DDD 小切片可继续收口用户域 `SysUserService` 中剩余 dataScope 组装逻辑，或把用户分页筛选提成 `UserPageQueryPolicy`。
