# Retro Summary - 20260620-123319

## What Changed

- Added an architecture test that fails when `SampleController` imports persistence Mappers.
- Removed stale import-only persistence dependencies from `SampleController`.

## What Worked

- The new assertion first failed on the existing source, proving it catches the boundary issue.
- Cleanup was behavior-neutral: constructor and endpoint delegation stayed unchanged.
- `SampleControllerTest` plus the architecture test passed together.

## Evidence

- RED: architecture test failed on Mapper imports.
- GREEN: architecture test passed, 3/0/0.
- Regression: `SampleControllerTest,DddSample001ApplicationServiceTest`, 78/0/0.
- Build and runtime: `test-compile`, `package`, backend restart and health check all passed.

## Risks Kept Open

- Other controllers/services may still have cross-domain user persistence dependencies.
- Full backend test suite and real-pre E2E were not executed.
- Worktree remains dirty; no commit or push was made.

## Harness Upgrade

No harness script change required. The useful upgrade is in code: an architecture regression test now protects the sample HTTP boundary.
