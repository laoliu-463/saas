# Git Intake 2026-07-10 12:50

## Metadata

- Branch: `codex/ddd-user-role-application`
- HEAD: `6598d62387b532c69f16d307820ef773488b9935`
- Tracking ref: `origin/codex/ddd-user-role-application`
- Tracking state: ahead 3 / behind 0（未 fetch，非远端实时证明）
- Staged: empty
- Fixed review point: `HEAD`

## Dirty Baseline

- Leaf dirty: 137
- Modified: 74
- Deleted: 8
- Untracked: 55
- Tracked diff: 82 files，2164 insertions / 1635 deletions
- `git diff --check`: PASS；3 个 CRLF/LF warning
- 所有 dirty 均视为受保护的前序工作；禁止 clean/reset/restore、覆盖写入和整体暂存。

## Dirty Classification

| Batch | Count | Classification | Task ownership |
| --- | ---: | --- | --- |
| backend DDD | 90 | `previous_partial` / `backend` / `sql_migration` | O/E、Y/A、T/S、P/C |
| API CLI / Apifox | 27 | `previous_partial` | Skill、MCP、OpenAPI 派生资产、回读证据 |
| cleanup | 8 | `cleanup_retire` | 已归档旧 report/retro 的工作区删除 |
| DDD docs/state/reports | 7 | `docs_state` / `report_only` | 矩阵、事件契约、DOMAIN_STATUS、daily evidence |
| G-4 frontend smoke | 3 | `previous_partial` / `frontend` | CSP 字体与角色 fallback |
| F-10 frontend guard | 1 | `previous_partial` / `frontend` | 前端业务规则边界静态测试 |
| frontend raw evidence | 1 | `report_only` | frontend agent-do 原始报告 |

- Path-level unknown: 0
- Author-level ownership cannot be proven from Git metadata；继续按潜在用户工作保护。

## Review Findings

### Standards

- 现有变更横跨多个主责领域，禁止作为单一 task/commit 提交。
- 34 个新增 contract 主要使用 `Files.readString + contains`；只可证明源码形状或证据索引，不能证明事务、SQL、权限、真实 API/DB 或 E2E。
- `latest-ddd-acceptance-report.md` 的 PASS 仅覆盖本地 acceptance；同时仍记录 unexpected dirty。

### Spec

- E-7：`ProductService` 两条成功路径恒向转链事件传 `idempotencyKey=null`，与事件 payload/幂等证据要求不一致。
- Y-4：`CommissionRule` 改为 `VersionedEntity` 后，update/delete 忽略 `updateById` 影响行数，并发冲突会被误报为成功。
- O-10/O-13：当前只证明订单失效状态与 `extraData` 映射的本地合同；不证明独立真实退款事实同步。
- Y-12/E-5：汇总刷新事件由 dashboard 订单增量路径发布，退款、重算和 backfill 覆盖边界尚未证明。

## Harness Risks

- `agent-do.ps1` 会无条件调用 `git-push-safe.ps1`；后者收集整个 dirty 工作区并批量暂存、推送，当前不可安全直接执行。
- `harness/reports` 当前 48 个直接文件，接近 50 上限。
- `harness/reports/evidence-20260710-120539.md` 超过 200 行，且生成时间晚于旧 limits PASS。
- 8 个删除 report/retro 均找到归档副本，但缺少本轮 cleanup manifest。
- GitHub Issues 远端只读核对因 API 网络超时未完成；本地镜像停在 2026-06-20。

## Decision

- Git Intake decision: `START_WITH_REGISTERED_DIRTY`
- Git terminal state: `PARTIAL_DIRTY_REMAINING`
- 当前只允许按单卡、单 Scope 小批继续；禁止整体 `git add`、自动 push 或远端部署。
- 下一允许任务：`LEDGER-RECONCILE-001`（docs-only），仅修正矩阵计数与明确的状态漂移。

## Planned Batch Order

1. 修正矩阵/状态台账和 cleanup manifest，恢复报告目录余量。
2. 单独修复 Y-4 optimistic-lock 冲突处理并补行为测试。
3. 单独修复 E-7 idempotency key 透传并补行为测试。
4. O/E、Y/A、T/S 后端 previous-partial 分组验证和提交。
5. SQL migration 独立验证与提交。
6. F-10、G-4、API CLI/Apifox 分别验证和提交。
7. reports/docs/cleanup 独立收口，运行 limits 与 Session Exit Gate。

## Prohibited Claims

- 当前不能声明 178 卡完成、业务闭环 PASS、真实退款闭环、real-pre P0 PASS、已提交或已推送。
- 8 张外部阻塞卡仍需有效抖音 access token 与真实转链/订单/寄样样本。
