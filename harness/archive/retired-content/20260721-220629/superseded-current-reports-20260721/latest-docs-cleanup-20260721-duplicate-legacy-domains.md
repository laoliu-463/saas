# 文档治理证据：归档冗余内容并压缩主入口

## 结论先说

本轮没有动代码，完成两件事：

1. 把有证据的重复、过期、孤立文档归档；
2. 把主入口改成“先结论、再动作、最后证据”的短版。

当前结果：`PARTIAL_DIRTY_REMAINING`。表达已收口，但仓库原有 dirty 状态和 Harness 历史债务仍未清零。

## 主文档压缩结果

| 文档 | 原长度 | 当前长度 | 处理结果 |
| --- | ---: | ---: | --- |
| `docs/02-V1业务流程与领域设计.md` | 282 行 | 128 行 | 只保留三条主链、领域职责、规则、状态机和验收提醒 |
| `docs/03-项目剩余事项与任务看板.md` | 648 行 | 98 行 | 只保留阻断项、已完成项、边界和执行顺序 |
| `docs/04-上线验收清单.md` | 538 行 | 85 行 | 只保留验收合同和 P0 状态，详细证据统一指向 05 |
| `docs/05-real-pre证据索引.md` | 928 行 | 82 行 | 只保留 P0 状态、证据入口、缺口和补证顺序 |
| `docs/06-技术架构与数据模型.md` | 241 行 | 148 行 | 只保留架构、边界、数据关系和接口主口径 |
| `docs/11-用户操作手册.md` | 462 行 | 208 行 | 改成角色、路径、状态表和故障排查 |
| `docs/README.md` / `docs/archive/README.md` | — | — | 增加“先结论、历史归档不作当前事实”规则 |

`02/03/04/05/06` 的完整旧版已保留到 `docs/archive/retired-content/`；用户手册原版仍可从 Git HEAD 历史恢复。

## 已归档内容

10 份 manifest 共记录 35 个 `archive` 项：

- 7 份逐字节重复的旧领域文档；
- 1 份无引用 SOP 草稿；
- 11 份无引用根报告；
- 1 份被统一证据报告替代的操作报告；
- 6 份无引用旧实施计划；
- 4 份无引用旧根文档；
- 5 份主文档压缩前原始版本。

清单位置：`harness/manifests/docs-cleanup-20260721-*.json`。

## 有意保留的内容

- `ddd-completion-evidence-matrix.md` 等矩阵：它们是数据，不是叙述性废话；删掉会损失验收关系。
- `harness/reports/current` 的业务、权限、订单、DDD、CI/CD 和部署报告：它们是证据，不因为长就删除。
- `docs/archive/2026-05-v1-doc-merge/`、旧 V2.2 文档和历史 records/runbooks/audits：仍有历史引用或事实价值。
- 7 月新任务和 dirty 报告：不改写，避免覆盖用户正在整理的证据。

## 统一写作规则

1. 第一屏先写一句话结论；
2. 每个问题只写“现象、判断、动作、验收”；
3. 一条事实只维护一个主源，其他地方只放链接；
4. 历史过程放 archive，不塞进当前入口；
5. `PASS`、`PARTIAL`、`BLOCKED`、`PENDING` 必须有证据路径；
6. 能用表格说清的，不写长段落；能用一句话说清的，不写三段。

## 验证

- 10 份 manifest：JSON 解析通过，共 35 个归档项。
- 5 份主文档原始版本：存在于 `docs/archive/retired-content/`。
- 主文档冲突标记：0。
- `git diff --check` 与 `git diff --cached --check`：PASS。
- `git ls-files -u`：空。
- 文档安全测试：FAIL；既有合同要求 `.env.real-pre` 不存在，但当前仓库存在该文件；本轮未读取、修改或提交其内容。
- Harness 限制检查：FAIL / `REPOSITORY_HEALTH=PARTIAL`；`harness/reports/current` 为 85 份，基线为 78，`DOMAIN_STATUS.md` 为 223 行，归档长文档被计为历史债务。
- 构建、Docker 重启、健康检查、业务验证：按 `Scope=docs` 跳过。

## 结论

`PARTIAL_DIRTY_REMAINING`：主入口已经从“长篇叙述”改成“短结论 + 明确动作 + 证据入口”；剩余长文档要么是矩阵，要么是历史证据，不属于可直接删除的冗余。仓库整体仍有既有 dirty 状态，未提交、未推送。

## Retro

本轮把“删掉长文”改成“保留证据、压缩入口”。后续只对新文档执行同一规则：先写结论，再写判断和动作，最后给证据；不再新增没有生命周期的长篇报告。
