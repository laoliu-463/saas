# Evidence Report: ORDER-P0-DUAL-SOURCE-SYNC

- **时间**: 2026-06-03 20:23 CST
- **环境**: local real-pre
- **分支**: feature/auth-system
- **HEAD**: 78bdf8fa (uncommitted changes)
- **工作区**: dirty — 本任务代码与前序报告/state 变更混合存在，未使用 `git add -A`
- **远端部署**: 未执行，等待用户明确授权

---

## 构建与测试

| 步骤 | 结果 |
|------|------|
| `mvn -f .\backend\pom.xml -Dtest=OrderSyncServiceTest,OrderSyncJobTest,OrderDualTrackAmountResolverTest,OrderSyncPersistenceServiceTest test` | PASS，37 tests / 0 failures |
| `mvn -f .\backend\pom.xml -Dtest=OrderApiTest,RealDouyinOrderGatewayTest,OrderControllerTest test` | PASS，43 tests / 0 failures |
| `mvn -f backend/pom.xml -DskipTests package` | PASS |
| `git diff --check` | PASS，仅提示 `harness/state/DOMAIN_STATUS.md` CRLF 将转 LF |

## Docker 与健康检查

| 项 | 结果 |
|------|------|
| `safety-check.ps1 -Env real-pre -Scope backend` | PASS |
| `restart-compose.ps1 -Env real-pre -Scope backend` | PASS，backend-real-pre 已重建并重启 |
| `verify-local.ps1 -Env real-pre -Scope backend` | PASS，`/api/system/health => {"status":"UP"}` |
| `npm run e2e:real-pre:p0:preflight` | PASS，输出目录 `runtime/qa/out/real-pre-preflight-20260603-201802` |

---

## 日志验收

```text
ORDER_SYNC_SETTLEMENT
api=buyin.colonelMultiSettlementOrders
mode=INCREMENTAL
fetched=0 inserted=0 updated=0

ORDER_SYNC_INSTITUTE
api=buyin.instituteOrderColonel
mode=INSTITUTE_RECENT
pages=1 fetched=100 inserted=100 updated=0
attributed=0 unattributed=100 noPickSource=0 noMapping=100 failed=0
```

结论：2704 继续执行且本轮无结算数据；6468 已作为事实订单源入库。

## 数据库验收

```sql
select count(*) from colonelsettlement_order;
-- 100
```

聚合结果：

| 指标 | 结果 |
|------|------|
| total | 100 |
| with_order_amount | 100 |
| with_pay_time | 100 |
| with_settle_amount | 0 |
| with_effective_fee | 0 |
| with_pick_source | 0 |
| attributed_channel | 0 |

字段抽样结论：

- `order_amount` 有值。
- `pay_time` 有值。
- `estimate_service_fee` / `estimate_tech_service_fee` 有值。
- `settle_amount` / `effective_service_fee` / `effective_tech_service_fee` 未由 6468 写入。
- `pick_source` 本轮上游样本未返回。
- `attribution_status=UNATTRIBUTED`，`attribution_remark=COLONEL_MAPPING_NOT_FOUND` 共 100 条。

## 页面/API 验收

管理员接口验证：

```text
GET /api/orders?page=1&size=5&attributionStatus=UNATTRIBUTED
HTTP 200, total=100, records=5

GET /api/orders/unattributed?page=1&size=5&unattributedReason=COLONEL_MAPPING_NOT_FOUND
HTTP 200, total=100, records=5
```

渠道正向可见性：PENDING。本轮 6468 样本没有 `pick_source`，且 `channel_user_id` 全为空，无法证明 mapping 命中后的渠道可见性。

---

## 结论

**PARTIAL**

本地 real-pre 已证明：

- 6468 `buyin.instituteOrderColonel` 已接入主订单同步。
- 订单表从 0 变为 100 条真实订单。
- 6468 只写事实/预估轨，未污染 2704 结算/有效轨。
- 管理员订单列表和未归因列表可见。

未完成项：

- 远端 real-pre 未部署/未验证。
- 本轮无 `pick_source` 样本，渠道 mapping 命中后的可见性仍待真实样本验证。
- 2704 本轮 `fetched=0`，结算轨 merge 仍需后续真实结算数据验证。
