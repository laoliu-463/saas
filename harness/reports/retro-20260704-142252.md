# Harness Retro Summary

## 1. Harness execution

- Time: 2026-07-04 14:22:52 +08:00
- Environment: real-pre
- Scope: full
- Branch: codex/ddd-user-role-application
- Commit: db9c6c9c
- Used agent-do.ps1: True
- Deploy remote requested: False

## 2. Repeated probing

Not collected automatically. Review command history manually if needed.

## 3. Script failures

- `agent-do.ps1 -Env real-pre -Scope full` exited non-zero because business preflight stopped at `BLOCKED_AUTH`.
- Build, Docker restart, backend health and frontend health passed before the preflight block.
- `latest-evidence-20260704.md` initially still contained prior OpenAPI evidence, so this round rebuilt it as DDD redline zero evidence.

## 4. Verification sufficiency

Scope=full. If Scope=docs, build/restart/business E2E may be intentionally skipped and must not be treated as PASS for code.

## 5. Harness issues exposed

- Evidence naming is still ambiguous: `agent-do.ps1` did not leave a task-specific timestamped DDD evidence file, while `latest-evidence-20260704.md` may already contain another task's report.
- For DDD closure, acceptance evidence must explicitly distinguish architecture PASS from real-pre business `BLOCKED_AUTH`.

## 6. Files to upgrade

- AGENTS.md: update only if execution rules changed.
- CURRENT_STATE.md: update if project state changed.
- TASK_ROUTING.md: update if routing gaps appeared.
- commands: update if script behavior failed or was ambiguous.
- evals: update if validation could not prove business result.
- skills: update if debugging workflow was incomplete.
- runbooks: update if humans/agents cannot execute directly.

## 7. Need new script

Not required for this round. Existing `check-ddd-acceptance.ps1` proved redline zero and dirty guards.

## 8. Need new Eval

Not required for this round. Business E2E remains externally blocked by Douyin authorization readiness.

## 9. Need update AGENTS.md

No rule change required. Current AGENTS rules already require evidence, restart, health check and honest BLOCKED reporting.

## 10. Must fix before next task

- Do not claim completion without evidence.
- Keep HARNESS_CHANGELOG.md current when Harness files change.
