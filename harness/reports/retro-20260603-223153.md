# Retro Summary - P0-SAMPLE-001-REMOTE-VERIFY

## 任务

- 任务编号：P0-SAMPLE-001-REMOTE-VERIFY
- 时间：2026-06-03 22:31
- 环境：remote real-pre
- 报告：`harness/reports/p0-sample-001-remote-verify-20260603-221004.md`

## 做得有效的部分

- 在远端存在数据前置缺口时，先用只读 SQL 固化事实，再区分核心链路验证与指定账号/分配前置验证。
- 避开会执行迁移 SQL 的远端部署脚本，改用只构建 backend/frontend 的最小部署路径，满足“所有 SQL 只读”和“不清库 / 不重建 volume”约束。
- 报告中明确写 `PARTIAL`，没有把 `channel_staff` 无私海达人、商品 `assignee_id` 全空这两个前置缺口包装成 PASS。

## 暴露的问题

- 远端 `channel_staff` 没有有效私海达人，导致无法按用户指定账号完成成功申请。
- 远端全部 `product_operation_state.assignee_id` 为空，导致无法严格验证“该商品分配给 biz_leader”。
- 额外观察到 `channel_staff` 可见该待审核单，需要另起 RBAC 专项确认是否符合业务边界。

## Harness 是否需要升级

本次无需升级 Harness 规则。现有 Git Intake、Dirty Classification、证据报告和 `PARTIAL` 结论约束足以表达当前事实。

## 后续动作

1. 准备远端渠道账号前置数据：给 `channel_staff` 认领有效达人，或提供可登录的等价渠道账号。
2. 准备远端招商分配前置数据：通过业务接口给一个 DISPLAYING 商品分配 `assignee_id`。
3. 单独执行 RBAC 专项：复核 `/api/samples?status=PENDING_AUDIT` 对 `channel_staff` 的可见性。
