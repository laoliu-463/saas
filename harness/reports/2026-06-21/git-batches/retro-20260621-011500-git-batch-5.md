# Retro Summary - GIT-BATCH-5 Frontend Auth Store

- Time: 2026-06-21 01:15:00 +08:00
- Scope: frontend/auth-request
- Harness upgrade: no

## What Changed

- Frontend request helpers now use the Pinia auth store as the token source.
- Request helper tests activate Pinia before token-refresh assertions.
- Login password input now has a 128-character cap.

## Lessons

- `pnpm` was not on the active PowerShell PATH, so validation used the explicit Corepack pnpm shim.
- The first full Vitest run timed out and ended with `EPIPE`; rerunning with `--reporter=dot` produced a valid PASS result.
- Browser validation should tolerate known unrelated CSP font errors only when the core assertions still pass and the risk is documented.

## Follow-Up

- Review the existing Google Fonts CSP violation separately.
- Keep backend U-7 addendum files out of this frontend commit.
