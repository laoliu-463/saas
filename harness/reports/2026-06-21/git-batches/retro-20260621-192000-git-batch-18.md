# Retro 2026-06-21 19:20 GIT-BATCH-18

## What changed

- Moved order-attribution data-scope interpretation to user-domain `DataScopePolicy`.
- Kept order-attribution query column choices at the caller: `co.user_id/co.dept_id` for page query and `user_id/dept_id` for summary query.
- Added a boundary test that blocks reintroducing local `switch(dataScope)` in `OrderAttributionService`.

## Evidence notes

- Targeted tests passed: 53.
- Full backend tests passed: 2465 run, 3 skipped.
- Package, Docker image build, compose backend restart, backend health, and auth guard probe passed.
- Early incomplete targeted/full logs were rejected and replaced by reruns with complete Maven evidence.

## Process note

- Staged-only validation remains required because the main workspace contains parallel uncommitted DDD slices.
- For long Maven runs on Windows, the command should append explicit `MAVEN_EXIT` to the log before the result is accepted.
- Direct code-review-graph MCP calls still failed with `Transport closed`, but the commit hook completed the local graph update successfully.

## Harness

- Archived GIT-BATCH-13 reports before adding this report pair.
- Harness upgrade: not needed.
