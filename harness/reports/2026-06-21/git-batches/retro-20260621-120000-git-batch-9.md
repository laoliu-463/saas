# Retro 2026-06-21 12:00 - GIT-BATCH-9

## What Changed

- Query application logic now talks to `UserQueryLookup`; MyBatis wrapper assembly lives in `SysUserQueryLookupAdapter`.
- Role assignment application logic now talks to `UserRoleAssignmentStore`; mapper/entity writes live in `SysUserRoleAssignmentStoreAdapter`.
- Boundary tests now guard query, assignable-user, and role-assignment application services against direct mapper/entity dependencies.

## Verification Lessons

- code-review-graph correctly raised high risk because the broader worktree is still dirty; staged-only temp worktree verification was required to isolate this batch.
- Moving query wrapper tests to the infrastructure adapter kept behavior parity visible while freeing the application service from persistence details.
- Full backend tests increased to 2432 and stayed green after adding the new boundary tests.

## Harness

- `git-batches` now contains 10 files after adding this evidence and retro; next batch must archive before writing more reports.
- No Harness script upgrade required.

## Follow-up

- Authenticated real-pre API smoke remains needed for user query and role assignment success paths.
- Remaining dirty user-domain files suggest another possible batch around `SysUserCRUDApplicationA` and additional query boundary guards.
