# GIT-BATCH-7 Retro - Current User And Master Data

## What Changed

- Added current-user and user-master-data application services as HTTP-facing
  user-domain entry points.
- Extracted permission and credential rules into user-domain policies.
- Added user and department read ports to keep `LegacyUserDomainFacade` from
  depending directly on legacy mappers/services for read models.

## What Worked

- The temp worktree process again proved the staged slice can compile and test
  independently from unrelated dirty files.
- Controller tests now verify controllers pass request context to application
  services and leave null-role normalization to the application layer.
- Integration coverage confirms password change persists state and writes the
  expected operation audit record.

## Process Notes

- The first `clean test` timed out, so it was treated as inconclusive. Rerunning
  the same focused suite without `clean` produced Maven exit code 0.
- Runtime logs surfaced an unrelated real-pre scheduled-task schema issue
  (`colonel_name` missing). It should not be hidden in this batch's conclusion.
- A post-push recheck found the local backend image tag had drifted after the
  first restart. Rebuilding from a clean HEAD worktree restored runtime evidence
  to the committed source state.
- No harness code was changed. Future harness work should add first-class
  support for temp-candidate Docker builds and compose project names.

## Next Slice

- Archive or merge `harness/reports/2026-06-21/git-batches` before adding more
  files; it is now at the 10-file directory limit.
- Candidate next batches:
  - order/data-scope routing
  - sample/colonel partner contact update
  - cleanup of stale DDD docs and old 2026-06-20 report addenda
