# Retro 2026-06-21 13:49 GIT-BATCH-13

## What changed

- Added a user-domain facade method for display names when consumers need only a readable name.
- Moved `DataApplicationService` away from full `UserOptionResponse` list consumption.
- Added a source boundary test so the DataApplication path cannot silently reintroduce full user DTO usage.

## Evidence notes

- Targeted tests passed: 60.
- Full backend tests passed: 2449 run, 3 skipped.
- Package, Docker image build, compose restart, backend health, and auth guard probe passed.
- code-review-graph succeeded for minimal context and file summaries.

## Process note

- Kept `AdminColonelPartnerController` and `ColonelActivityController` out of this commit because they belong to separate DDD slices.
- The staged-only temp worktree flow remained necessary because the main workspace has parallel uncommitted batches.

## Harness

- Archived GIT-BATCH-8 reports before adding this report pair.
- Harness upgrade: not needed.
