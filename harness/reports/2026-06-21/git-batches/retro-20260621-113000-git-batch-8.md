# Retro 2026-06-21 11:30 - GIT-BATCH-8

## What Changed

- `SysUserService` became a compatibility entry for user query paths instead of owning `QueryWrapper`, role fill, and org enrichment logic.
- `SysUserQueryApplicationService` now owns user pagination and department member query behavior.
- Assignable-user reads now go through user-domain ports and infrastructure adapters instead of direct mapper/entity usage in the application service and policy.
- User-domain policy classes no longer declare Spring stereotypes or import Spring utilities; `DomainPolicyConfig` is the composition root.

## Verification Lessons

- Full `clean test` initially failed on `DddPolicyLayerNoSpringDependencyTest` even on pure HEAD, proving an existing boundary debt rather than a query migration regression.
- Moving policy wiring to configuration restored the architecture guard and full backend suite.
- Running staged patches in a temp worktree prevented unrelated dirty changes from contaminating the evidence.
- A final runtime check caught image drift after push; the backend was rebuilt again from clean HEAD `fbd9745f` before closing the batch.

## Harness

- Archived old git-batch 3/4 reports to `harness/archive/by-date/report-packages/reports-20260621-git-batches-3-4.zip` before adding new reports.
- `harness/reports/2026-06-21/git-batches` remains within the 10-file directory limit after this batch.
- No Harness script upgrade required.

## Follow-up

- Authenticated real-pre smoke for `/api/users` and `/api/users/assignable` should be run with an explicit test token/account.
- `SysUserQueryApplicationService` is still a transition service with mapper/entity dependencies; further port extraction can be a later user-domain batch.
