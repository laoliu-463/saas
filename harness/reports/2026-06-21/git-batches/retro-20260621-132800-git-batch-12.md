# Retro 2026-06-21 13:28 GIT-BATCH-12

## What changed

- Narrowed user-domain facade consumption for Operation Log, Exclusive Merchant, Sample Filter, and Talent Query.
- Kept display-label formatting inside the user domain facade instead of repeating DTO formatting in consumers.
- Added architecture tests that fail if these consumers re-introduce `UserOptionResponse` or broad user list DTO lookups.

## Evidence notes

- Targeted tests passed: 48.
- Full backend tests passed: 2447 run, 3 skipped.
- Package, Docker image build, compose restart, backend health, and auth guard probe passed.
- code-review-graph timed out twice, so this batch used manual diff and `rg` fallback.

## Process note

- The dirty main workspace requires staged-only validation for every commit-sized slice.
- This batch used a detached temp worktree from clean HEAD plus `git diff --cached --binary`.

## Harness

- Archived GIT-BATCH-7 reports before adding this report pair.
- Harness upgrade: not needed.
