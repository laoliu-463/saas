# Retro 2026-06-21 14:53 GIT-BATCH-14

## What changed

- Added a user-domain ownership reference DTO for assignment flows that only need user existence and dept id.
- Moved Merchant/Talent assignment override away from full `UserOptionResponse`.
- Moved ColonelActivity display-name resolution away from full user DTO lookup.
- Added architecture tests for both boundaries.

## Evidence notes

- Targeted tests passed: 97.
- Full backend tests finally passed: 2453 run, 3 skipped.
- Package, Docker image build, compose restart, backend health, and auth guard probe passed.
- Earlier full-test attempts were explicitly not counted as PASS because one timed out and one had invalid logging placement.
- Post-push image drift was corrected by rebuilding from clean HEAD `61e1d985`.

## Process note

- Staged-only validation remains required because the main workspace contains parallel uncommitted DDD slices.
- The slow full-test attempt was caused by concurrent Maven/Testcontainers work; waiting for that process to end restored normal completion.

## Harness

- Archived GIT-BATCH-9 reports before adding this report pair.
- Harness upgrade: not needed.
