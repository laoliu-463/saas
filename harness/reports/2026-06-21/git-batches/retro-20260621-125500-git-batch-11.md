# Retro 2026-06-21 12:55 - GIT-BATCH-11

## What Changed

- Moved performance access policy out of service package into domain policy.
- Removed the last Spring utility dependency from the moved policy.
- Added an architecture boundary test for the migration.

## Evidence Chain

- Code graph was used before manual inspection.
- Full workspace test first exposed the policy Spring dependency.
- Targeted tests confirmed the policy boundary fix.
- Full workspace still failed because of unrelated dirty files.
- A staged-only clean worktree passed full backend tests.
- Real-pre backend was rebuilt from the staged worktree image and health checked.
- A post-push image drift was detected and corrected with a clean HEAD rebuild.

## Process Notes

- The staged-only verification pattern is still necessary while multiple domains have in-flight edits.
- The direct root cause for this batch was narrow: `PerformanceAccessScope` imported Spring only for `hasText`.
- Final runtime evidence must be checked after push because another local process can recreate the backend image during long runs.

## Harness Upgrade

No harness code change needed in this batch.

Candidate improvement remains: script the staged-only verification worktree flow.
