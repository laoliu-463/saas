# Harness Retro Summary

## 1. Harness execution

- Time: 2026-06-22 23:24:03 +08:00
- Environment: real-pre
- Scope: frontend manual supplement
- Branch: feature/ddd/DDD-VERIFY-001
- Commit at verification: 3a1b6341
- Used agent-do.ps1: previous run created 6601b048/3a1b6341; supplement was manual to avoid unrelated dirty-file capture.
- Deploy remote requested: false

## 2. Issue found

The direct `/product/manage/products` page had already used activity products, but activity-list and legacy activity routes still led to product-library context.

## 3. Fix pattern

All activity-product entrypoints now converge on `/product/manage/products?activityId={id}`. The page then reads query activity id and calls the activity product API.

## 4. Verification sufficiency

- Unit/regression tests cover data-source route construction, ActivityList button routing, router redirects, menu behavior, and ProductLibrary separation.
- Browser validation covers direct and legacy entrypoints against real-pre containers.

## 5. Harness issue exposed

Avoid running auto-commit harness commands while unrelated dirty files exist; previous agent-do captured backend policy files together with frontend changes.

## 6. Follow-up

No new harness script is required. A future eval could assert that activity product entrypoints never call `GET /api/products`.
