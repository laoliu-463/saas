# Evidence Report - 20260603-104601

| 项目 | 内容 |
| --- | --- |
| 时间 | 2026-06-03 10:46:01 |
| 环境 | local real-pre |
| 分支 | feature/auth-system |
| commit hash | 1ac7796f |
| 工作区是否干净 | 否。存在 U-2.5-B、TEST-1、Harness 门禁和来源不确定变更 |
| 任务报告 | `harness/reports/test-1-full-backend-failures-fix-20260603-104601.md` |
| 构建结果 | PASS：`mvn -f backend/pom.xml "-DskipTests" package` BUILD SUCCESS |
| 全量测试 | PASS：`mvn -f backend/pom.xml test`，1671 tests / 0 failures / 0 errors |
| Docker 状态 | PASS：real-pre backend/frontend/postgres/redis 均 healthy |
| 健康检查结果 | PASS：`verify-local.ps1 -Env real-pre -Scope backend` 返回 statusCode=200，body `{"status":"UP"}` |
| 业务验证结果 | PASS：失败单类、失败集合、U-2.5-B 定向和全量后端测试均通过 |
| 是否部署远端 | 否 |
| 远端健康检查结果 | 未执行，用户未要求远端部署 |
| 是否执行数据库写操作 | 否 |
| 是否重启容器 | 否 |
| 结论 | PASS for TEST-1；U-2.5-B 运行态加载和 real-pre 历史数据修复仍未执行 |
| 剩余风险 | 新 jar 未加载到 real-pre 容器；real-pre 历史 `sys_dept.dept_type` 未写库修复；工作区存在来源不确定 dirty 文件 |

## 命令证据摘要

- 复现：`mvn -f backend/pom.xml "-Dtest=SysUserServiceTest,SysUserControllerTest,CommissionRuleServiceTest,CommissionRuleControllerTest" test` 失败，53 tests / 3 failures / 7 errors。
- 单测：`SysUserServiceTest` PASS，20 tests / 0 failures。
- 单测：`SysUserControllerTest` PASS，9 tests / 0 failures。
- 单测：`CommissionRuleServiceTest` PASS，17 tests / 0 failures。
- 单测：`CommissionRuleControllerTest` PASS，7 tests / 0 failures。
- 组合：`mvn -f backend/pom.xml "-Dtest=SysUserServiceTest,SysUserControllerTest,CommissionRuleServiceTest,CommissionRuleControllerTest" test` PASS，53 tests / 0 failures。
- U-2.5-B 回归：`mvn -f backend/pom.xml "-Dtest=DeptTypeTest,SysDeptServiceTest,SysDeptControllerTest,DataScopeAspectTest" test` PASS，16 tests / 0 failures。
- 全量：`mvn -f backend/pom.xml test` PASS，1671 tests / 0 failures / 0 errors。
- 构建：`mvn -f backend/pom.xml "-DskipTests" package` PASS。
- `git diff --check` PASS。
- `safety-check.ps1 -Env real-pre -Scope code -DryRun`：FAIL_BY_SCOPE，`code` 不在 ValidateSet。
- `verify-local.ps1 -Env real-pre -Scope code`：FAIL_BY_SCOPE，`code` 不在 ValidateSet。
- `safety-check.ps1 -Env real-pre -Scope backend -DryRun`：PASS。
- `verify-local.ps1 -Env real-pre -Scope backend`：PASS。
- `docker compose -f docker-compose.real-pre.yml ps`：backend/frontend/postgres/redis healthy。

## Retro

本次无需 Harness 升级。发现的 Harness 参数口径差异：任务提示中的 `Scope=code` 与当前脚本 ValidateSet（`backend/frontend/full/docs`）不一致，已用 `Scope=backend` 执行等价验证并在报告中记录。
