# Retro Summary

## Task

- U-7 user-domain DDD slice: isolate `UserAccessPolicy` from `SysUser`.

## What Changed

- Introduced `UserAccessPolicy.AccessibleUser`.
- Moved `SysUser -> AccessibleUser` conversion to `SysUserService`.
- Strengthened `UserAccessPolicyTest` so the policy source cannot re-import `SysUser`.

## What Worked

- TDD loop was narrow and verifiable: missing read model produced the expected RED compile failure.
- Direct caller and broader user-domain regression passed after the boundary conversion.
- Policy package import scan now shows only `UserCredentialPolicy` as the remaining `SysUser` dependency.

## Follow-Up

- Next slice: isolate `UserCredentialPolicy` from `SysUser`.
- Continue classifying remaining user-domain dependencies in `auth/service/*`, application services and `LegacyUserDomainFacade`.
- Consider disabling or guarding scheduled jobs in affected Spring integration tests to remove Redis STOPPING shutdown noise.

## Harness Upgrade

- No new harness rule or script was needed.
