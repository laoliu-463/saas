# Retro Summary

## Task

- U-7 user-domain DDD slice: isolate `UserCredentialPolicy` from `SysUser`.

## What Changed

- Introduced `CredentialUser` for password-change policy input.
- Introduced `PasswordChangeUpdate` for password-change update output.
- Moved persistence entity conversion into `UserDomainService`.
- Added a policy source assertion that prevents re-importing `SysUser`.

## What Worked

- RED was precise: the test failed only because the target read models were absent.
- Direct caller and broader user-domain regressions passed after the service-boundary conversion.
- Policy package import scan now returns no persistence entity or mapper imports.

## Follow-Up

- Continue U-7 by classifying remaining Mapper/entity dependencies in `auth/service/*`, application services and `LegacyUserDomainFacade`.
- Keep authenticated real-pre password-change success-path validation blocked until a disposable test account or explicit password-change window is provided.
- Future report generation needs another capacity rotation because `harness/reports` and `harness/reports/2026-06-20` are again at their file-count limits.

## Harness Upgrade

- No new harness rule or script was needed.
