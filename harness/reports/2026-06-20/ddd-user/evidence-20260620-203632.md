# Evidence Report

## Metadata

- Time: 2026-06-20 20:36:32 +08:00
- Environment: real-pre
- Scope: backend
- Selected gate: Gate 3 - Domain Change
- Branch: feature/ddd/DDD-VERIFY-001
- Commit: 9792dc48
- Worktree: dirty
- Deploy remote: false

## Change Summary

- DDD slice: user domain / `OrgStructureApplicationService` application-layer boundary.
- `OrgStructureApplicationService` now receives `OrgAssignmentPolicy`, `OrgValidationPolicy` and `OrgEnrichmentPolicy`.
- Removed direct mapper/entity/infrastructure-adapter imports from `OrgStructureApplicationService`.
- Updated application-service and legacy-service parity tests to assemble the same policy path explicitly.
- Added a source boundary assertion preventing mapper/entity/infrastructure imports from returning to the application service.

## Evidence

- code-review-graph first: graph stats succeeded with 12210 nodes / 140900 edges; file summaries for target application services returned no node details, so source reads were used.
- RED: `OrgStructureApplicationServiceTest` failed because the application service still imported mapper/entity/infrastructure adapter classes.
- GREEN: `OrgStructureApplicationServiceTest` 13/0/0.
- Legacy parity regression: `OrgStructureServiceTest` 25/0/0.
- User-domain regression: selected 22-test-class command 217/0/0.
- Boundary scan: `OrgStructureApplicationService.java` had no mapper/entity/infrastructure import match after the slice.
- Constructor scan: only test assembly now calls `new OrgStructureApplicationService(...)`; production uses Spring policy injection.
- PASS: `mvn -f .\backend\pom.xml -DskipTests test-compile`.
- PASS: `mvn -f .\backend\pom.xml -DskipTests package`.
- PASS: backend restart on local real-pre via `restart-compose.ps1 -Env real-pre -Scope backend`.
- PASS: backend health returned `{"status":"UP"}` via `verify-local.ps1 -Env real-pre -Scope backend`.
- PASS: code-review-graph incremental update with minimal postprocess.
- PASS: `harness/scripts/check-harness-limits.ps1`.
- Report capacity rotation: archived `evidence-20260620-132146` and `retro-20260620-132200` to `harness/archive/by-date/report-packages/reports-20260620-ddd-user-basic-lookup/`, then moved `evidence/retro-20260620-142809` into `harness/reports/2026-06-20/`; duplicate day-copy `evidence-20260620-132146` was SHA256-matched and removed.

## Runtime State

- Docker: `backend-real-pre` rebuilt and healthy on `127.0.0.1:8081`.
- Business validation: backend user-domain tests covered org application service, legacy org service, facade, department service, current user, data scope, master data, controller and service paths.
- Git worktree: dirty before and after this slice.
- Commit/push: not executed due broad pre-existing dirty worktree changes.

## Conclusion

PARTIAL

## Residual Risk

- `OrgStructureApplicationService` no longer directly imports mapper/entity/infrastructure adapter classes.
- `SysDeptApplicationService` still contains mapper/entity dependencies because it is a CRUD transaction service and needs a separate slice.
- Wider `auth/service/*` and cross-domain permission/data-scope consumers still need classification.
- No frontend build or authenticated real-pre E2E was executed because this slice only changed backend user-domain composition boundaries.
- No remote deployment was requested or executed.
