# Evidence Report

## Metadata

- Time: 2026-06-20 21:00:45 +08:00
- Environment: real-pre
- Scope: backend
- Selected gate: Gate 3 - Domain Change
- Branch: feature/ddd/DDD-VERIFY-001
- Commit: bfb17058
- Worktree: dirty
- Deploy remote: false

## Change Summary

- DDD slice: user domain / `SysDeptApplicationService` leader-display lookup boundary.
- Added `OrgLeaderDisplayLookup` port and `SysOrgLeaderDisplayLookupAdapter`.
- `SysDeptApplicationService` no longer directly imports or injects `SysUserMapper` / `SysUser` for leader display lookup.
- Preserved legacy behavior for existing leader users with blank display names by returning `LeaderDisplay` instead of `Optional<String>`.

## Evidence

- RED: `SysDeptApplicationServiceBoundaryTest` failed before implementation because the application service still imported user persistence dependencies.
- GREEN: `SysDeptApplicationServiceBoundaryTest` 1/0/0.
- Behavior regression guard: `SysDeptServiceTest` 13/0/0, including existing leader user with blank `realName` / `username` keeping `dept.leader = null` instead of throwing.
- User-domain regression: selected 23-test-class command 219/0/0.
- Boundary scan: no `SysUserMapper` / `SysUser` match in `SysDeptApplicationService.java`.
- Remaining boundary scan: `SysDeptApplicationService.java` still imports `SysDeptMapper` / `SysDept`; this is deferred to a CRUD-focused slice.
- PASS: `mvn -f backend/pom.xml -DskipTests test-compile`.
- PASS: `mvn -f backend/pom.xml -DskipTests package`.
- PASS: backend restart on local real-pre via `restart-compose.ps1 -Scope backend`.
- PASS: backend health returned `{"status":"UP"}` via `verify-local.ps1 -Scope backend`.
- PASS: code-review-graph incremental update after code changes.
- PASS: `harness/scripts/check-harness-limits.ps1`.

## Runtime State

- Docker: `backend-real-pre` rebuilt and healthy on `127.0.0.1:8081`.
- Business validation: backend user-domain tests covered department service, org service, facade, current user, data scope, master data, controller and policy paths.
- Git worktree: dirty with broad pre-existing DDD changes.
- Commit/push: not executed due broad dirty worktree and no explicit user request.

## Tooling Notes

- `code-review-graph get_minimal_context` timed out after 120 seconds.
- Narrow graph checks were used instead: graph stats succeeded, semantic search found no focused node, and source-level checks were used as fallback evidence.
- First focused Maven GREEN command timed out at 120 seconds without a result; it was rerun with a longer timeout and passed.

## Conclusion

PARTIAL

## Residual Risk

- This slice only removes the `SysUserMapper` / `SysUser` dependency from `SysDeptApplicationService`; `SysDeptMapper` / `SysDept` remain for the later CRUD boundary slice.
- No frontend build or authenticated real-pre E2E was executed because this slice changed backend user-domain composition only.
- No remote deployment was requested or executed.

## Addendum: CRUD Persistence Boundary Slice

### Metadata

- Time: 2026-06-20 22:45:41 +08:00
- Scope: backend
- Slice: DDD-USER-MIGRATION-006 / U-7 `SysDeptApplicationService` CRUD persistence boundary
- Report mode: appended to this U-7 report because `harness/reports` and date archive directories were already at the 10-file limit.

### Change Summary

- Added `OrgDepartmentRepository` port and `SysOrgDepartmentRepositoryAdapter`.
- Moved `SysDeptMapper` / `SysDept` access out of `SysDeptApplicationService`.
- Kept `SysDeptService` as legacy compatibility shell and converted port records back to `SysDept` only at that old boundary.
- Added `UBIQUITOUS_LANGUAGE.md` and updated `CONTEXT.md` with user-domain DDD terms.

### Evidence

- PASS: `mvn -f backend/pom.xml "-Dtest=SysDeptServiceTest,SysDeptApplicationServiceBoundaryTest" test` => 14 tests, 0 failures, 0 errors.
- PASS: production source boundary scan found no `SysDeptMapper` / `SysDept` import in `SysDeptApplicationService.java`.
- PASS: `mvn -f backend/pom.xml -DskipTests package`.
- PASS: `restart-compose.ps1 -Env real-pre -Scope backend` rebuilt and recreated `backend-real-pre`.
- PASS: `verify-local.ps1 -Env real-pre -Scope backend` returned `{"status":"UP"}`.
- PASS: code-review-graph incremental update completed.
- PASS: `harness/scripts/check-harness-limits.ps1`.

### Conclusion

PASS for this U-7 slice.

### Residual Risk

- Full DDD migration remains active; this addendum only proves the SysDept application-service persistence boundary.
- `auth/service/*` still needs dependency classification and separate migration slices.
- No authenticated real-pre E2E or remote deployment was executed.
