# Retro Summary: DDD-USER-U7 Boundary Slices

## Scope

- Continue user-domain DDD refactor on true-route auth services and user-domain transitional layers.
- Preserve existing HTTP/API behavior while moving persistence details behind user-domain ports.

## Completed Slices

| Slice | Main Change | Result |
| --- | --- | --- |
| SysDept directory reads | `OrgUnitDirectoryLookup` + `OrgUnitDirectoryApplicationService` | PASS |
| SysDept stats reads | Stats operations moved behind directory lookup port | PASS |
| SysDept member pagination | `OrgUnitMemberLookup` + adapter | PASS |
| SysDept writes | `OrgUnitWriteApplicationService` | PASS |
| SysUser assignable users | `UserAssignableApplicationService` | PASS |
| SysUser role assignment | `SysUserRoleAssignmentApplicationService` | PASS |
| SysUser pagination/member reads | `SysUserQueryApplicationService` | PASS |
| UserAssignmentPolicy persistence boundary | `UserAssignmentLookup` + adapter | PASS |
| UserAssignable candidate boundary | `UserAssignableCandidateLookup` + adapter | PASS |
| SysUserRoleAssignment store boundary | `UserRoleAssignmentStore` + adapter | PASS |
| SysUserQuery query boundary | `UserQueryLookup` + adapter | PASS |

## Latest Slice

- Added `UserQueryLookup` and `SysUserQueryLookupAdapter`.
- Removed QueryWrapper / Mapper / Entity imports from `SysUserQueryApplicationService`.
- Kept data-scope decision in the application service and moved SQL wrapper construction to infrastructure.
- Added source boundary and infrastructure adapter tests.

## What Worked

- The RED boundary test failed on the exact architectural smell: query application service still built MyBatis wrappers directly.
- Splitting application orchestration tests from adapter SQL tests kept behavior evidence while improving the boundary.
- Existing SysUser facade and controller regression tests continued to pass.
- Scoped diff checks separated current-slice cleanliness from unrelated whitespace debt.

## Evidence

- RED: `SysUserQueryApplicationServiceBoundaryTest` failed before the query port existed.
- Focused GREEN: 21 tests, 0 failures.
- User-domain regression: 84 tests, 0 failures.
- Backend package: PASS.
- Backend real-pre restart: PASS.
- Local backend health: PASS, `{"status":"UP"}`.
- code-review-graph incremental update: PASS.
- Current-slice source scan: PASS.
- Scoped diff check: PASS.
- Staged diff check: PASS.
- Global diff check: BLOCKED_UNRELATED by pre-existing trailing whitespace outside this slice.

## Harness Upgrade

- No harness rule or script change is needed for this slice.
- Continue compact in-place evidence updates while the DDD report directory is near the 10-file limit.

## Next Step

- Continue U-7 by port-extracting `SysUserCRUDApplicationA/B` in smaller create/update/delete/read-model slices.
- Keep each slice behind a RED boundary test, focused behavior tests, package, restart, health check and evidence update.
