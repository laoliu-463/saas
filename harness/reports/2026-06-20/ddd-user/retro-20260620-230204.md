# Retro Summary

## Task

- Continue DDD user-domain refactor toward 100%.
- Current slice: true `SysDeptController` route directory query boundary.

## What Changed

- Introduced `OrgUnitDirectoryLookup` as a user-domain port.
- Introduced `OrgUnitDirectoryApplicationService` to assemble flat and tree organization-unit views.
- Moved `auth.service.SysDeptService.findAll/findTree` off direct `SysDeptMapper.findAllActive()` calls.
- Added source boundary and behavior tests for the new query boundary.

## What Worked

- The RED test exposed that prior DDD work had not yet reached the true HTTP-route service.
- A narrow directory-query port avoided changing CRUD, permission, member pagination or deletion constraints in the same slice.
- User-domain regression caught and then verified an existing `SysUserCRUDApplicationATest` status matcher compile blocker.

## Evidence

- `SysDeptServiceBoundaryTest`: 1/0/0.
- `OrgUnitDirectoryApplicationServiceTest`: 3/0/0.
- True entry focused tests: 7/0/0.
- User-domain regression: 230/0/0.
- `test-compile`, `package`, backend restart, health check and graph update passed.

## Harness Upgrade

- No harness rule or script change is needed for this slice.

## Next Step

- Continue U-7 on the true `auth.service.SysDeptService` route: extract `getById`, `findGroupsByParent`, `getStats` and `findMembers` read paths before moving CRUD writes.

## Addendum: 2026-06-20 23:54

### Task

- Continue U-7 by moving `auth.service.SysDeptService.getById` and `findGroupsByParent` to the user-domain directory application boundary.

### What Changed

- Added `OrgUnitDirectoryApplicationService.getById`.
- Completed `findGroupsByParent` behavior in `OrgUnitDirectoryApplicationService`.
- Delegated true-route `SysDeptService.getById` and `findGroupsByParent` to the application service.

### What Worked

- The existing boundary test already encoded the desired next state, so the RED condition was concrete and reproducible.
- Using an alternate Maven build directory avoided Windows `target` lock contention without killing unrelated Maven work.

### Evidence

- RED testCompile failure before implementation.
- Focused GREEN: 7 tests, 0 failures.
- Backend package, restart, health check, code-review-graph update and harness limits check passed.

### Harness Upgrade

- No harness rule or script change is needed for this slice.

### Next Step

- Continue U-7 on `auth.service.SysDeptService.getStats` and `findMembers` read paths before moving CRUD writes.

## Addendum: 2026-06-21 00:06

### Task

- Continue U-7 by moving `auth.service.SysDeptService.getStats` to the user-domain directory application boundary.

### What Changed

- Added stats operations to `OrgUnitDirectoryLookup`.
- Implemented stats counting in the infrastructure adapter.
- Added `OrgUnitDirectoryApplicationService.getStats`.
- Delegated true-route `SysDeptService.getStats` to the application service.

### What Worked

- Keeping count operations behind the directory lookup port preserved behavior without moving persistence concerns into the application service.
- The focused service and boundary tests verified both the old semantics and the new dependency direction.

### Evidence

- Focused GREEN: 9 tests, 0 failures.
- Boundary source scan passed.
- Backend package, restart, health check, code-review-graph context and harness limits check passed.

### Harness Upgrade

- No harness rule or script change is needed for this slice.

### Next Step

- Continue U-7 on `auth.service.SysDeptService.findMembers` read path before moving CRUD writes.

## Addendum: 2026-06-21 00:35

### Task

- Continue U-7 by moving `auth.service.SysDeptService.findMembers` to the user-domain directory application boundary.

### What Changed

- Added `OrgUnitMemberLookup`.
- Added `SysOrgUnitMemberLookupAdapter`.
- Added `OrgUnitDirectoryApplicationService.findMembers`.
- Delegated true-route `SysDeptService.findMembers` to the application service.

### What Worked

- The new member lookup port kept the existing `SysUserService.findDeptMembers` behavior as an adapter detail.
- Application tests now assert that organization-unit existence is validated before member pagination.
- Controller-focused tests confirmed the true HTTP entry still compiles and delegates correctly.

### Evidence

- RED testCompile failure before creating `OrgUnitMemberLookup`.
- Focused GREEN: 12 tests, 0 failures.
- True-entry GREEN: 15 tests, 0 failures.
- Backend package, restart, health check, code-review-graph update and harness limits check passed.

### Harness Upgrade

- No harness rule or script change is needed for this slice.

### Next Step

- Continue U-7 on true-route `auth.service.SysDeptService.create/update/delete` write paths.
