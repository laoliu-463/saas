# Retro Summary

## Task

- Continue DDD user-domain refactor toward 100%.
- Current slice: `SysDeptApplicationService` leader-display lookup boundary.

## What Changed

- Added `OrgLeaderDisplayLookup` as a domain port.
- Added `SysOrgLeaderDisplayLookupAdapter` to keep `SysUserMapper` access in infrastructure.
- Updated `SysDeptApplicationService` to call the port instead of `SysUserMapper`.
- Added a boundary test and a service-level behavior guard for blank leader display names.

## What Worked

- TDD loop caught the intended application-layer dependency violation before the implementation.
- The first port shape (`Optional<String>`) exposed a behavior risk before completion.
- Changing to `Optional<LeaderDisplay>` preserved the distinction between missing user and blank display value.

## Evidence

- `SysDeptApplicationServiceBoundaryTest`: 1/0/0.
- `SysDeptServiceTest`: 13/0/0.
- User-domain regression: 219/0/0.
- `test-compile`, `package`, backend restart and health check passed.
- code-review-graph incremental update passed.

## Harness Upgrade

- No harness rule or script change is needed for this slice.

## Next Step

- U-7 next: split `SysDeptApplicationService` CRUD persistence access from `SysDeptMapper` / `SysDept` into narrower ports or policies, with parity tests before moving behavior.

## Addendum: 2026-06-20 22:45

### Task

- Continue U-7 by removing `SysDeptMapper` / `SysDept` imports from `SysDeptApplicationService`.

### What Changed

- Added `OrgDepartmentRepository` as the user-domain organization persistence port.
- Added `SysOrgDepartmentRepositoryAdapter` as the MyBatis-backed infrastructure adapter.
- Updated `SysDeptApplicationService` to use `DepartmentRecord` instead of persistence entity types.
- Kept `SysDeptService` as the old compatibility shell for callers that still expect `SysDept`.
- Added `UBIQUITOUS_LANGUAGE.md` and updated `CONTEXT.md` with user-domain terminology.

### What Worked

- The existing `SysDeptServiceTest` remained a useful behavior guard through the legacy API.
- The boundary test now protects both user persistence and department persistence imports in the application service.

### Evidence

- `SysDeptApplicationServiceBoundaryTest`: 1/0/0.
- `SysDeptServiceTest`: 13/0/0.
- Backend package, backend container restart, local health check, code-review-graph update and harness limits check passed.

### Harness Upgrade

- No harness rule or script change is needed for this slice.

### Next Step

- Continue U-7 by classifying remaining `auth/service/*` user-domain persistence dependencies, especially `auth/service/SysDeptService`.
