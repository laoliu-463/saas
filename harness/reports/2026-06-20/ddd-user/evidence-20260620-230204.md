# Evidence Report

## Metadata

- Time: 2026-06-20 23:02:04 +08:00
- Environment: real-pre
- Scope: backend
- Slice: DDD-USER-U7 / SysDept true-entry directory query boundary
- Branch: feature/ddd/DDD-VERIFY-001
- Commit: bfb17058
- Worktree: dirty
- Deploy remote: false

## Change Summary

- Added `OrgUnitDirectoryLookup` port for active organization-unit directory reads.
- Added `SysOrgUnitDirectoryLookupAdapter` to keep `SysDeptMapper.findAllActive()` in infrastructure.
- Added `OrgUnitDirectoryApplicationService` for `findAll` and `findTree`.
- Updated the real HTTP-route service `auth.service.SysDeptService` so `findAll` / `findTree` delegate to the user-domain application service instead of directly calling `SysDeptMapper.findAllActive()`.
- Fixed existing `SysUserCRUDApplicationATest` compile blocker by matching `Integer status` with `Integer.valueOf(...)`.

## Evidence

- RED: `SysDeptServiceBoundaryTest` failed before implementation because `auth.service.SysDeptService` lacked `OrgUnitDirectoryApplicationService` and directly called `sysDeptMapper.findAllActive()`.
- GREEN: `SysDeptServiceBoundaryTest` 1/0/0.
- Behavior tests: `OrgUnitDirectoryApplicationServiceTest` 3/0/0.
- True entry tests: `SysDeptControllerTest,SysDeptServiceBoundaryTest,OrgUnitDirectoryApplicationServiceTest` 7/0/0.
- User-domain regression: selected 27-test-class command 230/0/0.
- Boundary scan: `OrgUnitDirectoryApplicationService` has no mapper/entity/infrastructure imports; `auth.service.SysDeptService` no longer contains `findAllActive()`.
- PASS: `mvn -f backend/pom.xml -DskipTests test-compile`.
- PASS: `mvn -f backend/pom.xml -DskipTests package`.
- PASS: backend restart on local real-pre via `restart-compose.ps1 -Scope backend`.
- PASS: backend health returned `{"status":"UP"}` via `verify-local.ps1 -Scope backend`.
- PASS: code-review-graph incremental update after code changes.
- PASS: `harness/scripts/check-harness-limits.ps1`.

## Runtime State

- Docker: `backend-real-pre` rebuilt and healthy on `127.0.0.1:8081`.
- Business validation: backend user-domain tests covered true SysDept controller entry, organization directory service, org service/facade, current user, data scope, master data, user CRUD and policy paths.
- Git worktree: dirty with broad pre-existing DDD changes.
- Commit/push: not executed due broad dirty worktree and no explicit user request.

## Tooling Notes

- `code-review-graph file_summary` still returned 0 results for `SysDeptApplicationService.java`; source-level checks were used as fallback evidence.
- Windows sandbox intermittently failed process creation with `CreateProcessAsUserW failed: 1312`; affected commands were rerun with explicit escalation.

## Conclusion

PARTIAL

## Residual Risk

- Only `auth.service.SysDeptService.findAll/findTree` were moved to the user-domain application boundary in this slice.
- `auth.service.SysDeptService` still directly depends on `SysDeptMapper` / `SysDept` for `findGroupsByParent`, `getStats`, `findMembers`, `getById`, `create`, `update` and `delete`.
- `domain.user.application.SysDeptApplicationService` still contains `SysDeptMapper` / `SysDept` and remains a separate legacy-DDD path to converge later.
- No frontend build or authenticated real-pre E2E was executed because this slice changed backend user-domain composition only.
- No remote deployment was requested or executed.

## Addendum: getById / findGroupsByParent Read Boundary

### Metadata

- Time: 2026-06-20 23:54:02 +08:00
- Scope: backend
- Slice: DDD-USER-U7 / `auth.service.SysDeptService` detail and child-group read boundary
- Report mode: appended to this U-7 report because `harness/reports` is at the 10-file limit.

### Change Summary

- Added `OrgUnitDirectoryApplicationService.getById`.
- Completed `OrgUnitDirectoryApplicationService.findGroupsByParent` with old behavior preserved: parent must exist, empty `deptType` means all groups, and non-group children are excluded.
- Updated true-route `auth.service.SysDeptService.getById` and `findGroupsByParent` to delegate to `OrgUnitDirectoryApplicationService`.

### Evidence

- RED reproduced: `mvn -f backend/pom.xml "-Dtest=SysDeptServiceBoundaryTest,OrgUnitDirectoryApplicationServiceTest" test` failed during testCompile because the application service lacked `getById` / `findGroupsByParent`.
- GREEN: `mvn -f backend/pom.xml test "-Dtest=SysDeptServiceBoundaryTest,OrgUnitDirectoryApplicationServiceTest" "-Dproject.build.directory=target-ddd-u7-readpath"` => 7 tests, 0 failures, 0 errors.
- PASS: source scan shows `auth.service.SysDeptService` returns `orgUnitDirectoryApplicationService.getById(id)` and `findGroupsByParent(parentId, deptType)`.
- PASS: `mvn -f backend/pom.xml -DskipTests package`.
- PASS: `restart-compose.ps1 -Env real-pre -Scope backend`.
- PASS: `verify-local.ps1 -Env real-pre -Scope backend` returned `{"status":"UP"}`.
- PASS: code-review-graph incremental update.
- PASS: `harness/scripts/check-harness-limits.ps1`.

### Tooling Notes

- `mvn clean test` could not delete `backend/target` because Windows had files locked; this was recorded as a local cleanup limitation, not a compile failure.
- A separate Maven build directory, `target-ddd-u7-readpath`, was used for the focused GREEN test to avoid concurrent `target` contention.

### Conclusion

PASS for this addendum slice.

### Residual Risk

- `auth.service.SysDeptService.getStats`, `findMembers`, `create`, `update` and `delete` still directly depend on `SysDeptMapper` / `SysDept`.
- No frontend build, authenticated real-pre E2E or remote deployment was executed for this backend-only boundary slice.

## Addendum: getStats Read Boundary

### Metadata

- Time: 2026-06-21 00:06:08 +08:00
- Scope: backend
- Slice: DDD-USER-U7 / `auth.service.SysDeptService.getStats` read boundary
- Report mode: appended to this U-7 report because `harness/reports` is at the 10-file limit.

### Change Summary

- Added organization-unit stats methods to the `OrgUnitDirectoryLookup` port.
- Implemented the stats port in `SysOrgUnitDirectoryLookupAdapter`, keeping `SysDeptMapper.countMembersUnderDept` and `countChildGroupsByType` in infrastructure.
- Added `OrgUnitDirectoryApplicationService.getStats`, preserving the existing member, recruiter-group and channel-group count semantics.
- Updated true-route `auth.service.SysDeptService.getStats` to delegate to `OrgUnitDirectoryApplicationService`.

### Evidence

- RED/GREEN focused command: `mvn -f backend/pom.xml test "-Dtest=SysDeptServiceBoundaryTest,OrgUnitDirectoryApplicationServiceTest" "-Dproject.build.directory=target-ddd-u7-stats"` => 9 tests, 0 failures, 0 errors.
- PASS: boundary source scan shows `auth.service.SysDeptService.getStats` delegates to `orgUnitDirectoryApplicationService.getStats(deptId)`, while `countMembersUnderDept` / `countChildGroupsByType` appear only in the infrastructure adapter.
- PASS: `mvn -f backend/pom.xml -DskipTests package`.
- PASS: `restart-compose.ps1 -Env real-pre -Scope backend`.
- PASS: `verify-local.ps1 -Env real-pre -Scope backend` returned `{"status":"UP"}` after retry.
- PASS: code-review-graph stats loaded after this slice: 12212 nodes, 140804 edges, 1527 files, last updated `2026-06-21T00:08:26`.
- PASS: `harness/scripts/check-harness-limits.ps1`.

### Conclusion

PASS for this addendum slice.

### Residual Risk

- This addendum supersedes the earlier `getStats` residual-risk item in this same report.
- `auth.service.SysDeptService.findMembers`, `create`, `update` and `delete` still directly depend on `SysDeptMapper` / `SysDept`.
- No frontend build, authenticated real-pre E2E or remote deployment was executed for this backend-only boundary slice.

## Addendum: findMembers Read Boundary

### Metadata

- Time: 2026-06-21 00:35:12 +08:00
- Scope: backend
- Slice: DDD-USER-U7 / `auth.service.SysDeptService.findMembers` read boundary
- Report mode: appended to this U-7 report because `harness/reports` is at the 10-file limit.

### Change Summary

- Added `OrgUnitMemberLookup` as a user-domain port for organization-unit member pagination.
- Added `SysOrgUnitMemberLookupAdapter` to keep the existing `SysUserService.findDeptMembers` call outside the true-route department service.
- Added `OrgUnitDirectoryApplicationService.findMembers`, preserving the old behavior of validating the target organization unit before member pagination.
- Updated true-route `auth.service.SysDeptService.findMembers` to delegate to `OrgUnitDirectoryApplicationService`.

### Evidence

- RED reproduced: focused test compile failed because `OrgUnitMemberLookup` did not exist.
- GREEN focused command: `mvn -f backend/pom.xml test "-Dtest=SysDeptServiceBoundaryTest,OrgUnitDirectoryApplicationServiceTest" "-Dproject.build.directory=target-ddd-u7-members"` => 12 tests, 0 failures, 0 errors.
- GREEN true-entry command: `mvn -f backend/pom.xml test "-Dtest=SysDeptControllerTest,SysDeptServiceBoundaryTest,OrgUnitDirectoryApplicationServiceTest" "-Dproject.build.directory=target-ddd-u7-members-controller"` => 15 tests, 0 failures, 0 errors.
- PASS: boundary source scan shows `auth.service.SysDeptService.findMembers` delegates to `orgUnitDirectoryApplicationService.findMembers(deptId, request)`, while `SysUserService.findDeptMembers` appears only in `SysOrgUnitMemberLookupAdapter`.
- PASS: `mvn -f backend/pom.xml -DskipTests package`.
- PASS: `restart-compose.ps1 -Env real-pre -Scope backend`.
- PASS: `verify-local.ps1 -Env real-pre -Scope backend` returned `{"status":"UP"}` after retry.
- PASS: code-review-graph incremental update after this slice.
- PASS: `harness/scripts/check-harness-limits.ps1`.

### Tooling Notes

- The first focused GREEN command attempt timed out after 124 seconds; the same command was rerun with a longer timeout and passed.

### Conclusion

PASS for this addendum slice.

### Residual Risk

- This addendum supersedes the earlier `findMembers` residual-risk item in this same report.
- `auth.service.SysDeptService.create`, `update` and `delete` still directly depend on `SysDeptMapper` / `SysDept`.
- No frontend build, authenticated real-pre E2E or remote deployment was executed for this backend-only boundary slice.
