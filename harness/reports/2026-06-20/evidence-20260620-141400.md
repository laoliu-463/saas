# Evidence Report

## Metadata

- Time: 2026-06-20 14:14:00 +08:00
- Environment: real-pre
- Scope: backend
- Selected gate: Gate 3 - Domain Change
- Branch: feature/ddd/DDD-VERIFY-001
- Commit: 8ac11580
- Worktree: dirty
- Deploy remote: false

## Change Summary

- DDD slice: user domain / `UserAccessPolicy` entity boundary.
- Added `UserAccessPolicy.AccessibleUser` as a policy read model with only `id` and `deptId`.
- `UserAccessPolicy` now consumes `AccessibleUser` instead of `SysUser`.
- `SysUserService` maps `SysUser` to `AccessibleUser` at the auth/service boundary.
- `UserAccessPolicyTest` no longer constructs `SysUser` and asserts the policy source does not import `SysUser`.

## Evidence

- RED: `mvn -f .\backend\pom.xml "-Dtest=UserAccessPolicyTest" test` failed because `AccessibleUser` did not exist.
- GREEN: `UserAccessPolicyTest` 5/0/0.
- Direct caller regression: `UserAccessPolicyTest,SysUserServiceTest` 25/0/0.
- User-domain regression: selected 20-test-class command 201/0/0.
- Boundary scan: policy package mapper/entity imports now only report `UserCredentialPolicy -> SysUser`.
- PASS: `mvn -f .\backend\pom.xml -DskipTests test-compile`.
- PASS: `mvn -f .\backend\pom.xml -DskipTests package`.
- PASS: backend restart on local real-pre via `restart-compose.ps1 -Env real-pre -Scope backend`.
- PASS: backend health returned `{"status":"UP"}` via `verify-local.ps1 -Env real-pre -Scope backend`.
- PASS: code-review-graph incremental update, 207 files re-parsed, 74 nodes and 938 edges updated.
- PASS: `harness/scripts/check-harness-limits.ps1`.

## Runtime State

- Docker: `backend-real-pre` rebuilt and recreated; local health check passed on `127.0.0.1:8081`.
- Business validation: backend user-domain tests covered user access, current user, org, data scope, master data, facade and controller paths.
- Git worktree: dirty before and after this slice.
- Commit/push: not executed due broad pre-existing dirty worktree changes.

## Conclusion

PARTIAL

## Residual Risk

- `UserCredentialPolicy` still imports `SysUser`; this is the next policy entity dependency to isolate.
- Wider `auth/service/*`, `domain/user/application/*` and `LegacyUserDomainFacade` Mapper/entity dependencies still need classification.
- User-domain regression emitted Redis scheduled-task STOPPING logs during test JVM shutdown, but Surefire result was 201/0/0.
- No frontend build or E2E was executed because this slice only changed backend user-domain policy boundaries.
- No remote deployment was requested or executed.
