# Evidence Report

## Metadata

- Time: 2026-06-20 14:28:09 +08:00
- Environment: real-pre
- Scope: backend
- Selected gate: Gate 3 - Domain Change
- Branch: feature/ddd/DDD-VERIFY-001
- Commit: 8ac11580
- Worktree: dirty
- Deploy remote: false

## Change Summary

- DDD slice: user domain / `UserCredentialPolicy` entity boundary.
- Added `UserCredentialPolicy.CredentialUser` as the current-user credential read model.
- Added `UserCredentialPolicy.PasswordChangeUpdate` as the password-change update decision.
- `UserCredentialPolicy` no longer imports or consumes `SysUser`.
- `UserDomainService` maps `SysUser -> CredentialUser` and `PasswordChangeUpdate -> SysUser` at the service boundary.

## Evidence

- RED: `mvn -f .\backend\pom.xml "-Dtest=UserCredentialPolicyTest" test` failed because `CredentialUser` and `PasswordChangeUpdate` did not exist.
- GREEN: `UserCredentialPolicyTest` 5/0/0.
- Direct caller regression: `UserCredentialPolicyTest,UserDomainServiceTest` 11/0/0.
- User-domain regression: selected 20-test-class command 202/0/0.
- Boundary scan: `rg "import com\.colonel\.saas\.entity|import com\.colonel\.saas\.mapper" backend/src/main/java/com/colonel/saas/domain/user/policy` returned `NO_MATCH`.
- PASS: `mvn -f .\backend\pom.xml -DskipTests test-compile`.
- PASS: `mvn -f .\backend\pom.xml -DskipTests package`.
- PASS: backend restart on local real-pre via `restart-compose.ps1 -Env real-pre -Scope backend`.
- PASS: backend health returned `{"status":"UP"}` via `verify-local.ps1 -Env real-pre -Scope backend`.
- PASS: code-review-graph incremental update, 207 files re-parsed, 12 nodes and 107 edges updated.
- PASS: `harness/scripts/check-harness-limits.ps1` after report capacity rotation.

## Runtime State

- Docker: `backend-real-pre` rebuilt and recreated; local health check passed on `127.0.0.1:8081`.
- Business validation: backend user-domain tests covered credential policy, password change service boundary, current user, org, data scope, master data, facade and controller paths.
- Git worktree: dirty before and after this slice.
- Commit/push: not executed due broad pre-existing dirty worktree changes.

## Conclusion

PARTIAL

## Residual Risk

- User-domain policy package no longer imports persistence entities or mappers.
- Wider `auth/service/*`, `domain/user/application/*` and `LegacyUserDomainFacade` Mapper/entity dependencies still need classification.
- Authenticated real-pre password-change success path was not executed to avoid changing a real account password without an approved test account.
- No frontend build or E2E was executed because this slice only changed backend user-domain policy boundaries.
- No remote deployment was requested or executed.
