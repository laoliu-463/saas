# Evidence 2026-06-20 13:37

## 结论

PARTIAL

本切片 PASS：`OrgAssignmentPolicy` 已从 `SysDeptMapper` / `SysDept` 直接依赖改为用户域 `OrgNodeLookup` 端口，行为回归、构建、real-pre 后端重启和健康检查均通过。整体 DDD-U-7 仍 PARTIAL：`OrgEnrichmentPolicy` 等仍待端口隔离。

## 证据

- 时间：2026-06-20 13:37:27 +08:00
- 环境：local `real-pre`
- 分支：`feature/ddd/DDD-VERIFY-001`
- HEAD：`8ac11580bcd73bb4f78e894ad17fd2d296021c6c`
- 工作区：dirty；存在大量既有 DDD / docs / harness 未提交变更，本轮未回滚无关改动。
- 文件：`OrgNodeLookup.java`、`SysOrgNodeLookupAdapter.java`、`OrgAssignmentPolicy.java`、`OrgStructureApplicationService.java`、`OrgAssignmentPolicyTest.java`、`OrgEnrichmentPolicyTest.java`
- RED：`mvn -f .\backend\pom.xml "-Dtest=OrgAssignmentPolicyTest" test` 失败，原因：`OrgNodeLookup` / `OrgNode` 不存在。
- GREEN：`mvn -f .\backend\pom.xml "-Dtest=OrgAssignmentPolicyTest" test`，11/0/0。
- 组织回归：`mvn -f .\backend\pom.xml "-Dtest=OrgAssignmentPolicyTest,OrgEnrichmentPolicyTest,OrgStructureApplicationServiceTest,OrgStructureServiceTest" test`，56/0/0。
- 用户域回归：`mvn -f .\backend\pom.xml "-Dtest=OrgAssignmentPolicyTest,OrgEnrichmentPolicyTest,OrgValidationPolicyTest,OrgStructureApplicationServiceTest,OrgStructureServiceTest,UserAccessPolicyTest,UserChannelCodePolicyTest,UserMasterDataControllerTest,UserMasterDataServiceTest,CurrentUserControllerTest,CurrentUserApplicationServiceTest,UserDomainServiceTest,LegacyUserDomainFacadeTest,DataScopeAspectTest,DataScopePolicyTest,DataScopePolicyParityTest,UserCredentialPolicyTest,SysUserServiceTest,SysUserControllerTest" test`，195/0/0。
- 编译：`mvn -f .\backend\pom.xml -DskipTests test-compile`，PASS。
- 构建：`mvn -f .\backend\pom.xml -DskipTests package`，PASS。
- 重启：`powershell -NoProfile -ExecutionPolicy Bypass -File .\harness\scripts\commands\restart-compose.ps1 -Env real-pre -Scope backend`，PASS。
- 健康检查：`powershell -NoProfile -ExecutionPolicy Bypass -File .\harness\scripts\commands\verify-local.ps1 -Env real-pre -Scope backend`，PASS，`{"status":"UP"}`。
- 注释一致性修正后复验：`mvn -f .\backend\pom.xml "-Dtest=OrgAssignmentPolicyTest" test`，11/0/0。
- 最终构建复验：`mvn -f .\backend\pom.xml -DskipTests package`，PASS（13:42）。
- 最终运行态复验：`restart-compose.ps1 -Env real-pre -Scope backend` + `verify-local.ps1 -Env real-pre -Scope backend`，PASS，`{"status":"UP"}`。
- 边界检查：`rg "SysDeptMapper|entity\.SysDept" backend/src/main/java/com/colonel/saas/domain/user/policy/OrgAssignmentPolicy.java` 无命中。
- 剩余 Mapper 检索：Mapper import 仅在 `domain/user/infrastructure/*` 适配器和未处理的 `OrgEnrichmentPolicy` 中存在。
- Harness limits：`powershell -NoProfile -ExecutionPolicy Bypass -File .\harness\scripts\check-harness-limits.ps1`，PASS。
- code-review-graph：incremental update 完成；207 files re-parsed，FTS rebuilt。

## 风险

- `OrgEnrichmentPolicy` 仍直接依赖 `SysDeptMapper` / `SysRoleMapper`，下一刀继续端口隔离。
- `OrgStructureApplicationService` 仍保留 Mapper 构造器签名并内部构造过渡适配器；这是兼容旧调用点的过渡状态。
- 本轮未执行前端构建或 E2E，因为修改范围限定在后端用户域策略与组织结构服务。
- 未提交、未 push；当前工作区存在大量既有未提交变更。

## 下一步

- U-7：继续收口 `OrgEnrichmentPolicy` 的部门/角色查询端口。
- 随后分类 `auth/service/*`、`domain/user/application/*`、`LegacyUserDomainFacade` 中的 Mapper / legacy service 依赖。
