# Evidence 2026-06-20 13:51

## 结论

PARTIAL

本切片 PASS：`OrgEnrichmentPolicy` 已从 `SysDeptMapper` / `SysRoleMapper` 和持久化实体直接依赖改为用户域 `OrgEnrichmentLookup` 端口，组织展示、角色展示、组织变更备注行为回归通过。整体 DDD 到 100 仍 PARTIAL。

## 证据

- 时间：2026-06-20 13:51:30 +08:00
- 环境：local `real-pre`
- 分支：`feature/ddd/DDD-VERIFY-001`
- HEAD：`8ac11580bcd73bb4f78e894ad17fd2d296021c6c`
- 工作区：dirty；存在大量既有 DDD / docs / harness 未提交变更，本轮未回滚无关改动。
- 文件：`OrgEnrichmentLookup.java`、`SysOrgEnrichmentLookupAdapter.java`、`OrgEnrichmentPolicy.java`、`OrgStructureApplicationService.java`、`OrgEnrichmentPolicyTest.java`
- RED：`mvn -f .\backend\pom.xml "-Dtest=OrgEnrichmentPolicyTest" test` 失败，原因：`OrgEnrichmentLookup` / `RoleSummary` 不存在。
- GREEN：`mvn -f .\backend\pom.xml "-Dtest=OrgEnrichmentPolicyTest" test`，9/0/0。
- 组织回归：`mvn -f .\backend\pom.xml "-Dtest=OrgAssignmentPolicyTest,OrgEnrichmentPolicyTest,OrgValidationPolicyTest,OrgStructureApplicationServiceTest,OrgStructureServiceTest" test`，71/0/0。
- 用户域回归：`mvn -f .\backend\pom.xml "-Dtest=OrgAssignmentPolicyTest,OrgEnrichmentPolicyTest,OrgValidationPolicyTest,OrgStructureApplicationServiceTest,OrgStructureServiceTest,UserAccessPolicyTest,UserChannelCodePolicyTest,UserMasterDataControllerTest,UserMasterDataServiceTest,CurrentUserControllerTest,CurrentUserApplicationServiceTest,UserDomainServiceTest,LegacyUserDomainFacadeTest,DataScopeAspectTest,DataScopePolicyTest,DataScopePolicyParityTest,UserCredentialPolicyTest,SysUserServiceTest,SysUserControllerTest" test`，196/0/0。
- 编译：`mvn -f .\backend\pom.xml -DskipTests test-compile`，PASS。
- 构建：`mvn -f .\backend\pom.xml -DskipTests package`，PASS。
- 重启：`powershell -NoProfile -ExecutionPolicy Bypass -File .\harness\scripts\commands\restart-compose.ps1 -Env real-pre -Scope backend`，PASS。
- 健康检查：`powershell -NoProfile -ExecutionPolicy Bypass -File .\harness\scripts\commands\verify-local.ps1 -Env real-pre -Scope backend`，PASS，`{"status":"UP"}`。
- 边界检查：`rg "import com\.colonel\.saas\.mapper|import com\.colonel\.saas\.entity" backend/src/main/java/com/colonel/saas/domain/user/policy/OrgAssignmentPolicy.java OrgEnrichmentPolicy.java OrgValidationPolicy.java` 无命中。
- Harness limits：PASS。

## 风险

- `OrgStructureApplicationService` 仍保留 Mapper 构造器签名并内部构造过渡适配器；这是兼容旧调用点的过渡状态。
- 策略包中 `CurrentUserPermissionPolicy` / `UserAccessPolicy` / `UserCredentialPolicy` 仍有实体级依赖，需后续判断是否属于领域模型迁移或持久化泄漏。
- `auth/service/*`、`domain/user/application/*`、`LegacyUserDomainFacade` 中 Mapper / legacy service 依赖仍需分类。
- 本轮未执行前端构建或 E2E，因为修改范围限定在后端用户域组织展示策略。
- 未提交、未 push；当前工作区存在大量既有未提交变更。

## 下一步

- U-7：分类 `auth/service/*`、`domain/user/application/*`、`LegacyUserDomainFacade` 的 Mapper / legacy service 依赖。
- 检查 `CurrentUserPermissionPolicy` / `UserAccessPolicy` / `UserCredentialPolicy` 的实体依赖是否需要端口或领域 record 收口。
