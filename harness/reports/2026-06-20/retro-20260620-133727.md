# Retro 2026-06-20 13:37

## 结论

PARTIAL

本轮完成 U-7 的一个可验证小切片：`OrgAssignmentPolicy` 不再直连持久化 Mapper，新增 `OrgNodeLookup` 端口与 `SysOrgNodeLookupAdapter` 过渡适配器。整体 DDD 收口仍未完成。

## 做对的事

- 先写端口边界测试并拿到 RED，避免直接重构后再补证明。
- 端口只暴露组织分配策略需要的最小事实，没有把 `SysDept` 泄漏进 policy。
- 旧应用服务构造器保持兼容，降低对既有 `OrgStructureServiceTest` 与应用服务测试的冲击。
- 报告目录满额前先移动同日旧报告到 `harness/reports/2026-06-20/`，保持 10/10/200 规则。

## 发现的问题

- `OrgEnrichmentPolicy` 仍是下一处策略层 Mapper 泄漏点。
- `OrgStructureApplicationService` 当前仍是 Mapper 到端口适配器的过渡工厂，后续需要决定是否改为直接注入端口。
- code-review-graph 增量更新仍显示节点/边更新为 0，可能对未跟踪文件的实体识别不足；已用 Maven 和 rg 补足证据。

## Harness 升级

本轮无需升级 Harness。现有 limits、restart、verify 脚本足够覆盖该小切片。

## 下一步

继续 U-7：`OrgEnrichmentPolicy` 端口隔离，优先拆出部门缓存查询与角色查询端口，并保持现有组织展示/审计备注行为不变。
