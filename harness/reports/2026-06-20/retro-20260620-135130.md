# Retro 2026-06-20 13:51

## 结论

PARTIAL

本轮完成 U-7 的 `OrgEnrichmentPolicy` 端口隔离。组织三策略 `OrgAssignmentPolicy`、`OrgValidationPolicy`、`OrgEnrichmentPolicy` 已不再直接导入持久化 Mapper / Entity。

## 做对的事

- 先把测试改成端口驱动并拿到 RED，失败点明确是 `OrgEnrichmentLookup` 不存在。
- 端口只暴露组织展示需要的最小读模型：有效组织节点与角色摘要。
- Mapper 读取和实体转换集中到 `SysOrgEnrichmentLookupAdapter`，策略层只保留展示规则。
- 复用了 `OrgNodeLookup.OrgNode`，避免重复定义组织节点读模型。
- 报告根目录满额前先移动旧报告到同日子目录，保持 10/10/200 规则。

## 发现的问题

- 应用服务仍是过渡工厂；后续可考虑改为注入端口，减少内部 new 适配器。
- 当前策略包仍有非组织三策略的实体依赖，需要逐个判断是可接受领域模型还是持久化泄漏。
- code-review-graph 对未跟踪文件的实体识别仍偏弱，Maven、rg 和源码复查仍是必要补证。

## Harness 升级

本轮无需升级 Harness。现有 limits、restart、verify 脚本足够覆盖该后端小切片。

## 下一步

继续 U-7：先做依赖分类清单，再从 `auth/service/*` 或剩余 policy 实体依赖中选择下一个最小切片。
