# Retro Summary - 2026-06-18 P0 Pending Reduction

- Time: 2026-06-18 16:50:00 +08:00
- Env: remote real-pre
- Branch: feature/ddd/DDD-VERIFY-001
- Commit: c275aa6e deployed evidence baseline; test/report update pending commit

## What Changed

- Updated P0 34 performance-dashboard validation to inspect current nested tracks:
  `/performance/summary` estimate/effective and `/dashboard/metrics` estimate/settle.
- Replaced the stale formula with current business formulas:
  estimate profit = income - expense - tech fee; effective profit = income - expense.
- Updated P0 33 sample-chain candidate lookup to assign an existing approved in-library unassigned product to `biz_staff` through the legal `/assignee` endpoint.
- Re-ran full P0 and reduced 33 from missing audit precondition to only real-order auto-completion pending.

## Evidence

- Single 34 step: PASS.
- Single 33 step: PASS at Playwright level; step summary remains PENDING only for real成交 auto-completion.
- Latest full P0: `/opt/saas/app/runtime/qa/out/real-pre-p0-20260618-164518`.
- Latest P0 status: PENDING.
- PASS steps: preflight, 08, 31, 34, 35, 36 cleanup plan.
- PENDING steps: 32 order attribution, 33 sample chain.

## Remaining Blockers

- Real orders exist, but attributed count and `pick_source` count are both zero.
- Active native mappings do not cover real order `colonel_buyin_id`; attribution replay dry-run returned `attributed=0` and `safeToUpdate=0`.
- Sample state machine is verified through `PENDING_TASK`; automatic completion still needs a real attributed成交 order.
- HTTPS/domain production readiness is still unverified: Nginx listens on 80 only.

## Harness Upgrade

- Needed: P0 preflight should include data-readiness probes for native mapping coverage, attributed orders, real成交 sample-completion candidates, and HTTPS/domain listener state.
