# Retro Summary - 2026-06-18 CI/CD Pipeline

## What Changed

- Converted Jenkins pipeline from deploy-by-default to CI-first.
- Added explicit `DEPLOY_REAL_PRE` gate for local real-pre deployment.
- Required `DEPLOY_BRANCH` only when deployment is requested.
- Added `CONFIRM_REAL_PROMOTION_WRITE` for real promotion write approval.
- Updated Jenkins deployment documentation to match the executable pipeline.
- Added `packages: [.]` to `frontend/pnpm-workspace.yaml` so pnpm 9 works in CI.
- Updated backend tests that were stale relative to current constructor, lock and job-log behavior.

## Evidence

- Backend full test suite: PASS, 2225 tests.
- Backend package: PASS.
- Frontend install/test/build: PASS, 640 tests.
- Safety check: PASS.
- Docker Compose config: PASS.
- Local real-pre app containers restarted: PASS.
- Local health check after restart: PASS.
- Harness limits: PASS.

## Diagnosis Notes

- Initial backend failures were test drift, not production-code failures.
- `SysConfigServiceEventTest` discovery failure was not stable; the target test and later full test passed after recompilation.
- `mvn clean test` was locally blocked by a Windows file lock on `backend/target/classes`, likely from Java language services; this should not be treated as a test failure.

## Harness Upgrade

- No Harness script upgrade was required.
- Evidence report format was kept within the existing report contract.

## Follow-Up

- Run the updated Jenkins job once on the target controller in CI-only mode.
- Run a controlled Jenkins deployment with `DEPLOY_REAL_PRE=true` and explicit `DEPLOY_BRANCH` when deployment is intended.
- Keep unrelated dirty worktree changes out of the CI/CD task commit.
- Before the next remote deploy, configure `LOGISTICS_KD100_CALLBACK_URL` in `/opt/saas/env/.env.real-pre`; Kuaidi100 subscription is enabled and deployment must block without this callback.
- Re-run `scripts/real-pre-startup-check.sh` before `scripts/deploy-real-pre.sh`; the check now catches missing Kuaidi100 callback configuration earlier.
