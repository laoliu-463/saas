# Retro Summary: ORDER-P0-DUAL-SOURCE-SYNC

**时间**: 2026-06-03 20:25 CST
**任务**: ORDER-P0-DUAL-SOURCE-SYNC — 将 6468 事实订单源接入订单同步
**前置**: ORDER-SETTLEMENT-DUAL-TRACK-VERIFY (B+)

---

## 本次完成事项

1. 接入 6468 事实源：`OrderSyncService.syncInstituteOrdersRecentWindow()` + `OrderSyncJob.syncInstituteOrdersRecent()`。
2. 增加独立调度与水位：`ORDER_SYNC_INSTITUTE`、`order:sync:institute_recent_last_time`、默认 24h 窗口、10 分钟 cron。
3. 复用双轨解析器：6468 使用 `applyInstituteFactToOrder()`，只写 `orderAmount/actualAmount/estimate_*`。
4. 按来源保护轨道：`syncSource=INSTITUTE` 时保留已有结算/有效轨；2704 更新时保留已有预估快照。
5. 补齐实体/Mapper：映射 `pay_time`、`order_create_time`，新增非持久化 `syncSource`。
6. 运行态验证：本地 real-pre 6468 拉取并入库 100 条真实订单。

## 关键决策

- 6468 不填充、不清零 `settle_amount/effective_*`，这些字段留给 2704。
- `mergeSettlementSnapshot()` 只在 incoming 结算字段为空或 0 时保留 existing，避免覆盖未来 2704 的有效 incoming 值。
- 未修改 Dashboard summary；后续另开 `ANALYTICS-DUAL-TRACK-SUMMARY-FIX`。
- 未执行远端部署；项目规则要求远端部署必须由用户明确授权。

## 证据链

- 单元/相关测试：37 + 43 tests，均 0 failures。
- 构建：`mvn -f backend/pom.xml -DskipTests package` PASS。
- 本地后端重启与健康：PASS。
- real-pre preflight：PASS。
- 运行日志：`ORDER_SYNC_INSTITUTE api=buyin.instituteOrderColonel fetched=100 inserted=100`。
- DB：`colonelsettlement_order count(*)=100`，100 条均有事实/预估字段，结算/有效字段未由 6468 写入。
- API：管理员 `/api/orders` 与 `/api/orders/unattributed` 均返回 `total=100`。

## 遗留事项

1. 远端部署与远端 DB 验证待用户明确授权。
2. 本轮上游样本没有 `pick_source`，渠道 mapping 命中后的正向可见性仍 PENDING。
3. 2704 本轮 `fetched=0`，真实结算轨合并需后续有结算样本时再验证。
4. 当前工作区为混合 dirty；由于 `agent-do.ps1` 最终会 `git add -A`，本次使用 Harness 子脚本完成验证，未自动提交。

## Harness 升级判断

本次不需要修改 Harness 规则。暴露的问题是当前 `agent-do.ps1` 的自动 `git add -A` 不适合混合 dirty 工作区，已有 Git 批次治理规则覆盖该风险；后续应按批次提交。
