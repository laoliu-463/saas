# Evidence Report (DDD-BASE-001)

## 1. 基础信息

* **时间：** 2026-06-09 12:05:00
* **环境：** Local (Windows, MINGW)
* **分支：** `feature/auth-system`
* **Commit Hash：** `7f72e51c`
* **Commit Message：** `DDD-BASE-001 add refactor safety toggles`
* **工作区状态：** 干净（DDD-BASE-001 提交后无其他未提交改动；另有 24 个未跟踪的 `harness/reports/*` 文件属于其他 audit 任务，不属于本任务范围）

---

## 2. 执行结果

* **编译构建：** 通过
  * `mvn -Dtest=DddRefactorPropertiesTest test` — Tests run: 2, Failures: 0, Errors: 0, Skipped: 0（Time elapsed: 0.188s），BUILD SUCCESS
  * `mvn test`（前次会话全量回归）— Tests run: 1781, Failures: 0, Errors: 0, Skipped: 0，BUILD SUCCESS（4:05）
* **Docker 状态：** 跳过（本任务不涉及容器/服务重启）
* **健康检查：** 跳过（无服务行为变更）
* **业务验证：**
  * `git diff --check`：通过（无多余空白字符）。
  * `git show --stat 7f72e51c` — 5 files changed, 181 insertions(+)。
  * `git diff 7f72e51c~1 7f72e51c -- backend/src/main/resources/application.yml` — 末尾追加 14 行 `ddd.refactor.*` 段，无任何已有键值改动。
* **是否远端部署：** 否
* **code-review-graph 影响面：**
  * 改动的函数/类：**0**
  * 受影响 flow：**0**
  * 测试空缺：**0**
  * 整体 risk score：**0.00**

---

## 3. 变更文件清单（精确 5 个，无任何额外文件）

| 文件 | 类型 | 增减 |
| --- | --- | --- |
| `backend/src/main/resources/application.yml` | 修改（追加） | +14 |
| `backend/src/main/resources/application-test.yml` | 修改（追加） | +14 |
| `backend/src/main/resources/application-real-pre.yml` | 修改（追加） | +14 |
| `backend/src/main/java/com/colonel/saas/config/DddRefactorProperties.java` | 新增 | +93 |
| `backend/src/test/java/com/colonel/saas/config/DddRefactorPropertiesTest.java` | 新增 | +46 |

---

## 4. application.yml 末尾配置快照（git diff HEAD~1 输出片段）

```yaml
ddd:
  refactor:
    enabled: ${DDD_REFACTOR_ENABLED:false}
    user-scope:
      enabled: ${DDD_REFACTOR_USER_SCOPE_ENABLED:false}
    order-attribution:
      enabled: ${DDD_REFACTOR_ORDER_ATTRIBUTION_ENABLED:false}
    performance-calc:
      enabled: ${DDD_REFACTOR_PERFORMANCE_CALC_ENABLED:false}
    product-display:
      enabled: ${DDD_REFACTOR_PRODUCT_DISPLAY_ENABLED:false}
    sample-policy:
      enabled: ${DDD_REFACTOR_SAMPLE_POLICY_ENABLED:false}
```

`application-test.yml` 与 `application-real-pre.yml` 末尾追加了完全一致的 14 行（每个 profile 独立验证，不互相覆盖）。

---

## 5. 关键测试输出摘录

```text
[INFO] Running com.colonel.saas.config.DddRefactorPropertiesTest
[INFO] Tests run: 2, Failures: 0, Errors: 0, Skipped: 0, Time elapsed: 0.188 s
[INFO] BUILD SUCCESS
```

* `defaultFlagsShouldAllBeFalse` — 根 + 5 个子开关默认全 `false`。
* `nestedSubFlagsShouldDefaultToFalseAfterConstruct` — 嵌套类独立构造后默认 `false`。

---

## 6. 结论与评估

* **结论：** `PASS`
* **评估：** 阶段性目标达成。5 个配置文件 / Java 类已落位，6 个开关全部默认 `false`，回归测试钉死了"误开"语义，**线上行为 0 变更**。
* **阶段定位：** Phase 0.5（安全开关基线）已就位。本证据**不**代表任何领域已通过 DDD 改造，仅代表开关层就绪。

---

## 7. 剩余风险

* **风险 1：默认值的"双重锚定"**。当前测试只验证 Java 端默认值；若未来有人仅修改 YAML 而忘了同步 Java 字段初始值，可能出现"测试通过但运行时为 true"的盲区。建议在 Phase 1 防护测试中追加 `@SpringBootTest` 集成用例，从真实 Spring 上下文读取 `DddRefactorProperties` 进行二次断言。
* **风险 2：根开关语义目前只写在 Javadoc 上，没有运行时强制约束**。后续 Phase 1 实现中，若子开关为 `true` 而根开关为 `false`，应拒绝启动或在日志中 WARN。当前任务**未**强制此约束（避免越界），由 Phase 1 落实。
