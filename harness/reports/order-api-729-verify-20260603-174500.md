# ORDER-API-729-VERIFY 抖店 61/2704 接口真实调用核查报告

> 任务 ID：ORDER-API-729-VERIFY
> 执行时间：2026-06-03 17:45
> 执行模式：只读核查（无代码/容器/数据库/部署修改）

---

## 1. 任务目标

核查本地 real-pre 环境是否真实调用抖店开放平台文档
https://op.jinritemai.com/docs/api-docs/61/2704
「团长分次结算订单 / 团长分析结算订单」对应接口。

---

## 2. Git Intake

- branch: `feature/auth-system`
- commit: `78bdf8fa docs(harness): GIT-BATCH-4-REPORTS record batch 4 state`
- dirty: 3 modified（与本任务无关，属于 P0-ORDER-001 前置工作）
  - `backend/src/main/java/com/colonel/saas/job/JobLockKeys.java`
  - `backend/src/main/java/com/colonel/saas/job/OrderSyncJob.java`
  - `backend/src/main/java/com/colonel/saas/service/OrderSyncService.java`
  - `backend/src/main/resources/application.yml`
- untracked: 3 报告文件

---

## 3. 接口目标

- 文档：https://op.jinritemai.com/docs/api-docs/61/2704
- 业务名称：团长分次结算订单 / 团长分析结算订单
- 目标方法/路径：
  - `buyin.colonelMultiSettlementOrders`
  - `/buyin/colonelMultiSettlementOrders`
  - `BuyinColonelMultiSettlementOrdersRequest`
- 排除接口：
  - `buyin.instituteOrderColonel`（旧团长订单接口）
  - 普通订单详情接口
  - mock gateway / fake gateway / local fallback

---

## 4. 代码检索证据

| 关键词 | 文件 | 行号 | 结论 |
|---|---|---|---|
| `colonelMultiSettlementOrders` | `OrderApi.java` | L45, L125-157 | 定义常量并实现调用 |
| `buyin.colonelMultiSettlementOrders` | `OrderApi.java` | L157 | `douyinApiClient.post(COLONEL_MULTI_SETTLEMENT_METHOD, params)` |
| `listColonelMultiSettlementOrders` | `RealDouyinOrderGateway.java` | L100, L197 | Gateway 层调用 OrderApi |
| `colonelMultiSettlementOrders` | `DouyinOrderGateway.java` | L33-36 | 接口注释明确 buyin.colonelMultiSettlementOrders |
| `buyin.instituteOrderColonel` | `OrderApi.java` | L44 | 仅作为 ORDER_LIST_METHOD 常量，用于 listSettlement 方法（旧接口，非主同步链路） |
| `instituteOrderColonel` | `backend/src` | 0 处生产调用 | 排除接口无生产调用 |
| `Mock/Fake/Fallback` | `backend/src/main` | 仅业务 fallback（非 mock gateway） | ProductQuickSampleService 的 fallback 与订单无关 |
| `DOUYIN_TEST_ENABLED` | `.env.real-pre` | L36 | `false` |
| `APP_TEST_ENABLED` | `.env.real-pre` | L56 | `false` |

---

## 5. 调用链证据

| 层级 | 文件 | 方法 | 证据 |
|---|---|---|---|
| Job | `OrderSyncJob.java` | `syncOrders()` | `@Scheduled(cron = "0 */10 * * * ?")`，L61；调用 `orderSyncService.syncLatestWindow()` |
| Job | `OrderSyncJob.java` | `syncPayRecent()` | `@Scheduled(cron = "0 */30 * * * ?")`，L91；调用 `orderSyncService.syncPayRecentWindow()` |
| Service | `OrderSyncService.java` | `syncLatestWindow()` | L119-132，计算时间窗口后调用 `syncByTimeRange()` |
| Service | `OrderSyncService.java` | `fetchSettlement()` | L462，调用 `douyinOrderGateway.listSettlement(request)` |
| Gateway | `RealDouyinOrderGateway.java` | `listSettlement()` | L94-110，调用 `orderApi.listColonelMultiSettlementOrders(...)` |
| API | `OrderApi.java` | `listColonelMultiSettlementOrders()` | L125-157，构建参数后调用 `douyinApiClient.post(COLONEL_MULTI_SETTLEMENT_METHOD, params)` |
| SDK/HTTP | `DouyinApiClient.java` | `post()` | 最终 HTTP POST 到 `https://openapi-fxg.jinritemai.com/buyin/colonelMultiSettlementOrders` |
| Mapper/落库 | `OrderSyncService.java` | `mapOrder()` | L517-548，从 rawPayload 提取字段并映射到 `ColonelsettlementOrder` 实体 |
| 持久化 | `OrderSyncPersistenceService` | `persistOrder()` | L427，upsert 到 `colonelsettlement_order` 表 |

---

## 6. 配置开关证据

- active profile: `real-pre`（`.env.real-pre` L7: `SPRING_PROFILES_ACTIVE=real-pre`）
- APP_TEST_ENABLED: `false`（`.env.real-pre` L56）
- DOUYIN_TEST_ENABLED: `false`（`.env.real-pre` L36）
- DOUYIN_REAL_UPSTREAM_MODE: `live`（`.env.real-pre` L37）
- ORDER_SYNC_ENABLED: `true`（`.env.real-pre` L44）
- gateway bean: `RealDouyinOrderGateway`（`@ConditionalOnProperty(name = "douyin.test.enabled", havingValue = "false", matchIfMissing = true)` L49）
- 是否 mock/fallback: **否**。`DOUYIN_TEST_ENABLED=false` 激活 `RealDouyinOrderGateway`，不注入 `TestDouyinOrderGateway`。

---

## 7. 运行态日志证据

容器：`saas-active-backend-real-pre-1`
状态：Up 5 hours (healthy)

### 7.1 确认真实调用的关键日志

```
2026-06-03T09:30:31.255Z  INFO  gateway=RealDouyinOrderGateway, upstreamMode=live,
  appKey=7623****7199, shopId=56591058, authId=7351155267604218149

2026-06-03T09:30:31.476Z  INFO  Douyin API call success,
  method=buyin.colonelMultiSettlementOrders

2026-06-03T09:30:31.476Z  INFO  Order sync completed,
  range=[1780471714, 1780478971], pages=0, fetched=0, created=0, updated=0, attributed=0

2026-06-03T09:30:31.478Z  INFO  OrderSyncJob done,
  window=[1780471714, 1780478971], pages=0, inserted=0, skipped=0
```

### 7.2 上游异常（非 mock/fallback，是真实上游超时）

```
2026-06-03T09:20:24.712Z  WARN  Douyin API retry scheduled,
  method=buyin.colonelMultiSettlementOrders, attempt=1/3, delayMs=400,
  reason=ResourceAccessException

2026-06-03T09:20:35.060Z  ERROR  Douyin API request failed,
  method=buyin.colonelMultiSettlementOrders, exception=ResourceAccessException

Caused by: org.springframework.web.client.ResourceAccessException:
  I/O error on POST request for
  "https://openapi-fxg.jinritemai.com/buyin/colonelMultiSettlementOrders":
  Remote host terminated the handshake

2026-06-03T09:40:34.975Z  ERROR  Douyin API business error,
  method=buyin.colonelMultiSettlementOrders, code=20001,
  subCode=dop.service-timeout-error,
  logId=20260603174034BB1033FD638798D4BFED,
  msg=服务内部访问超时，请重试
```

### 7.3 日志结论

- 定时任务每 10 分钟触发（`OrderSyncJob.syncOrders`）。
- 使用 `RealDouyinOrderGateway`，upstream 模式为 `live`。
- 实际 HTTP POST 到 `https://openapi-fxg.jinritemai.com/buyin/colonelMultiSettlementOrders`。
- 部分请求因 SSL 握手失败或上游内部超时而失败（抖音侧问题，非本地 mock）。
- 成功请求返回 0 条订单（当前窗口内无新订单数据）。

---

## 8. 数据库证据

- 最近订单数：`colonelsettlement_order` 表 **0 行**
- `colonel_order_settlement` 表 **0 行**
- `order_sync_dedup_claim` 表 **0 行**
- `order_detail` 表 **0 行**
- `order_performance` 表 **0 行**
- `pick_source_mapping` 表 **13 行**（商品域预置映射，与订单同步无关）
- 最近订单是否有 raw_payload：**无数据**（表为空）
- 是否包含 pick_source / settle_time / commission 等字段：**无法验证**（无数据行）

---

## 9. 最终结论

**B. 代码有接口实现，运行态已确认调用，但当前无订单数据落库**

详细判定：

| 判定条件 | 结果 |
|---|---|
| 代码中存在 `buyin.colonelMultiSettlementOrders` | **是**（`OrderApi.java` L45, L157） |
| real-pre 配置下 `DOUYIN_TEST_ENABLED=false` | **是**（`.env.real-pre` L36） |
| `APP_TEST_ENABLED=false` | **是**（`.env.real-pre` L56） |
| 没有走 mock/fallback | **是**（注入 `RealDouyinOrderGateway`） |
| OrderSyncJob 调用 RealDouyinOrderGateway | **是**（日志证明） |
| 日志证明执行 SDK/API 请求 | **是**（日志含 `method=buyin.colonelMultiSettlementOrders`，URL 为 `https://openapi-fxg.jinritemai.com/buyin/colonelMultiSettlementOrders`） |
| 订单落库 | **否**（表为空，API 返回 0 条或上游超时） |

结论说明：

- 代码层完整实现了 `buyin.colonelMultiSettlementOrders` 调用链。
- 运行态日志证明确实在真实调用抖店 API（非 mock）。
- 但当前订单表为空，原因是：
  1. 成功调用时 API 返回 0 条订单（当前窗口内无新结算订单）；
  2. 部分调用因上游 SSL 握手失败或 `dop.service-timeout-error` 而失败。
- 因此判定为 **B 类**：代码和运行态已确认真实调用，但尚无数据落库证据。

> 如果后续有真实通过系统转链产生的订单，API 返回数据后订单将正常落库，可升级为 A 类。

---

## 10. 风险与下一步

### 当前风险

1. **上游不稳定**：最近 6 小时内多次出现 SSL 握手失败和 `dop.service-timeout-error`，属抖音侧问题。
2. **订单表为空**：无法验证落库字段完整性（`pick_source`、`settle_time`、`commission`、`colonel_buyin_id` 等）。
3. **归因未验证**：无订单数据，无法验证 `default_channel_id`、`default_recruiter_id` 归因逻辑。

### 下一步建议

1. **监控上游稳定性**：持续观察 `buyin.colonelMultiSettlementOrders` 的成功率；如果持续超时，联系抖店技术支持。
2. **扩大同步窗口**：手动触发一次大范围时间窗口同步（如过去 7 天），验证是否能拉到历史订单。
3. **等待真实样本**：等真实通过系统转链产生的订单出现后，重新核查落库字段完整性。
4. **升级为 A 类**：一旦有订单数据落库，执行 `select * from colonelsettlement_order limit 5` 验证 `extra_data`（raw_payload）字段，确认后可升级为 A 类结论。

---

## 附录：核查范围与限制

- 未修改 Java / Vue / SQL / Docker / env 文件。
- 未重启容器。
- 未执行数据库写操作。
- 未执行 git add / commit / push。
- 所有结论均有文件路径、代码行号或命令输出作为证据。
- 无法确认订单落库字段（因表为空），已在结论中明确标注。
