# Evidence Report - 20260603-101503

| 项目 | 内容 |
| --- | --- |
| 时间 | 2026-06-03 10:15:03 |
| 环境 | local real-pre |
| 分支 | feature/auth-system |
| commit hash | 1ac7796f（任务开始基线，尚未提交本轮修改） |
| 工作区是否干净 | 否。任务开始前已有 Harness 门禁相关 dirty 变更；本轮新增用户域 U-2.5-B 修改 |
| 任务报告 | `harness/reports/user-domain-u2_5b-dept-type-minimal-fix-20260603-101503.md` |
| 构建结果 | PASS：`mvn -f backend/pom.xml "-DskipTests" package` BUILD SUCCESS |
| 单元测试 | PASS：`DeptTypeTest,SysDeptServiceTest,SysDeptControllerTest` 共 12 tests 通过；`DataScopeAspectTest` 共 4 tests 通过 |
| Docker 状态 | PASS：real-pre backend/frontend/postgres/redis 均 healthy |
| 健康检查结果 | PASS：`verify-local.ps1 -Env real-pre -Scope backend` 返回 backend statusCode=200, body `{"status":"UP"}` |
| 业务验证结果 | PARTIAL：完成用户域 dept_type 单测与后端健康；后端全量测试仍有 6 个失败；未重启容器，运行态未加载新 jar |
| 是否部署远端 | 否 |
| 远端健康检查结果 | 未执行，用户未要求远端部署 |
| 结论 | PARTIAL |
| 剩余风险 | 后端全量 `mvn test` 失败（1671 tests, 6 failures）；未重启容器；未执行 real-pre DB 写库；历史 `sys_dept.dept_type` 仍为只读对账 `department:3`；跨域 Mapper、PerformanceAccessScope、DataScopeResolver、UserDomainFacade、sys_role_menu FK CASCADE 均未处理 |

## 命令证据摘要

- RED：`mvn -f backend/pom.xml "-Dtest=SysDeptServiceTest" test` 失败，证明旧常量体系仍生效。
- GREEN：`mvn -f backend/pom.xml "-Dtest=DeptTypeTest,SysDeptServiceTest,SysDeptControllerTest" test` 通过。
- 补充：`mvn -f backend/pom.xml "-Dtest=DataScopeAspectTest" test` 通过。
- 构建：`mvn -f backend/pom.xml "-DskipTests" package` 通过。
- 全量：`mvn -f backend/pom.xml test` 失败，1671 tests / 6 failures / 0 errors。失败类为 `SysUserServiceTest`、`SysUserControllerTest`、`CommissionRuleServiceTest`、`CommissionRuleControllerTest`。
- Harness：`safety-check.ps1 -Env real-pre -Scope code -DryRun` 不支持该 Scope；等价 `-Scope backend -DryRun` 通过。
- Harness：`verify-local.ps1 -Env real-pre -Scope code` 不支持该 Scope；等价 `-Scope backend` 通过。
- SQL：只读查询 `sys_dept` 当前分布仍为 `department:3`。

## Retro

本次无需 Harness 升级；仅更新任务状态、决策索引、changelog 和 evidence。
