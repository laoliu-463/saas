# ORDER-DETAIL-TAB-FIX-001 — 订单明细表前端字段扩列与“渠道”文案统一

## 1. 任务范围

- 环境：本地 `real-pre`
- 分支：`feature/auth-system`
- 相关提交：`db934d99 fix: order detail tab columns and channel wording`
- 本任务范围：仅修复数据平台订单明细 Tab 前端展示，将订单级明细表对齐为 V1 合同 16 列，并统一“渠道”文案。
- 明确未做：未修改后端 Java 业务代码、SQL migration、Docker/compose、订单同步、订单归因、业绩计算、`performance_records`、`settle_amount`、`pick_source` 逻辑。

## 2. 修改文件清单

| 文件 | 说明 |
| --- | --- |
| `frontend/src/views/data/OrderDetailTab.vue` | 表格由 8 列扩展为 16 列；“媒介”改为“渠道”；新增订单状态与 7 个金额列；金额统一人民币元格式；null 显示 `-`，真实 0 显示 `¥0.00`；保留商品图片信息展示。 |
| `frontend/src/views/data/OrderDetailTab.test.ts` | 补充 16 列表头、渠道文案、无媒介、无毛利、金额格式、null/0、商品图片、空合作方/招商/渠道兼容字段测试。 |
| `frontend/src/api/data.ts` | 增加 `OrderDetailRecord` / `OrderDetailPageResponse` 类型，字段与后端 `OrderDetailVO` 对齐；接口路径和运行行为不变。 |
| `harness/reports/evidence-20260604-221838.md` | `agent-do.ps1 -Scope frontend` 自动生成 evidence。 |
| `harness/reports/content-retire-20260604-221838.md` | `agent-do.ps1` 自动生成内容维护候选报告。 |
| `harness/reports/retro-20260604-221901.md` | `agent-do.ps1` 自动生成 retro，目前未提交。 |

执行偏差：用户要求“本任务完成后先不要自动提交”，但 `agent-do.ps1` 内置 Git 步骤自动创建并推送了 `db934d99`。本报告之后未再执行提交或回滚。

## 3. 字段映射矩阵

| 最终表头 | 前端读取字段 |
| --- | --- |
| 订单ID | `orderId` / `order_id` |
| 活动信息 | `activityName` / `activity_name`；`activityId` / `activity_id` |
| 商品信息 | `productImage` / `product_pic` 等图片字段；`productName`；`productId`；店铺、数量、佣金率、服务费率沿用既有展示 |
| 合作方信息 | `partnerName` / `shopName` / `merchantName`；`colonelName` / `colonelUserName` |
| 推广者 | `talentName` / `authorName`；`talentDouyinId` / `talentId` / `talentUid`；`videoId` |
| 渠道 | `channelName` / `channelUserName`，兼容读取 `mediaName`，UI 不使用“媒介”文案 |
| 招商 | `recruiterName`，兼容 `colonelName` / `colonelUserName` |
| 订单状态 | `orderStatusText` / `settleStatusText` / `orderStatus` |
| 订单额 | `payAmount` / `actualAmount` / `orderAmount`；`settleAmount` |
| 服务费收入 | `estimateServiceFee` / `serviceFeeIncome`；`effectiveServiceFee` |
| 技术服务费 | `estimateTechServiceFee` / `technicalServiceFee`；`effectiveTechServiceFee` |
| 服务费支出 | `estimateServiceFeeExpense` / `serviceFeeExpense` / `serviceFeeCost`；`effectiveServiceFeeExpense` |
| 服务费收益 | `estimateServiceProfit` / `serviceFeeProfit`；`effectiveServiceProfit` |
| 招商提成 | `estimateRecruiterCommission` / `recruiterCommission`；`effectiveRecruiterCommission` |
| 渠道提成 | `estimateChannelCommission` / `channelCommission`；`effectiveChannelCommission` |
| 订单时间 | `payTime` / `orderCreateTime`；`deliveryTime`；`settleTime`；`expireTime` |

## 4. 16 列最终表头证据

Vitest 断言 `headers === finalHeaders`，最终表头为：

```text
订单ID, 活动信息, 商品信息, 合作方信息, 推广者, 渠道, 招商, 订单状态,
订单额, 服务费收入, 技术服务费, 服务费支出, 服务费收益, 招商提成, 渠道提成, 订单时间
```

real-pre 页面 smoke：

- 结果：`runtime/qa/out/order-detail-tab-fix-001-20260605-020048/result.json`
- 左侧截图：`runtime/qa/out/order-detail-tab-fix-001-20260605-020048/order-detail-tab.png`
- 金额列截图：`runtime/qa/out/order-detail-tab-fix-001-20260605-020048/order-detail-tab-amount-columns.png`
- `/api/data/orders/detail`：HTTP 200，`total=933`，首屏 `recordCount=20`
- 页面提取表头：16 个目标字段全部存在，`missingHeaders=[]`

说明：审查报告基线是 882 单；本次页面 smoke 时 real-pre 订单总数已增长到 933，属于运行态真实同步继续增长，不是本任务代码改变。

## 5. “媒介” → “渠道”修复证据

- `OrderDetailTab.vue` 表头为 `title: '渠道'`。
- `OrderDetailTab.test.ts` 断言 `not.toContain('媒介')`。
- real-pre smoke `forbiddenText.hasMediaWording=false`。
- 渠道字段兼容读取 `mediaName`，但 UI 表头和页面文案不新增“媒介”别名。

## 6. 金额字段展示证据

单测覆盖：

- `payAmount=20.00` 展示 `¥20.00`
- `estimateServiceFee=0.60` 展示 `¥0.60`
- `estimateTechServiceFee=0.06` 展示 `¥0.06`
- `estimateServiceProfit=0.54` 展示 `¥0.54`
- `estimateServiceFeeExpense=0` / `estimateChannelCommission=0` 展示 `¥0.00`

页面 smoke 右侧截图覆盖：

- 订单额
- 服务费收入
- 技术服务费
- 服务费支出
- 服务费收益
- 招商提成
- 渠道提成

## 7. null 与 0 金额展示规则

- `formatMoney(null | undefined | '')` 返回 `-`
- `formatMoney(0)` 返回 `¥0.00`
- 单测覆盖 `settleAmount=null` 显示 `结算:-`
- 单测覆盖 `estimateServiceFee=null` 时显示 `预估:-`，且不误显示为 `¥0.00`

## 8. V1 不展示毛利证据

- 本次未新增任何毛利列。
- `OrderDetailTab.vue` 表头不包含“毛利”。
- `OrderDetailTab.test.ts` 断言页面文本不包含“毛利”。
- real-pre smoke `forbiddenText.hasGrossProfitWording=false`。

## 9. 测试与验证结果

| 验证项 | 结果 | 证据 |
| --- | --- | --- |
| TDD RED | PASS | 修改测试后首次 `npm run test -- OrderDetailTab.test.ts` 失败：现状为 8 列且第 6 列“媒介”。 |
| 前端 typecheck | PASS | `npm run typecheck` |
| OrderDetailTab Vitest | PASS | `npm run test -- OrderDetailTab.test.ts`，3 tests PASS |
| 前端 build | PASS | `npm run build`，仅 Vite chunk size warning |
| diff whitespace | PASS | `git diff --check` |
| safety-check dry run | PASS | `safety-check.ps1 -Env real-pre -Scope frontend -DryRun` |
| Harness frontend | PASS | `agent-do.ps1 -Env real-pre -Scope frontend`：build PASS，frontend-real-pre rebuild/recreate，`/healthz` PASS，`e2e:real-pre:p0:preflight` PASS |
| Evidence report | PASS | `harness/reports/evidence-20260604-221838.md` |
| 页面 smoke | PASS | `runtime/qa/out/order-detail-tab-fix-001-20260605-020048/result.json`；16 列、无“媒介/毛利”、人民币金额格式、截图均通过 |
| code-review-graph | REVIEWED | `detect_changes HEAD~1` 风险分 0.65；变更集中于前端展示和报告，无跨域业务逻辑修改 |

## 10. 未解决问题

- `ORDER-DOMAIN-AUDIT-001` 基线：882 单 100% 未归因仍为 `BLOCKED_BY_SAMPLE`。
- 仍需要业务侧真实系统转链订单样本，订单样本需带 `pick_source` 并命中 `pick_source_mapping`，才能继续验证渠道归因闭环。
- 本任务未修复、未绕过、未声称解决订单归因样本问题。

## 11. 是否可进入远端部署

- 本地 real-pre 前端构建、容器重启、健康检查、preflight、页面 smoke 均通过。
- 远端部署未执行，因为用户未明确要求远端部署。
- 从前端展示角度：可以进入远端部署准备。
- 从业务闭环角度：渠道归因闭环仍 `BLOCKED_BY_SAMPLE`，不能因本次前端展示修复而宣称业务闭环 PASS。

## 12. 结论

结论：`PASS_WITH_GIT_PROCESS_DEVIATION`

订单明细表前端展示已对齐 V1 16 列合同，“媒介”表头已统一为“渠道”，金额列按人民币元展示，null 与 0 的展示规则已由测试覆盖并在页面 smoke 中验证。剩余阻塞只与真实 `pick_source` 归因样本有关，不属于本任务修复范围。
