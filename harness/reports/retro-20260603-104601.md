# Retro - TEST-1 full backend failures fix

## 时间

2026-06-03 10:46:01

## 做了什么

- 复现指定失败集合，实际为 53 tests / 3 failures / 7 errors。
- 修复 4 个后端测试类的测试夹具/断言问题。
- 验证单类、组合、U-2.5-B 定向、全量后端测试、package、diff check、Harness backend safety-check 和 verify-local。

## 有效做法

- 先从 surefire 报告提取每个失败，再读对应源码，避免把 failures 直接归因到 U-2.5-B。
- 对 `CommissionRuleServiceTest` 复用既有 `OrderControllerTest` 的 MyBatis-Plus `TableInfoHelper` 初始化模式，避免改生产 service。

## 风险与后续

- 当前任务提示中的 `Scope=code` 与脚本实际 ValidateSet 不一致，后续任务提示应改为 `Scope=backend` 或更新脚本支持 `code`。
- 工作区存在任务前和来源不确定变更，提交前必须拆分。
- 本次无需 Harness 升级。
