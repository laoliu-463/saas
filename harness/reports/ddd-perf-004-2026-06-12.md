# DDD-PERF-004 Report

| Field | Value |
| --- | --- |
| task_id | DDD-PERF-004 |
| date | 2026-06-12 |
| branch | feature/ddd/DDD-PERF-004 |
| status | COMPLETED_TARGETED |

## Summary

Added `OrderPerformanceQueryFacade` and `LegacyOrderPerformanceQueryFacade`
to expose performance enrichment through a slim `OrderPerformanceDTO`
projection. Order list / detail / data-platform consumers should depend on
this facade instead of `PerformanceRecordMapper` directly.

## Changes

| Component | Action |
| --- | --- |
| `OrderPerformanceDTO` | New BFF-friendly DTO with spec fields |
| `OrderPerformanceBatchResponse` | New batch response wrapper |
| `OrderPerformanceQueryFacade` | New interface, 5 methods |
| `LegacyOrderPerformanceQueryFacade` | Delegates to existing services, maps DTOs |
| `LegacyOrderPerformanceQueryFacadeTest` | 8 unit tests covering happy + edge paths |

## Tests

```
mvn -Dtest=LegacyOrderPerformanceQueryFacadeTest test
```

Result: 8 tests run, 0 failures, 0 errors, 0 skipped.

## Out of scope (deliberately deferred)

- `OrderController` / `OrderService` rewiring — tracked in a separate wiring
  ticket to keep the diff focused.
- `migrate-all.sql` service_fee_expense columns — also deferred (see PERF-004
  handover risk note).

## Baseline risk

`mvn clean test` carries pre-existing baseline failures unrelated to this
task. Must be triaged before CLEAN / VERIFY phase.