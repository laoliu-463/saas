# DDD-BASE-002 Evidence: Characterization Baseline

任务ID: DDD-BASE-002
时间: 2026-06-10T10:00:00+08:00
执行�? Claude (Harness)
分支: feature/auth-system

## 1. 改动文件清单

### 新增

| 路径 | 用�?| 行数 |
| --- | --- | --- |
| `backend/src/test/java/com/colonel/saas/service/CharacterizationBaselineTest.java` | 10 个行为冻结测�?| 493 |
| `harness/reports/ddd-base-002-characterization.md` | 任务主报�?| 120+ |

### 修改

| 路径 | 用�?| 变更说明 |
| --- | --- | --- |
| `backend/src/test/java/com/colonel/saas/testsupport/BaseIntegrationTest.java` | 测试基座 | `applySchemaCompatibilityPatches()` 新增 `dashboard_performance_daily` �?CREATE（idempotent�?|

## 2. 验证证据

### 2.1 Targeted run

```bash
cd backend
mvn test -Dtest=CharacterizationBaselineTest -DfailIfNoTests=false
```

输出�?
```
[INFO] Tests run: 10, Failures: 0, Errors: 0, Skipped: 0, Time elapsed: 43.11 s
[INFO] BUILD SUCCESS
```

10 个测试方法全�?PASS�?
- test01_UserLoginAndPermissionsBaseline
- test02_ProductLibraryBaseline
- test03_LinkConversionBaseline
- test04_SampleLifecycleAndAttributionCompleteBaseline
- test05_OrderAttributionAndCalculationsBaseline
- test06_DashboardSummaryBaseline
- test07_QuickSampleApplyCharacterizationBaseline
- test08_OrderListAndActivityProductCharacterizationBaseline
- test09_OrderDetailCharacterizationBaseline
- test10_PerformanceFormulaEdgeCasesCharacterizationBaseline

### 2.2 全量测试

```bash
cd backend
mvn test
```

输出�?
```
[INFO] Tests run: 1806, Failures: 0, Errors: 0, Skipped: 0
[INFO] BUILD SUCCESS
[INFO] Total time:  05:22 min
[INFO] Analyzed bundle 'colonel-saas' with 727 classes
```

- 1806 个测试全 PASS
- 0 失败 / 0 错误 / 0 跳过
- JaCoCo 727 classes 已分�?- 行为冻结测试与既�?1796 个测试无冲突

## 3. 行为冻结�?�?代码引用

| 测试 | 关键断言 | 行号 |
| --- | --- | --- |
| test01 | `assertThat(current.roleCodes()).contains("CHANNEL_LEADER")` | CharacterizationBaselineTest.java:84-99 |
| test02 | `assertThat(page.getRecords()).extracting("id").contains("10901825L")` | CharacterizationBaselineTest.java:113-128 |
| test03 | `assertThat(mapping).hasSize(1); pickSource.startsWith("MOCK")` | CharacterizationBaselineTest.java:142-159 |
| test04 | `assertThat(pending.status()).isEqualTo(6)` (3�?�? 三段跃迁) | CharacterizationBaselineTest.java:179-194 |
| test05 | `assertThat(perf.getServiceProfit()).isEqualTo(900L)` (大额断言) | CharacterizationBaselineTest.java:212-227 |
| test06 | `assertThat(summary.getOrderCount()).isGreaterThan(0)` | CharacterizationBaselineTest.java:241-256 |
| test07 | `assertThat(apply.getItems()).hasSize(1); apply.getSuccess()` | CharacterizationBaselineTest.java:270-285 |
| test08 | `assertThat(records).extracting("orderId").contains("MOCK_SEED_TALENT_D_ORDER")` | CharacterizationBaselineTest.java:299-314 |
| test09 | `assertThat(detail.getDiagnosis()).isNotNull(); detail.getDiagnosis().getReasonCode() == null` | CharacterizationBaselineTest.java:380-394 |
| test10 | `assertThat(estimateServiceProfit).isNotEqualTo(effectiveServiceProfit)` | CharacterizationBaselineTest.java:478-485 |

## 4. 关键 Schema 补丁

`BaseIntegrationTest.java:141-149`�?
```java
jdbcTemplate.execute("""
        CREATE TABLE IF NOT EXISTS dashboard_performance_daily (
            stat_date DATE NOT NULL PRIMARY KEY,
            order_count BIGINT NOT NULL DEFAULT 0,
            order_amount BIGINT NOT NULL DEFAULT 0,
            service_fee_net BIGINT NOT NULL DEFAULT 0,
            updated_at TIMESTAMP NOT NULL DEFAULT CURRENT_TIMESTAMP
        )
        """);
```

**原因**：`OrderSyncedEventListener` 同步触发 `DashboardPerformanceSummaryService.applyOrderSynced()` 写入 `dashboard_performance_daily`�?Testcontainer 复用�?init 脚本不包含此表（real-pre �?`alter-v1-gaps-20260522.sql`），导致 `seedAll` 期间监听器抛
"relation does not exist" 异常。本补丁**�?*新增�?+ idempotent + 不修�?init 脚本，符�?DDD-BASE-002 约束�?
## 5. 关键代码引用

### 5.1 业绩双轨不对称（test10 Case3�?
`PerformanceCalculationService.java` �?`calculateTrack(effectiveServiceFee, 0L, expense, talentCommission, ...)` 调用 �?effective 轨显式传 `0L` 作为 tech-fee 替代�?
```java
// estimate track uses real estimateTechServiceFee
calculateTrack(estimateServiceFee, estimateTechServiceFee, expense, ...)
// effective track passes 0L
calculateTrack(effectiveServiceFee, 0L, expense, ...)
```

### 5.2 DiagnosisInfo 永远非空

`OrderQueryService.java`�?
```java
DiagnosisInfo diagnosis = new DiagnosisInfo();  // 永远构�?if (order.getStatus() == OrderStatus.STATUS_UNATTRIBUTED) {
    diagnosis.setReasonCode(...);  // 仅未归因订单填充
    diagnosis.setReasonText(...);
    diagnosis.setSuggestion(...);
}
```

## 6. 收尾验证

- `git status`：仅上述三处变更（`A CharacterizationBaselineTest.java`、`M BaseIntegrationTest.java`、`A ddd-base-002-characterization.md`�?- `git diff --stat`：变更可控，**无生产代码改�?*
- 后续 commit 将使用类�?`test`（按项目 git-workflow 规范�?
## 7. 结论

- **状�?*：PASS
- **覆盖**�?0 个核心闭环行为冻�?+ 1806 个回归测试全�?- **可重复�?*：`mvn test -Dtest=CharacterizationBaselineTest` 43.11s 跑完
- **下一�?*：提�?DDD-BASE-002 commit，启�?DDD-USER-001
