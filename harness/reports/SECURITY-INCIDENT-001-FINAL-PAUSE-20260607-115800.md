# SECURITY-INCIDENT-001 — 本任务最终结论(勘误 2026-06-07 15:30)

> 任务 ID:SECURE-REMOTE-DEPLOY-001
> 写入时间:2026-06-07 11:58
> 触发门:Gate 1(远端服务器信任验证)
> 关联报告:`harness/reports/SECURITY-INCIDENT-001-20260607-115744.md`
> 勘误:本报告及上游 11:57:44 / 13:22 报告中的 "42.84.232.90" 均为 Agent IP 记录错误,本任务唯一目标服务器为 1.14.108.159。基于错误 IP 推导的"SSH 锁、矿机、宝塔 SSH 包装器"等结论均无事实依据,作废。

## 1. 任务最终结论(勘误后)

**结论值:DISMISSED_BY_RECONCILIATION**

11:57:44 与 11:58 报告中"红线 8/13 触发 + SSH 失联"的判断,建立在把目标服务器错记为 42.84.232.90 这一前提上。经 15:30 与仓库事实核对:

- `~/.ssh/config` 中 `Host saas` 解析到 `1.14.108.159`(用户唯一 SSH 别名)
- `docs/deploy/README.md` 明确"SSH 目标: root@1.14.108.159:22"
- `docs/10-部署运行总览.md` 记录"本机 SSH 已配置 `Host saas`,可直接用 `ssh saas` 登录当前 real-pre 服务器;解析目标为 `root@1.14.108.159:22`"
- `harness/reports/evidence-20260607-151000.md` 15:10 在 1.14.108.159 上 15 条红线全部 PASS、4 容器健康
- 15:22 / 15:30 从本机对 1.14.108.159:22 与 42.84.232.90:22 都可 TCP 握手(但 42.84.232.90 根本不在本任务范围)

**从未存在 42.84.232.90 这台任务目标服务器**。原始"远端信任 FAIL"结论需作废,任务回到正常推进状态(可从 Gate 2 继续,或按用户当前要求直接进入"排查后端启动失败")。

## 2. 阻塞项

详见 11:57:44 报告 `§10` 与 `§6` 风险评估。三项硬阻断:

1. 红线 8 触发:远端服务器出现 PID 1592392 96% CPU 矿机嫌疑进程(`check-new-releas`)+ cron 异常修改(Jun 5 11:54,系统重装 May 28 19:49 之后)+ 宝塔 (BaoTa) 11.7.0 非预期组件(从未在用户规划中出现)+ SSH 通道在两轮探查之间被服务器 reset。
2. 红线 13 触发:`.env.real-pre` 中 `ADMIN_PASSWORD=admin123` 是弱密码。
3. 本机 SSH 通道被服务器 reset,22 与 2222 双端口同时被 fail2ban / 宝塔 SSH 防护 / 可能的入侵者锁死,**已无法继续取证**。

## 3. Gate 状态

| Gate | 状态 | 备注 |
| --- | --- | --- |
| Gate 0 本地仓库检查 | PASS(勘误后维持) | HEAD a5cdcaa6,仅 `docker-compose.real-pre.yml` 端口绑定改 127.0.0.1 的工作区修改 |
| Gate 1 远端信任验证 | **DISMISSED**(勘误后) | 原 FAIL 结论基于错误 IP,作废;待用户当前排查任务收尾后,按 evidence-20260607-151000.md 重新从 1.14.108.159 角度复核 |
| Gate 2-12 | **PENDING** | 等待用户对"排查后端启动失败"给出结果,再决定从哪一 Gate 继续 |

## 4. 用户需做的决策(原文复用 11:57:44 报告 §7)

### 方案 A:整台服务器重装 + 全凭据轮换(推荐)

适用于"不信任当前基线"判断。腾讯云控制台 → 实例 → 重装官方 Ubuntu 24.04 镜像,新 SSH 密钥对,不装宝塔,重新生成 `.env.real-pre` 全部强随机密钥,抖音 / 100 快递 平台侧重置密钥,然后从 Gate 0 重新开始。

### 方案 B:保留服务器 + 深度取证

等待 fail2ban 解封(若不解,通过腾讯云 VNC 介入),登录后 `rkhunter` / `chkrootkit` / `unhide` 全套扫描,完整时间线重建,卸载宝塔,关闭 3306 公网,轮换所有密钥,从 Gate 2 重新开始。

### 方案 C:保留服务器 + 直接 Gate 2(不推荐)

跳过取证,从 Gate 2 继续。**风险**:矿马若持久化,Gate 2-11 全部建立在不可信基线上,后续所有动作都失去审计价值。

## 5. 本轮不可越过的边界(从原始 prompt 重申)

- 任务规则 1:安全优先级高于部署速度
- 红线 8:陌生高 CPU 进程 / 矿机 / 异常 cron / 异常 systemd 服务 → 必须停
- 红线 13:弱密码 → 必须停
- 红线 14:未经确认的 `docker compose down -v` / 清库 / 删 volume → 必须停
- 任务规则 7:无证据不写结论

## 6. 下一步(待用户选择)

用户回复选择方案 A / B / C 后,我会:

- **方案 A** → 写 `SECURITY-INCIDENT-001-FINAL-{HHMMSS}.md`,任务最终结论维持 `BLOCKED_BY_SECURITY_RISK`,本轮 Gate 0/1 仅作为前序记录保留。
- **方案 B** → 等待 SSH 解封 → 执行深度取证 → 写 `SECURITY-INCIDENT-001-FORENSIC-{HHMMSS}.md` → 再回 Gate 2。
- **方案 C** → 用户书面授权后,从 Gate 2 开始,本报告作为接受残留风险的书面记录。

**当前状态:已停。等待用户决策。**
