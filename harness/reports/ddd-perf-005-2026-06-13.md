# DDD-PERF-005 Evidence

## 结论
PARTIAL

## 证据
- **文件**:
  - `backend/src/main/java/com/colonel/saas/domain/performance/application/ExclusiveMerchantApplicationService.java`
  - `backend/src/test/java/com/colonel/saas/domain/performance/application/ExclusiveMerchantApplicationServiceTest.java`
  - `harness/reports/evidence-20260613-134053.md`
- **命令**:
  - `mvn -Dtest=ExclusiveMerchantApplicationServiceTest test`
    - RED: 新增 SQL 口径测试先失败，证明旧实现仍读 `colonelsettlement_order/user_id/settle_colonel_commission`。
    - GREEN: 修改后 4/4 通过。
  - `mvn -Dtest=ExclusiveMerchantPolicyTest,PerformanceAttributionPolicyTest,ExclusiveMerchantApplicationServiceTest test`
    - 10/10 通过。
  - `mvn -Dtest=ExclusiveMerchantServiceTest,ExclusiveMerchantQueryServiceTest,ExclusiveEvaluateJobTest,ExclusiveMerchantPolicyTest,PerformanceAttributionPolicyTest,ExclusiveMerchantApplicationServiceTest test`
    - 29/29 通过。
  - `mvn -Dtest=DddCrossDomainMapperGuardTest,SysConfigServiceEventTest test`
    - 5 run, 0 failures, 1 skipped。
  - `powershell -File harness/scripts/commands/agent-do.ps1 -Env real-pre -Scope full ...`
    - 后端 build PASS。
    - 前端 `npm ci` 因 `happy-dom` node_modules 文件锁 `EPERM` 失败，full scope 未完成。
  - `powershell -File harness/scripts/commands/restart-compose.ps1 -Env real-pre -Scope backend`
    - backend-real-pre 重新构建并重启。
  - `powershell -File harness/scripts/commands/verify-local.ps1 -Env real-pre -Scope backend`
    - backend health 200 `{"status":"UP"}`。
- **接口**:
  - `GET http://127.0.0.1:8081/api/system/health` PASS。
- **日志**:
  - 证据报告：`harness/reports/evidence-20260613-134053.md`。

## 修改摘要
- `ExclusiveMerchantApplicationService` 的独家商家判定统计从订单表切到业绩域 `performance_records`。
- 统计口径使用 `default_recruiter_user_id + partner_id + effective_service_fee + settle_time`。
- 招商部门通过 `UserDomainFacade.getUsersByIds()` 批量补齐，避免新增用户 Mapper 依赖。
- `PerformanceAttributionPolicy` 保持独家商家只覆盖最终招商，不覆盖最终渠道。

## 风险
- full scope 未通过，原因是前端 `npm ci` 在 Windows 上删除 `frontend/node_modules/happy-dom/...` 时被文件锁拒绝；这不是后端编译失败，但阻断 full DoD。
- `DDD-TALENT-004` 提交 `46c0aa64` 尚未合入当前 `DDD-PERF-005` HEAD，独家达人链路不能在当前分支声明完成。
- 当前工作区存在非本轮暂存 rename：`harness/handovers/DDD-SPRINT1-P0-handover.md -> harness/tasks/DDD-SPRINT1-P0-handover.md`，未纳入本报告结论。
- real-pre 未执行金额对账 SQL 与 dashboard 对账，只完成后端 targeted tests 和健康检查。

## 下一步
- 释放或重建前端 `node_modules` 文件锁后重跑 full `agent-do`。
- 合入或重新验证 `DDD-TALENT-004` 后，再继续独家链路整体验收。
- 如需提交，本任务应只提交 DDD-PERF-005 相关后端文件与本报告，避免混入 handover rename。
