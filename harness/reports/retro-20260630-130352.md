# Harness Retro Summary

## 1. Harness execution

- Time: 2026-06-30 13:03:52 +08:00
- Environment: real-pre
- Scope: full
- Branch: codex/ddd-user-role-application
- Commit: d605f67c
- Used agent-do.ps1: True
- Deploy remote requested: True

## 2. Repeated probing

Not collected automatically. Review command history manually if needed.

## 3. Script failures

No deploy-script failure. `agent-do.ps1 -DeployRemote true` completed with PASS.

Post-deploy manual validation found failures that were not executed by `agent-do`:

- `npm run e2e:real-pre:p0`: FAIL.
- `npm run e2e:real-pre:roles`: FAIL.

## 4. Verification sufficiency

Scope=full with remote deploy. Build, local restart, local health, local preflight, remote deploy and remote health were sufficient to prove deployment completion.

They were not sufficient to prove real-pre P0 acceptance. Full P0 and roles must remain separate gates until `agent-do` includes them or explicitly records them as skipped.

## 5. Harness issues exposed

`agent-do -DeployRemote true` reports PASS after remote health and preflight, but does not run the full documented post-deploy gates:

- `npm run e2e:real-pre:p0`
- `npm run e2e:real-pre:roles`

This can make a deployment look fully green unless the operator runs the additional checks manually.

## 6. Files to upgrade

- AGENTS.md: update only if execution rules changed.
- CURRENT_STATE.md: update if project state changed.
- TASK_ROUTING.md: update if routing gaps appeared.
- commands: update if script behavior failed or was ambiguous.
- evals: update if validation could not prove business result.
- skills: update if debugging workflow was incomplete.
- runbooks: update if humans/agents cannot execute directly.

## 7. Need new script

Consider adding an optional `-PostDeployFullE2E` flag or making deploy remote collect P0/roles results as PASS/PARTIAL/FAIL.

## 8. Need new Eval

Review manually.

## 9. Need update AGENTS.md

Review manually.

## 10. Must fix before next task

- Do not claim completion without evidence.
- Keep HARNESS_CHANGELOG.md current when Harness files change.
- Do not treat remote deploy PASS as real-pre P0 PASS.
