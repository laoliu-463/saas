# DDD-ORDER-006 任务报告

## Metadata

- Task: DDD-ORDER-006 split order query model from sync model
- Date: 2026-06-13
- Branch: feature/ddd/DDD-ORDER-006
- Scope: order query view model
- Remote deploy: false

## Change Summary

- 新增 `OrderQueryView` 和 `OrderDetailView` 作为订单只读响应模型。
- 新增 `OrderListAssembler` 和 `OrderDetailAssembler`，将旧 Entity/DTO 转换为只读 View。
- `OrderDomainFacade` / `LegacyOrderDomainFacade` 返回 query view，不再直接暴露订单 Entity。
- `OrderController` 列表、未归因列表、详情接口返回 query view，保持运行时字段结构兼容。

## Verification

| Check | Result | Evidence |
| --- | --- | --- |
| Main compile | PASS | Maven 重新编译 742 个 main source |
| Controller/routing tests | PASS | 30 tests, 0 failures, 0 errors |
| DDD order routing | PASS | `DddOrder003RoutingTest` 8/8 |
| Existing order controller regression | PASS | `OrderControllerTest` 22/22 |

Command:

```powershell
mvn clean "-Dtest=DddOrder003RoutingTest,OrderControllerTest" test
```

## Boundary Check

- 本任务只拆查询响应模型，不改订单同步入库模型。
- 未新增 migration。
- 未改订单金额、归因、提成、毛利计算规则。
- Controller 仍委派既有查询逻辑和 Facade，未新增跨域业务计算。

## Remaining Risk

- 当前验证覆盖 Controller/routing，尚未补充完整 JSON snapshot 测试。
- 最新 full Harness 结果需以本任务后续 `agent-do -Scope full` evidence 为准。

## Conclusion

PARTIAL_PASS until latest Harness full verification and evidence report are committed.
