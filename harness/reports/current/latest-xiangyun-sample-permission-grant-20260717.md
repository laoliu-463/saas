# 玄同寄样权限补充证据

## 元数据

- 时间：2026-07-17（Asia/Shanghai）
- 环境：远端 real-pre
- 远端分支：`feature/auth-system`
- 远端 commit：`1ed7dd2abef5bcce86221da06ad9db4d21c81446`
- 目标账号：`玄同 / 壮云`
- 目标用户 ID：`1c34b680-30b2-41ec-bdc7-2dde1f37e786`
- 业务数据：未修改

## 变更

通过管理员接口执行：

`PUT /api/users/1c34b680-30b2-41ec-bdc7-2dde1f37e786/roles`

请求结果：HTTP/API `200`。

为保留原有招商身份，角色列表设置为：

- `biz_staff` / 招商专员
- `channel_staff` / 渠道专员

当前数据库核验结果：两条角色关联均为有效，两个角色的数据范围编码均为 `1`。

## 验证

- 管理员登录接口：`200`
- 角色分配接口：`200`，`操作成功`
- 远端操作日志：记录该账号角色分配请求 `PUT .../roles`，响应码 `200`
- 远端 backend/frontend 容器：healthy
- `/api/system/health`：`UP`
- 未执行寄样创建请求，未写入寄样单，未调用第三方寄样接口

## 结论

PASS：玄同已保留 `biz_staff` 并新增 `channel_staff`，当前运行时角色权限可覆盖寄样提交入口。

## 注意事项

- 角色变更会在新登录时写入 JWT；玄同需要退出当前浏览器账号后重新登录。
- `channel_staff` 不只是单独的寄样权限，还包含渠道角色相关能力；当前系统没有独立的“仅寄样”用户权限编码。
- 实际寄样提交仍需满足商品入库、达人选择/认领、资格、地址和重复申请等业务校验；本次未写业务数据验证这些分支。

## Retro 结论

本次先核对现有 `biz_staff` 身份，再通过角色分配接口追加 `channel_staff`，避免覆盖原招商角色。后续若要只授予寄样动作，应将寄样动作从角色注解中拆为独立权限编码，而不是继续叠加业务角色。
