# DDD-ORDER-004 Report

| Field | Value |
| --- | --- |
| task_id | DDD-ORDER-004 |
| date | 2026-06-12 |
| branch | feature/ddd/DDD-ORDER-004 |
| status | PARTIAL_BLOCKED |

## Summary

Extracted default-only order attribution into `OrderDefaultAttributionPolicy`, `OrderAttributionInput`, `OrderDefaultAttributionResult`, and `OrderDefaultAttributionResolver`. `OrderAttributionRouter` routes to the new path when `ddd.refactor.enabled` and `ddd.refactor.order-attribution.enabled` are true; legacy `AttributionService` remains the default fallback.

This task is not DONE because backend full `mvn clean test` failed on unrelated existing baseline tests. Per task rules, later dependent tasks are blocked until the full backend baseline is green or explicitly accepted as a known baseline failure.

## Changes

| Component | Action |
| --- | --- |
| `OrderAttributionInput` | New input VO from order + raw payload |
| `OrderDefaultAttributionResult` | New result with default channel/recruiter only |
| `OrderDefaultAttributionPolicy` | Resolve + apply; no exclusive/final/commission logic |
| `OrderDefaultAttributionResolver` | IO via pick_source adapter + `ProductDomainFacade` |
| `OrderPickSourceMappingAdapter` | Order-domain pick_source lookup |
| `ProductDomainFacade` | Added assignee + activity default recruiter queries |
| `OrderAttributionRouter` | Switch-based delegation |

## Tests

```
mvn "-Dtest=OrderDefaultAttributionPolicyTest,OrderDefaultAttributionResolverTest,OrderAttributionRouterTest,OrderSyncServiceTest" test
mvn "-Dtest=DddCrossDomainMapperGuardTest,DddSlimOrder002RoutingTest" test
mvn clean test
```

Result:
- targeted ORDER-004 tests: PASS, 58 tests.
- architecture guard: PASS, 4 tests run, 1 maintenance test skipped.
- backend full test: FAIL, 2069 tests run, 3 failures, 55 errors, 1 skipped.

## API / sync behavior

- Default switches off → unchanged legacy attribution.
- Switches on → pick_source mapping for channel; product/activity recruiter via facade; product domain errors do not block order persist.

## Out of scope (unchanged)

- Performance exclusive merchant/talent application
- Final attribution / commission / gross profit
- Order list BFF

## Blocker

Full backend failures include `PendingActivationAccessPolicyTest`, `PerformanceAccessScopeTest`, `LogisticsGatewayHealthServiceTest`, `ProductDomainEventPublisherTest`, `ActivityPromotionSupportTest`, `SysUserMapperTest`, and `CharacterizationBaselineTest`. Failing classes are outside ORDER-004 scope; no rollback was applied because the ORDER-004 targeted and architecture tests passed.
