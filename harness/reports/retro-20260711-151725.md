# Harness Retro Summary

## 1. Harness execution

- Time: 2026-07-11 15:17:25 +08:00
- Environment: real-pre
- Scope: full
- Branch: codex/ddd-sample-query-completion
- Commit: 306a7f85
- Used agent-do.ps1: True
- Deploy remote requested: False

## 2. Repeated probing

- `agent-do.ps1` itself completed once; no repeated business probe was needed.
- The first post-run Harness check unexpectedly returned `PASS` while the active worktree had 61 report files. Evidence showed the script checked `D:\Projects\SAAS\harness` instead of the worktree.

## 3. Script failures

- `rtk` is unavailable in this environment, so read-only diagnostics used native PowerShell after the command-not-found evidence was captured.
- `check-harness-limits.ps1` had a hard-coded main-workspace path. A temporary 51-file Harness regression test reproduced the false `PASS` before the fix.
- Final review found that a real violation still returned process exit code 0. A second RED/GREEN cycle now enforces violation=1 and compliant=0.

## 4. Verification sufficiency

- Full `real-pre` verification for commit `306a7f85` passed: backend package, frontend type/build, Docker rebuild/restart, backend/frontend health, business preflight, evidence generation and both remote pushes.
- No remote `real-pre` deployment was requested or performed.
- The later Harness-only correction is covered by the RED/GREEN path regression test and the worktree-local 50/50/200 check.

## 5. Harness issues exposed

- Root cause: `check-harness-limits.ps1` embedded `D:\Projects\SAAS\harness`, so worktree execution inspected and rewrote the main workspace report.
- Fix: resolve the Harness root from `$PSScriptRoot`, return meaningful process exit codes, and run the regression through the current PowerShell host.
- Capacity cleanup: archive 12 superseded reports and ZIP the 517-line evidence. `harness/reports` now has exactly 50 direct files.

## 6. Files to upgrade

- Upgraded: `harness/scripts/check-harness-limits.ps1`.
- Added regression: `harness/scripts/tests/check-harness-limits-path.test.ps1`.
- Added archive manifest and content-retirement evidence.
- Updated `harness/rules/changelog.md`; no routing, runbook or AGENTS change is required.

## 7. Need new script

No. The existing checker needed a path fix, not another execution entry point.

## 8. Need new Eval

Yes, completed in this task: the portable worktree path regression test.

## 9. Need update AGENTS.md

No. The 50/50/200 policy remains unchanged.

## 10. Must fix before next task

- Complete final review, stage the ignored archive batch explicitly, commit, and push both remotes.
