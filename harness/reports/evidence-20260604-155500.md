# Evidence Report — 系统订单明细列表复刻

| 字段 | 值 |
|------|-----|
| 时间 | 2026-06-04 15:55 |
| 环境 | local real-pre |
| 分支 | 未提交（工作区修改） |
| commit hash | f86c0ea0 (最新) |
| 工作区是否干净 | 否 — 含本任务改动 + 预存在的其他文件改动 |

## 构建结果

| 项目 | 结果 |
|------|------|
| 后端 `mvn compile` | ✅ PASS |
| 后端 `OrderServiceTest` | ⚠️ 17/26 PASS，9 个预存在错误（MyBatis-Plus lambda 缓存未初始化，非本次引入） |
| 前端 `vue-tsc --noEmit` | ✅ PASS（0 errors） |
| 前端 `vitest orders/index.test.ts` | ✅ PASS（32/32） |
| 前端 `vitest orders/` (全部) | ✅ PASS（34/34） |
| 前端 `vitest api/order.test.ts` | ✅ PASS（24/24） |

## Docker 状态

未重启（本任务为纯代码改造，未涉及环境配置变更，需用户确认后手动重启）。

## 健康检查

未执行（需 Docker 重启后验证）。

## 业务验证

### 字段覆盖矩阵（第一阶段产出）

| 截图字段 | DB/Entity | API 返回 | 前端使用 | 缺口处理 |
|----------|-----------|----------|----------|----------|
| orderId | ✅ | ✅ | ✅ | — |
| orderTypeText | ✅ (新增) | ✅ (enrichment) | ✅ | int→text 映射 |
| contentTypeText | ✅ (新增) | ✅ (extra_data JSONB) | ✅ | extra_data 投影 |
| activityName | ❌ DB 无 | ✅ (enrichment) | ✅ | activity 表批量 JOIN |
| activityId | ✅ | ✅ | ✅ | — |
| productImage | ✅ | ✅ | ✅ | — |
| productName | ✅ | ✅ | ✅ | — |
| productId | ✅ | ✅ | ✅ | — |
| shopName | ✅ | ✅ | ✅ | — |
| itemNum/quantity | ✅ | ✅ | ✅ | — |
| commissionRate | ✅ | ✅ | ✅ | — |
| serviceFeeRate | ✅ | ✅ | ✅ | — |
| talentName | ✅ | ✅ | ✅ | — |
| talentId | ✅ | ✅ | ✅ | — |
| awemeId | ✅ (新增) | ✅ (extra_data JSONB) | ✅ | extra_data 投影 |
| channelName | ✅ | ✅ | ✅ | — |
| colonelUserName | ✅ | ✅ | ✅ | — |
| payTime | ✅ | ✅ | ✅ | — |
| deliveryTime | ✅ (新增) | ✅ (extra_data JSONB) | ✅ | extra_data 投影 |
| settleTime | ✅ | ✅ | ✅ | — |
| expireTime | ✅ (新增) | ✅ (extra_data JSONB) | ✅ | extra_data 投影 |

### "媒介"→"渠道" 替换

- ✅ 前端 index.vue 中 grep "媒介" = 0 匹配
- ✅ 列标题使用"渠道"
- ✅ 测试断言 `html.not.toContain('媒介')` 通过

### 空值处理

- ✅ `formatTime()` 函数对 null/undefined 返回空字符串
- ✅ `firstDisplayValue()` 函数跳过 null/undefined/空字符串
- ✅ 渠道未归因时显示"未归因"
- ✅ 测试断言 `html.not.toContain('null')` 和 `html.not.toContain('undefined')` 通过

### 双轨金额

- ✅ 未修改 payAmount/settleAmount 逻辑
- ✅ 未修改业绩计算规则

## 是否部署远端

否（未要求）。

## 远端健康检查

未执行。

## 变更文件清单

### 本任务修改（后端）

| 文件 | 变更说明 |
|------|----------|
| `ColonelsettlementOrder.java` | 新增 7 个 @TableField(exist=false) 展示字段 |
| `ColonelsettlementOrderMapper.java` | 扩展 SQL 添加 4 个 JSONB 投影字段 |
| `OrderService.java` | 新增 activityMapper 依赖、3 个 helper、扩展 enrichment |
| `OrderServiceTest.java` | 添加 activityMapper mock |
| `OrderSyncControllerTest.java` | 添加 activityMapper mock |

### 本任务修改（前端）

| 文件 | 变更说明 |
|------|----------|
| `index.vue` | 7 列重构、row-selection、新增 render 函数、CSS |
| `index.test.ts` | 新增 8 个测试用例，更新 3 个测试用例 |

### 预存在的未提交改动（非本任务）

`OrderController.java`、`OrderSyncService.java`、`DataApplicationService.java`、`OrderVO.java`、`migrate-all.sql`、`ColonelsettlementOrderMapper.xml`、`OrderControllerTest.java`

## 结论：PARTIAL

- ✅ 后端字段审查完成，字段覆盖矩阵已生成
- ✅ 后端改造完成，编译通过
- ✅ 前端 7 列复刻完成，类型检查和全部测试通过
- ⚠️ 未重启 Docker / 未做健康检查（需用户确认）
- ⚠️ 9 个预存在的后端测试错误未修复（MyBatis-Plus 缓存问题，非本次引入）

## 剩余风险

1. 预存在的其他文件未提交改动可能与本任务改动产生冲突
2. 需 Docker 重启后实际验证 API 返回数据在前端渲染效果
3. `extra_data` 中的 `awemeId`/`deliveryTime`/`expireTime` 字段名可能存在多种 key 格式，SQL 已做 COALESCE 多 key 兼容，但需实际数据验证
