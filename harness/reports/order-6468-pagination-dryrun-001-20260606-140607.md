# ORDER-6468-PAGINATION-DRYRUN-001

## Metadata

- Time: 2026-06-06 14:06:07 +08:00
- Environment: local real-pre
- Branch: feature/auth-system
- Runtime commit: c85077aa
- Remote deploy: not requested
- Endpoint: `POST /api/orders/6468-pagination-dry-run`
- Request window: `2026-06-03 00:00:00` ~ `2026-06-06 13:30:00`
- Page size: 100
- Max pages: 80
- Max orders: 50000

## Goal

Add a read-only 6468 pagination dry-run path that:

- follows 6468 `data.cursor` even when `has_more / hasMore` is absent;
- does not write `colonelsettlement_order` or `performance_records`;
- aggregates candidate scopes against baseline `3739 / 79400.07 / 1434.01 / 121.36 / settlement=0`;
- stops safely on repeated cursor, page/order limits, or upstream error.

## Code Changes

- Added `Order6468PaginationDryRunService`.
- Added `POST /orders/6468-pagination-dry-run` under `OrderController`.
- Added tests for:
  - continuing pagination when `hasMore=false` but 6468 cursor exists;
  - repeated cursor stop;
  - local de-dup candidate aggregation;
  - read-only controller entry;
  - partial summary when upstream fails after successful pages.

## Verification

- `mvn -f backend/pom.xml "-Dtest=Order6468PaginationDryRunServiceTest,OrderControllerTest,OrderSyncControllerTest" test`: PASS, 26 tests.
- `mvn -f backend/pom.xml test`: PASS, 1734 tests.
- `agent-do.ps1 -Env real-pre -Scope full`: PASS.
- Backend health: PASS.
- Frontend health: PASS.
- `npm run e2e:real-pre:p0:preflight`: PASS.

## Read-only Proof

Dry-run DB snapshot before:

```text
orders=1414|performance=1414|anti_join=0
```

Dry-run DB snapshot after:

```text
orders=1414|performance=1414|anti_join=0
```

Result:

```text
unchanged=true
```

## Dry-run Result

The endpoint returned a partial result because upstream stopped at page 59 request with:

```text
Douyin API error: code=20000, subCode=isp.service-error:256, endpoint=buyin.instituteOrderColonel, msg=服务打瞌睡了，请稍后再试
```

Stop state:

```text
pagesFetched=58
rawOrderRows=5800
uniqueOrders=5800
duplicateOrders=0
stopReason=UPSTREAM_ERROR
lastCursor=6953419296782030400
readOnly=true
```

Baseline:

```text
orderCount=3739
orderAmountCent=7940007
serviceFeeIncomeCent=143401
techServiceFeeCent=12136
settlementOrderCount=0
```

Candidate summaries:

| Candidate | Count | Order amount | Service fee income | Tech service fee | Settlement orders | Delta count | Delta order amount |
| --- | ---: | ---: | ---: | ---: | ---: | ---: | ---: |
| ALL_RAW | 5800 | 11499780 | 234992 | 19641 | 0 | +2061 | +3559773 |
| PAY_SUCC | 5309 | 10488471 | 214273 | 17870 | 0 | +1570 | +2548464 |
| NON_REFUND_NON_CANCELLED | 5311 | 10492031 | 214328 | 17876 | 0 | +1572 | +2552024 |
| PAY_TIME_RANGE | 5800 | 11499780 | 234992 | 19641 | 0 | +2061 | +3559773 |
| CREATE_TIME_RANGE | 5800 | 11499780 | 234992 | 19641 | 0 | +2061 | +3559773 |
| UPDATE_TIME_RANGE | 5797 | 11495732 | 234912 | 19634 | 0 | +2058 | +3555725 |
| NEW_AFTER_LOCAL_DEDUP | 5071 | 10073064 | 204613 | 16983 | 0 | +1332 | +2133057 |

## Stage Conclusion

`ORDER-6468-PAGINATION-DRYRUN-001` implementation is verified as read-only and cursor-aware.

The business reconciliation is still **PARTIAL_BY_UPSTREAM_ERROR**. The current partial result already proves the broad window and the listed simple candidate scopes do not align with `3739 / 79400.07 / 1434.01 / 121.36`, but it does not prove the final correct historical backfill scope.

## Next Required Step

Before any write-side backfill:

1. Re-run dry-run by narrower time segments or after upstream stabilizes.
2. Identify a candidate scope that matches the 3739 baseline.
3. Only then proceed to `ORDER-HISTORY-BACKFILL-DRYRUN-3739-001`.

No DB backfill was executed in this task.
