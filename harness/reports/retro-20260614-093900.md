# P0 Order Settlement Retro

## 结论

PARTIAL

## 本轮完成

- 新增 `ADR-009`，冻结金额单位、预估/结算双轨、effective fallback 禁止和 profit 待确认项。
- 修复 `OrderAmountMapperPolicy`，阻断 `estimate_*`、预估技术服务费和费率推导污染 `effective_*`。
- 同步修复运行态默认会走到的 `OrderDualTrackAmountResolver`，避免 feature flag 关闭时仍使用旧口径。
- 修复 `Order1603SettlementDryRunService` 分页停止条件：`has_more` 缺失但 `nextCursor` 存在时继续翻页，并增加 cursor 去重。

## 验证

- targeted backend tests: `52 tests, 0 failures, 0 errors`
- backend build: PASS
- frontend build: PASS
- Docker restart: PASS
- local health check: PASS
- real-pre P0 preflight: PASS
- harness limits: PASS

## 未通过或阻塞

- 2026-06-13 结算窗口复审未通过真实上游验证：`timeType=settle` 返回 `282:无效订单查询时间类型`。
- only-in-DB 订单溯源未执行，本轮不能判断 3,629/3,630 单是否可作为结算事实。
- `/api/performance/summary` 时间过滤未补，看板仍不能声明可对账。

## 经验

- 只修 mapper 不足够；当前默认配置可能走 legacy resolver，必须同时修补实际运行入口。
- 1603 分页是否正确不能只看 `has_more`，还要保留 `nextCursor` 和 cursor 去重证据。
- 结算窗口真实复审必须在上游 time_type 口径确认后再跑，当前只能记录为阻塞，不能写 PASS。

## Harness

- 本轮无需升级 harness 脚本。
- `agent-do.ps1` 的 git 阶段会收集整个 dirty worktree；后续若工作区已有用户改动，应先拆分提交或显式跳过自动 git 阶段。
