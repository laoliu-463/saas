# 回滚

## 什么时候用

部署失败、健康检查失败、关键业务回归或需要撤回已发布版本时使用。

## 执行命令

```text
停止当前发布 → 确认 current / previous 版本 → 通过 Jenkins rollback 入口执行 → 验证健康与业务
```

## 成功标准

目标版本、操作人、原因、锁状态、健康检查和业务验证均记录在 Jenkins artifact 与 evidence 中。

## 失败回滚

回滚本身失败时停止自动重试，保留现场并升级值班负责人；不得直接 SSH 修改容器或数据库事实。
