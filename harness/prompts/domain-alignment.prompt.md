# Prompt: domain alignment

请先做领域对齐，不要直接改代码：

1. 读取 `harness/DOMAIN_MAP.md`。
2. 读取 `docs/01-V1交付范围与边界.md`。
3. 读取当前相关领域合同和流程文档。
4. 判断主责领域、关联领域、禁止越界项。
5. 输出需要修改的层：前端、后端、SDK、数据库、测试、文档。
6. 给出验证证据清单。

输出：

```md
主责领域：
关联领域：
V1 边界：
禁止越界：
修改层：
验证方式：
阶段性结论：
```

